# Explorer — 프로젝트 가이드

멀티 디렉토리 파일탐색기 (Electron + TypeScript + React, Windows). 기획·설계·구현 산출물은 `docs/` 참조.

## 하네스: 개발 워크플로 (dev-harness)

**목표:** 기획 → 설계 → 구현을 dev-harness(프로젝트 플러그인)로 진행하고, 문서와 코드를 항상 일치시킨다.

**트리거:** 기획/설계/구현 진행 요청 시 dev-harness 스킬(`dev-harness:dev-orchestrator` 기획·설계, `dev-harness:team-dev` 구현)을 사용한다. 단순 질문은 직접 응답 가능.

### 📌 문서 동기화 게이트 (상시 — 필수)

**개발 진행 중 기획문서와 개발문서를 항상 코드와 동기화한다.** 다음 시점마다 `doc-sync` 스킬을
실행해 `doc-synchronizer` 에이전트(`subagent_type: "doc-synchronizer"`, `model: "opus"`)로
문서 상태(✅/🟡/🔜)를 실제 구현과 일치시킨다:

1. **각 구현 Phase 완료 직후** (team-dev Phase 3 루프에서 한 Phase가 QA 통과할 때마다). **동기화를 끝내기 전에는 다음 Phase로 넘어가지 않는다.**
2. **전체 통합 검증 시** (team-dev Phase 5).
3. 사용자가 "문서 동기화/상태 갱신"을 요청할 때(전체 감사).

- 동기화 대상: 기획 `docs/PRD.md`·`features.md`·`user-stories.md`·`flows.md` / 개발 `docs/roadmap.md`·`docs/architecture/traceability.md`.
- 상태의 단일 출처는 `docs/roadmap.md §0.5 진행 현황`. 나머지 문서를 여기에 맞춘다.
- 이 게이트는 **상태 표기·추적성만** 갱신한다. 요구사항/수용기준/스코프 변경은 PM→사용자 결정.
- 스코프 일탈(기획에 없는데 구현됨)·Must 미구현·문서-코드 충돌은 동기화 보고로 사용자에게 올린다.

> 절차 상세: `.claude/skills/doc-sync/SKILL.md` · 에이전트: `.claude/agents/doc-synchronizer.md`

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-07 | 문서 동기화 게이트 추가 (doc-synchronizer 에이전트 + doc-sync 스킬 + CLAUDE.md 게이트) | `.claude/agents/doc-synchronizer.md`, `.claude/skills/doc-sync/`, `CLAUDE.md` | 개발 진행 중 기획·개발 문서를 코드와 항상 동기화하라는 요청 |
| 2026-06-08 | dev-harness 표기를 "전역 플러그인" → "프로젝트 플러그인"으로 갱신 | `CLAUDE.md` | dev-harness 플러그인을 project 스코프로 설치 |
| 2026-06-08 | doc-sync 게이트 실행: §M(에픽12 M1·M2·M3 외부연계·원격) 구현 완료·통합 QA PASS → 상태 표기 🔜→✅ 동기화(실 동작 런타임 스모크 🟡 정직 구분·verify 누계 ~1015) | `docs/roadmap.md §0.5·§3-M·§4`, `docs/PRD.md §M·§6`, `docs/features.md §M`, `docs/user-stories.md 에픽12`, `docs/flows.md F14~F16`, `docs/architecture/traceability.md §1-M` | §M 구현 완료·QA PASS 후 문서-코드 상태 동기화 |
| 2026-06-08 | doc-sync 게이트 실행: §N(에픽13 N1 즐겨찾기 경로 워터마크·N2 즐겨찾기 드래그 정렬) 구현 완료·통합 QA PASS → 상태 표기 🔜→✅ 동기화(실 GUI 동작 런타임 스모크 🟡 정직 구분·렌더러 전용·신규 채널 0·키 `Alt+Shift+↑/↓` PRD §8 정합·핵심 verify 302/0) | `docs/roadmap.md §0.5`, `docs/PRD.md §6·§8`, `docs/features.md §N`, `docs/user-stories.md 에픽13`, `docs/flows.md F17~F18`, `docs/architecture/traceability.md §1-N·§2` | §N 구현 완료·QA PASS 후 문서-코드 상태 동기화 |
| 2026-06-09 | doc-sync 게이트 실행(ref.md 3건): ① §M 결함 수정 2건(M3 원격 디렉토리 더블클릭 ENOENT·M1 외부 드래그 오버레이 잔존) → **직전 "§M 코드 정합 ✅"의 ✅위장 드리프트 정직 정정**·코드 정합 회복(실 동작 🟡 유지·신규 스코프 아님), ② **비계획 구현 "파일/폴더 상단 고정(pin)" 스코프 일탈로 플래그**(기획문서 정식 추가 안 함·MoSCoW 무변경·roadmap §0.5 개발현황에만 사실 기록·PM/사용자 정식 편입 결정 대기). 기준일 6개 문서 2026-06-09 통일·verify 누계 ~1015→~1040(domain 60·store 121·persistence 101) | `docs/roadmap.md §0.5(추가 2026-06-09)`, `docs/PRD.md 헤더·변경이력`, `docs/features.md 배너`, `docs/user-stories.md 헤더·에픽12`, `docs/flows.md 헤더`, `docs/architecture/traceability.md §1-M` | §M 결함 수정 후 ✅위장 드리프트 정정 + pin 비계획 구현 스코프 일탈 플래그 |
| 2026-06-09 | doc-sync 게이트 실행(ref.md 동작 변경 2건): ① **§O 상단 고정 "정렬 최상단"→"스크롤 고정(sticky)"**(목록/자세히 전용·그리드는 정렬 상단 유지·`FileListView.tsx` sticky 밴드·키보드 보정·`sort.ts#applyPins`·selectors 불변=렌더 계층), ② **원격 주소창 경로-only 입력**(§M M3 / H5 교차·`PanelToolbar.tsx` 호스트 프리픽스 고정·`navigate.ts` 원격 URI validate-path 우회). 상태 단일 출처 roadmap §0.5·§1 요약표 §O 행·§3-M MP5 부기 갱신 → product-planner(기획 4종)·chief-architect(traceability §1-O·§1-M·§1-H) 반영분과 정합. **상태 "구현 완료(코드)·실 GUI 런타임 스모크 🟡" 유지(✅위장 금지)**·신규 채널 0·신규 의존성 0·렌더러 계층·**verify 누계 ~1040 불변·회귀 0**(domain 60·store 121·persistence 101·perf 25·remote-route 47). 6개 문서 기준일 2026-06-09 통일·모순 0 | `docs/roadmap.md §0.5·§1·§3-M`, `docs/PRD.md §O`, `docs/features.md §O1·§H5`, `docs/user-stories.md 에픽14·US-7.5/12.4`, `docs/flows.md F19·F16`, `docs/architecture/traceability.md §1-O·§1-M·§1-H` | ref.md 동작 변경 2건 구현·검증 후 문서-코드 상태 동기화 |
| 2026-06-09 | doc-sync 게이트 실행: **파워기능 M6 1차 3종(§P P1 듀얼 패널 폴더 비교(메타·단일깊이)·§R R1 고급 일괄 이름변경·§T T3 정렬/필터 프리셋, US-15.1/17.1/19.3) 구현 완료·통합 검증 PASS → 상태 표기 🔜→구현 완료(코드)·실 GUI 🟡 동기화.** 상태 단일 출처 roadmap §0.5(추가 2026-06-09 M6 줄 + §1 요약표 M6 행 + 헤더)에 기록 → PRD §6 MoSCoW 3항목 `[ ]`→`[x]`·§12 마일스톤 M6 행·features §P/§R/§T·user-stories 에픽15/17/19·백로그표·flows F20/F22/F30·traceability §1-P1/§1-R1/§1-T3(🔜 설계→구현 완료·실재 파일 매핑) 정합. **렌더러+세션영속·신규 IPC 채널 0·신규 npm 의존성 0·`SESSION_SCHEMA_VERSION` 1→2(T3). verify domain 132·store 162·persistence 123·operations 75·perf 25·p5 52(0 fail·회귀 0)·누계 ~1040→~1090.** **정직 한계(✅위장 금지): 실 GUI(프리셋·일괄rename 실 파일 왕복·Ctrl+Z 실복원·비교 진입/미러/동기스크롤) 런타임 스모크 🟡·P1 해시/재귀 비교 M7 연기 🔜·나머지 §P~§U 11종(Q1·R2·R3·R4·S1·S2·T1·T2·U1·U2·U3) 🔜 미착수(M7~M9).** 스코프/MoSCoW 문구 무변경(상태표기·추적성만)·6개 문서 기준일 2026-06-09 통일·모순 0 | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/PRD.md §6·§12·헤더`, `docs/features.md §P/§R/§T·배너`, `docs/user-stories.md 에픽15/17/19·백로그표·헤더`, `docs/flows.md F20/F22/F30·헤더·IA`, `docs/architecture/traceability.md §1-P1/§1-R1/§1-T3` | 파워기능 M6 3종 구현 완료·통합 검증 PASS 후 문서-코드 상태 동기화 |
| 2026-06-09 | doc-sync 게이트 실행: **파워기능 M7 2차(공용 해시 인프라 W1·전송 큐 인프라 W2 + 4기능: §R R2 중복 파일 찾기·R3 전송 큐 매니저·R4 복사 시 체크섬 검증·§P P1 해시/재귀 비교 확장, US-17.2/17.3/17.4·15.1) 구현 완료·통합 검증 PASS → 상태 표기 🔜→구현 완료(코드)·실 워커/GUI 🟡 동기화.** 상태 단일 출처 roadmap §0.5(추가 2026-06-09 M7 줄 + §1 요약표 M7 행 + 헤더 M7 추가)에 기록 → PRD §6 MoSCoW 4항목(R2·R3·R4·P1 해시/재귀) `[ ]`→`[x]`·§12 마일스톤 M7 행·features §P/§R(R2·R3·R4 헤더·배너)·user-stories 에픽15/17(US-15.1·17.2~17.4 헤더·체크박스·백로그표·헤더)·flows F20/F23/F24/F25(헤더·IA·예외)·traceability §1-P1/§1-R2/§1-R3/§1-R4(🔜 설계→구현 완료·실재 파일 매핑·유령 0) 정합. **신규 IPC 채널 `hash:*`(compare/dup/verify/cancel)·`queue:*`(list/state/pause/resume/retry/set-concurrency) 추가(동결 예외 — Should 신기능·기존 8선례 동일 규약)·신규 npm 의존성 0(SHA-256=node:crypto 내장). 신규 verify `hash` 46·`queue` 47 + domain 168·store 207·persistence 128(전부 0 fail·회귀 0)·누계 ~1090→~1200.** **정직 한계(✅위장 금지): 실 동작(해시 워커 잡·큐 스케줄러 일시정지/재개·중복 정리·복사후 검증 트리거·해시/재귀 실 GUI 비교) 런타임 스모크 🟡·R4 비원자 복사 검증 타이밍·원격 큐 일시정지 미배선 정직 표기·나머지 §P~§U 7종(Q1·S1·S2·T1·T2·U1·U2·U3) 🔜 미착수(M8~M9).** 스코프/MoSCoW 문구 무변경(상태표기·추적성만)·6개 문서 기준일 2026-06-09 통일·모순 0 | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/PRD.md §6·§12·헤더`, `docs/features.md §P/§R·배너`, `docs/user-stories.md 에픽15/17·백로그표·헤더`, `docs/flows.md F20/F23/F24/F25·헤더·IA`, `docs/architecture/traceability.md §1-P1/§1-R2/§1-R3/§1-R4` | 파워기능 M7 4종 구현 완료·통합 검증 PASS 후 문서-코드 상태 동기화 |
| 2026-06-09 | doc-sync 게이트 실행: **§O pin 정식 편입 상태 동기화** — 직전(2026-06-09) "스코프 일탈 플래그" 였던 파일/폴더 상단 고정(pin)이 **사용자 정식 편입 결정 → product-planner가 PRD §6 Should·features §O(O1)·user-stories 에픽14(US-14.1)·flows F19 정식 편입 + chief-architect traceability §1-O 매핑** 됨에 따라, 상태의 단일 출처 roadmap §0.5(추가 줄 정정 + §1 요약표 §O·§N 행 추가 + 헤더 §O 추가)를 "정식 편입 완료·구현 완료(코드)·실 GUI 런타임 스모크 🟡"로 갱신. 전 문서 §O 상태/범례/기준일(2026-06-09) 정합 확인(모순 0)·verify 60/121/101 재확인·traceability §1-O 유령 매핑 0 교차 확인. **스코프/MoSCoW 문구 무변경(product-planner 확정 유지)·상태표기/추적성만.** | `docs/roadmap.md §0.5·§1 요약표·헤더`, (정합 확인) `docs/PRD.md §6·§6 근거`, `docs/features.md §O`, `docs/user-stories.md 에픽14·백로그표`, `docs/flows.md F19`, `docs/architecture/traceability.md §1-O` | pin 정식 편입 결정 후 roadmap 단일 출처 상태 동기화 + 전 문서 정합 |
| 2026-06-09 | doc-sync 게이트 실행: **T3 정렬/필터 프리셋 폐기 동기화** — **사용자 결정으로 T3(US-19.3·§T·F30) 코드 전면 제거(폐기)** 됨에 따라 상태를 "✅ 구현 완료"에서 **"제거(폐기)"** 로 정직 정정(✅위장 방지). 코드 사실(grep 부재 확인): `domain/rules/filterComposition.ts`·`app/stores/presetsSlice.ts`·`app/usecases/presets.ts`·`ui/preset/PresetBar.tsx`·`ui/preset/PresetManageDialog.tsx`(디렉토리째) 삭제·`selectors.computeVisible`→기존 `filterEntries` 환원·`FilterState` extPatterns/tagColors/diffOnly 및 `FilterPreset`/`PresetSort`/`PresetFilter`/`TagColor`/`SessionSnapshot.filterPresets` DTO 제거·`applyPresetToPanel`/`presetManageOpen` 제거·**`SESSION_SCHEMA_VERSION` 2→1 환원**(defaults.ts + domain/session 미러). **범위 한정: §T 3기능 중 T3만 폐기·T1(태그)·T2(폴더용량)는 🔜 미착수(M8) 유지·§T 챕터 보존.** ADR-012는 프리셋·filterComposition 부분만 폐기 주석·태그(T1) 영속 부분은 유효 유지. 경과 보존(은폐 금지): "M6 구현 완료(✅) → 2026-06-09 폐기·코드 제거". 무손상(유지): §O applyPins·P1 compareDiffOnly·R4 verifyOnCopy(SETTINGS)·M7 전체. verify 누계 정정: domain 146·store 201·persistence 112·operations 75·hash 46·queue 47·p5 52·fs 19·scan 39·perf 25(전부 0 fail). 7개 문서 기준일 2026-06-09 통일·모순 0 | `docs/roadmap.md §0.5·§1 요약표`, `docs/PRD.md §6·§12·§6 근거`, `docs/features.md §T`, `docs/user-stories.md 에픽19·US-19.3·백로그표`, `docs/flows.md F30`, `docs/architecture/traceability.md §1-T3·§1-T1·헤더`, `docs/architecture/adr/ADR-012·ADR-000-index`, `docs/pm/M6-implementation-plan.md` | 사용자 결정으로 T3 폐기·코드 제거 후 문서-코드 상태 정직 정정(✅→제거) |
| 2026-06-10 | doc-sync 게이트 실행: **비계획 구현 3건(V1~V3) 스코프 일탈 플래그** — V1 대시보드 스캔결과 표 "동작" 컬럼(📂이동·✨Google AI 모드 질의·신규 채널 `shell:open-external`)·V2 드라이브 레이블 일관화(신규 채널 0·`fs:drives` label 단일 출처 재사용)·V3 탐색기 "AGT-Finder로 열기"(신규 푸시 채널 `app:open-path`·NSIS HKCU 4종 등록·perMachine:false). **3건 모두 PRD/features/user-stories/flows 미수록 신규 스코프 → 기획문서 정식 편입 안 함·MoSCoW 무변경·roadmap §0.5(2026-06-10 단락)+traceability §1-V 사실 기록만·PM/사용자 정식 편입 결정 대기.** 신규 채널 2종은 P1 동결 후 신기능 선례(preview:read·shell:open-terminal·analyze:scan:*·hash:*·queue:* 등)와 동일 규약·동결 위반 아님. typecheck node+web PASS·ESLint 0·verify 누계 약 1200 불변(헤드리스 범위 밖)·실 GUI/설치/우클릭 미검증 🟡(✅위장 금지) | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/architecture/traceability.md §1-V` | 비계획 구현 3건 스코프 일탈 플래그 + 코드 사실 기록 |
| 2026-06-10 | doc-sync 게이트 실행: **견고성·성능 개선 6건(V4~V9) 상태표기·추적성 동기화** — V4 복사 엔진 성능 최적화(하이브리드 복사·적응형 병렬 `engine.ts`·`fileOpWorker.ts pickConcurrency`)·V6 휴지통 실패 메시지 actionable화(`describeTrashFailure`)·V7 다중선택 드래그 이동 수정(`FileListView` mousedown→mouseup 분리)·V8 휴지통 실패→영구삭제 폴백(`operationsBridge`·확인 게이트)·V9 EPERM 견고 영구삭제(`forceRemove`/`removeWithRetry`·실 FS 스모크 통과) **5건=기존 P4/A3 기능 개선(스코프·MoSCoW·신규 채널 무변경·기획문서 무수정)** / **⚠️ V5 robocopy 고속 미러=신규 invoke 채널 `op:robocopy:start`+신규 스코프(§P1 미러 확장) → 스코프 일탈로 플래그·기획 정식 편입 안 함·PM/사용자 결정 대기**(진행/취소/완료는 기존 `op:*` 재사용·EVENT_CHANNELS 무변·삭제는 항상 휴지통·`/PURGE` 없음·복사분 undo 미제공·신규 채널은 동결 후 선례 동일 규약=동결 위반 아님). 상태 단일 출처 roadmap §0.5(2026-06-10 V4~V9 단락 + §1 요약표 "견고성·성능 6건" 행 + 헤더)에 사실 기록 → traceability §1-V4(V4~V9 매핑·유령 0). **`npm run typecheck`(node+web) PASS·변경 파일 ESLint 0·`verify:ops` 35/0·일회성 실 FS 스모크 통과(복사 병렬 80/80·forceRemove 읽기전용/정션)·verify 누계 약 1200 불변(신규 케이스 0)·회귀 0.** **정직 한계(✅위장 금지): V4 대용량/병렬 처리량·V5 robocopy 실행/stdout 파싱/실 GUI·NSIS 설치는 미실행 → 런타임/설치 스모크 🟡.** 기획 4종(PRD/features/user-stories/flows)은 임의 편입 금지(상태표기/추적성만·V5 편입 결정 대기) | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/architecture/traceability.md §1-V4·§1-V 헤더·메모` | 견고성·성능 6건(V4~V9) 코드 사실 동기화 + V5 robocopy 스코프 일탈 플래그 |
| 2026-06-10 | doc-sync 게이트 실행: **버그 수정·UX 개선 7건(F1·F2·B1·B3·B4·I8·I9) 상태표기·추적성 동기화** — F1 asar 가상화 ENOENT 수정(`fileOpWorker.ts process.noAsar`·`engine.ts forceRemove`)·F2 특수파일 EACCES best-effort 삭제(`engine.ts forceRemove` 재설계·실 FS 스모크 통과)·B1 기본 시작위치 새 탭 적용(`tabsSlice.ts newTab` startLocation·`"E:"`→`"E:\\"`)·B3 미러 재귀 복사 평탄화 수정(**기존 `op:start` 옵셔널 `baseDir` 확장·신규 채널 0**·contracts/guard/op.handlers/OperationManager/protocol/fileOpWorker/engine/fileOps/compare·실 FS 스모크 통과 top/sub/deep 구조 보존)·B4 휴지통 실패 폴백 UX(`operationsBridge.ts` 실패 trash op dismiss)·I8 익명 통계 도움말(`SettingsDialog.tsx` 수집 항목·로컬 전용·외부 전송 0)·I9 브라우저 기본 단축키 차단(`KeyboardDispatcher.tsx` F11/F3/Ctrl+P/줌·비-dev F5/F12/리로드/개발자도구 preventDefault·앱 매핑 통과). **7건 모두 기존 기능(P4 copy/move/delete·trash·P3 탭·E6 설정·키보드·§P1 미러) 버그 수정/UX 개선 — 스코프·MoSCoW·신규 채널 무변경(기획문서 무수정·스코프 일탈 아님·상태표기/추적성만). B3은 기존 `op:start` 계약을 옵셔널 `baseDir`로 확장한 것=신규 채널 아님(미지정 시 기존 copy 동작 동치).** 상태 단일 출처 roadmap §0.5(2026-06-10 버그·UX 7건 단락 + §1 요약표 "버그·UX 7건" 행 + 헤더)에 사실 기록 → traceability §1-V(F1·F2·B1·B3·B4·I8·I9 매핑·유령 0·§1-V 제목 9건→16건). **`npm run typecheck`(node+web) PASS·변경 파일 ESLint 0·`verify:ops` 35/0(회귀 0)·F2·B3 일회성 실 FS 스모크 통과·신규 verify 케이스 0·verify 누계 약 1200 불변.** **정직 한계(✅위장 금지): F1 asar(Electron 한정 미재현)·B1 새 탭 시작위치·B4 폴백·I8 표기·I9 실 브라우저 단축키 차단의 실 GUI/실 동작은 미검증 → 런타임 스모크 🟡.** **보류 백로그 4건(구현 아님·잔여): B2 전송큐 일시정지 미반영(설계 한계·청크 awaitResume 구조개선 필요)·I5 다중 드래그 고스트 스택 비주얼·I6 새 탭 워크스페이스 피커·I7 단축키 도움말 설정 분리 — 🔜 미착수(roadmap §0.5 비고). 기획 4종 무수정(상태표기/추적성만) | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/architecture/traceability.md §1-V·§1-V 헤더·메모`, `CLAUDE.md` | 버그·UX 7건 코드 사실 동기화 + 보류 백로그 4건 비고 |
| 2026-06-10 | doc-sync 게이트 실행: **파워기능 M8 6종(S1 내용 검색 grep·S2 명령 팔레트·T1 파일 태그/색상 라벨·T2 폴더 용량 인라인·U1 Space 퀵룩·U2 브레드크럼 드롭다운, US-18.1~20.2) + 신규 Should §W1(자세히 컬럼 헤더·너비 드래그, US-21.1) 구현 완료(코드)·통합 검증 PASS → 상태 표기 🔜→구현 완료(코드)·실 GUI/실 워커 🟡 동기화.** **S1만 신규 IPC 채널 `search:content:*` 5종(grep 워커·ADR-010·외부 ripgrep 비채택·node 내장)·나머지 5종+§W1 신규 채널 0·신규 npm 의존성 0**(S2/U1=`preview:read`/commandBus 재사용·T1=세션 메타 `tagsByPath`·T2=`analyze:scan:*` 재사용·U2=`fs:tree-children` 재사용·§W1=세션 영속 `detailsColumnWidths`). **T1은 T3 폐기 때 삭제됐던 `filterComposition.ts` 태그 합성을 `domain/rules/tags.ts`+`computeVisible`로 재설계(traceability §1-T1 "재설계 필요" 메모 해소).** 상태 단일 출처 roadmap §0.5(2026-06-10 M8 단락 + §1 요약표 M8·§W1 행 + 헤더)에 기록 → PRD §6 MoSCoW 6항목 `[ ]`→`[x]`·§8 단축키(`Ctrl+Shift+P`/`Space` 구현 완료)·§12 마일스톤 M8 행 ✅·features §S/§T/§U 배너·제목·수용기준·user-stories 에픽18~21·백로그표·flows F26~F29/F31/F32·IA·예외·traceability §1-S1/§1-S2/§1-T1/§1-T2/§1-U1/§1-U2(🔜 설계→구현 완료·실재 파일 매핑·유령 0)·§1-W 정합 정합. **`npm run build`(typecheck node+web + electron-vite) PASS·ESLint 0·신규 `verify:search` 58·`verify:palette` 20·`verify:contentsearch` 38 + domain 204·store 222·persistence 119(전부 0 fail·회귀 0)·verify 누계 약 1200→약 1390.** **정직 한계(✅위장 금지): 헤드리스 verify는 순수 로직·store·세션 영속·계약 불변식만 증명. 실 GUI/실 워커(grep 스트리밍·결과 점프·미리보기 표출·팔레트 검색/실행·Space 퀵룩·태그 부여/필터·폴더용량 실 스캔·브레드크럼 ▾ 이동·컬럼 헤더 드래그/키보드 리사이즈/재시작 후 폭 유지)는 런타임 스모크 🟡·Electron 앱 미실행.** M8 잔여 0 — 파워기능 잔여는 M9(Q1 압축·U3 멀티윈도우) 2종. 스코프/MoSCoW 문구 무변경(상태표기/추적성만)·7개 문서 기준일 2026-06-10 통일·모순 0 | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/PRD.md §6·§8·§12·헤더`, `docs/features.md §S/§T/§U·배너`, `docs/user-stories.md 에픽18~21·백로그표·헤더`, `docs/flows.md F26~F29/F31/F32·헤더·IA·예외`, `docs/architecture/traceability.md §1-S1/§1-S2/§1-T1/§1-T2/§1-U1/§1-U2·헤더`, `CLAUDE.md` | 파워기능 M8 6종 + §W1 구현 완료·통합 검증 PASS 후 문서-코드 상태 동기화 |
| 2026-06-10 | doc-sync 게이트 실행: **파워기능 M9 2종(Q1 압축파일 `archive://` 어댑터·U3 탭 색상/잠금·탭 분리(새 창), US-16.1/20.3) + 신규 Should §U4 탭 사용자 지정 이름(US-20.4)·§X1 좌측 사이드바 빠른 위치 ▸ 다운로드(US-22.1) 구현 완료(코드)·통합 검증 PASS → 상태 표기 🔜→구현 완료(코드)·실 GUI/실 워커/멀티윈도우 🟡 동기화. 이로써 §P~§U 14종 전부 완료(M6~M9 종료·T3 폐기).** **신규 IPC 채널: Q1 `archive:open/list/close/extract/add` 5종·U3 탭 분리 `window:split-tab`/`window:get-init` 2종·X1 `fs:known-folders` 1종(전부 P1 동결 후 신기능 선례 동일 규약·동결 위반 아님)·U3 색상/잠금·U4 신규 채널 0. 신규 npm 의존성 `yauzl`+`yazl` 2종(Q1 zip·MIT·네이티브 0)·`SESSION_SCHEMA_VERSION` 무변경.** 상태 단일 출처 roadmap §0.5(2026-06-10 M9 단락 + §1 요약표 "M9 2종"·"§U4"·"§X1" 행 + 헤더)에 기록 → PRD §6 MoSCoW 2항목(Q1·U3) `[ ]`→`[x]`·§8 단축키 노트·§12 마일스톤 M9 행 ✅·features §Q1/§U3 배너·제목·수용기준·§U 챕터 제목·user-stories 에픽16/20(US-16.1·20.3) 제목·체크박스·백로그표·헤더·flows F21/F33·헤더·IA·예외·traceability §1-Q1/§1-U3(🔜 설계→구현 완료·실재 파일 매핑·유령 0)·§1-U4/§1-X1 정합 확인. **`npm run build`(typecheck node+web + archiveWorker.js 번들) PASS·ESLint 0·부팅 스모크(did-fail 0) 정상·신규 `verify:archive` 56·`verify:archiveui` 43 + store 258·persistence 132 등 전체 스윕 0 fail·회귀 0·verify 누계 약 1390→약 1500.** **정직 한계(✅위장 금지): 헤드리스 verify·코드 정합·부팅 스모크(창 표시)만 ✅. 실 기능 동작(zip 실 열기/추출/추가 IPC 왕복·op:* 진행률·멀티 윈도우 실 분리/이동/복원·탭 인라인 이름변경·색상/잠금·다운로드 노드 이동)은 런타임 스모크 🟡. U3 정직 한계: 멀티 윈도우 세션 복원은 주 창만(분리 창 reopen-only·재시작 복원 안 함·의도적 MVP). 개발 잔여(🔜) 0 — 남은 것은 P7 런타임(성능 실측·코드서명·NSIS) + 보류 백로그(B2·I5~I7).** 스코프/MoSCoW 문구 무변경(상태표기/추적성만·§U4·§X1은 사용자 명시 요청으로 product-planner가 이미 편입)·7개 문서 기준일 2026-06-10 통일·모순 0 | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/PRD.md §6·§8·§12·헤더`, `docs/features.md §Q/§U/§X·배너`, `docs/user-stories.md 에픽16/20/22·백로그표·헤더`, `docs/flows.md F21/F33·헤더·IA·예외`, `docs/architecture/traceability.md §1-Q1/§1-U3·§1-U4/§1-X1·헤더`, `CLAUDE.md` | 파워기능 M9 2종 + §U4·§X1 구현 완료·통합 검증 PASS 후 문서-코드 상태 동기화 (§P~§U 14종 전부 완료) |
| 2026-06-11 | doc-sync 게이트 실행: **§H H4 터미널 열기(`shell:open-terminal`) 결함 수정 1건 상태표기·추적성 동기화** — Node 22+(libuv 1.48+)에서 Windows Terminal이 Microsoft Store 앱 실행 별칭(APPEXECLINK·0바이트 `wt.exe`)으로 설치된 경우 `execFile('wt.exe')`가 콜백 오류(ENOENT)가 아니라 **동기 EINVAL throw** → `shell:open-terminal` 핸들러 오류로 새어 폴백 미도달하던 결함을 `src/main/os/shell.ts#openTerminal` **3단 폴백 재설계**로 수정(① `spawnDetached`(신규·동기 throw 흡수·detached/unref·즉시 resolve로 터미널 종료까지 invoke 대기하던 부수 결함 해소) `wt.exe -d` 직접 spawn → ② `execFileNoThrow`(신규)+PowerShell 릴레이[env `EXPLORER_TERMINAL_DIR`·문자열 합성 0·후행 `\`→`\\`] → ③ `powershell.exe -NoExit` 창). **기존 H4 기능 결함 수정 — 스코프/MoSCoW/신규 IPC 채널/신규 npm 의존성 전부 무변경·상태 등급 변화 없음(✅ 코드 정합·실 GUI 🟡 유지)·ADR-005 명령행 합성 0 유지·`shell.handlers.ts` 무변경(git diff: shell.ts 84+/25-만 변경 — 채널/계약/핸들러 무변).** 상태 단일 출처 roadmap §0.5(2026-06-11 결함 수정 단락·기존 2026-06-09 "§M 결함 수정" 형식)에 사실 기록 + §1 요약표 H장 행 메모 + 헤더/§0.5 기준일 2026-06-11. traceability §1-H(H4) 매핑 메모를 "`wt.exe→powershell -NoExit`·`execFile`"에서 실코드(3단 폴백·`spawnDetached`/`execFileNoThrow`)에 맞춰 **드리프트 정정**(+헤더 갱신 줄). **기획 4종(PRD/features/user-stories/flows) 무수정**(스코프 무변경·수용기준 "wt 우선→PowerShell 폴백·셸 미경유 인자 배열" 행동 계약은 수정 후에도 충족=모순 없음). **검증: `npm run typecheck:node` PASS(재실행)·`npm run build` PASS·ESLint 0·일회성 노드 스모크(한글 경로·`E:\` 동기 EINVAL 흡수→폴백 도달→`errorMessage:''`·throw 0)·verify 누계 무변(신규 케이스 0).** **정직 한계(✅위장 금지): 실 GUI/인터랙티브 세션 WT 창 표시는 미검증(작업 컨텍스트가 스토어 앱 활성화 불가 — 2차 릴레이 WT 기동은 사용자 세션 한정·단 2차 실패 시 3차 PowerShell 창 폴백으로 터미널은 열림) → 런타임 스모크 🟡.** 드리프트/스코프 일탈 0 | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/architecture/traceability.md §1-H·헤더` | §H H4 터미널 열기 결함 수정 후 문서-코드 상태 동기화 + traceability 매핑 메모 드리프트 정정 |
| 2026-06-11 | doc-sync 게이트 실행: **워크스페이스 자동 저장(US-5.8 동작 확장·사용자 직접 요청) 상태표기·추적성 동기화** — 선택 워크스페이스(`uiSlice.currentWorkspace`)는 세션 자동저장이 같은 스냅샷을 해당 워크스페이스 파일에 이중 기록·`SessionSnapshot.currentWorkspace?` 비파괴 영속(`SESSION_SCHEMA_VERSION` 1 불변)·`WorkspaceDialog` 배지/선택 해제. **신규 IPC 채널 0(기존 `workspace:save`·`session:save` 재사용)·신규 npm 의존성 0·기존 US-5.8(P6 Should) 동작 확장(스코프/MoSCoW 무변경).** 상태 단일 출처 roadmap §0.5(2026-06-11 단락)+§1 P6 행 부기 → traceability §1("명시적 워크스페이스"→"+자동 저장" 매핑·유령 0)·user-stories US-5.8 제목 배지/백로그표 정합. **`typecheck`(node+web) PASS·변경 8종 ESLint 0·`verify:store` 264/0·`verify:persistence` 138/0·domain 204·p5 52·operations 75(회귀 0)·누계 약 1500→약 1513.** 정직 한계(✅위장 금지): 실 GUI(배지/선택 해제·자동저장 IPC 왕복·재시작 후 복원·삭제/이름변경 추적·손상 폴백) 런타임 스모크 🟡. **드리프트(결정 대기): US-5.8 수용기준 문구가 "명시적 저장"만 기술 — 자동 저장 동작 정식 반영 여부 PM/사용자 결정 대기(문구 무수정·상태표기/추적성만).** | `docs/roadmap.md §0.5·§1 요약표`, `docs/architecture/traceability.md §1·헤더`, `docs/user-stories.md US-5.8·백로그표`, `CLAUDE.md` | 워크스페이스 자동 저장(US-5.8 동작 확장) 구현 완료·검증 후 문서-코드 상태 동기화 + 수용기준 문구 반영 결정 대기 플래그 |
| 2026-06-11 | doc-sync 게이트 실행: **워크스페이스 자동 저장(US-5.8) 후속 증분 — 상태바 워크스페이스 칩 노출** 상태표기·추적성 동기화. 직전 2026-06-11 자동 저장 단락의 후속 증분(사용자 직접 요청): 하단 StatusBar에 `🗂 이름·상태` 칩(5종 dirty/saving/saved+시각/error/idle·미선택 숨김·클릭=워크스페이스 관리). 저장 상태는 zustand store.subscribe 재귀 루프 회피를 위해 **`session.ts` 내 경량 pub/sub(`useSyncExternalStore`·구독 경로 밖·in-flight 가드)** 로 분리. **기존 US-5.8(P6 Should)/US-5.7(M) 동작 확장·가시화 — MoSCoW/스코프/신규 IPC 채널/신규 의존성/SCHEMA 무변경·렌더러 전용·상태표기/추적성만.** roadmap §0.5(2026-06-11 자동 저장 단락 후속 증분 bullet)+§1 P6 행에 기록 → traceability §1 상태바(US-5.7)·워크스페이스(US-5.8) 매핑에 `StatusBar.tsx`·session pub/sub 5심볼 반영(유령 0)·user-stories US-5.7/US-5.8 배지·백로그표 부기. `typecheck`(node+web) PASS·변경 파일 ESLint 0·**`verify:store` 264→274**(WSS 상태 전이 10케이스)·`verify:p5` 52·`verify:persistence` 138·`verify:domain` 204(전부 0 fail·회귀 0)·verify 누계 약 1513→약 1523. **정직 한계(✅위장 금지): 칩 렌더·5종 상태 전환·시각 표기·클릭 다이얼로그는 Electron 미실행 → 런타임 스모크 🟡.** **드리프트(결정 대기·기존 항목 포함): US-5.8 수용기준 본문이 "명시적 저장"만 기술 — 자동 저장·상태바 칩 동작 미반영·문구 정식 반영 여부 PM/사용자 결정 대기.** 6개 문서 기준일 2026-06-11 통일·모순 0 | `docs/roadmap.md §0.5·§1 P6`, `docs/architecture/traceability.md §1 상태바·워크스페이스·헤더`, `docs/user-stories.md US-5.7·US-5.8·백로그표`, `CLAUDE.md` | 워크스페이스 자동 저장 상태바 노출(후속 증분) 구현·검증 후 문서-코드 상태 동기화 |
| 2026-06-12 | doc-sync 게이트 실행: **§Y Windows 셸 컨텍스트 메뉴 연동 1종(Y1·US-23.1·F37·ADR-013) 구현 완료(코드)·통합 QA PASS → 상태 표기 🔜(설계 완료·구현 전)→구현 완료(코드)·실 GUI/실 패키지 🟡 동기화.** 신규 invoke 채널 `shell:context-verbs`/`shell:invoke-verb` 2종(P1 동결 후 신기능 선례 동일 규약·동결 위반 아님)·신규 npm/네이티브 의존성 0(상주 PowerShell `shellVerbsWorker.ps1`+COM Verbs)·`FileOpErrorCode` `'EVERB'` 비파괴 확장·`SESSION_SCHEMA_VERSION` 무변. 상태 단일 출처 roadmap §0.5(2026-06-12 §Y 단락 + §1 요약표 §Y 행 + 헤더)에 기록 → PRD §6 MoSCoW `[ ]`→`[x]`·§12 후속(M10) 행·features §Y/§Y1 배너·user-stories 에픽23/US-23.1·flows F37·IA·예외·traceability §1-Y(🔜 설계→구현 완료·**예정 파일을 실재 파일로 정정**: `shellVerbsBlacklist.ts`·`shellVerbsSection.ts` 신규 분리 드리프트·유령 0) 정합. **typecheck(node+web)/build PASS(out/main/shellVerbsWorker.ps1 생성)·ESLint 0·신규 `verify:shellverbs` 75/0·일회성 실 노드 스모크(ps1 한글 경로 왕복·실 COM 열거·블랙리스트 필터·가짜 verbId EVERB 거부·dispose 좀비 0) 통과.** **정직 한계(✅위장 금지): 실 GUI(섹션 표출·verb 클릭→외부 프로그램 DoIt·다중선택/원격/archive 숨김·한글 display)·실 패키지 설치본(asar ps1·ExecutionPolicy·`npm run dist` 미수행)은 런타임 스모크 🟡. 별도 트랙: `verify:worker` FAIL은 §Y 무관 사전 환경 결함(Node 22.17 워커 atomics·clean HEAD 동일 재현·§Y 회귀 아님).** 스코프/MoSCoW 문구·수용기준 본문 무변경(상태표기/추적성만)·6개 문서 기준일 2026-06-12 통일·모순 0 | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/PRD.md §6·§12·헤더·변경이력`, `docs/features.md §Y·§Y1·배너`, `docs/user-stories.md 에픽23·US-23.1·백로그표`, `docs/flows.md F37·IA·예외·헤더`, `docs/architecture/traceability.md §1-Y·헤더`, `CLAUDE.md` | §Y Windows 셸 컨텍스트 메뉴 연동 구현 완료·통합 QA PASS 후 문서-코드 상태 동기화 + traceability 설계→구현 파일 매핑 드리프트 정정 |
| 2026-06-12 | doc-sync 게이트 실행: **§Y1 후속 확장(셸 컨텍스트 메뉴 "단일 선택 전용 → 단일+다중 선택" 동작 확장) 상태표기·추적성 동기화.** 직전 §Y 구현(단일 한정)의 후속 동작 확장 — 다중 선택 우클릭 시에도 "Windows 메뉴" 섹션이 선택 전체를 하나로 묶어(압축·보내기) 노출. **기존 §Y1(US-23.1) 동작 확장(신규 챕터/스코프 추가 아님)·신규 IPC 채널 0(기존 `shell:context-verbs`/`shell:invoke-verb` 재사용·계약만 확장 `path: string`→`paths: string[]`)·신규 npm/네이티브 의존성 0·`SESSION_SCHEMA_VERSION` 무변·상태 등급 무변(구현 완료(코드)·실 GUI 🟡 유지·다중도 동일 한계).** 코드 사실(Grep/Read): `contracts.ts`(paths 배열)·`guard.ts zShellPaths`·`shell.handlers.ts`(각 경로 로컬 한정·guard·존재 검증 후 배열 위임)·`os/shellVerbs.ts listVerbs(paths)/invokeVerb(paths,verbId)`·`os/shellVerbsWorker.ps1`(**단일=COM `Shell.Application.Verbs()` 무회귀·다중=C# `Add-Type` `IShellFolder::GetUIObjectOf→IContextMenu` 다중 PIDL·Add-Type 실패 시 다중만 빈목록 폴백·apidl 수동 IntPtr 마샬링**)·`shellVerbsBlacklist.ts`(`VERB_TRANSLATIONS` 영→한 표시명 사전·`translateVerbDisplay`·display 만 한국어화·verbId 원문 보존)·renderer `contextVerbs(paths)`/`invokeVerb(paths,verbId)`·`uiSlice.ContextMenuState.targetPaths?`·`contextMenu.ts`(`!multi` 게이트 제거)·`shellVerbs.ts`(선택키[정렬+NUL] TTL 캐시·경합 가드). 상태 단일 출처 roadmap §0.5(2026-06-12 §Y1 후속 확장 단락 추가 + §1 요약표 §Y 행 부기) → traceability §1-Y(다중 선택·IContextMenu 경로·paths 계약·targetPaths·VERB_TRANSLATIONS 매핑 정정·헤더 줄·유령 0)·PRD §12 status 컬럼 verify 수치만 갱신. **typecheck(node+web)/build PASS·변경 TS ESLint 0·`verify:shellverbs` 75→95(i18n 8+다중/빈paths 12)·`verify:store` 281/0·일회성 실 노드 스모크(단일/다중 list·다중 invoke[Copy as path] 두 경로 클립보드) 통과.** **정직 한계(✅위장 금지): 헤드리스 verify·노드 스모크·코드 정합만 ✅. 실 GUI(다중 선택 시 실제 "압축으로 하나 묶기"/"보내기" 클릭 동작)는 런타임 스모크 🟡·다중 invoke 는 비파괴 verb(Copy as path)로만 실증·부수효과 verb 실 GUI 클릭 미검증 🟡.** **드리프트(결정 대기·문구 무수정): features §Y1·user-stories US-23.1·flows F37 수용기준/예외/Non-goal 이 "단일 선택 한정·다중 선택 시 섹션 숨김·다중 일괄 invoke 비범위"로 기술 — 구현이 이를 정반대(다중 노출·일괄 invoke)로 확장·기획 4종 본문 무수정(상태표기/추적성만)·정식 반영 여부 PM/사용자 결정 대기.** 스코프/MoSCoW 무변경 | `docs/roadmap.md §0.5·§1 요약표`, `docs/architecture/traceability.md §1-Y·헤더`, `docs/PRD.md §12(status 수치)`, `CLAUDE.md` | §Y1 단일→단일+다중 선택 동작 확장 구현·검증 후 문서-코드 상태 동기화 + 수용기준 단일선택 문구 드리프트 보고 |
| 2026-06-14 | doc-sync 게이트 실행: **ref.md 백로그 처리 6건(①~⑤·⑦) 상태표기·추적성 동기화** — ① 탭 루트 잠금(`Tab.lockedRoot`·`domain/rules/tabLock.ts#isWithinLockedRoot`·`panelsSlice` navigate/navBack/navForward 가드·navUp 경유·`PanelToolbar` 🏠 "루트로" 버튼·세션 비파괴 영속)·② 주소창 단일 클릭 편집 진입 제거(`PanelToolbar` onClick=활성화만·편집은 더블클릭/Ctrl+L)·③ FTP 폴더 업로드 재귀(`remoteTransfer.startUpload` walk+원격 mkdir)·④ FTP/SFTP 접속 초기 폴더(`pwd`/`cwd`·`RemoteConnectRes.initialPath` 체인 6파일)·⑤ FTP 업로드 충돌 정책 배선(`remote.handlers`+`startUpload` skip/rename/overwrite — 기존 `RemoteUploadReq.conflictPolicy` 미배선 결함 수정)·⑦ 작업 후 주소 포커스(②와 동일 원인 추정·가설). **신규 IPC 채널 0(기존 `remote:connect`/`remote:upload` 계약 확장)·신규 npm 의존성 0·`SESSION_SCHEMA_VERSION` 무변(비파괴 optional `lockedRoot`).** 상태 단일 출처 roadmap §0.5(2026-06-14 단락 + §1 요약표 M9/§U·P6 행 부기 + 헤더 2026-06-14)에 "구현 완료(코드)·실 동작/실 GUI 🟡" 등급으로 기록 → traceability §M(US-12.3·12.5 ③④⑤ 매핑)·§1-U3(① tabLock 체인 매핑·스코프 확장 플래그)·PRD 헤더 갱신일. **검증: `typecheck`(node+web)/`build` PASS·ESLint 0·`verify:domain` 204→215·`verify:store` 281→291·`verify:remote` 24→36·`verify:remote-route` 47(전부 0 fail·회귀 0)·누계 약 1539→약 1572.** **정직 한계(✅위장 금지): FTP 3종(③④⑤) 실 FTP/SFTP 서버 미연동·헤드리스 스텁 증명만 🟡·①②⑦ Electron 미실행 실 GUI 🟡·⑦은 ②와 동일 원인 가설(미검증).** **드리프트(결정 대기·문구 무수정): ①(US-20.3 수용기준=닫기 가드만)·③(US-12.5 폴더 재귀 미명시)·④(US-12.3 초기 폴더 진입 미명시)는 동작/스코프 확장 → 기획 정식 편입 여부 PM/사용자 결정 대상으로 플래그(상태표기/추적성만·기획 4종 본문 무수정). ⑤=기존 계약 실배선(결함 수정·편입 불요)·②=기존 UX 수정.** 6개 문서 기준일 2026-06-14 통일·모순 0 | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/architecture/traceability.md §M·§1-U3·헤더`, `docs/PRD.md 헤더`, `CLAUDE.md` | ref.md 백로그 6건 구현·검증 후 문서-코드 상태 동기화 + ①③④ 스코프 확장 플래그 |
| 2026-06-14 | **백로그 스코프 확장 3건(①③④) 정식 편입** — 직전 2026-06-14 doc-sync가 "결정 대기"로 플래그했던 ① 탭 루트 잠금(US-20.3 동작 확장)·③ FTP 폴더 재귀 업로드(US-12.5)·④ FTP/SFTP 접속 초기 폴더(US-12.3)를 **사용자 정식 편입 결정 → product-planner가 기획 4종에 반영**: user-stories(US-20.3 수용기준 4개·US-12.3 1개·US-12.5 2개 추가·에픽12/20 헤더·백로그표)·features(§M3 연결 진입/업로드 재귀/충돌 배선 3행·§U3 잠금 2분리·수용기준)·PRD(§6 MoSCoW §M3/§U3 동작확장 부기·§8 U3 노트·§12·변경이력)·flows(F16 초기폴더/재귀업로드/충돌 분기·F33 루트 잠금 흐름·§1 IA·§3 예외). **MoSCoW/우선순위 무변경(§M3·§U3 Could 유지)·신규 IPC 채널 0·신규 의존성 0·`SESSION_SCHEMA_VERSION` 무변.** ⑤ 충돌정책=기존 features §M3 "D4 준용" 수용기준의 업로드측 실배선 보완(누락 보완). 상태 단일 출처 roadmap §0.5(2026-06-14 단락 ①③④ "⚠️ 결정 대기" → "✅ 정식 편입 완료" 플래그 해소)에 정합. **상태 🟡 유지(✅위장 금지): 루트 잠금 실 GUI·FTP 3종 실 서버 미검증 — 정식 편입은 스코프 결정이며 런타임 검증과 별개.** traceability 는 직전 doc-sync 매핑 유지(아키텍트 영역·미수정). 6개 문서 기준일 2026-06-14·모순 0 | `docs/user-stories.md 에픽12/20·US-12.3/12.5/20.3·백로그표`, `docs/features.md §M3·§U3·배너`, `docs/PRD.md §6·§8·§12·변경이력`, `docs/flows.md F16·F33·IA·예외`, `docs/roadmap.md §0.5(플래그 해소)`, `CLAUDE.md` | ①③④ 스코프 확장 사용자 정식 편입 결정 → product-planner 기획 4종 반영 + roadmap 플래그 해소 |
| 2026-06-14 | doc-sync 게이트 실행: **§Z Agentic 자연어 파일 에이전트 읽기 전용 범위 구현 완료(코드)·통합 검증 PASS → 상태 표기·추적성 동기화 + 🚧 임시 추적 섹션 제거.** **읽기 전용 범위(US-24.1·24.3·24.4·24.5) 🔜→구현 완료(코드)·실 동작 🟡 / 쓰기(Z2 plan·Z3 confirm-execute=US-24.2)는 사용자 "읽기 전용으로 완성" 결정으로 🔜 deferred(`agent:confirm`=EUNSUPPORTED).** 코드 사실(Grep/Read 실측): `src/main/agent/*`(AgentOrchestrator·agentKeyStore[safeStorage]·providerConfigStore·providerProbe·readBackend·realReadDeps·scope·toolRegistry·limits·modelRouter·models·provider/[LLMProvider·Anthropic·OpenAI·InternalOpenAICompat·createProvider·guardedFetch·ssrfGuard·normalize/])·`ipc/agent.handlers.ts`·`contracts.ts`(agent DTO·`AgentRunReq.context{cwd,selection,locations}`)·`channels.ts AGENT_*`·renderer `infra/api`·`agentSlice`·`usecases/agent`·`ui/agent/AgentPanel`(`Ctrl+Shift+A`/팔레트/사이드바)·`ui/settings/AgentSettings`. 읽기 도구 7종 실배선(list_directory·search_content·read_preview·scan_folder·find_duplicates·compare_folders·list_locations+finish)·멀티 제공자 추상화·function-calling 정규화(스트리밍 델타)·SSRF 가드(화이트리스트·IPv4-mapped IPv6·DNS 리바인딩)·BYO 키 safeStorage·실 probe·경로 스코프(시스템 폴더 차단). **신규 IPC 채널 `agent:*`(동결 후 신기능 선례 동일 규약)·신규 npm 의존성 `@anthropic-ai/sdk`+`openai` 2종(네이티브 0)·`SESSION_SCHEMA_VERSION` 무변·`verify:agent` 201/0·`verify:eslint-agent` 20/0·typecheck(node+web)/build PASS·ESLint 0·회귀 0(store 291·domain 215)·부팅 exit 0.** 상태 단일 출처 roadmap §0.5(2026-06-14 §Z 단락 + §1 요약표 §Z 행 + 헤더)에 기록 → traceability §1-Z(신규 섹션·US-24.1/24.3/24.4/24.5 구현 완료·코드·🟡 / US-24.2 🔜 deferred·실재 파일 매핑·유령 0)·PRD §6/§12/MoSCoW 근거(§Z)·features §Z 배너/제목·user-stories 에픽24/US-24.1~24.5 배지·백로그표·flows F38~F41 헤더·IA 정합. **🚧 임시 추적 섹션 제거: 읽기 전용 구현이 완료돼 상태가 roadmap §0.5(단일 출처)로 이관됨에 따라 `## 🚧 진행 중: Agentic …` 섹션을 사용자 지시("구현 완료 시 삭제")대로 통째 삭제 — 단 Z2/Z3 쓰기 deferred 사실은 roadmap §0.5·traceability §1-Z·user-stories US-24.2에 보존(정보 손실 0).** **정직 한계(✅위장 금지): 헤드리스 verify·코드 정합만 ✅·실 SDK 왕복·실 스트림·실 probe·실 GUI·실 SSRF 네트워크는 미검증(API 키+Electron 필요)·창 렌더 미확인 → 실 동작 🟡.** 스코프/MoSCoW 무변경(상태표기/추적성만·체크박스 `[ ]` 유지)·6개 문서 기준일 2026-06-14 통일·모순 0 | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/architecture/traceability.md §1-Z·헤더`, `docs/PRD.md §6·§12·MoSCoW 근거·헤더·변경이력`, `docs/features.md §Z·배너`, `docs/user-stories.md 에픽24·US-24.1~24.5·백로그표·배너`, `docs/flows.md F38~F41·IA·배너`, `CLAUDE.md(🚧 섹션 제거)` | §Z 읽기 전용 범위 구현 완료·통합 검증 PASS 후 문서-코드 상태 동기화 + 🚧 임시 추적 섹션 제거(roadmap §0.5 이관) + US-24.2 쓰기 deferred 보존 |
| 2026-06-14 | doc-sync 게이트 실행(증분): **§Z 에이전트에 `open_tab` 내비게이션 도구 추가 — "읽기 도구 7종" → "도구 8종" 상태표기·추적성 동기화.** 직전 §Z 읽기 전용(읽기 도구 7종·`verify:agent` 201/0) 이후 `toolRegistry.ts`에 **`open_tab`(`mode:'navigate'` — 신규 분류·'read'/'write'와 구분) 추가**: 새 탭 열고 지정 경로로 이동하는 **비파괴 내비게이션 액션(파일 미변경·확인 불요·즉시 실행)이며 파일 쓰기 도구 아님**(move/copy/rename/mkdir/trash 등 쓰기는 여전히 미구현·deferred). 메커니즘: 도구 실행→`ToolRunCtx.dispatchAction`→`agent.handlers.ts`가 `agent:event`로 `{type:'action',action:'open-tab',path}` 푸시→렌더러 `tabsSlice.newTab(path)`→AgentPanel "🗂 새 탭". **`AgentEvent` 유니온에 `action` 변형 비파괴 추가·`tool-call.mode`에 `'navigate'` 추가·신규 IPC 채널 0(기존 `agent:event` 재사용)·네이티브 0·`SESSION_SCHEMA_VERSION` 무변·상태 등급 무변(읽기 전용 범위 구현 완료(코드)·실 GUI 🟡·쓰기 US-24.2 deferred 🔜 유지).** 상태 단일 출처 roadmap §0.5(2026-06-14 §Z 증분 단락 + §1 요약표 §Z 행 + 헤더)에 기록 → traceability §1-Z(open_tab+`AgentEvent action` 매핑·`verify:agent` 225)·기획 4종(features §Z Z1-a 내비 도구 행·user-stories US-24.1 open_tab 1줄·flows F 에이전트 도구 8종 메모·PRD 헤더/§6/§12/MoSCoW 근거 verify 수치) 상태표기만 정합. **검증: `verify:agent` 201→225/0·`verify:eslint-agent` 20/0·typecheck(node+web)/build PASS·ESLint 0·회귀 0(store 291·domain 215).** **"읽기 도구 7종/`verify:agent` 201/0" 잔존 표기 6개 문서 일괄 8종(7+open_tab)·225/0로 정합(전환 서술용 1건 제외)·모순 0.** **정직 한계(✅위장 금지): 헤드리스 verify·코드 정합만 ✅·실 GUI(open_tab 실 새 탭 표출/경로 이동)·실 SDK는 🟡.** 스코프/MoSCoW·수용기준 본문 무변경(상태표기/추적성만)·6개 문서 기준일 2026-06-14 통일 | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/architecture/traceability.md §1-Z·헤더`, `docs/PRD.md 헤더·§6·§12·MoSCoW 근거·변경이력`, `docs/features.md §Z(Z1-a·배너)`, `docs/user-stories.md US-24.1·백로그표`, `docs/flows.md F38~F41 에이전트 플로우`, `CLAUDE.md` | §Z `open_tab` 내비게이션 도구 추가(증분) 후 "7종→8종"·"201→225" 상태표기·추적성 동기화 |
| 2026-06-12 | doc-sync 게이트 실행: **버그 수정 2라운드(총 20건+보너스 2건) 상태표기·추적성 동기화** — 전문 검증 에이전트 팬아웃+메인 검증으로 시스템 전반 결함 수정. **전부 기존 기능 버그 수정/견고성 개선 — 스코프/MoSCoW/신규 IPC 채널 무변경(`ThumbnailReq.mtime`·`conflictPolicy`=기존 계약 옵셔널 확장)·신규 npm 의존성 0·기획 4종(PRD/features/user-stories/flows) 무수정·상태표기/추적성만.** 라운드1(10건): `engine.ts`(폴더 이동 데이터 손실 방지 `tryRemove`·심볼릭/정션 링크 보존)·`OperationManager.ts`(큐 재개 `Atomics.notify`)·`tabsSlice.ts`(reopenTab ViewState 복원)·`contentSearch.ts`(anchorIndex)·`os/robocopy.ts`(`/UNICODE`+utf16le 한글)·`batchRename.ts`(순환 임시명 복구)·`dedupSlice.ts`(jobId 리셋)·`os/shellVerbs.ts`(데드 분기)·`op.handlers.ts`(robocopy errno 보존). 라운드2(10건+보너스2): archive 추출 conflictPolicy 배선(데이터 손실)·원격 다운로드 conflictPolicy 배선(데이터 손실)·`sort.ts naturalCompare`(>15자리 정밀도)·썸네일 캐시 mtime 키(stale 방지)·`iconCache.ts` LRU 상한·`FtpAdapter.ts`(취소 시 세션 보존)·`ZipWriter`+`ZipReader`(신규 `openZipForRewrite`·재작성 손상 경쟁)·`hashWorker.ts`(취소 verify 거짓 양성)·`CompareView.tsx`(ref 안정)·`dragIntent.ts`(드라이브 루트 순환 가드)+보너스(ZipReader fd 누수·ZipWriter 빈 폴더 엔트리). 상태 단일 출처 roadmap §0.5(2026-06-12 버그 수정 2라운드 단락 + §1 요약표 M7 hash·M9 Q1·§3-M 메모 부기) → traceability §1(conflictPolicy §1-Q1/§1-M·썸네일 mtime §1-L·`openZipForRewrite`·`ThumbnailReq.mtime` 신규 심볼·naturalCompare·dragIntent 영향 메모·유령 0). **typecheck/build/ESLint 0·verify 전 스윕 0 fail(domain 204·archive 56·archiveui 43·hash 46·store 281·persistence 147·remote 24·remote-route 47·remote-trust 42·operations 75·ops 35·contentsearch 38·search 58·concurrency 63·shellverbs 95 등)·zip 재작성/naturalCompare/dragIntent/engine 실 스모크 통과.** **정직 한계(✅위장 금지·🟡): FtpAdapter 취소(실 FTP 미연동)·conflictPolicy(archive 추출·원격 다운로드 typecheck+build만·실 op 왕복 미검증)·썸네일 mtime·robocopy 인코딩·일부 GUI 런타임 스모크 🟡. 미수정(보고만·낮은 확신): ZipReader CP949 한글 엔트리명(iconv 필요)·watchBridge statSync 네트워크 동기 블로킹. 오탐 1건(수정 안 함): `paths.ts FORBIDDEN_NAME_CHARS`(공백·하이픈 허용·NUL 거부 정상).** 기준일 2026-06-12 통일·모순 0 | `docs/roadmap.md §0.5·§1 요약표·§3-M`, `docs/architecture/traceability.md §1·헤더`, `CLAUDE.md` | 버그 수정 2라운드 20건+보너스 2건 코드 사실 동기화(상태표기/추적성만·기획 4종 무수정) + 미수정 2건·오탐 1건 보고 |
| 2026-06-15 | doc-sync 게이트 실행: **백로그 ①/§U3 탭 분할 패널 루트 잠금 결함 수정 상태표기·추적성 동기화** — 잠금 시 각 분할 패널이 "자기 경로"로 독립 고정돼야 하는데 **선택(활성) 패널 경로 하나로 모든 분할 패널이 같이 잠기던 결함**을, 단일 `Tab.lockedRoot(string)` → 패널별 맵 `Tab.lockedRoots(Record<panelId,string>)` 로 변경해 수정. `toggleTabLock`(탭의 모든 패널 순회·각자 경로 캡처)·`lockedRootForPanel`(`lockedRoots[panelId]` 패널 자기 루트 반환)·`restoreWindows`(스냅샷 panelId→새 panelId 재매핑)·`PanelToolbar`(🏠 버튼 패널 자기 루트)·`session.ts`(맵 직렬화)·`defaults.ts coerceTab`(맵 검증 + **구버전 단일 `lockedRoot`→활성 패널 매핑 하위호환** + 미잠금 시 제거)·`tabLock.ts`(주석). **기존 기능(백로그 ①/§U3) 버그 수정 — 스코프/MoSCoW/수용기준 문구 무변경·신규 IPC 채널 0·신규 npm 의존성 0·`SESSION_SCHEMA_VERSION` 무변(비파괴 필드명 변경 + 구버전 호환)·상태 등급 무변(구현 완료(코드)·실 GUI 🟡 유지)·상태표기/추적성만.** 상태 단일 출처 roadmap §0.5(2026-06-15 단락 + 2026-06-14 ① 단락 교차 주석 + §1 요약표 M9 행 ① 심볼 정정 + 헤더 2026-06-15) → traceability §1-U3 드리프트 정정(`Tab.lockedRoot`/`TabSnapshot.lockedRoot` 단일 유령 심볼 → `lockedRoots` 패널별 맵·`restoreWindows` 재매핑·`coerceTab` 하위호환·🏠 패널 자기 루트·유령 0)·기획 4종(PRD §6/§12·features §U3·user-stories US-20.3·flows F33/IA) 코드-심볼 토큰만 `lockedRoots`로 정정. **검증: typecheck(node+web) PASS·`verify:store` 296/0(신규 SPLITLOCK 분할 잠금 테스트: 좌/우 패널 각자 독립 잠금·서로 루트 이동 차단·자기 하위 허용)·`verify:persistence` 147/0·coerce 하위호환 3케이스 일회성 OK.** **정직 한계(✅위장 금지): 헤드리스 verify·코드 정합만 ✅·실 GUI(분할 패널 각자 독립 잠금·🏠 복귀·재시작 후 `lockedRoots` 복원) 런타임 스모크 미실행 🟡.** 역사 기록 보존(은폐 금지): 2026-06-14 ①(단일 lockedRoot) 변경이력/검증 기록 원문 유지 + 교차 주석으로 현행 lockedRoots 명시. 6개 문서 기준일 2026-06-15 통일·모순 0·의사결정 필요 항목 없음 | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/architecture/traceability.md §1-U3·헤더`, `docs/PRD.md §6·§12·헤더`, `docs/features.md §U3·배너`, `docs/user-stories.md US-20.3·§U 배너·백로그표`, `docs/flows.md F33·IA`, `CLAUDE.md` | 백로그 ①/§U3 탭 분할 패널 루트 잠금 결함 수정 후 문서-코드 상태 동기화 + traceability 단일→패널별 맵 드리프트 정정 |
| 2026-06-22 | doc-sync 게이트 실행: **P7 배포 트랙 — GitHub Releases 기반 자동 업데이트(electron-updater) 도입 상태표기·추적성 동기화** — 직전까지 P7에서 "🔜 향후/미착수"였던 자동 업데이트(PRD §7·ADR-006 계획 항목·**스코프 일탈 아님**)가 구현·게시됨에 따라 "🔜→구현 완료(코드)·1.9.2 실 게시 ✅·실 설치본 자동 업데이트 왕복 🟡"로 갱신. 코드 사실(Grep/Read): 신규 `src/main/os/autoUpdate.ts`(`initAutoUpdate()`·`electron-updater` `checkForUpdatesAndNotify`·`app.isPackaged` 한정·throw 0 격리·CJS default import 후 구조분해로 named-import 크래시 우회)·`src/main/index.ts`(`app.whenReady()` 내 `diskTypeService.refresh()` 직후 `initAutoUpdate()` 호출)·`electron-builder.yml`(`publish: null`→`github`·SimJaeSugn/AGT-Explorer·`releaseType: release`)·`package.json`(`electron-updater ^6.8.9`·`release` 스크립트·버전 1.9.0→1.9.2)·`.gitignore`(`electron-builder.env`)·`README.md` 신규·`ADR-006` 갱신. **신규 npm 의존성 `electron-updater` 1종(네이티브 0)·신규 IPC 채널 0(메인 전용·렌더러 미관여)·`SESSION_SCHEMA_VERSION` 무변.** 상태 단일 출처 roadmap §0.5(2026-06-22 단락 + §0.5 P7 행 + §1 요약표 P7 행 + §P7 단계 산출물/DoD + 헤더/§0.5 기준일 2026-06-22) → traceability §1-P7(자동 업데이트 매핑 행 추가·유령 0·헤더 줄)·PRD §7 릴리스/패키징·§12 M4·헤더 기준일. 기획 4종 중 features/user-stories/flows는 P7 패키징/devops 항목이라 무수정(자동 업데이트는 사용자 노출 기능 아님·스코프 일탈 아님). **검증: `npm run typecheck:node` PASS·electron-vite `npm run build` PASS·빌드된 메인을 실제 electron으로 부팅 검증 통과(SyntaxError/exception 0·정상 기동)·1.9.2 GitHub Releases 실 게시 완료(exe·blockmap·latest.yml 3종)·깨진 1.9.1 삭제·verify 누계 불변(헤드리스 범위 밖·신규 케이스 0).** **정직 한계(✅위장 금지): 코드 정합·typecheck·build·부팅·실 게시까지 ✅. "패키징 NSIS 설치본 실제 설치 후 자동 업데이트 왕복(감지→다운로드→재시작 설치)"는 미검증 🟡·코드서명 미적용(미서명 빌드).** P7 전체는 여전히 🟡(성능 실측·실제 코드서명·NSIS 설치/왕복 실측 잔여). 스코프/MoSCoW/수용기준 본문 무변경(상태표기·추적성만)·기준일 2026-06-22 통일·모순 0·의사결정 필요 항목 없음 | `docs/roadmap.md §0.5·§1 요약표·§P7·헤더`, `docs/architecture/traceability.md §1-P7·헤더`, `docs/PRD.md §7·§12·헤더`, `CLAUDE.md` | P7 자동 업데이트(electron-updater·GitHub Releases) 구현·1.9.2 실 게시 후 문서-코드 상태 동기화 |
| 2026-06-28 | doc-sync 게이트 실행: **v1.13.0 게시 UI/UX 변경 4건(동작 확장 3 + 렌더러 버그 수정 1) 상태표기·추적성 동기화** — 커밋 `c176405`·태그/게시 `v1.13.0`(GitHub Releases)·`package.json` 1.12.4→1.13.0. ① **§Y1 Windows 셸 verb 섹션을 "Windows 메뉴 ▸" 단일 하위 메뉴(플라이아웃)로 모음**(`shellVerbsSection.ts WinVerbsMenuItem.children` 신규·`buildWinVerbsSection` separator+단일 submenu·`ContextMenu.tsx` 기존 Flyout 재사용·`scripts/verify-shellverbs.ts` submenu 분기로 갱신·메뉴 길이 단축 UX) · ② **컨텍스트 메뉴 "터미널 열기(Claude)" 추가**(`ShellOpenTerminalReq.launch?:'claude'`·`guard.zShellOpenTerminalReq` `z.literal('claude').optional()`·`shell.ts openTerminal(dir,launch?)` 3단 폴백 모두 `powershell -NoExit -Command claude`·`shell.handlers`·`infra/api`·`open.ts openTerminalAt(path,launch?)`·`contextMenu.ts` 빈영역·파일/폴더 양쪽 `terminal-claude` 행·고정 리터럴만·주입 무해) · ③ **사이드바 빠른 위치 "문서·사진" 추가**(`KnownFoldersDTO.pictures` 신설·`fs.handlers.ts app.getPath('pictures'/'documents')`·`lucide.tsx FileTextIcon`/`ImageIcon`·`Sidebar.tsx` 문서·사진 노드·노출 조건 확장) · ④ **작은/중간/큰 아이콘 보기 파일명 2줄 하단 잘림 수정**(`tokens.ts gridCell` small 72→84·medium 96→100·large 120→124·`gridCellFor` 단일 출처·렌더러 전용). **전부 기존 기능 동작 확장(①§Y1·②H4·③§X1) 또는 렌더러 버그 수정(④J3) — 신규 IPC 채널 0(전부 기존 계약 옵셔널 확장/렌더러 전용)·신규 npm 의존성 0·`SESSION_SCHEMA_VERSION` 무변·MoSCoW/스코프/수용기준 본문 무변경(상태표기·추적성만).** 상태 단일 출처 roadmap §0.5(2026-06-28 단락 + §1 요약표 H장/J장/§X1/§Y1 행 부기 + 헤더 2026-06-28) → traceability 헤더 갱신줄·§1-Y(submenu·`verify:shellverbs` 95→99)·H4 `shell:open-terminal`(launch 확장)·§1-X1(pictures·문서/사진 노드·정직 범위 정정)·J3 그리드(`tokens.gridCell`) 매핑 정정·PRD §12 §Y 행 verify 95→99·헤더 기준일/§Y verify 75→99 정합. **검증: `npm run build`(typecheck node+web + electron-vite) PASS·`verify:shellverbs` 99/0·변경 파일 ESLint 0·부팅 스모크 미실행.** **정직 한계(✅위장 금지·🟡): 실 GUI(Windows 메뉴 하위 펼침·터미널(Claude) 실 claude 기동·빠른위치 문서/사진 클릭 이동·작은 아이콘 실 렌더 잘림 해소)는 Electron 미실행으로 런타임 스모크 🟡.** **드리프트(결정 대기·기획 본문 무수정): (a) §Y1 features/US-23.1 "단일 선택 한정" 기존 드리프트 유지(2026-06-12 보고분)·submenu화는 표시 변경이라 수용기준 위배 아님 / (b) features §X1·US-22.1 제목/수용기준이 "빠른 위치 ▸ 다운로드"만 명시 — 바탕화면(2026-06-11)+문서·사진(2026-06-28) 노드는 본문 미수록 동작 확장 / (c) features §H4·US-7.4 수용기준이 "터미널 열기"만 명시 — "터미널 열기(Claude)" 신규 메뉴 항목 본문 미수록. (b)(c) 기획 정식 편입 여부 PM/사용자 결정 대기(본문 무수정·상태표기/추적성만).** 상태 출처 3종(roadmap §0.5·traceability·PRD) 기준일 2026-06-28 통일·문서 간 §Y1/§X1/H4/J3 상태표기 모순 0 | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/architecture/traceability.md 헤더·§1-Y·H4·§1-X1·J3`, `docs/PRD.md §12·헤더`, `CLAUDE.md` | v1.13.0 UI/UX 4건 구현·게시 후 문서-코드 상태 동기화 + §X1·H4 동작 확장 드리프트 보고 |
| 2026-06-30 | doc-sync 게이트 실행: **⚠️ 비계획 구현 1건(V14 드라이브 토폴로지 변경 자동 갱신 — USB 등 연결/해제) 스코프 일탈 플래그 + 상태표기·추적성 동기화** — 사용자 보고 버그("USB 연결 시 우측 패널 드라이브 미갱신") 수정. 코드 사실(Grep/Read 실측): 신규 `src/main/os/deviceChange.ts`(`initDeviceChangeWatch(win)`·`BrowserWindow.hookWindowMessage(0x0219 WM_DEVICECHANGE)`·1.2초 디바운스 `broadcastDrivesChanged`→열린 전 창 `fs:drives-changed` 푸시 + `driveTypeService.refresh()`/`diskTypeService.refresh()`·`process.platform!=='win32'` no-op·hookWindowMessage 실패 try/catch 격리 throw 0·창 closed 타이머 정리)·`src/main/index.ts`(primary 창 직후 `initDeviceChangeWatch(mainWindow)`)·`channels.ts`(`FS_DRIVES_CHANGED:'fs:drives-changed'` + EVENT_CHANNELS 등록)·`contracts.ts`(`DrivesChangedEvt`=Record<string,never> + IpcEventMap)·`preload/api.ts`(`fs.onDrivesChanged`)·`renderer/infra/api`(`subscribeDrivesChanged`)·신규 `renderer/app/usecases/drivesBridge.ts`(`initDrivesBridge` 전역 1회 구독→수신 시 ① `loadDrives()` ② `path===''\|\|isMyPc(path)` 패널만 `refresh(id,{preserve:true})` ③ `loadDriveUsage()`)·`app/stores/sidebarSlice.ts`(`loadDrives` 병합화 — 살아있는 드라이브 노드 expanded/childPaths/loading 보존·라벨 갱신·신규 추가·사라진 루트 제거)·`renderer/ui/App.tsx`(부팅 `initDrivesBridge()` 1회). **신규 invoke 채널 0·신규 푸시 evt 1종 `fs:drives-changed`만(P1 동결 후 푸시 신기능 선례 `app:open-path`·`fs:watch:*` 동일 규약·EVENT_CHANNELS 정식 등록·동결 위반 아님)·신규 npm 의존성 0(Electron 내장 `hookWindowMessage`·네이티브 0)·`SESSION_SCHEMA_VERSION` 무변.** **⚠️ 스코프 판정: 이 동작(드라이브 연결/해제 자동 감지·갱신)은 PRD/features/user-stories/flows 미수록 신규 스코프(스코프 일탈) — J2(US-9.2 패널 실시간 갱신·`fs:watch:*`)는 "열린 디렉토리 내부 파일 변경(생성·삭제·이름변경·이동)" 워처일 뿐 드라이브 목록(토폴로지) 변경(마운트/언마운트)은 수용기준 미명시·별개 동작. 기획 4종 본문 무수정·MoSCoW 무변경 — roadmap §0.5(2026-06-30 단락 + §1 요약표 "비계획 1건 V14" 행 + 헤더 2026-06-30) + traceability(헤더 갱신줄 + §1-V V14 행/메모·유령 0)에 코드 사실만 기록·PM/사용자 정식 편입 결정 대기(V1~V3·V11~V12 스코프 일탈 플래그 선례 동일).** **검증: `npm run build`(typecheck node+web + electron-vite) PASS·변경 9파일 ESLint 0·`verify:store` 296/0(loadDrives 병합 변경 포함 회귀 0)·`verify:persistence` 147/0·verify 누계 불변(헤드리스 범위 밖·신규 케이스 0).** **정직 한계(✅위장 금지·🟡): 헤드리스 검증(타입체크·빌드·ESLint·store/persistence)만 통과 — 실 USB 물리 연결/해제→WM_DEVICECHANGE 수신→사이드바/"내 PC" 패널/대시보드 실 GUI 갱신은 Electron 미실행·물리 디바이스 부재로 미검증 → 런타임 스모크 🟡.** 상태 출처 2종(roadmap §0.5·traceability) 기준일 2026-06-30 통일·모순 0 | `docs/roadmap.md §0.5·§1 요약표·헤더`, `docs/architecture/traceability.md 헤더·§1-V(V14)`, `CLAUDE.md` | V14 드라이브 토폴로지 자동 갱신 비계획 구현 스코프 일탈 플래그 + 코드 사실 동기화 |
| 2026-06-30 | **V14 드라이브 자동 갱신 사용자 정식 편입 결정 → 기획 4종 반영 + traceability 정식 편입 + roadmap 플래그 해소** — 직전 2026-06-30 doc-sync가 "⚠️ 비계획·스코프 일탈·결정 대기"로 플래그했던 V14(USB 등 이동식/네트워크 드라이브 연결·해제 토폴로지 변경 자동 감지→사이드바 트리·"내 PC" 패널·대시보드 재열거)를 **사용자 정식 편입 결정**으로 product-planner + chief-architect 역할로 기획 4종에 정식 추가: user-stories(에픽9 신규 **US-9.8** 드라이브 연결/해제 자동 갱신·수용기준 6개·백로그표·헤더)·features(§J **J8** 신규 기능 항목·수용기준·§J 배너)·PRD(§6 MoSCoW Should 1항목 `[x]`·§12 마일스톤 후속 **M12** 행·헤더 기준일 2026-06-28→2026-06-30·변경이력)·flows(신규 **F42** USB 연결/해제→`WM_DEVICECHANGE`→1.2초 디바운스→`fs:drives-changed` 푸시→재열거·§3 예외 행·헤더). **배치 원칙: 기존 J2(US-9.2 패널 실시간 감시·`fs:watch:*`)와 인접하되 별개 동작**(디렉토리 내부 변경 워처 ≠ 드라이브 마운트/언마운트) — J2 수용기준 미변경·§J에 별도 항목 신설. **MoSCoW Should·신규 invoke 채널 0·신규 푸시 evt 1종 `fs:drives-changed`·신규 npm/네이티브 의존성 0(Windows 내장 `BrowserWindow.hookWindowMessage`)·`SESSION_SCHEMA_VERSION` 무변(편입은 스코프 결정·코드/검증 무변).** traceability §1-V V14 행·헤더·§V 추가(2026-06-30) 메모를 "⚠️ 비계획·스코프 일탈" → "정식 편입(US-9.8 매핑)"으로 격상(실재 파일 매핑 유지·유령 0). roadmap §0.5(2026-06-30 단락)·§1 요약표 V14 행의 스코프 일탈 플래그를 "✅ 정식 편입 완료(2026-06-30·US-9.8)"로 해소(역사 보존 — 기존 비계획 기록 원문 유지·플래그만 해소). **상태 🟡 유지(✅위장 금지): 정식 편입은 스코프 결정이며 실 USB 물리 연결/해제→실 GUI 자동 갱신·NSIS 설치본 자동 업데이트 왕복은 미검증 — 런타임 검증과 별개.** 6개 문서 기준일 2026-06-30 통일·모순 0 | `docs/user-stories.md 에픽9·US-9.8·백로그표·헤더`, `docs/features.md §J·J8·배너`, `docs/PRD.md §6·§12·헤더·변경이력`, `docs/flows.md F42·F10 인접·§3 예외·헤더`, `docs/architecture/traceability.md §1-V·헤더`, `docs/roadmap.md §0.5·§1 요약표·헤더`, `CLAUDE.md` | V14 사용자 정식 편입 결정 → 기획 4종 반영 + traceability 정식 편입 + roadmap 플래그 해소 |
| 2026-07-08 | doc-sync 게이트 실행(사용자 직접 요청·v1.14.0 예정): **미커밋 구현 2건 상태표기·추적성 동기화 + P7 자동 업데이트 매핑 드리프트 정정** — ① **§H H4 "터미널 열기(관리자 권한)" 동작 확장**(기존 `shell:open-terminal`에 `ShellOpenTerminalReq.admin?:true` 비파괴 옵셔널 확장·`main/os/shell.ts#openTerminalElevated()` 신규[PowerShell `Start-Process -Verb RunAs` UAC 승격 릴레이·`Get-Command wt.exe` 1회 판정·경로 env `EXPLORER_TERMINAL_DIR`·인자 PS 배열·명령행 합성 0·ADR-005 유지]·`shell.handlers.ts openTerminal(cwd,launch,admin)`·`guard.ts admin=z.literal(true).optional()`·`renderer infra/api·open.ts openTerminalAt(path,launch,admin)`·`contextMenu.ts` 빈영역·파일/폴더 두 블록 `terminal-admin` 항목) — **기존 H4 동작 확장·신규 채널 0**(2026-06-28 launch='claude' 확장과 동일 패턴) / ② **P7 자동 업데이트 UX 확장(시작 시 확인 + 설정 아이콘·"소프트웨어 정보" 배지)**(기존 사용자 주도 인프라 `autoUpdate.ts`·`update:*` 4종·`update.handlers.ts` 재사용·렌더러 `uiSlice.updateAvailable`/`updateLatestVersion`/`setUpdateAvailable`·`update.ts initUpdateBridge()`[`update:status` 구독+시작 시 `checkForUpdate()` 1회·다운로드 안 함]·`App.tsx` 부트·`TabBar.tsx UpdateBadge`·`SettingsDialog.tsx` About 배지) — **PRD §7·ADR-006 자동 업데이트 계획 범위 내 UX 확장·신규 채널 0·렌더러 전용**. **둘 다 기존 기능·기존 계획 범위 내 — 신규 IPC 채널 0·신규 npm 의존성 0·`SESSION_SCHEMA_VERSION` 무변·MoSCoW/스코프/수용기준 문구 무변경(상태표기·추적성만)·스코프 일탈 아님.** **⚠️ 드리프트 정정(중요): 직전 2026-06-22 P7 자동 업데이트 매핑이 "`checkForUpdatesAndNotify`·자동 다운로드·신규 IPC 채널 0·렌더러 미관여·메인 전용"으로 기술됐으나, 그 이후 커밋 `5fbff28`(v1.9.3 "소프트웨어 정보" 탭)·`e8a5eb8`(v1.9.4 "사용자 주도 자동 업데이트")로 메커니즘이 사용자 주도(autoDownload=false·`checkForUpdate/downloadUpdate/quitAndInstallUpdate`·`update:*` 4종·`update.handlers.ts`·렌더러 `updateApi`/설정 About)로 전면 교체됐고 이 두 커밋이 doc-sync 없이 머지됨 → 문서가 유령 심볼(`checkForUpdatesAndNotify`)·오기("신규 채널 0·렌더러 미관여")를 보유. traceability §1-P7 자동 업데이트 행·헤더·roadmap §0.5 P7 행·§P7 산출물을 실코드로 정정(역사 보존 — 2026-06-22 단락 원문 유지·경위 명시).** **검증: `npm run typecheck`(node+web) PASS·변경 12파일 ESLint 0·`npm run verify:shell-h4h6` 25/0(①의 옵셔널 admin 비파괴 확장·무회귀 재확인)·`npm run build`(electron-vite) PASS(②)·verify 누계 불변(계약 비파괴 확장/렌더러 계층·신규 케이스 0).** **정직 한계(✅위장 금지·🟡): 헤드리스 verify·typecheck·build·코드 정합만 ✅. ① 실 UAC 승격 터미널 기동(UAC 승인→관리자 wt/PowerShell 창)은 인터랙티브 GUI 세션 필요 → 미검증 🟡·② 실 "새 버전 있음" 배지 표출·확인 왕복은 설치본(packaged)+GitHub Releases 상위 버전 게시 시에만 발생(dev=EUNSUPPORTED→배지 없음) → 미검증 🟡.** **⚠️ 드리프트(결정 대기·기획 본문 무수정): ①의 "터미널 열기(관리자 권한)" 신규 메뉴 항목은 features §H4·US-7.4 수용기준("터미널 열기"만) 미수록(2026-06-28 "터미널 열기(Claude)" (c) 드리프트와 동종)·기획 정식 편입 여부 PM/사용자 결정 대기.** 기획 4종(PRD/features/user-stories/flows) 본문 무수정(P7 devops·H4 세부 메뉴는 2026-06-22·2026-06-28 선례대로 상태표기/추적성만). 상태 단일 출처 roadmap §0.5(2026-07-08 단락)·§1 요약표 H장/P7 행·헤더/§0.5 기준일 2026-07-08 통일·모순 0 | `docs/roadmap.md §0.5·§1 요약표·§P7·헤더`, `docs/architecture/traceability.md §1-H(H4 admin)·§1-P7(사용자 주도 정정)·헤더`, `CLAUDE.md` | 미커밋 구현 2건(§H H4 관리자 터미널·P7 자동 업데이트 UX) 동기화 + P7 자동 업데이트 매핑 드리프트 정정 + ① 수용기준 드리프트 결정 대기 플래그 |
| 2026-07-14 | doc-sync 게이트 실행(증분): **⚠️ 비계획 구현 1건(V15 가칭 — `Ctrl+N` 새 창(현재 위치 복제) 단축키) 스코프 일탈 플래그 + 상태표기·추적성 동기화** — Windows 탐색기 관례의 `Ctrl+N`을 `window.new` 명령으로 추가하고, 기존 §U3 멀티윈도우 인프라의 **dead code(정의만·호출부 0)** 였던 `renderer/app/usecases/windowSplit.ts#openEmptyWindow()`를 진입점으로 배선. 코드 사실(Grep/Read·`git status` 실측·변경 3파일·렌더러 전용): `renderer/domain/keybindings/index.ts`(`{chord:'ctrl+n', commandId:'window.new', context:'global', label:'새 창(현재 위치 복제)', group:'창'}` — KEYBINDINGS 단일 출처·신규 group '창' 첫 등장)·`renderer/app/usecases/commandBus.ts`(`case 'window.new'`→`openEmptyWindow()`)·`windowSplit.ts`(본문 로직 무변경·JSDoc만 정정). 동작: 활성 탭 스냅샷(`buildTabSnapshot`)을 **기존 `window:split-tab`** 으로 넘겨 새 창 생성 — 탭 분리(`splitTabToNewWindow`·US-20.3)와 달리 **소스 탭을 닫지 않음(복제)**·새 창=primary=false → 세션 자동저장 미참여(§U3 정직 한계 승계). 파생(추가 코드 0): 명령 팔레트·단축키 도움말이 `keyBindingRegistry.listBindings()` 파생이라 자동 노출. **신규 IPC 채널 0(기존 `window:split-tab` 재사용)·신규 npm 의존성 0·`SESSION_SCHEMA_VERSION` 무변·main 무변경.** **⚠️ 스코프 판정(일탈·결정 대기): `Ctrl+N`은 PRD §8 단축키 표 미수록·features/user-stories/flows에 "새 창(복제)" 항목/스토리 없음(§U3·US-20.3은 소스 탭을 옮기는 "탭 분리"로 복제와 별개 동작) — 인프라는 기존 §U3 재사용이나 사용자 노출 신규 명령·신규 전역 단축키·팔레트/도움말 신규 항목이 늘어남 → 기획 4종 본문 무수정·MoSCoW 무변경·roadmap §0.5(2026-07-14 단락 + §0.5 표 V15 행 + M9/§U3 행 부기 + 헤더 2026-07-14) + traceability(헤더 갱신줄 + §2 단축키 표 `Ctrl+N` 행 + §1-U3 진입점 부기·유령 0)에 코드 사실만 기록·PM/사용자 정식 편입 결정 대기(선례 V1~V3·V5·V14·2026-06-18 compact 창).** **검증: `npm run typecheck`(node+web) PASS·변경 3파일 ESLint 0·`verify:palette` 20/0·`verify:domain` 215/0·`verify:store` 296/0(회귀 0·신규 verify 케이스 0)·일회성 레지스트리 스모크 OK(`global/list ctrl+n`→`window.new`·`dialog`/`addressEdit` 미발화·`ctrl+shift+n`=`file.newFolder` 무충돌·부팅 충돌 assert 통과)·verify 누계 불변.** **정직 한계(✅위장 금지·🟡): Electron 미실행 — 실제 `Ctrl+N` 입력→새 창 표출→현재 위치 부팅 실 GUI 동작 미검증.** 상태 출처 2종(roadmap §0.5·traceability) 기준일 2026-07-14 통일·모순 0(기획 4종 기준일은 편입 결정 전이라 무변 — 2026-06-30 V14 선례) | `docs/roadmap.md §0.5·§0.5 표(V15·M9 행)·헤더`, `docs/architecture/traceability.md 헤더·§2 단축키 표·§1-U3`, `CLAUDE.md` | `Ctrl+N` 새 창(복제) 단축키 비계획 구현 스코프 일탈 플래그 + 코드 사실 동기화 |
| 2026-07-14 | **`Ctrl+N` 새 창(현재 위치 복제) 사용자 정식 편입 결정 → 기획 4종 반영(product-planner) + 개발문서 플래그 해소·추적성 정식 편입(doc-sync) + v1.15.0 실 게시 기록** — 직전 2026-07-14 doc-sync가 "⚠️ 비계획(V15 가칭)·스코프 일탈·결정 대기"로 플래그했던 `Ctrl+N` 새 창(복제)을 **사용자 정식 편입 결정** → product-planner가 기획 4종에 반영: PRD(§6 Should `[x]`+🟡 배지·**§6 "MoSCoW 분류 근거(2026-07-14 §U5)" 표 신설**·**§8 단축키 표 신규 그룹 "창" + `Ctrl+N` 행**·§12 후속 **M13** 행·헤더 2026-07-14·변경이력)·features(**§U5 신규 절** — U3 이동 vs U5 복제 비교표·수용기준 9개·§U 챕터 제목 "…·탭·창 UX"·배너)·user-stories(**US-20.5 신규 스토리** — 수용기준 9개 `[x]`7/`[ ]`2[정직 한계·범위 밖]·백로그 요약표·에픽20 제목/배너)·flows(**F43 신규 플로우** — `Ctrl+N`→`window.new`→`buildTabSnapshot`→`window:split-tab`→새 창 부팅→소스 탭 유지·§1 IA "창(멀티 윈도우) (U3·U5)"·§3 예외 2행[새 창 열기 실패/새 창 세션 미복원]). **식별자: 기능 §U5 · 스토리 US-20.5 · 플로우 F43 · MoSCoW Should(규모 S).** 개발문서(doc-sync): roadmap §0.5 2026-07-14 단락의 "⚠️ 스코프 일탈·결정 대기" → **"✅ 정식 편입 완료(2026-07-14·§U5·US-20.5·F43·Should)"** 로 플래그 해소(**역사 보존 — 원 비계획 기록 원문 유지·해소 경위 명시**·선례 2026-06-30 V14)·§0.5 상태표 "V15 가칭" 행 → **§U5 정식 식별자로 격상**·§1 요약표/§P7/헤더 정합. traceability: **§1-P~1-U 표에 `U5` 매핑 행 신설**(§1-U3 인접·**U3=탭 이동(소스 탭 닫음) vs U5=현재 위치 복제(소스 탭 유지)** 구분 명시 — 실재 확인: `renderer/domain/keybindings/index.ts`[`ctrl+n`→`window.new`·group '창'·L66~67]·`commandBus.ts`[`case 'window.new'`·L117~119]·`windowSplit.ts#openEmptyWindow`[L87~93·`buildTabSnapshot(activeTabId)`→`windowApi.splitTab`·`closeTab` 미호출·실패 토스트]·기존 `window:split-tab`/`windowManager.createSplitWindow` 재사용·**유령 매핑 0**)·§2 단축키 표 `Ctrl+N` 행의 "⚠️ PRD §8 미수록·결정 대기" → **"PRD §8 정식 수록(그룹 '창')"** 정정·헤더 갱신줄. **[릴리스] v1.15.0 게시 완료(P7 배포 트랙)**: `package.json` 1.14.0→**1.15.0**·커밋 `acc30bf`·태그 `v1.15.0`(main+태그 push)·`npm run release`(electron-vite build + electron-builder NSIS + `--publish always`) → **GitHub Releases v1.15.0 실 게시(draft=false)**·자산 3종(`AGT-Finder-Setup-1.15.0.exe`·`.exe.blockmap`·`latest.yml`)·릴리스 노트 작성 — 빌드 1회 실패(`dist\win-unpacked\resources\app.asar` 파일 잠금·RELEASE_DEPLOY_PROMPT §8 함정 3) 후 `dist` 삭제·재시도 성공(**코드/설정 변경 0**). roadmap §0.5 P7 행·§1 요약표 P7·§P7 DoD·§5 정리에 "v1.15.0 NSIS 패키징+실 게시 ✅" 기록(2026-06-22 "1.9.2 실 게시" 기록 방식 준수). **신규 IPC 채널 0·신규 npm 의존성 0·`SESSION_SCHEMA_VERSION` 무변·렌더러 전용(main 무변경)·MoSCoW/스코프 결정은 사용자 몫(코드/검증 무변).** **검증 재확인: `verify:palette` 20/0·`verify:domain` 215/0·`verify:store` 296/0(회귀 0)·typecheck(node+web) PASS·ESLint 0·NSIS 패키징+실 게시 ✅.** **정직 한계(✅위장 금지·🟡): 실 `Ctrl+N` 입력→새 창 표출·현재 위치 부팅 실 GUI 미검증·설치본 자동 업데이트 왕복 미검증·코드서명 미적용(미서명 빌드·SmartScreen 경고 정상). 새 창(primary=false)은 세션 자동저장 미참여 → 재시작 복원 안 됨(§U3 한계 승계·의도적·US-20.5 미체크 수용기준 2건·flows §3 예외 수록).** 6개 문서 기준일 2026-07-14 통일·모순 0·의사결정 필요 항목 없음(잔여 드리프트는 기존 미결 항목만: §Y1 다중선택·H4 터미널(Claude)/(관리자 권한)·§X1 문서/사진 노드·2026-06-18 compact 창) | `docs/roadmap.md §0.5·§0.5 표(§U5 행)·§1 요약표·§P7·헤더`, `docs/architecture/traceability.md 헤더·§1-U5(신설)·§1-U3·§2 단축키 표`, `CLAUDE.md` | `Ctrl+N` 새 창(복제) 정식 편입 결정 → 개발문서 플래그 해소 + §U5 추적성 정식 편입 + v1.15.0 게시 기록 |
| 2026-07-14 | doc-sync 게이트 실행: **렌더러 버그 수정 1건(설정 > 레이아웃 > 단축아이콘 — 아이콘 키 문자열이 한글 라벨과 겹쳐 보이던 결함) 상태표기·추적성 동기화 + v1.15.1 실 게시 기록** — 코드 사실(`git show 34bbcef`·Grep/Read 실측·변경 1파일[+package.json 버전]·렌더러 전용): `renderer/ui/settings/SettingsDialog.tsx#LayoutCategory`의 단축아이콘(V11 아이콘바 커스터마이즈) 목록이 `<Icon>` 컴포넌트 대신 **아이콘 팩 키 문자열**(`it.icon`=`'panelLeft'`·`'splitView'` 등 영문)을 `<span aria-hidden style={{width:16, textAlign:'center'}}>`에 텍스트로 출력 → 16px 칸을 넘친 영문이 옆의 한글 라벨(`it.label`)과 시각적으로 겹치던 결함을 **`<Icon name={it.id === 'theme.toggle' ? 'theme' : it.icon} size={16} />`**(+`import { Icon } from '@renderer/ui/icons/lucide'`)로 수정 — 상단 아이콘바 실제 렌더 경로(`ui/toolbar/IconBar.tsx#IconButton`·`theme.toggle`→`'theme'` 특수 처리 포함)와 **동일 경로로 통일**. 회귀 점검: `ICON_BAR_ITEMS`(`ui/toolbar/iconBarItems.ts`) 아이콘 키 25종 전부 `ui/icons/lucide.tsx ICON_PATHS`에 실재(빈 아이콘 회귀 0)·같은 파일 좌측 네비 `CATEGORIES`의 `{c.icon}`은 **이모지**라 텍스트 출력이 정상(수정 대상 아님). **기존 기능(H1 아이콘바 / V11 설정 단축아이콘) 렌더러 전용 표시 버그 수정 — 스코프·MoSCoW·수용기준·신규 IPC 채널 0·신규 npm 의존성 0·`SESSION_SCHEMA_VERSION`/`SETTINGS_SCHEMA_VERSION` 무변·스코프 일탈 아님 → 기획 4종(PRD/features/user-stories/flows) 본문 무수정(2026-06-12 "버그 수정 2라운드" 선례)·개발문서 2종 + CLAUDE.md만 갱신(상태표기/추적성만).** 상태 단일 출처 roadmap §0.5(2026-07-14 렌더러 버그 수정 단락 + §0.5 상태표 "비계획 3건 V11~V13" 행 부기 + P7 행 v1.15.1 게시 + §5 정리 P7 bullet) → traceability(헤더 갱신줄 + §1-H H1 아이콘바 행[`IconButton`→`Icon`·아이콘 키 25종 `ICON_PATHS` 실재·설정 커스터마이즈 UI 교차] + §1-V V11 행[`LayoutCategory` `<Icon>` 렌더 경로·이모지 예외 명시]·유령 매핑 0). **[릴리스] v1.15.1 게시 완료(P7 배포 트랙)**: `package.json` 1.15.0→**1.15.1**·커밋 `34bbcef`·태그 `v1.15.1`·**GitHub Releases 실 게시(draft=false)**·자산 3종(`AGT-Finder-Setup-1.15.1.exe`·`.exe.blockmap`·`latest.yml`)·릴리스 노트 작성·**코드서명 미적용(미서명 빌드)**(2026-06-22 "1.9.2 실 게시"·2026-07-14 "v1.15.0 실 게시" 기록 방식 준수). **검증: `npm run typecheck:web` PASS·변경 파일 ESLint 0·`npm run build`(electron-vite) PASS·NSIS 패키징 성공·verify 누계 불변(렌더러 표시 계층·신규 verify 케이스 0·회귀 0).** **정직 한계(✅위장 금지·🟡): Electron 미실행 — 실제 설정 화면에서 겹침 해소·아이콘 정상 표출되는 실 GUI 동작 미검증 → 런타임 스모크 권장.** 상태 출처 2종(roadmap §0.5·traceability) 기준일 2026-07-14 통일·모순 0·**신규 드리프트/스코프 일탈/의사결정 필요 항목 0**(잔여는 기존 미결 항목만: §Y1 다중선택·H4 터미널(Claude)/(관리자 권한)·§X1 문서/사진 노드·2026-06-18 compact 창) | `docs/roadmap.md §0.5·§0.5 상태표·§P7·§5`, `docs/architecture/traceability.md 헤더·§1-H(H1)·§1-V(V11)`, `CLAUDE.md` | 설정 단축아이콘 아이콘 렌더 결함 수정(v1.15.1 게시) 후 문서-코드 상태 동기화 |
