# 추적성 매핑 (기획 → 설계) — AGT-Finder (코드네임 Explorer)

> **갱신: 2026-06-12(doc-sync·§Y 구현 완료·통합 QA PASS → 상태 동기화 + 설계→구현 드리프트 정정)** — **§1-Y를 "🔜 설계 완료·구현 전"→"구현 완료(코드)·실 GUI/실 패키지 🟡"로 갱신**: ADR-013 설계 → T1~T6 구현 → 통합 QA PASS([qa-integration-Y](../reviews/qa-integration-Y.md)). 예정 파일/심볼을 **실재 파일로 정정**(grep·typecheck/build PASS·`verify:shellverbs` 75/0·유령 매핑 0): `main/os/shellVerbs.ts#shellVerbsService`·**`main/os/shellVerbsBlacklist.ts`(신규 분리·설계엔 없던 순수 로직 파일)**·`main/os/shellVerbsWorker.ps1`·`shell.handlers.ts`(`SHELL_CONTEXT_VERBS`/`SHELL_INVOKE_VERB`)·`guard.ts §Y1`·`main/index.ts`(before-quit dispose)·`renderer/app/usecases/shellVerbs.ts#loadWinVerbs`/`invokeWinVerb`·**`shellVerbsSection.ts#buildWinVerbsSection`(신규 분리)**·`contextMenu.ts`/`ContextMenu.tsx`/`uiSlice`·`preload/api.ts`·`renderer/infra/api/index.ts`·`dto/index.ts ShellVerbDTO`+`FileOpErrorCode 'EVERB'`. **신규 IPC 채널 2종·신규 의존성 0·`SESSION_SCHEMA_VERSION` 무변·실 GUI/실 패키지 런타임 스모크 🟡(✅ 위장 금지)·`verify:worker` FAIL은 §Y 무관 사전 환경 결함(§Y 회귀 아님).** 상태 단일 출처 roadmap §0.5(2026-06-12 §Y 단락). 아래는 직전 설계 단계 기록(경과 보존).
> **갱신: 2026-06-12(chief-architect·§Y Windows 셸 컨텍스트 메뉴 연동 설계)** — **§1-Y 신규 섹션 추가(US-23.1·F37·§Y1 ↔ 설계 컴포넌트 매핑·당시 상태 🔜 설계 완료·구현 전·위 2026-06-12 doc-sync 줄에서 구현 완료로 갱신됨)**: [ADR-013](./adr/ADR-013-shell-context-menu-verbs.md) 설계 완료에 따라 Y1 2행(verb 조회·병합 렌더 / verb 실행 fire-and-forget)을 신규 매핑. **예정 구현 대상**: `main/os/shellVerbs.ts`(`ShellVerbsService` 상주 PowerShell 워커·요청 큐·타임아웃·블랙리스트·index+표시명 결합 식별)·`main/os/shellVerbsWorker.ps1`(고정 stdin JSON 루프·COM `Shell.Application` `Verbs()`/`DoIt()`)·`shell.handlers.ts`/`guard.ts`(확장)·`renderer/app/usecases/shellVerbs.ts`(신규·TTL 캐시)·`contextMenu.ts`/`ui/contextmenu/ContextMenu.tsx`/`uiSlice`(확장·"Windows 메뉴" 섹션 병합·로딩 UX). **신규 IPC 채널 2종 `shell:context-verbs`·`shell:invoke-verb`(invoke·EVENT_CHANNELS 무변·P1 동결 후 신기능 선례 동일 규약·동결 위반 아님)·신규 npm 의존성 0·신규 네이티브 의존성 0(COM·PowerShell 내장·`showProperties` 선례)·`SESSION_SCHEMA_VERSION` 무변경.** ADR-005 전면 준수(sender·zod·guardPath·로컬 한정·명령행 합성 0=경로는 stdin JSON·실행 표면 미추가). **상태 🔜 설계 완료·구현 전 — 위 파일은 아직 코드 없음(유령 매핑 아님·설계 예정 대상·구현 후 doc-sync가 상태 갱신·✅ 위장 금지).** 정직 한계(ADR-013): canonical name 부재→블랙리스트 표시명 정규화 매칭(영/한)·서브메뉴 평탄화 best-effort·다중선택 섹션 숨김·fire-and-forget. 상태 단일 출처 roadmap §0.5.
> **갱신: 2026-06-11(doc-sync·비계획 3건 V11~V13)** — **§1-V에 비계획 구현 3건 매핑 추가 + §1-X1에 desktop 노드 부기**: **⚠️ V11 설정 팝업 좌우 2단 구성 + 단축아이콘(신규)**(`SettingsDialog.tsx` 전면 재구성·좌 카테고리/우 항목·레이아웃 카테고리 단축아이콘 그룹별 체크박스→`toggleIconBarItem`/`resetIconBar`→`iconBarHidden` 영속·딥링크 미구현 후속) · **⚠️ V12 상단 아이콘바 아이콘 드래그 위치 변경(신규)**(`IconBar.tsx` HTML5 draggable·`commitReorder`→`reorderIconBar`→`iconBarOrder` 영속·`iconBarItems.ts#resolveIconBarItems(order, hidden)` 헬퍼) · **V13 빠른 위치 "바탕화면"**(§X1 사소 노드·`Sidebar.tsx` `knownFolders.desktop`·백엔드 이미 제공·UI 노출만). **V11·V12는 PRD/features/user-stories/flows 미수록 신규 스코프(스코프 일탈) — 기획 정식 편입 아님·PM/사용자 결정 대기 / V13은 §X1 확장(스코프 일탈 아님).** 3건 모두 **신규 IPC 채널 0·신규 npm 의존성 0·`SETTINGS_SCHEMA_VERSION` 무변경**(`SettingsSnapshot.iconBarHidden?`/`iconBarOrder?` 비파괴 옵셔널 추가·`defaults.ts asStrArray` 폴백·`uiSlice setIconBarHidden/setIconBarOrder`). `typecheck`(node+web) PASS·ESLint 0·**`verify:persistence` 138→147(coerce 9)·`verify:store` 274→281(`resolveIconBarItems` 7)**·0 fail·회귀 0·유령 매핑 0(2026-06-11 Grep/Read). 실 GUI 런타임 스모크 권장 🟡(✅ 위장 아님). 상태 단일 출처 roadmap §0.5(2026-06-11 비계획 구현 3건 단락).
> **갱신: 2026-06-11(doc-sync·렌더러 UI 변경 3건)** — ① **§1-W에 "자세히 보기 열 헤더 클릭 정렬" 행 추가(W1 동작 확장)**: `FileListView.tsx#ColumnHeader`에 정렬 props(`sortKey`/`sortDir`/`onSort`)+신규 `SortLabel` 버튼·`role="row"`/`role="columnheader"`/`aria-sort`·헤더 클릭=name/size/ext/mtime 정렬·재클릭 방향 토글(▲/▼)·`<button>` 키보드. **기존 `panelsSlice#setSort` 재사용(신규 채널 0·신규 의존성 0·정렬 도메인 불변).** ② **정렬 UI 진입점 이전 정정**: `PanelToolbar.tsx`의 정렬 `<select>`+방향 버튼+`setSort` 구독 제거 → 진입점이 PanelToolbar 드롭다운 → ColumnHeader(SortLabel)로 이전(유령 매핑 방지). ③ **§1 텔레메트리 행·§1-V I8 행을 "UI 제거"로 정정(드리프트 정정)**: `SettingsDialog.tsx`에서 "익명 사용 통계" 체크박스·`telemetryOptIn` 구독·`changeTelemetryOptIn` import 제거 — 단 정의(`uiSlice telemetryOptIn`·`usecases/settings changeTelemetryOptIn`·`SettingsStore`·`p5.verify` "기본 false 유지")는 잔존(grep 확인). **3건 모두 렌더러 전용·신규 IPC 채널 0·신규 npm 의존성 0·`SESSION_SCHEMA_VERSION` 무변경·`typecheck`(node+web) PASS·verify 누계 약 1523 불변(신규 케이스 0)·회귀 0·유령 매핑 0(2026-06-11 git diff/grep). 스코프/MoSCoW 무수정(상태표기/추적성만). 실 GUI(헤더 클릭 정렬·방향 토글·키보드·aria·텔레메트리 항목 부재)는 런타임 스모크 권장 🟡(✅ 위장 아님).** **드리프트(결정 대기·보고): 텔레메트리(P6 Should·결정-D5)는 PRD/features/user-stories에 "설정 화면 체크박스"로 기술됐으나 UI 진입점이 제거됨 — 수용기준 문구 반영/철회 PM·사용자 결정 대기·doc-sync 문구 무수정.** 상태 단일 출처 roadmap §0.5(2026-06-11 렌더러 UI 변경 3건 단락).
> **갱신: 2026-06-11(doc-sync·워크스페이스 자동 저장)** — **§1 "명시적 워크스페이스" 행을 "명시적 워크스페이스 + 자동 저장"으로 확장 매핑**(US-5.8 동작 변경/확장·사용자 직접 요청). 현재 선택 워크스페이스(`uiSlice.currentWorkspace`/`setCurrentWorkspace`)는 세션 자동저장(`session.ts#startSessionAutosave`)이 같은 스냅샷을 해당 워크스페이스 파일에 이중 기록하고, `SessionSnapshot.currentWorkspace?`(비파괴·`SESSION_SCHEMA_VERSION` 1 불변·`defaults.coerceSession` 비-빈 문자열만 보존)로 영속·부팅 복원(`session.ts#restoreSession`). `usecases/workspace.ts`(save/load 성공 시 선택 설정·delete/rename 선택 추적·로드 실패 시 해제)·`ui/workspace/WorkspaceDialog.tsx`(현재 선택 배지·선택 해제 버튼) 실재 확인(2026-06-11 Grep·유령 매핑 0). **신규 IPC 채널 0(기존 `workspace:save`·`session:save` 재사용)·신규 npm 의존성 0·신규 ADR 0.** 상태 "✅ 코드 정합·실 GUI 🟡". 상태 단일 출처 roadmap §0.5(2026-06-11 워크스페이스 자동 저장 단락)/§1 P6 행. **기획 4종·MoSCoW 무수정(추적 매핑 정합만) — 단 US-5.8 수용기준 본문이 "명시적 저장"만 기술해 자동 저장 동작과 부분 모순(수용기준 문구 정식 반영 여부 PM/사용자 결정 대기·doc-sync 보고).** **후속 증분(2026-06-11): 자동 저장 가시화를 위해 §1 "상태바"(US-5.7) 행에 `StatusBar.tsx` 워크스페이스 칩(`🗂 이름·상태` 5종)·`session.ts` 저장 상태 pub/sub(`getWorkspaceSaveStatus`/`subscribeWorkspaceSaveStatus`/`noteWorkspaceSaved`/`resetWorkspaceSaveStatus`·`useSyncExternalStore`·구독 경로 밖 in-flight 가드) 매핑 추가(2026-06-11 Grep·유령 0). 신규 채널 0·신규 의존성 0·렌더러 전용·`verify:store` 264→274. 상태바 칩도 US-5.8 수용기준 미기술=같은 결정 대기 항목.**
>
> **후속(2026-06-11, doc-sync·워크스페이스 관리 → 설정 페이지 통합·V11 후속):** 독립 `ui/workspace/WorkspaceDialog.tsx`를 폐지(git rm·App.tsx 마운트/import 제거·`uiSlice.openWorkspace`/`closeWorkspace` 액션 제거·`workspaceOpen` 가드 필드만 항상 false로 잔존)하고, 본문을 다이얼로그 크롬 없는 재사용 패널 **신규 `ui/workspace/WorkspacePanel.tsx`** 로 추출해 **`SettingsDialog.tsx#WorkspaceCategory`** 에 임베드. 진입은 모두 **`uiSlice.openSettings(category?)`** 딥링크로 단일화(`commandBus#workspace.manage`·`StatusBar` 칩·`TabBar` 설정 버튼 → `openSettings('workspace')`/`openSettings()`). 설정 카테고리는 `uiSlice.settingsCategory`(layout/system/workspace/shortcuts)·`setSettingsCategory`로 스토어 단일 출처화(좌측 네비+딥링크 공유). **이로써 V11 정직 한계였던 "아이콘 클릭→카테고리 딥링크 미구현"이 해소(구현 완료).** US-5.8 기능 동일·진입 UX만 독립 팝업→설정 페이지로 변경. 위 §1 US-5.8 행 매핑은 `WorkspaceDialog.tsx`(유령)→`WorkspacePanel.tsx`+`SettingsDialog#WorkspaceCategory`로 정정·StatusBar 칩 클릭 `openWorkspace()`(유령)→`openSettings('workspace')`로 정정. **신규 IPC 채널 0·신규 npm 의존성 0·`SESSION_SCHEMA_VERSION` 무변경·렌더러 전용. 검증: `npm run build`(typecheck node+web + electron-vite) PASS·ESLint 0·`verify:store` 281/0·`verify:p5` 52/0·`verify:p6` 26/0(회귀 0). 실 GUI(설정 워크스페이스 페이지 저장/불러오기/이름변경/삭제·칩→딥링크·탭 설정 버튼) 런타임 스모크 🟡.** **드리프트(결정 대기): US-5.8 수용기준 본문이 "워크스페이스 관리 다이얼로그"를 명시 — 진입 UX가 설정 페이지로 바뀜·문구 무수정(상태표기/추적성만).**
> **갱신: 2026-06-11(doc-sync)** — **§1-H H4(터미널 열기·`shell:open-terminal`) 매핑 메모를 실제 코드에 맞춰 갱신**(드리프트 정정): 직전 매핑은 `main/os/shell.ts#openTerminal`을 "`wt.exe -d`→`powershell.exe -NoExit` 폴백·`execFile`"로 기술했으나, 2026-06-11 결함 수정으로 코드가 **3단 폴백(`spawnDetached`(신규)+`wt.exe -d` 직접 spawn → `execFileNoThrow`(신규)+PowerShell 릴레이[env `EXPLORER_TERMINAL_DIR`] → `powershell.exe -NoExit` detached spawn)** 으로 재설계됨에 따라 메모를 실코드와 일치시킴. 사유: Node 22+ 스토어 별칭 `wt.exe` 동기 EINVAL throw가 핸들러로 새던 결함(폴백 미도달). `shell.handlers.ts`는 **무변경**(매핑 정합 유지·"throw 금지·Result.err 전파" 계약을 shell.ts가 실제로 준수). **스코프/신규 채널/신규 의존성 무변경·기존 H4 결함 수정·상태 등급 변화 없음(✅ 코드 정합·실 GUI 🟡 유지). ADR-005 명령행 합성 0 원칙 유지.** typecheck(node+web)/build PASS·ESLint 0·실 WT 창 표시는 사용자 세션 스모크 🟡. 상태 단일 출처 roadmap §0.5(2026-06-11 단락). 기획 4종·MoSCoW 무수정(추적 매핑 메모 정합만).
> **갱신: 2026-06-10(6차·chief-architect)** — **신규 Should 2종 매핑 추가 + 파워기능 Q1·U3 행 경로 정정**: ① **§1-U4 추가**(탭 사용자 지정 이름·features §U4·user-stories US-20.4·flows F35·PRD §6 Should) — §N/§O/§W와 동일 렌더러 전용·세션 영속(비파괴) 패턴(신규 IPC 채널 0·신규 npm 의존성 0·신규 ADR 0·`SESSION_SCHEMA_VERSION` 불변). `domain/entities Tab.customName?`·`shared/dto TabSnapshot.customName?`·`tabsSlice setTabName/clearTabName`·`ui/tabbar/TabBar.tsx TabRenameInput/우클릭 이름 바꾸기`·`main/persistence/defaults.ts coerceTab`·`usecases/session.ts` 실재 확인(유령 0). ② **§1-X1 추가**(좌측 사이드바 "빠른 위치 ▸ 다운로드"·features §X1·user-stories US-22.1·flows F36·PRD §6 Should) — **신규 IPC 채널 `fs:known-folders` 1종(이 영역의 유일 신규 채널·P1 동결 후 신기능 선례 동일 규약)**·신규 npm 의존성 0·신규 ADR 0. `shared/ipc/channels.ts FS_KNOWN_FOLDERS`·`shared/ipc/contracts.ts`·`shared/dto KnownFoldersDTO`·`main/ipc/fs.handlers.ts`·`preload/api.ts`·`renderer/infra/api`·`sidebarSlice knownFolders/loadKnownFolders`·`ui/sidebar/Sidebar.tsx 빠른 위치/QuickFolderNode` 실재 확인(유령 0·현재 다운로드만 렌더·desktop/documents/home 예약). 두 행 모두 상태 "구현 완료(코드)·실 GUI 🟡"(✅ 위장 아님). ③ **§1-Q1·§1-U3 행 경로 정정**: 직전까지 "(예정)·🔜 설계" 라벨이던 두 행이 이제 **실 구현 파일·실 채널을 가리키도록 경로 정정**(Q1=`main/archive/*`·`renderer/app/usecases/archive.ts`·`renderer/domain/rules/{archiveSafePath,transferRoute}.ts`·`shared/archive/{safePath,archivePath}.ts`·신규 채널 `archive:open/list/close/extract/add`·`main/ipc/archive.handlers.ts`; U3=`renderer/app/usecases/windowSplit.ts`·`main/windows/windowManager.ts`·`Tab.color?/locked?`·신규 채널 `window:split-tab`/`window:get-init`·`main/ipc/window.handlers.ts` — 2026-06-10 Grep/Glob 확인·유령 0). **상태 컬럼 "🔜 설계"는 본 차수에서 변경하지 않음**(상태 단일 출처 roadmap §0.5/§1 요약표 — doc-synchronizer가 별도 동기화). **roadmap §0.5/§1·PRD/features/user-stories/flows 무수정**(추적 매핑 전용·product-planner가 기획 4종 처리 완료). 주의: 본 §X(빠른 위치)는 기존 어느 X 사용처와도 다른 신규 챕터다.
> **갱신: 2026-06-10(5차·doc-sync)** — **M8 파워기능 6종(S1·S2·T1·T2·U1·U2) 매핑 상태 "🔜 설계" → "구현 완료(코드)/실 GUI 🟡"로 동기화**: §1-P~1-U 표의 §1-S1(내용 검색 grep·`main/search/`·`grepWorker`·`search:content:*` 5종 신규 채널)·§1-S2(명령 팔레트·`ui/palette/CommandPalette`·`Ctrl+Shift+P`·신규 채널 0)·§1-T1(파일 태그·`domain/rules/tags.ts` 7색·`tagsSlice`·세션 메타 `tagsByPath`·**T3 폐기로 삭제됐던 filterComposition 태그 합성을 T1에서 재설계해 메모 해소**·신규 채널 0)·§1-T2(폴더 용량·`folderSize.ts`·analyze:scan 재사용·신규 채널 0)·§1-U1(Space 퀵룩·`QuickLookOverlay`·preview:read 재사용·신규 채널 0)·§1-U2(브레드크럼 드롭다운·`BreadcrumbDropdown.tsx`·fs:tree-children 재사용·신규 채널 0) 6행을 실재 파일로 정정(2026-06-10 Grep/Read 확인·유령 매핑 0). 단축키 표 `Ctrl+Shift+P`·`Space` 행도 구현 완료로 정정. **M8 잔여 0 — 파워기능 잔여는 M9(Q1·U3) 2종.** **상태 단일 출처 roadmap §0.5·§1 요약표.** 헤드리스 `verify:search` 58·`verify:palette` 20·`verify:contentsearch` 38(신규) + domain 204·store 222·persistence 119(전부 0 fail) 충족·실 GUI/실 워커 런타임 스모크 🟡(✅ 위장 아님). §1-W(컬럼 헤더·너비 드래그)는 4차에서 이미 "구현 완료(코드)/실 GUI 🟡"로 정합 — 본 차수 무변경.
> **갱신: 2026-06-10(4차)** — **§1-W 추가**: §W1 자세히 보기 컬럼 헤더 + 너비 드래그 조절(features §W1·user-stories 에픽21/US-21.1·flows F34·PRD §6 Should)의 추적 매핑을 신규 추가. **§N/§O와 동일한 렌더러 전용·세션 영속(비파괴) 패턴**(신규 IPC 채널 0·신규 npm 의존성 0·신규 ADR 0·`SESSION_SCHEMA_VERSION` 1 불변). 순수 규칙 `domain/rules/columnWidths.ts`(`clampColumnWidth`/`coerceDetailsColumnWidths`)·`app/stores/columnsSlice.ts`(`detailsColumnWidths`/`setDetailsColumnWidth`/`hydrateColumns`)·`ui/panel/views/FileListView.tsx`(`ColumnHeader`/`ColumnDivider` sticky 헤더 밴드·§O pin 밴드 위)·`usecases/session.ts`·`main/persistence/defaults.ts`(coerce 미러)·`shared/dto/index.ts`(`SessionSnapshot.ui.detailsColumnWidths?`) 실재 확인(유령 매핑 0). 상태 "구현 완료(코드)·실 GUI 🟡"(✅ 위장 아님). 헤드리스 `verify:domain` 28케이스(clampColumnWidth·coerceDetailsColumnWidths) 충족. **roadmap §0.5/§1 상태표는 doc-synchronizer가 별도 동기화** — 본 갱신은 추적 매핑만. 상태 단일 출처 roadmap §0.5. (주의: 본 §W는 roadmap M7 "W1/W2 해시·전송 큐 인프라" 라벨과 다른 챕터다 — product-planner가 기획문서에서 구분함.)
> **갱신: 2026-06-10(3차)** — **§1-V에 버그 수정·UX 개선 7건(F1·F2·B1·B3·B4·I8·I9) 매핑 추가**: F1 asar 가상화 ENOENT 수정(`fileOpWorker.ts process.noAsar`·`engine.ts forceRemove`)·F2 특수파일 best-effort 삭제(`engine.ts forceRemove` 재설계·실 FS 스모크 통과)·B1 기본 시작위치 새 탭 적용(`tabsSlice.ts newTab`)·B3 미러 재귀 복사 평탄화 수정(**기존 `op:start` 옵셔널 `baseDir` 확장·신규 채널 0**·contracts/guard/op.handlers/OperationManager/protocol/fileOpWorker/engine/fileOps/compare·실 FS 스모크 통과)·B4 휴지통 실패 폴백 UX(`operationsBridge.ts` dismiss)·I8 익명 통계 도움말(`SettingsDialog.tsx`)·I9 브라우저 기본 단축키 차단(`KeyboardDispatcher.tsx`). **7건 모두 기존 기능 개선 — 스코프·MoSCoW·신규 채널 무변경(기획 무수정·스코프 일탈 아님).** `npm run typecheck`(node+web) PASS·ESLint 0·`verify:ops` 35/0·유령 매핑 0·verify 누계 약 1200 불변·회귀 0. 실 GUI/asar/설치는 미검증 🟡. 보류 백로그 4건(B2·I5·I6·I7) 🔜 미착수. 상태 단일 출처 roadmap §0.5.
> **갱신: 2026-06-10(2차)** — **§1-V에 견고성·성능 개선 6건(V4~V9) 매핑 추가**: V4 복사 엔진 성능 최적화(`engine.ts` 하이브리드 복사·적응형 병렬·`fileOpWorker.ts pickConcurrency`)·⚠️V5 robocopy 고속 미러(**신규 채널 `op:robocopy:start`·스코프 일탈=§P1 미러 확장**·`os/robocopy.ts`·`OperationManager#startRobocopyMirror`·진행/완료=op:* 재사용)·V6 휴지통 실패 메시지 actionable화(`describeTrashFailure`)·V7 다중선택 드래그 이동 수정(`FileListView` onRowMouseDown/onRowClickSelect)·V8 휴지통 실패→영구삭제 폴백(`operationsBridge`·`uiSlice ConfirmDeleteState.reason?`)·V9 EPERM 견고 영구삭제(`engine.ts forceRemove`/`removeWithRetry`·실 FS 스모크 통과). **V4·V6·V7·V8·V9는 기존 기능 개선(스코프·채널 무변경) / ⚠️V5만 신규 채널+스코프 일탈(PM/사용자 정식 편입 결정 대기·V5 신규 채널은 동결 후 선례 동일 규약·동결 위반 아님).** `npm run typecheck`(node+web) PASS·ESLint 0·`verify:ops` 35/0·유령 매핑 0(실재 확인)·verify 누계 약 1200 불변(헤드리스 범위 밖·신규 케이스 0). 실 GUI/robocopy/설치 미검증 🟡. 상태 단일 출처 roadmap §0.5.
> **갱신: 2026-06-10** — **⚠️ 비계획 구현 3건(V장 가칭·기획 미수록·스코프 일탈)의 추적 매핑을 §1-V로 신규 추가**(V1 대시보드 동작 컬럼[신규 채널 `shell:open-external`]·V2 드라이브 레이블 일관화[신규 채널 0]·V3 탐색기 "AGT-Finder로 열기"[신규 푸시 채널 `app:open-path`·NSIS HKCU 등록]). **기획문서에는 추가하지 않음**(스코프 일탈 — roadmap §0.5 개발현황 사실 기록 + 이 추적 매핑만·PM/사용자 정식 편입 결정 대기). 신규 채널 2종은 P1 동결 후 신기능 선례와 동일 규약(동결 위반 아님)·신규 npm 의존성 0. typecheck PASS·ESLint 0·유령 매핑 0·verify 누계 약 1200 불변(헤드리스 범위 밖). 실 GUI/설치/우클릭은 미검증 🟡. 상태 단일 출처 roadmap §0.5.
> **갱신: 2026-06-09(3차)** — product-planner가 **ref.md 동작 변경 2건을 기존 챕터 수용기준 갱신으로 반영(신규 챕터/US 신설 0)**함에 따라 해당 매핑을 변경 구현 파일로 갱신: **① §1-O 상단 고정 동작 "정렬 최상단"→"스크롤 고정(sticky)"**(목록/자세히 전용·그리드 미적용) — `FileListView.tsx` sticky 밴드(`sticky`/`stickyCount`/`stickyBandH`/`pinnedSet`/`renderRow`/`stickyRows`·`position:sticky top:0 zIndex:3`·키보드 scrollIntoView `stickyBandH` 보정) 렌더 계층 추가, `sort.ts#applyPins`·`selectors.ts#computeVisible`는 **불변**(sticky 는 표시 위치 고정·정렬 아님). **② §1-M M3(US-12.4)·§1-H H5(US-7.5) 원격 주소창 경로-only 입력** — `PanelToolbar.tsx`(`parseRemotePath`로 호스트 프리픽스 `sftp://host` 고정 표시·경로만 편집·commit 시 `makeRemotePath` 결합·전체 URI `isRemotePath` 방어) + `navigate.ts#validateAndNavigate`(`isRemotePath` 분기로 원격 URI 로컬 `fs:validate-path` 우회·바로 navigate). **신규 IPC 채널 0·신규 npm 의존성 0·신규 ADR 0**(전부 기존 렌더 계층·`domain/rules/remoteLocation` 단일출처 내 변경) → ADR 불요. 실재 심볼 Grep/Read 확인(유령 매핑 0). 상태 모두 "구현 완료(코드)·실 GUI 🟡". 상태 단일 출처 roadmap §0.5.
> **갱신: 2026-06-09(2차)** — **"파일/폴더 상단 고정(pin)"이 product-planner에 의해 §O / 에픽14 / US-14.1 / F19(PRD §6 Should)로 정식 편입됨에 따라 §1-O 추적 매핑을 신규 추가**(직전 "스코프 일탈로 정식 추적 제외" → 정식 항목 승격). 구현은 이미 완료·verify 충족(코드 정합), 실 GUI 동작은 런타임 스모크 🟡. 신규 IPC 채널 0·신규 npm 의존성 0·신규 ADR 0(렌더러 도메인 순수함수 `applyPins`+`sidebarSlice.pinnedByDir`+기존 `SidebarSnapshot` 비파괴 확장·세션 영속 승계). 매핑 8파일+verify 3종 실재 확인(유령 매핑 0). 상태 단일 출처 roadmap §0.5.
> **갱신: 2026-06-09** — §M M3 원격 디렉토리 진입(ENOENT)·M1 외부 드래그 오버레이 잔존 **결함 2건 수정**(§1-M M3/M1 행 + 결함 수정 주석에 반영·코드 정합 회복·실 동작 🟡 유지·신규 스코프 아님). 또한 **ref.md 아이디어 기반 비계획 구현 "파일/폴더 상단 고정(pin)"이 발생했으나 기획문서에 정식 기능이 없는 스코프 일탈**이라 본 추적성 매핑에는 정식 항목으로 추가하지 않는다(정식 편입 결정 시 추가 — **2026-06-09 2차로 §O 정식 편입돼 §1-O 추가됨**) — 구현 사실은 roadmap §0.5 "추가(2026-06-09)" 줄에만 기록(`domain/rules/sort.ts#applyPins`·`selectors.ts#computeVisible`·`sidebarSlice.pinnedByDir`·`contextMenu.ts`·`FileListView`·`shared/dto SidebarSnapshot.pinnedByDir`·`persistence/defaults.coercePinnedByDir`·`session.ts`). 상태 단일 출처 roadmap §0.5. **2026-06-08** — §M 외부 연계 3기능(M1 외부 D&D·M2 클립보드 CF_HDROP 양방향·M3 FTP/SFTP, US-12.1~12.5·F14~F16) **구현 완료·통합 QA PASS → §1-M 상태 🔜→✅ 동기화**(매핑 17파일 실재 확인·[final-traceability-M](../reviews/final-traceability-M.md) PASS). 신규 **[ADR-007](./adr/ADR-007-remote-protocol-and-network-boundary.md)**(ADR-005 부분 개정·네트워크 경계 D5→D7·자격증명 safeStorage/DPAPI·원격=Main 스레드). 신규 채널군 `dnd:*`·파일 `clipboard:*`·`remote:*`. 신규 npm 의존성 `ssh2-sftp-client`·`basic-ftp`(M3만). 실 동작은 런타임 스모크 권장 🟡. **2026-06-08 추가 — §N 즐겨찾기 UX 2기능(N1 즐겨찾기 경로 워터마크·N2 즐겨찾기 드래그 정렬, US-13.1/13.2·F17~F18) 구현 완료·통합 QA PASS → §1-N 매핑 행 상태 🔜→✅ 동기화**(매핑 파일 실재 확인: N1 `domain/rules/favoriteWatermark.ts`·`ui/panel/FavoriteWatermark.tsx`·`Panel.tsx`·`ui/theme/palette.ts`/`tokens.ts`, N2 `ui/sidebar/useFavoriteReorder.ts`·`sidebarSlice.reorderFavorite`·`Sidebar.tsx`·키 `Alt+Shift+↑/↓`·[qa-integration-N](../reviews/qa-integration-N.md) PASS). N1·N2는 기존 즐겨찾기(C4)·J7 별칭(`SidebarSnapshot.favoriteLabels`)의 비파괴 확장·렌더러 전용·신규 채널 0·신규 의존성 0. **§N 구현 완료로 개발 잔여(🔜) 0 — §2 키 표에 `Alt+Shift+↑/↓`(N2 재정렬) 추가. 실 GUI 동작은 런타임 스모크 권장 🟡.**
> 작성: 시니어 아키텍트 · 2026-06-06 · **갱신: 2026-06-07**(G장 릴리스/도구 추적 추가 · H장 UX/레이아웃 확장 추적 추가: 아이콘바·사이드바 토글·분할 크기조절 + **터미널 열기(신규 채널 `shell:open-terminal`)·경로 직접 입력·파일 유형 아이콘(예약 채널 `shell:icon` 정식 구현)** · **I장 분석·접근성 추적 추가: 사용량 대시보드(신규 채널 `analyze:scan:*` 5종·recharts MIT lazy 청크)·블루라이트 차단 테마(`BLUELIGHT_PALETTE`·`ThemeMode` 4종)** · **J장 보기·실시간·뷰어·브랜딩 추적 추가: 박스 선택·패널 실시간 갱신(신규 채널 `fs:watch:*`)·보기 5종(`ViewMode`)·AGT-Finder 브랜딩(appId `com.agtfinder.app`)·미리보기 2단 뷰어(highlight.js/marked/dompurify)·미리보기 폭 조절·즐겨찾기 별칭 · **F5/F6 복사·이동 매핑 제거(2026-06-07 사용자 결정)** · **P7 릴리스 안정화 헤드리스분 추적 추가(§1-P7): 접근성(`useFocusTrap`·모달 6종 ARIA/Esc·행 ARIA·focus-visible)·WCAG AA(`verify:contrast`)·성능 불변식(`windowing.ts`·`verify:perf`)·F장 매트릭스(`verify:fmatrix`)·보안 audit/sourcemap/코드서명 설정 — 헤드리스 ✅ / 실측·실제 서명·실케이스 🟡**)
> 목적: PRD/features/user-stories의 각 주요 기능이 **어느 컴포넌트·모듈·IPC 채널·ADR**로 실현되는지 추적한다.
> 약어: SA=[system-architecture.md](./system-architecture.md), SW=[software-architecture.md](./software-architecture.md), DS=[directory-structure.md](./directory-structure.md)

---

## 1. 핵심 기능 → 설계 매핑

> **코드 네임스페이스 범례(참조 무결성)**:
> - `feat-Xn` = features.md 영역코드(A~F + 번호). 예: `feat-D1`=현재폴더 검색, `feat-D3`=미리보기 패널.
> - `결정-Dn` = PRD 11장 결정기록(D1~D5). 예: `결정-D3`=세션 저장 범위, `결정-D4`=단축키 충돌 회피, `결정-D5`=로컬·옵트인 보안.
> - `US-x.y` = user-stories.md.
> 동일 표 안에서 `feat-Dn`과 `결정-Dn`은 **서로 다른 대상**이므로 반드시 접두어로 구분한다.

| 기능(추적원) | UI 컴포넌트 | 유스케이스/스토어 | 도메인 규칙 | IPC 채널 | 관련 ADR |
|---|---|---|---|---|---|
| **탭 관리** (US-1.1, A1) | TabBar | `tab.*` usecase / tabsSlice | closedHistory 스택 | session:save/load | ADR-002 |
| **2분할 패널** (US-1.2, A2) | LayoutHost, Panel | `layout.toggleSplit2`, `panel.focusNext` / tabsSlice | — | — | ADR-002 |
| **4분할** (US-1.4, S) ✅ | `ui/layout/LayoutHost.tsx`(grid-4 2x2 row-major) | `tabsSlice.toggleGrid4`·`focusPanelDir` / `usecases/fileOps.ts`(grid-4 `activeIdx^1`) | grid-4 포커스 순환·`ctrl+shift+\`→`layout.toggleGrid4` | — | ADR-002 |
| **패널 간 이동/복사(D&D·클립보드)** (US-1.3, A3) | dnd/, FileListView | D&D usecase, 클립보드 usecase (~~`panel.copyToOther`/`moveToOther`(F5/F6)~~ — **2026-06-07 코드에서 제거**, `usecases/fileOps.ts`의 `copyToOtherPanel`/`moveToOtherPanel` 삭제·`panelPaths` 헬퍼는 D&D용 보존) | `resolveDragIntent`(드라이브/수정키), 순환이동 차단, 동일폴더 무시 | op:start, op:progress, op:conflict, op:resolve, op:done, op:cancel | ADR-003, ADR-005 |
| **목록 보기/정렬** (US-2.1, B1/B2) | FileListView(details/list/grid) | view usecase / panelsSlice | 자연정렬·폴더우선 | fs:list:start/chunk/done | ADR-004 |
| **생성/이름변경** (US-2.2, B3 Must) | FileListView 인라인편집(`F2`/`Ctrl+Shift+N`/새 파일) | file usecase | 금지문자·예약명·중복명 검증(EINVAL/EEXIST) | **fs:mkdir, fs:create-file, fs:rename** | ADR-003 |
| **삭제(휴지통/영구)** (US-2.2, B3 Must) | FileListView, ConfirmDialog | file usecase | 영구삭제 사전 확인 | op:trash, op:delete, dialog:confirm-permanent-delete | ADR-003, ADR-005 |
| **파일 실행/열기** (B6 Must) | `FileListView` 더블클릭/`Enter` · `ui/contextmenu/ContextMenu.tsx`(우클릭) | `usecases/open.ts`(activateEntry·openWithEntry·showPropertiesFor) · `usecases/contextMenu.ts`(buildMenuItems) | (OS 위임) | shell:open ✅, shell:show-properties ✅(컨텍스트 메뉴 "속성"에서 호출), **shell:open-with(S) ✅** (`main/os/shell.ts#openWith` OpenAs_RunDLL·`shell.handlers.ts`·`guard.ts#zShellOpenWithReq`·컨텍스트 메뉴 "연결 프로그램으로 열기") | ADR-005 |
| **컨텍스트 메뉴(우클릭)** (B6, P4 산출물 — P6 시점 구현·드리프트 해소) ✅ | `ui/contextmenu/ContextMenu.tsx`(경계 보정·키보드) · `FileListView`(onContextMenu) · `App.tsx`(마운트) | `usecases/contextMenu.ts`(buildMenuItems·openRowContextMenu·openEmptyContextMenu) · `uiSlice.contextMenu`(상태/openContextMenu·closeContextMenu) | 단일/다중/빈영역·My PC 항목 표시 규칙(+단일 폴더·빈 영역 "터미널 열기") | 메뉴 클릭 → 동일 commandId(`panel.activate`·`file.copy/cut/rename/trash/deletePermanent/paste/newFolder`·`panel.refresh`) 또는 `openWithEntry`·`showPropertiesFor`·`openTerminalAt` → shell:open-with·shell:show-properties·**shell:open-terminal(H4, 신규)** | ADR-005 |
| **복사/잘라내기/붙여넣기** (US-2.3, B4) | 컨텍스트메뉴/단축키 | clipboard usecase | "복사본" 명명 | clipboard:copy-files/cut-files/paste-target, op:start | ADR-003 |
| **복사/이동 충돌 해결** (US-2.4, feat-D4) | ConflictDialog | operations usecase / operationsSlice | 덮어쓰기/병합/둘다유지/모두적용 판정 | op:conflict, op:resolve, op:done(실패목록) | ADR-003 |
| **주소표시줄/이동버튼** (US-3.1, C1/C2) | Toolbar, 주소표시줄 | nav usecase / panelsSlice(navHistory) | 경로 유틸 | fs:validate-path, fs:list:start | — |
| **트리 사이드바** (US-3.2, C3) | Sidebar | sidebar usecase / sidebarSlice | — | fs:drives, fs:tree-children | — |
| **즐겨찾기/최근** (US-3.3, C4/C5) | Sidebar | sidebar usecase / sidebarSlice | — | session:save/load(영속) | — |
| **현재 폴더 검색** (US-4.1, feat-D1) | SearchBar | search usecase / panelsSlice(filter) | 필터/하이라이트(순수) | (Renderer 내 필터, IPC 불필요; 200ms 폴백 SW §6.3) | ADR-004 |
| **확장자/이름 필터** (US-4.2, feat-D2) | SearchBar/필터바 | search usecase | 패턴 매칭 규칙 | — | — |
| **미리보기 패널** (US-4.3, feat-D3, S) ✅ / **2단 뷰어 확장**(feat-J5) ✅ | `ui/preview/PreviewPanel.tsx`(+J5 `PreviewInfoCard` 2단) + `renderers/*`(Image/Text/Meta/Unsupported **+J5 Code(highlight.js)/Markdown(marked+DOMPurify)**) | `uiSlice.previewOpen`·`commandBus#preview.toggle`(+J6 `uiSlice.previewWidth`) | 형식 분기·상한·바이너리 판별·lang/isMarkdown(`FileSystemService.readPreview`) | **preview:read**(신규, `preview.handlers.ts`·`dto.PreviewData`) — J5 신규 채널 0(재사용) | SA 8장. J5 의존성 highlight.js/marked/dompurify lazy(§1-J 참조) |
| **다중 선택/일괄** (US-5.1, E3) | FileListView | selection usecase / selectionSlice | 선택집합 연산, count/totalSize 파생 | (op:start로 일괄) | ADR-002 |
| **진행률/취소** (US-5.2, E4) | ProgressDialog, StatusBar | operations usecase / operationsSlice | — | op:progress(200ms 스로틀), op:cancel, op:done | ADR-005 |
| **대용량 폴더 빠른 첫 렌더(성능)** (US-5.6) | FileListView(가상 스크롤) | panelsSlice 스트림 적재 | — | fs:list:start/chunk/done, fs:list:cancel | ADR-004, ADR-005 |
| **테마** (US-5.3, feat-E2) | theme/ | ui usecase / uiSlice | — | settings:get/set(theme) | — |
| **설정** (feat-E6) | 설정 화면 / uiSlice(settings) | settings usecase | 시작위치·숨김/확장자 표시·최근 개수 적용 규칙 | settings:get/set | — |
| **키보드 워크플로** (US-5.4, feat-E1, PRD 8장) | keyboard/(Registry·Dispatcher·CommandBus) | 모든 usecase | keybindings 맵(단일 출처) | (명령→해당 채널) | ADR-002 |
| **자동 세션 복원** (US-5.5, 결정-D3) | App 부트스트랩 | session usecase / 다중 슬라이스 | 스냅샷 직렬화 규칙(closedHistory 제외) | session:load/save | SA 5장, ADR-002 |
| **상태바** (US-5.7, feat-E5) | `ui/statusbar/StatusBar.tsx`(항목수·선택 합계·경로 + **[2026-06-11] 워크스페이스 칩 `🗂 이름·상태` 5종 dirty/saving/saved+시각/error/idle·클릭=`openSettings('workspace')`(설정 워크스페이스 페이지 딥링크·[2026-06-11 독립 팝업 폐지])**) | selection/operations 셀렉터 + **`session.ts#getWorkspaceSaveStatus`/`subscribeWorkspaceSaveStatus`(`useSyncExternalStore` 구독·US-5.8 자동 저장 가시화·구독 경로 밖 pub/sub)** | 합계 파생 | — | — |
| **명시적 워크스페이스 + 자동 저장** (US-5.8, S, 결정-D3) ✅ 코드/실 GUI 🟡 | **`ui/workspace/WorkspacePanel.tsx`(저장/불러오기/이름변경/삭제·현재 선택 배지·선택 해제 — 다이얼로그 크롬 없는 재사용 패널)** · **`ui/settings/SettingsDialog.tsx#WorkspaceCategory`(`<WorkspacePanel onLoaded={close}/>` 임베드·진입은 `openSettings('workspace')` 딥링크·[2026-06-11 독립 `WorkspaceDialog.tsx` 폐지=git rm·App.tsx 마운트 제거])** · **`ui/statusbar/StatusBar.tsx`(워크스페이스 칩·저장 상태 가시화·클릭=`openSettings('workspace')`·[2026-06-11])** | `usecases/workspace.ts`(save/load 성공 시 선택 설정·delete/rename 선택 추적 + **명시 저장 `noteWorkspaceSaved(true)`·불러오기 `resetWorkspaceSaveStatus()`**)·`tabsSlice.resetWorkspace`+`session.ts#applySnapshot`(복원 단일화)·`session.ts`(buildSessionSnapshot 직렬화·restoreSession 부팅 복원·startSessionAutosave 선택 시 이중 기록 + **저장 상태 pub/sub `WorkspaceSaveState`/`getWorkspaceSaveStatus`/`subscribeWorkspaceSaveStatus`/`noteWorkspaceSaved`/`resetWorkspaceSaveStatus`·상태 전이 dirty→saving→saved/error·in-flight 가드**)·`uiSlice.currentWorkspace`/`setCurrentWorkspace` | 스냅샷 재사용·`SessionSnapshot.currentWorkspace?`(비파괴·SCHEMA 1)·`defaults.coerceSession`(비-빈 문자열만 보존) | workspace:save/list/load/delete(`main/persistence/WorkspaceStore.ts`·`workspace.handlers.ts`)+session:save(자동 저장 시 이중 기록·신규 채널 0) | SA 5장 |
| **휴지통 연동** (B5, F장) | Sidebar(휴지통) | file usecase | — | op:trash, (복원/비우기 S) | SA 4.2 |
| **단일 인스턴스/세션 안전** (PRD 7장) | — | — | — | (Main 단일 인스턴스 락, 원자적 저장) | ADR-005, SA 5장 |
| **Windows 특수케이스(롱패스/링크/네트워크/권한)** (F장) | 패널 오류상태 표시 | DirectoryView status | — | fs:list:error(denied 등), FileOpError 전파 | ADR-003, ADR-005 |
| **텔레메트리 옵트인** (결정-D5) — 정의 ✅ / **설정 UI 진입점 제거(2026-06-11)** | ~~`SettingsDialog` 체크박스~~ → **UI 제거(2026-06-11): `SettingsDialog.tsx`에서 "익명 사용 통계" 체크박스·`telemetryOptIn` 구독·`changeTelemetryOptIn` import 제거.** 정의(`telemetryOptIn` 상태·`changeTelemetryOptIn` 유스케이스·`SettingsStore` 영속·`p5.verify` "기본 false 유지")는 잔존 — 사용자 설정 진입점만 사라짐(드리프트 보고·결정 대기) | `settings.ts#loadSettings`(부팅 재수화) | 전송 코드 전무·`.eslintrc.cjs` main `no-restricted-imports`(node:http/https/net/dgram) 정적 가드 | telemetry:set-opt-in(기본 off), **telemetry:get-opt-in**(신규, 부팅 재수화·`session.handlers.ts`) | ADR-005 |

---

## 1-G. 패키징 / 릴리스 / 개발 도구(G장) → 구현 파일 매핑

> features §G(앱 아이콘·원클릭 빌드·가상 스크롤 결함 수정)를 사후 정식 편입한 영역. 사용자 UI 기능이 아니라 릴리스 산출·도구·결함 수정이므로 IPC/도메인 규칙 칸 대신 **실제 구현 파일**로 추적한다(2026-06-07 코드 확인 ✅).

| 기능(추적원) | 구현 파일(실경로) | 비고 |
|---|---|---|
| **앱 아이콘 / 브랜딩** (G1, US-6.1, M·릴리스) | `resources/icon.ico`, `resources/icon.png`, `scripts/gen-icon.ps1`, `electron-builder.yml`(`win.icon: resources/icon.ico`) | 겹친 폴더 디자인. dev 창 아이콘도 동일 적용 |
| **원클릭 인스톨러 빌드** (G2, US-6.2, 도구) | `build-installer.ps1`(루트) | 의존성 점검→typecheck→build→`npm run package`(NSIS)→인스톨러 경로 출력 |
| **가상 스크롤 뷰포트 높이 결함 수정** (G3, US-5.6 보강) | `src/renderer/ui/panel/views/FileListView.tsx`(콜백 ref + ResizeObserver, `viewportH`), `src/renderer/index.html`(전역 CSS 리셋) | 기존 Must "가상 스크롤" 품질 결함 수정 — 신규 스코프 아님 |

---

## 1-H. UX / 레이아웃 확장(H장, Should) → 구현 파일 매핑

> features §H(상단 아이콘바·사이드바 토글·분할 크기조절·터미널 열기·경로 직접 입력·파일 유형 아이콘)를 2026-06-07 정식 편입(PRD §6 Should·§8 `Ctrl+B`·user-stories 에픽7). 기존 동작(분할 A2·사이드바 C3·단축키 E1·주소 표시줄 C1·아이콘 B1)을 더 빠르게 호출·조정·구체화하는 UX 확장이다. H1~H3·H5는 신규 IPC 채널이 아니라 **renderer commandId·세션 스냅샷 DTO 확장**으로, H4는 **신규 채널 `shell:open-terminal`**, H6은 **P1 예약 채널 `shell:icon` 정식 구현(호출부 추가)**으로 추적한다(2026-06-07 코드 확인 ✅).

| 기능(추적원) | UI 컴포넌트 | 유스케이스/스토어 | 도메인 규칙 | commandId / 채널 / DTO | 비고 |
|---|---|---|---|---|---|
| **상단 전역 아이콘바** (H1, US-7.1, S) ✅ | `src/renderer/ui/toolbar/IconBar.tsx`·`iconBarItems.ts`(4그룹 20버튼·activeWhen·`aria-pressed`·툴팁 단축키) · `src/renderer/ui/App.tsx`(마운트) | `app/usecases/commandBus.ts`(execCommand 수렴) · `settings.ts#toggleThemeMode` · `ui/theme/applyTheme.ts#systemPrefersDark` | 버튼 활성조건·토글 상태 표시 규칙 | **신규 commandId**: `sidebar.toggle`·`theme.toggle`·`view.setMode.list`·`view.setMode.details`(renderer 내부, IPC 채널 아님) | 키보드/컨텍스트메뉴와 동일 commandId 경로 수렴 |
| **사이드바 온오프 토글** (H2, US-7.2, S) ✅ | `IconBar` 사이드바 버튼 | `app/stores/sidebarSlice.ts#toggleSidebar`(`sidebarCollapsed`·세션 영속 기존) | — | `domain/keybindings/index.ts`(`ctrl+b`→`sidebar.toggle`) | ※ 실제 토글은 런타임 DOM 의존 → 런타임 스모크 권장 |
| **분할 패널 크기조절** (H3, US-7.3, S) ✅ | `src/renderer/ui/layout/SplitDivider.tsx` · `LayoutHost.tsx`(2분할 flex/4분할 grid 컨테이너 ref 측정·2축 독립) | `app/stores/tabsSlice.ts#setSplitRatio`(클램프 `SPLIT_MIN_RATIO=0.15`~0.85) · `usecases/session.ts`(`splitRatios` 직렬화) | `ui/layout/splitMath.ts#ratioFromPoint`(순수함수) · `domain/entities`(`Tab.splitRatios`·`SPLIT_MIN_RATIO`) | **DTO 확장**: `shared/dto TabSnapshot.splitRatios?` · `main/persistence/defaults.ts#coerceSplitRatios`(정규화) | ※ 실제 드래그는 런타임 DOM 의존 → 런타임 스모크 권장. 세션 스냅샷 필드 확장이며 P1 IPC 채널 동결과 무관 |
| **우클릭 "터미널 열기"** (H4, US-7.4, S) ✅ | `usecases/contextMenu.ts`(단일 폴더·빈 영역 "터미널 열기" 항목, 파일·My PC 미표시) · `ui/contextmenu/ContextMenu.tsx` | `app/usecases/open.ts#openTerminalAt`·`terminalErrorMessage` · `infra/api`(`shellApi.openTerminal`) | 실존 디렉토리 경로만 진입점 표시 | **신규 채널 `shell:open-terminal`**: `channels.ts`·`contracts.ts ShellOpenTerminalReq` → `main/os/shell.ts#openTerminal`(**[2026-06-11 결함 수정] 3단 폴백**: ① `spawnDetached`(신규·동기 throw 흡수·`detached`/`stdio:'ignore'`·`'spawn'` 시 unref) `wt.exe -d <dir>` 직접 spawn → ② `execFileNoThrow`(신규)+PowerShell 릴레이(`powershell.exe -NoProfile -NonInteractive -Command`로 `wt.exe -d $d`·경로는 env `EXPLORER_TERMINAL_DIR`·문자열 합성 0·후행 `\`→`\\`) → ③ `powershell.exe -NoExit`(cwd=경로) detached spawn) · `shell.handlers.ts`(sender·zod·guardPath·stat 디렉토리 검증·**무변경**: "throw 금지·전부 Result.err 전파" 계약을 shell.ts가 실제로 지킴) · `guard.ts#zShellOpenTerminalReq` | ADR-005(명령행 문자열 합성 0·인자 배열/cwd/env로만 경로 전달). P1 동결 후 신규 채널(P6 `preview:read`·`telemetry:get-opt-in` 동일 선례). ※ 네이티브 `wt.exe` 실행(특히 스토어 별칭 2차 릴레이)은 사용자 세션 런타임 스모크 권장 🟡(§0.5 2026-06-11 결함 수정 단락) |
| **디렉토리 경로 직접 입력** (H5, US-7.5, S) ✅ | `src/renderer/ui/toolbar/PanelToolbar.tsx`(단일 클릭 편집 진입·`closest('button')` 가드 · **[2026-06-09 추가·M3 US-12.4 교차 보강] 원격 경로일 때 호스트 프리픽스(`sftp://host`) 고정 표시·경로만 편집**: `remoteLoc=parseRemotePath(path)`·편집 진입 시 `editValue`=`loc.remotePath`(경로만)·commit 시 `makeRemotePath`로 호스트 결합·전체 URI 입력 `isRemotePath` 방어) | 기존 `validateAndNavigate`·`Ctrl+L`·더블클릭 재사용 · **[2026-06-09] `usecases/navigate.ts#validateAndNavigate` 원격 URI 우회 분기(`isRemotePath`→로컬 `fs:validate-path` 우회·바로 navigate)** | 경로 검증·인라인 오류(기존) · **`domain/rules/remoteLocation`(`parseRemotePath`/`makeRemotePath`/`isRemotePath`)** | 신규 채널·단축키·DTO **없음**(진입 방식·원격 표시 확장만) | C1·US-3.1 동작을 단일 클릭 진입으로 구체화 · **원격 경로-only 입력은 §1-M M3(US-12.4) 행과 동일 파일·동작(교차 보강·중복 매핑 아님)** |
| **파일 유형별 OS 아이콘** (H6, US-7.6, S) ✅ | `src/renderer/ui/panel/views/FileListView.tsx`(`OSIcon`) | `app/usecases/icons.ts`(`getCachedIcon`·`requestIcon`·`subscribeIcon`·`iconKeyFor`) · `infra/icon/iconCache.ts`(`iconKeyFor`·`iconRequestFor`·디듀프·구독) | 확장자 단위 캐시·per-file path 키·가상 스크롤 `iconRef` 지연 | **예약 채널 `shell:icon` 정식 구현(호출부 추가)**: `contracts.ts ShellIconReq/Res` → `main/os/icon.ts`(`getFileIconDataUrl`·`cacheKeyFor`·LRU512·실패 비캐싱) · `shell.handlers.ts SHELL_ICON`(sender·zod·검증) | ADR-005. **P1 동결 후 "P2/P4 시스템 아이콘 캐시"로 예약·미구현이던 채널을 정식 구현 → 예약→구현 드리프트 해소**. ※ 네이티브 `app.getFileIcon` 실행 런타임 스모크 권장 |

---

## 1-I. 분석·시각화 / 접근성 테마(I장, Should) → 구현 파일 매핑

> features §I(디렉토리 사용량 대시보드·블루라이트 차단 테마)를 2026-06-07 정식 편입·구현(PRD §6 Should·§10 R4/R5·user-stories 에픽8). I1은 **신규 채널 `analyze:scan:*` 5종**(P1 동결 이후 추가 — P6 `preview:read`·H4 `shell:open-terminal` 동일 선례)으로, 디스크 요약은 기존 `DriveDTO`+`diskSpace()` 재사용(backend 신규 0)이며 차트는 **recharts(MIT) lazy 청크**로 추적한다. I2는 테마 토큰 1종 추가(신규 채널 0). 구현 명명은 코드가 단일 출처다 — **DTO는 `ScanResult.root`/`ScanEntry.bytes`**(계획서의 `rootPath`/`sizeBytes`/`ScanEntryDTO`는 미채택, 아래 명명 드리프트 주석). 2026-06-07 코드 확인 ✅(typecheck/lint 0·`verify:scan` 28/0·QA PASS).

| 기능(추적원) | UI 컴포넌트 | 유스케이스/스토어 | 도메인/엔진 규칙 | commandId / 채널 / DTO | 비고 |
|---|---|---|---|---|---|
| **디렉토리 사용량 대시보드** (I1, US-8.1, S) ✅ | `src/renderer/ui/dashboard/DashboardModal.tsx`(+`DashboardModalBody.tsx` **React.lazy**) · recharts 도넛/막대+표 병행 · `ui/toolbar/iconBarItems.ts`(`dashboard.open`) · `ui/App.tsx`(자동 팝업) · SettingsDialog("시작 시 대시보드 표시" 토글) | `app/stores/analyzeSlice.ts` · `app/usecases/dashboard.ts`(scanId 상관 브리지) · `app/stores/uiSlice.ts`(`showDashboardOnStartup` 기본 `true`) | `src/main/operations/scanEngine.ts`(재귀 집계·Top10 힙·순환 realpath Set·skipped·취소·truncated) · `src/main/workers/scanWorker.ts`·`scanProtocol.ts`(SharedArrayBuffer 취소) · `src/main/operations/ScanManager.ts`(scanId·200ms 스로틀·푸시) | **신규 채널 `analyze:scan:start/progress/done/error/cancel`(5종)**: `channels.ts`·`contracts.ts` → `main/ipc/analyze.handlers.ts`(guardPath·디렉토리 검증). **DTO `ScanResult{root,totalBytes,totalItems,topFolders,topFiles,skipped,canceled,truncated}`·`ScanEntry{path,name,bytes,isDir}`**. 디스크 요약=기존 `DriveDTO.totalBytes/freeBytes`+`diskSpace()` 재사용(신규 0) | ADR-005. **신규 의존성 recharts 3.8.1(MIT)·831kB lazy 청크 분리.** ※ (가능 시) 파일 유형별 비중 인사이트는 선택항 미구현 🔜·네이티브 statfs/대용량 스캔 성능 실측은 런타임 스모크 권장 |
| **블루라이트 차단 테마** (I2, US-8.2, S) ✅ | `ui/settings/SettingsDialog`(테마 4종 선택) | `app/usecases/settings.ts#toggleThemeMode`(light↔dark 유지) | `ui/theme/palette.ts BLUELIGHT_PALETTE`(13토큰·#FBF0D9) · `ui/theme/applyTheme.ts ResolvedTheme`(bluelight **독립 resolved**·light 폴백 아님) | `ThemeMode` 4종(+`'bluelight'`) · `main/persistence/defaults.ts THEME_MODES` · `main/ipc/guard.ts zThemeMode` 화이트리스트. 신규 채널 **없음** | WCAG AA 통과(본문 11.04:1·muted 5.25:1). E2·US-5.3 선택지 1종 확장(기존 테마 동작 무변경) |

> **계획서 명명 드리프트(코드=단일 출처)**: `docs/I-dashboard-bluelight-plan.md`는 DTO를 `ScanEntryDTO`·`sizeBytes`·`rootPath`로 기재했으나 **구현은 `ScanEntry`·`bytes`·`root`** 로 확정됐다(QA 관찰-1). traceability/roadmap은 구현 명명을 기록한다. 또한 계획서는 scanWorker/scanProtocol을 `src/main/operations/`로 적었으나 실제 위치는 **`src/main/workers/`** 다(코드 기준 기록).

---

## 1-J. 보기 · 실시간 갱신 · 뷰어 확장 · 브랜딩(J장, Should) → 구현 파일 매핑

> features §J(박스 선택·패널 실시간 갱신·보기 5종·AGT-Finder 브랜딩·미리보기 2단 뷰어·미리보기 폭 조절·즐겨찾기 별칭)를 2026-06-07 정식 편입·구현(PRD §6 Should·J4 M릴리스·user-stories 에픽9). J2는 **신규 채널 `fs:watch:*` 4종**(P1 동결 이후 추가 — P6 `preview:read`·H4 `shell:open-terminal`·I장 `analyze:scan:*` 동일 선례)으로, J1/J3/J5(렌더러)·J6/J7은 신규 IPC 채널이 아니라 **slice·DTO 확장**(ViewMode·previewWidth·favoriteLabels)으로 추적한다. **식별자 권위 기준 = 기획문서 feat-J1~J7/US-9.1~9.7**(계획서 내부 번호 J1~J8 아님). 2026-06-07 코드 확인 ✅(typecheck/lint 0·`verify:watch` 77/0·`verify:store` 76/0·QA PASS·빌드 성공; J2 보류 2건(선택/스크롤 보존·UNC 폴링 폴백) 구현 완료 + 매핑 네트워크 드라이브 `GetDriveType` 연동 완료 ✅, `subst`·일부 클라우드(`DriveType≠4`)만 미포함 한계).

| 기능(추적원) | UI 컴포넌트 | 유스케이스/스토어 | 도메인/엔진 규칙 | 채널 / DTO | 비고 |
|---|---|---|---|---|---|
| **드래그 박스 선택(러버밴드)** (feat-J1, US-9.1, S) ✅ | `src/renderer/ui/panel/views/FileListView.tsx`(러버밴드 오버레이·경계 자동 스크롤) | `src/renderer/app/stores/selectionSlice.ts#boxSelect`(교체/누적/범위 모드) | `src/renderer/domain/rules/boxSelect.ts`(사각형 교차 판정 순수함수·가상 스크롤 마운트 항목 포함) | 신규 채널·DTO **없음**(렌더러 내부 선택 연산) | ※ 실제 마우스 드래그·자동 스크롤은 런타임 DOM 의존 → 런타임 스모크 권장 |
| **패널 실시간 갱신(워처)** (feat-J2, US-9.2, S) ✅ | `FileListView`(자동 갱신 반영·`pendingScrollRestore` 1회성 스크롤 복원) | `src/renderer/app/usecases/watchBridge.ts`(watchId 상관·경로 교체 시 이전 감시 해제·리소스 정리·onEvent→`softRefresh`) · `src/renderer/app/stores/panelsSlice.ts`(`softRefresh`·`capturePreserve`·`_applyPreserve`·`pendingScrollRestore`·navigate 3종 `resetSelection`) · `src/renderer/app/stores/selectionSlice.ts#setSelection` | `src/main/fs/WatchService.ts`(non-recursive `fs.watch`·디바운스 병합·권한/네트워크/미지원 격리 throw 0→onError + **UNC + 매핑 네트워크 드라이브 eager 폴링·reactive fs.watch error 폴백·4s readdir diff·stat 승계·pollBusy 재진입 가드·>20k 항목 비활성·stop 정리·미지 고정 드라이브 lazy refresh trigger**) · `src/main/os/driveType.ts`(`DriveTypeService` — PowerShell CIM `Win32_LogicalDisk DriveType=4`=`DRIVE_REMOTE` execFile 조회·네트워크 드라이브 문자 캐시 원자 Set 교체·throttle·재진입 가드·빈집합 폴백·헤드리스 `queryFn`/`setNetworkDriveLetters` 주입) · `src/main/fs/paths.ts`(`isUncPath`·`isNetworkDriveRoot`(driveType 캐시 동기 조회)·`isLikelyRemotePath`) · `src/main/index.ts`(부팅 non-blocking refresh) | **신규 채널 `fs:watch:start/event/stop/error`(4종)**: `channels.ts`·`contracts.ts` → `main/ipc/watch.handlers.ts`(sender·guardPath·디렉토리 검증). **폴링·`GetDriveType` 연동은 backend 내부·계약 변경 0(투명)·신규 npm 의존성 0(PowerShell 시스템 내장)** | ADR-005(execFile 셸 미경유·고정 상수 스크립트·`^[A-Z]:` 화이트리스트 파싱). P1 동결 후 신규 채널(선례 동일 규약). 보류 2건 구현 완료: **선택/스크롤 보존 ✅·UNC 폴링 폴백 ✅**. **매핑 네트워크 드라이브(`X:\`) `GetDriveType` 연동 완료 → eager 폴링 적용 ✅**. 잔여 한계: `subst`·일부 클라우드(`DriveType≠4`) 미포함(reactive 폴백 유지). ※ 네이티브 워처 실제 이벤트 런타임 스모크 권장 |
| **Windows 표준 보기 5종** (feat-J3, US-9.3, S) ✅ | `FileListView`(아이콘 그리드 3종·H6 `OSIcon`/`shell:icon` 재사용·가상 스크롤) · `src/renderer/ui/toolbar/PanelToolbar.tsx`(보기 드롭다운) | `app/stores/panelsSlice.ts`(패널별 `view` 기억) | 그리드 2차원 레이아웃·박스 선택(J1) 교차 정합 | **DTO `ViewMode`**(`'icons-large'|'icons-medium'|'icons-small'|'list'|'details'`, `shared/dto`) · `main/persistence/defaults.ts VIEW_MODES` 화이트리스트 | 신규 IPC 채널 없음(아이콘은 기존 `shell:icon` 재사용) |
| **브랜딩 AGT-Finder** (feat-J4, US-9.4, M릴리스) ✅ | `src/renderer/index.html`(`<title>AGT-Finder`) | — | — | (패키징/식별자) `package.json`(name `agt-finder`·description) · `electron-builder.yml`(**`appId: com.agtfinder.app`**·`productName: AGT-Finder`) · `src/main/windows/mainWindow.ts` · `src/main/index.ts` · `src/main/persistence/paths.ts`(userData 경로) | 코드네임 "Explorer"는 내부 ExplorerApi 타입·주석만 유지(사용자 노출 0). ※ exe/인스톨러 파일명·바로가기는 패키징 산출물 런타임 확인 권장 |
| **미리보기 2단 뷰어(정보+확장 뷰어)** (feat-J5, US-9.5, S) ✅ | `src/renderer/ui/preview/PreviewPanel.tsx`(상단 `PreviewInfoCard`+하단 뷰어) · `renderers/CodePreview.tsx`(highlight.js 강조) · `renderers/MarkdownPreview.tsx`(marked+DOMPurify) · 기존 Image/Text/Meta/Unsupported 폴백 | `uiSlice.previewOpen`·`commandBus#preview.toggle`(`Ctrl+P` 재사용) | `FileSystemService.readPreview`(lang/isMarkdown 판별·상한·바이너리) | **기존 `preview:read` 재사용**(신규 채널 0) · 신규 의존성 **highlight.js(BSD-3)·marked(MIT)·dompurify(MPL-2.0)** lazy 청크 | ADR-005·CSP. 마크다운 DOMPurify 새니타이즈·원격 로드/eval 차단(읽기 전용) |
| **미리보기 패널 폭 조절** (feat-J6, US-9.6, S) ✅ | H3 `ui/layout/SplitDivider.tsx` 재사용(미리보기-본문 분할선) | `app/stores/uiSlice.ts#previewWidth`(클램프·기본 폭 복귀) · `usecases/session.ts`(직렬화·영속) | `ui/layout/splitMath.ts#ratioFromPoint`(순수함수 재사용) | **DTO 확장**: `shared/dto`(`ui.previewWidth`) | 토글 off→on 후 폭 유지. H3 메커니즘 재사용·다른 경계(가로 축) |
| **즐겨찾기 별칭 변경** (feat-J7, US-9.7, S) ✅ | `src/renderer/ui/sidebar/Sidebar.tsx`(인라인 편집·우클릭/`F2`·Enter/Esc) | `app/stores/sidebarSlice.ts`(별칭 설정/초기화·basename 폴백) · `usecases/session.ts`(영속) | 표시 전용(경로 불변)·빈 별칭 basename 폴백·중복 허용 | **DTO 확장**: `shared/dto SidebarSnapshot.favoriteLabels` · `main/persistence/defaults.ts`(정규화) | 기존 `F2` 재사용·신규 채널 없음 |

> **F5/F6 매핑 제거(2026-06-07 사용자 결정·은폐 금지)**: 본 추적성의 §1 "패널 간 이동/복사" 행과 §2 단축키 표 `F5`/`F6` 행이 가리키던 `panel.copyToOther`/`moveToOther` commandId·`usecases/fileOps.ts`의 `copyToOtherPanel`/`moveToOtherPanel` 함수는 **코드에서 제거**됐다(`domain/keybindings`·`commandBus`·`fileOps`·`ShortcutHelp`). 위 두 곳을 코드 기준으로 정정했다 — **유령 매핑(코드 없는데 매핑 잔존) 제거**. 패널 간 복사/이동은 D&D(A3)·클립보드(`Ctrl+C/X/V`)가 단일 경로로 유지된다(이 매핑은 그대로 유효).

> **식별자 매핑 주의(reviewer 중대-1)**: 계획서 `J-ref-batch-plan.md`의 내부 번호(J1~J8)와 기획문서 feat-ID(feat-J1~J7·US-9.1~9.7)가 다르다. 본 추적성은 **기획문서 feat-J1~J7/US-9.1~9.7을 권위 기준**으로 작성했다(박스선택=J1/9.1, 실시간갱신=J2/9.2, 보기5종=J3/9.3, 브랜딩=J4/9.4, 뷰어=J5/9.5, 폭조절=J6/9.6, 별칭=J7/9.7, F5/F6제거=feat 없음·기존 정정).

---

## 1-K. 되돌리기 · 휴지통 관리 · 유형별 비중(K장, Should) → 구현 파일 매핑

> features §K(되돌리기 `Ctrl+Z` 다단계 undo·휴지통 관리 화면·파일 유형별 비중)를 2026-06-07 정식 편입·구현(PRD §6 Should·user-stories 에픽10). roadmap §0.5 "P6 미구현 잔여 🔜"의 되돌리기·휴지통 관리 화면을 정식 구현하고(P6 잔여 해소), I1 "(가능하면) 유형별 비중" 선택항을 K3로 정식화했다. K2는 **신규 채널 `trash:*` 3종**(P1 동결 이후 추가 — `preview:read`·`shell:open-terminal`·`analyze:scan:*`·`fs:watch:*` 동일 선례), K1·K3은 신규 IPC 채널 0(K1=renderer undo 스택+기존 `op:*`/`fsApi`/`trashApi` 역연산, K3=기존 `analyze:scan:*`·`scanEngine` byCategory 1패스 확장)이다. 2026-06-07 코드 확인 ✅(typecheck/lint 0·`verify:recyclebin` 37/0·`verify:store` 99/0·`verify:scan` 39/0·빌드 성공·QA PASS).

| 기능(추적원) | UI 컴포넌트 | 유스케이스/스토어 | 도메인/엔진 규칙 | 채널 / DTO | 비고 |
|---|---|---|---|---|---|
| **되돌리기 `Ctrl+Z` 다단계 undo** (feat-K1, US-10.1, S) ✅ | (진입은 `commandBus` `file.undo`·아이콘바/메뉴; 토스트 안내) | `src/renderer/app/stores/undoSlice.ts`(`undoStack`·`UNDO_STACK_CAP=50`·`UndoEntry` 판별유니온 rename/create/move/copy/trash·`pushUndo`/`popUndo`) · `src/renderer/app/usecases/undo.ts`(`performUndo`·kind별 역연산·역연산 전 충돌 선검증·빈 스택 안내) · `usecases/fileOps.ts`/`operationsBridge.ts`(작업 시 `undoMeta` hook 적재·역연산 op엔 undoMeta 미부여) · `commandBus.ts`(`file.undo`→`performUndo`·`notYet` 제거) | rename↔rename·create→휴지통·move→역move·copy→사본휴지통·trash→`trashApi.restore`·영구삭제(delete) 미적재 | **신규 채널 0**(기존 `op:*`·`fsApi.validatePath`·`trashApi` 재사용)·`Ctrl+Z` global(입력/편집 미발화) | ADR-005·US-2.4 충돌 안전. **정직 한계: copy-undo 보수적(사본 경로 충돌 시 미생성·중단)·영구삭제 되돌릴 수 없음(미push·토스트).** ※ undo 역연산 네이티브 동작 런타임 스모크 권장 |
| **휴지통 관리 화면(복원·비우기)** (feat-K2, US-10.2, S) ✅ | `src/renderer/ui/trash/TrashDialog.tsx`(목록[이름·원경로·삭제일·크기]·복원·비우기 확인·focus trap[중첩 확인 포함]·Esc) · 사이드바/아이콘바 ④도구 진입 | `src/renderer/app/stores/trashSlice.ts`·`src/renderer/app/usecases/trash.ts` · `uiSlice.trashOpen`+가드 | `src/main/os/recycleBin.ts`(PowerShell Shell COM·env 주입·`$Recycle.Bin` 화이트리스트·`Move-Item` 폴백·throw 0·헤드리스 주입) | **신규 채널 `trash:list`/`trash:restore`/`trash:empty`(3종)**: `channels.ts`·`contracts.ts`(`TrashItemDTO`) → `main/ipc/trash.handlers.ts`(sender·zod·화이트리스트 재검증·`confirmed` 게이트) | ADR-005(임의 경로 실행 차단·검증된 항목에만 위임). P1 동결 후 신규 채널(선례 동일 규약·invoke). verify:recyclebin 37. ※ 휴지통 COM 실제 동작 런타임 스모크 권장 |
| **파일 유형별 비중 인사이트** (feat-K3, US-10.3, S) ✅ | `src/renderer/ui/dashboard/charts/CategoryBar.tsx`+표(비중%·절대 용량·파일 수·가장 큰 유형) · `DashboardModalBody.tsx` | `app/stores/analyzeSlice.ts`(byCategory 소비) | `src/main/operations/categorize.ts`(7카테고리 확장자 매핑·소문자 정규화·미매핑/확장자 없음→기타) · `src/main/operations/scanEngine.ts`(byCategory **1패스 집계·추가 I/O 0**) | **신규 채널 0**(기존 `analyze:scan:*` 재사용) · **DTO `ScanResult.byCategory?: CategoryUsage[]`**(비파괴 확장) | recharts 재사용(신규 의존성 0)·바이트 내림차순. I1 스캔 비차단·진행률·취소·순환 차단 규칙 승계. verify:scan 39 |

> **K장 신규 채널·의존성 정합(드리프트 아님)**: `trash:*` 3종은 P1 "전 채널 타입 동결" 이후 추가됐으나 P6 `preview:read`·H4 `shell:open-terminal`·I장 `analyze:scan:*`·J장 `fs:watch:*`과 동일 선례·동일 guard/zod/Result·sender 검증·화이트리스트·`confirmed` 게이트 규약을 따른다(product-planner가 PRD §6 Should·features §K2·US-10.2로 정식 편입). 신규 npm 의존성 0(`recycleBin.ts`는 PowerShell 시스템 내장·`execFile`). K1·K3은 신규 IPC 채널 0. **유령 매핑 없음(모든 매핑이 실 파일 경로와 일치, 2026-06-07 코드 확인).**

---

## 1-L. 그리드 보기 이미지 썸네일(L장, Should) → 구현 파일 매핑

> features §L(그리드 보기 이미지 썸네일 자체 생성)을 2026-06-07 정식 편입·구현(PRD §6 Should·user-stories 에픽11). roadmap §0.5 "P6 미구현 잔여 🔜"의 **마지막 항목 "그리드 썸네일 이미지 자체 생성"**을 정식 구현(P6 잔여 전부 해소 → §L 완료 시점 2026-06-07 기준 개발 잔여 🔜 0이었음; 이후 2026-06-08 §M 3건 신규 편입으로 현재 개발 잔여는 §M 3건 — roadmap §0.5 참조). J3(보기 5종 ✅)의 아이콘 그리드에서 이미지 파일만 형식 아이콘 대신 실제 내용 축소 썸네일로 표시, 그 외/실패는 H6 OS 아이콘 폴백. L1은 **신규 채널 `preview:thumbnail`(invoke 1종)**(P1 동결 이후 추가 — `preview:read`·`shell:open-terminal`·`analyze:scan:*`·`fs:watch:*`·`trash:*` 동일 선례)으로, **신규 npm 의존성 0**(Electron 내장 nativeImage). 2026-06-07 코드 확인 ✅(typecheck/lint 0·`verify:thumbnail` 33/0·빌드 성공·QA PASS US-11.1 8/8 결함 0).

| 기능(추적원) | UI 컴포넌트 | 유스케이스/스토어 | 도메인/엔진 규칙 | 채널 / DTO | 비고 |
|---|---|---|---|---|---|
| **그리드 보기 이미지 썸네일 자체 생성** (feat-L1, US-11.1, S) ✅ | `src/renderer/ui/panel/views/FileListView.tsx`(`ThumbnailIcon` — 그리드 icons-* 분기 한정·이미지=`<img objectFit:contain>` 비율보존 레터박스·미로드/null=`OSIcon` 폴백·로드 전 OSIcon 자리표시·details/list는 OS 아이콘 유지) | `src/renderer/infra/icon/thumbnailCache.ts`(iconCache 동형 전역 캐시·negCache·in-flight 디듀프·`useSyncExternalStore` 구독·`thumbnailKeyFor`·`requestThumbnail`) · `app/usecases/thumbnails`(경유) | `src/renderer/domain/image.ts`(`isThumbnailableExt` 이미지 판별·`thumbSizeFor` 셀 px×DPR→버킷 스냅, dpr=Math.min(2,devicePixelRatio)) | **신규 채널 `preview:thumbnail`(invoke)**: `channels.ts`(`PREVIEW_THUMBNAIL`)·`contracts.ts`(`ThumbnailReq`/`ThumbnailRes`·`Result<ThumbnailRes>`) → `main/ipc/preview.handlers.ts`(`handleGuarded`·`guardPath`)·`main/os/thumbnail.ts`(`getThumbnailDataUrl`·nativeImage `createFromPath`→비율보존 resize(긴 변=size 단일축)→`toDataURL`·`THUMB_MAX_BYTES` 30MB·LRU `MAX_THUMB_CACHE` 256·실패 비캐싱·세마포어 4·헤드리스 nativeImage 주입)·`guard.ts`(`zThumbnailReq`·`THUMB_SIZE_BUCKETS [32,48,64,96,128]` 화이트리스트) | ADR-005(렌더러 직접 파일 접근 없음·`guardPath`·H6 동일 보안 모델). dataUrl만(CSP `img-src data:`)·size 버킷 화이트리스트(DoS 차단). **신규 의존성 0**(Electron 내장 nativeImage). verify:thumbnail 33. ※ nativeImage 실 디코드·GUI 그리드 `<img>` 교체·1만 항목 비차단 런타임 스모크 권장 |

> **L장 신규 채널·의존성 정합(드리프트 아님)**: `preview:thumbnail`(invoke 1종)은 P1 "전 채널 타입 동결" 이후 추가됐으나 P6 `preview:read`·H4 `shell:open-terminal`·I장 `analyze:scan:*`·J장 `fs:watch:*`·K장 `trash:*`과 동일 선례·동일 guard/zod/Result·sender 검증·`guardPath`·size 버킷 화이트리스트 규약을 따른다(product-planner가 PRD §6 Should·features §L1·US-11.1로 정식 편입 — J3에서 보류했던 "이미지 썸네일 자체 생성"의 정식 구현). **신규 npm 의존성 0**(`thumbnail.ts`는 Electron 내장 nativeImage). **유령 매핑 없음(모든 매핑이 실 파일 경로와 일치, 2026-06-07 코드 확인 — `src/main/os/thumbnail.ts`·`src/renderer/infra/icon/thumbnailCache.ts`·`src/renderer/domain/image.ts`·`FileListView.tsx ThumbnailIcon` 실재 확인).**

---

## 1-P7. 릴리스 안정화 — 성능·접근성·보안·패키징(P7) → 구현 파일 매핑

> 로드맵 P7(안정화·성능검증·접근성·패키징, PRD §3·§7 NFR)의 **헤드리스 구현분**을 추적한다. 사용자 UI 신기능이 아니라 검증 하니스·접근성 보강·빌드 설정이므로 **실제 구현 파일·verify 스크립트·문서**로 추적한다(2026-06-07 코드 확인 ✅). **헤드리스로 증명된 것만 ✅, GUI 실행/인증서/실 환경 필요분은 🟡(런타임 잔여) — ✅ 위장 아님.**

| 기능(추적원) | 구현 파일(실경로) / 검증 | 상태 |
|---|---|---|
| **접근성 — 포커스 트랩·모달 ARIA·키보드** (PRD §7 접근성, US-5.4) | `src/renderer/ui/keyboard/useFocusTrap.ts`(첫 포커스·Tab 순환·opener 복귀·Esc 위임) · 모달 5종 `role=dialog`/`aria-modal`/Esc(`ConfirmDialog`·`DashboardModal`·`ConflictDialog`·`SettingsDialog`·`ProgressDialog`·[2026-06-11 `WorkspaceDialog` 폐지→워크스페이스 관리는 `SettingsDialog` 내 `WorkspacePanel` 임베드로 흡수=설정 모달 트랩 공유]) · `FileListView`(행 `aria-posinset/setsize`) · `:focus-visible`(전역+인라인 outline 제거) · Shift+F10 컨텍스트 메뉴 | ✅ 코드(실 스크린리더 발화·포커스 육안은 런타임 🟡) |
| **WCAG AA 대비** (PRD §7 접근성) | `scripts/verify-contrast.ts`(4팔레트 LIGHT/DARK/BLUELIGHT 주요 토큰쌍 AA 전수·실패 0) · `ui/theme/palette.ts`(LIGHT text-muted·DARK danger 미세 보정) | ✅ |
| **성능 측정 하니스(불변식)** (PRD §3·§7 1.5초/200ms, US-5.6) | `src/renderer/ui/panel/views/windowing.ts`(computeWindow 순수함수·`startIdx≤endIdx` 클램프) · `scripts/verify-perf.ts`(25: windowing 불변식·1만 항목 DOM 후보 수십개·200ms 스로틀·검색 필터) · `docs/P7-perf-measurement.md`(실측 절차) | ✅ 헤드리스 불변식 / 🟡 실측 숫자(GUI 런타임) |
| **F장 QA 매트릭스(코드경로)** (PRD F장 Windows 특수케이스) | `scripts/verify-fmatrix.ts`(32: 롱패스·정션 순환·UNC/매핑 판정·ENOENT/ENOTDIR throw 0) · `docs/P7-qa-matrix.md` | ✅ 헤드리스분 / 🟡 실케이스(실 네트워크·symlink·ACL deny EACCES) |
| **보안 audit·sourcemap·코드서명 설정** (PRD §7 보안, ADR-005/006) | `docs/P7-security-audit.md`(npm audit 9건 판정·릴리스 차단 아님) · `electron.vite.config.ts`(sourcemap main/preload true·renderer `'hidden'`) · `electron-builder.yml`(`!out/**/*.map` 제외·`CSC_LINK`/`CSC_KEY_PASSWORD`·`signingHashAlgorithms`) | ✅ 점검·설정(준비완료) / 🟡 실제 서명(.pfx 인증서)·NSIS 설치 실측 |

> **P7 정직 구분 주석**: 위 매핑은 코드·verify·문서로 **헤드리스 증명된 부분만 ✅**다. 성능 실측 숫자·실제 코드서명·NSIS 설치/실행/제거 실측·F장 실케이스·실 스크린리더는 GUI 실행/인증서/실 환경이 필요해 **🟡 런타임 잔여**다. **P7 전체는 아직 🟡(릴리스 미완)** — roadmap §0.5·§3 P7 단계와 동일 기준. npm audit 9건은 major 업그레이드 필요(비파괴 fix 0)·빌드 툴체인+electron 본체(ADR-005 완화)로 **릴리스 차단 아님**(사용자 결정 보류).

---

## 1-M. 외부 연계(§M, 2026-06-08 편입) → 구현 매핑 — ✅ 구현 완료·QA PASS (실 동작 런타임 스모크 🟡 · 2026-06-09 M3/M1 결함 수정 반영)

> features §M(M1 외부 D&D·M2 클립보드 CF_HDROP 양방향·M3 FTP/SFTP)·user-stories 에픽12(US-12.1~12.5)·flows F14~F16. **신규 [ADR-007](./adr/ADR-007-remote-protocol-and-network-boundary.md)** 가 보안·네트워크 경계·프로세스 배치를 정의(ADR-005 부분 개정). 신규 채널은 ADR-003 단일출처 규약(P1 동결 후 `preview:read`·`shell:open-terminal`·`analyze:scan:*`·`fs:watch:*`·`trash:*`·`preview:thumbnail` 동일 선례)으로 추가됐다. **상태: 3건 전부 ✅ 구현 완료·통합 QA PASS(2026-06-08·MP0~MP5)·매핑 17파일 실재 확인(허위/누락 매핑 0)** — 아래 표 모듈/채널은 계획이 아니라 실재 코드. 상태 단일 출처는 roadmap §0.5. **실 동작(외부 앱 실드롭·탐색기 양방향 왕복·실 SFTP/FTP/FTPS·실 DPAPI·실 전송)은 런타임 스모크 권장 🟡**(✅ 위장 아님).

| 기능(추적원) | UI 컴포넌트(계획) | 유스케이스/스토어(계획) | 도메인 규칙(계획) | IPC 채널(계획) | 관련 ADR | 상태 |
|---|---|---|---|---|---|---|
| **외부 프로그램으로 D&D 복사** (M1, US-12.1, F14, S) | `ui/dnd/useDrag.ts`(외부 드래그 시작·**2026-06-09 native `dragstart` 시 `endDrag()` 호출 + `onDragEnd` 노출 — 오버레이 잔존 결함 수정**)·`FileListView`(그리드/리스트 행 `onDragEnd` 연결) | `usecases/externalDrag.ts`(도착지 외부 판정→위임)·기존 A3 D&D 분기 | `domain/rules/transferRoute.ts`(외부=복사 고정)·기존 `resolveDragIntent` | **`dnd:start-drag`**(신규·invoke) → `main/os/dragdrop.ts`(`webContents.startDrag`·경로 정규화/존재/권한/로컬 한정)·`dnd.handlers.ts`(sender·zod) | ADR-007 ⑦, ADR-005 | ✅ (실 OLE 드롭 🟡) |
| **클립보드 외부 연계(CF_HDROP 양방향)** (M2, US-12.2, F15, S) | 컨텍스트메뉴/단축키(기존)·붙여넣기 활성 판정 | `usecases/clipboardExternal.ts`(쓰기/읽기·내부/외부 우선순위)·기존 clipboard usecase | 기존 "복사본" 명명·D4 충돌 준용 | **`clipboard:write-files`/`clipboard:read-files`/`clipboard:has-files`**(신규·기존 텍스트 폴백 `clipboard:copy-files`/`cut-files`/`paste-target`/`read` 대체·확장) → `main/os/shellClipboard.ts`(CF_HDROP·Preferred DropEffect 바이트)·`clipboard.handlers.ts`. 붙여넣기는 기존 `op:start(copy/move)` 재사용 | ADR-007 ⑦, ADR-005 | ✅ (실 왕복 🟡) |
| **FTP/SFTP 접속(자격증명 OS 보관소)** (M3, US-12.3, F16, C) | `ui/remote/`(연결 다이얼로그·사이드바 "원격"·호스트키/비암호화 경고) | `usecases/remote.ts`(연결/프로필 CRUD)·`stores/remoteSlice.ts` | — | **`remote:connect`/`remote:disconnect`**·**`remote:cred:save/has/delete`**·**`remote:profile:list/upsert/delete`**·푸시 **`remote:host-key`/`remote:session-error`** → `main/remote/RemoteSessionManager.ts`·`SftpAdapter`(ssh2-sftp-client)·`FtpAdapter`(basic-ftp)·`main/os/credentials.ts`(safeStorage/DPAPI) | **ADR-007 ①②③④⑤⑥**, ADR-005 | ✅ (실 서버/DPAPI 🟡) |
| **원격 탐색·다운로드** (M3, US-12.4, F16, C) | `ui/remote/`·기존 `FileListView`(원격 entries 재사용)·`PanelHeader`(원격 경로 배지) · **[2026-06-09 추가] `ui/toolbar/PanelToolbar.tsx` 원격 주소창 경로-only 입력**: `parseRemotePath(path)`로 `remoteLoc` 산출·원격일 때 호스트 프리픽스(`{remoteLoc.protocol}://{remoteLoc.host}`)는 고정 표시(L159·L174)·편집 진입 시 `editValue`=경로만(`loc.remotePath`, L67~68)·commit 시 `makeRemotePath(protocol,host,경로)`로 호스트와 결합(L80~85·전체 URI 직접 입력도 `isRemotePath(raw)` 분기로 방어 처리) | `usecases/remote.ts#listRemoteDir`(탐색·다운로드 · **2026-06-09 각 항목 path를 `makeRemotePath(protocol,host,path)`로 원격 URI 재구성 후 주입 — 디렉토리 더블클릭 ENOENT 결함 수정**)·**`usecases/navigate.ts#validateAndNavigate`(2026-06-09: `isRemotePath(target)` 분기로 원격 URI 타깃은 로컬 `fs:validate-path` 우회하고 바로 `navigate` — 원격 검증 채널 없음·잘못된 경로는 패널 오류로 표면화, L31~35)**·`usecases/navigation`(location.kind 분기)·기존 operations | `transferRoute.ts`(원격→로컬=download)·`domain/paths/index.ts`(`parentOf`·`baseName`·`breadcrumbs`·`normalizeDisplay`가 **2026-06-09부터 `remoteLocation` 규칙 위임 — 원격 URI를 win32 경로로 오해하던 결함 수정**)·**`domain/rules/remoteLocation`(`parseRemotePath`·`makeRemotePath`·`isRemotePath` — 주소창/네비 공용 단일출처)**·원격 응답 불신 검증 | **`remote:list`/`remote:stat`/`remote:mkdir`/`remote:rename`/`remote:delete`**·**`remote:download`** → `remoteTransfer.ts`(.part 임시명·원자 rename)·진행률 **기존 `op:progress`/`op:conflict`/`op:done`** 재사용 · **주소창 경로-only 입력은 신규 채널 0**(렌더러 표시/파싱 + 기존 navigate 분기) | ADR-007 ⑤⑥, ADR-003 | ✅ 코드(주소창 경로-only 입력 포함) / 실 GUI·서버 🟡 |
| **원격 업로드·연결 끊김/타임아웃** (M3, US-12.5, F16, C) | `ui/remote/`(세션 오류 표시·패널 격리) | `usecases/remote.ts`(업로드·재시도 안내) | `transferRoute.ts`(로컬→원격=upload) | **`remote:upload`**·**`op:cancel`** 재사용·푸시 **`remote:session-error`**(세션 격리·D4 충돌/E4 진행률 재사용) | ADR-007 ⑤⑥, ADR-005 | ✅ (실 전송/끊김 🟡) |

> **§M 신규 채널 정합(드리프트 아님·구현 완료)**: `dnd:*`·파일 `clipboard:*`·`remote:*`는 P1 "전 채널 동결" 이후 추가됐고, 기존 6선례(`preview:read`·`shell:open-terminal`·`analyze:scan:*`·`fs:watch:*`·`trash:*`·`preview:thumbnail`)와 동일 규약(`shared/ipc` 단일출처·invoke/이벤트·`Result`·sender·zod·경로/세션 검증)을 따른다(product-planner가 PRD §6 §M·features §M·US-12.1~12.5로 정식 편입). **신규 npm 의존성: `ssh2-sftp-client`(Apache-2.0)·`basic-ftp`(MIT) 2종(M3만)·`src/main/remote/`에서만 import(ESLint 화이트리스트·`verify:eslint-remote` 29).** M1·M2는 신규 npm 의존성 0(Electron 내장 startDrag·clipboard buffer·safeStorage). **2026-06-08 구현 완료 — 위 표 매핑은 실 파일 경로로 확정됨(17파일 실재·doc-synchronizer 동기화 완료).**

> **§M 결함 수정·드리프트 정정(2026-06-09·은폐 금지·✅위장 정정)**: 직전 동기화(2026-06-08)는 §M을 "구현 완료·코드 정합 ✅ / 실 동작 🟡"로 표기했으나, 실제로는 **M3 원격 디렉토리 더블클릭 진입(ENOENT)·M1 외부 드래그 오버레이 정리가 코드 레벨에서 깨져 있었다** — "코드 정합 ✅"가 부분적으로 거짓이었던 드리프트(런타임 스모크가 잡았어야 할 결함). 2026-06-09 수정으로 코드 정합 회복: M3=`usecases/remote.ts#listRemoteDir` 원격 URI 재구성 + `domain/paths/index.ts` 원격 URI 인식(remoteLocation 위임)·`verify:domain` +8·`verify:remote-route` 47 회귀 0, M1=`ui/dnd/useDrag.ts` native dragstart 시 endDrag + `FileListView` onDragEnd·`verify:dnd` 29 회귀 0. **실 SFTP/FTP 원격 진입·실 OLE 드래그 오버레이 정리의 실 GUI 동작은 여전히 런타임 스모크 권장 🟡.** 신규 스코프 아님(기존 M3·M1 결함 수정·계약/채널 변경 0). 상태 단일 출처 roadmap §0.5 머리말 §M 줄·"추가(2026-06-09)" 줄.
>
> **§M 보안 추적(필수 수용 기준)**: 자격증명 평문 금지·OS 보관소만(D6) → `os/credentials.ts`(safeStorage/DPAPI)·비밀 DTO/로그 미수록(ADR-007 ③⑥). 네트워크 경계 D5→D7 부분 개정 → `src/main/remote/` ESLint 화이트리스트(ADR-007 ②)·`.eslintrc.cjs` overrides. **구현 시 `.eslintrc.cjs` main override `no-restricted-imports`에 기존 8개(`http`/`https`/`net`/`dgram`±node:)는 유지하고 `node:tls`·원격 라이브러리(`ssh2`·`ssh2-sftp-client`·`basic-ftp`)를 신규 차단 추가 + `src/main/remote/**` 예외(allow)**(현 코드는 tls·원격 라이브러리 미차단이므로 "추가"가 필요). 원격 응답 불신(traversal·심볼릭·호스트키 TOFU·평문 FTP 경고·.part 임시명) → ADR-007 ⑥.

---

## 1-N. 즐겨찾기 UX 향상(§N, 2026-06-08 편입) → 설계 매핑 — ✅ 구현 완료·QA PASS (실 GUI 동작 런타임 스모크 🟡)

> features §N(N1 즐겨찾기 경로 워터마크·N2 즐겨찾기 드래그 정렬)·user-stories 에픽13(US-13.1~13.2)·flows F17~F18. **상태: 2건 모두 ✅ 구현 완료·통합 QA PASS([qa-integration-N](../reviews/qa-integration-N.md): 블로커·높음 0·핵심 verify 302/0+contrast 실패 0·typecheck/lint/build 0·신규 채널 0·렌더러 전용).** 경과(은폐 금지·🔜→✅): 2026-06-08 기획 편입(🔜 미착수) → 설계([architecture-review-N](../reviews/architecture-review-N.md) PASS 조건부·키 `Alt+Shift+↑/↓` 확정 정정 반영) → 구현 → QA PASS → 이번 doc-sync로 🔜→✅ 동기화(직전 "현재 개발 잔여 = §N 2건"은 정정됨). 상태 단일 출처는 roadmap §0.5(머리말 §N 줄). **아래 표 모듈/파일은 실재 코드로 확인됨**(§1-M와 동일 — 구현 파일 실재). 설계 단일 출처: [system-architecture §5-N](./system-architecture.md)·[software-architecture §12](./software-architecture.md)·[directory-structure §7](./directory-structure.md). **신규 IPC 채널 0 · 신규 npm 의존성 0 · 신규 ADR 0**(작은 UX — 기존 C4 즐겨찾기·J7 별칭·ADR-002·SA §5 세션 영속 승계, [SA §5-N.4](./system-architecture.md) 근거). N1·N2는 기존 즐겨찾기(C4)·J7 별칭(US-9.7·`SidebarSnapshot.favoriteLabels`)의 비파괴 확장이며, Main FS/OS 접근·신규 채널 없이 **렌더러 도메인 규칙·`sidebarSlice`·기존 `SidebarSnapshot.favorites` 배열 순서**로 처리된다.

| 기능(추적원) | UI 컴포넌트 | 유스케이스/스토어 | 도메인/영속 | IPC 채널 / DTO | 관련 ADR | 상태 |
|---|---|---|---|---|---|---|
| **즐겨찾기 경로 워터마크** (N1, US-13.1, F17, S) ✅ | `ui/panel/Panel.tsx`(`position:relative` 배경 레이어 마운트)·`ui/panel/FavoriteWatermark.tsx`(목록 뒤 zIndex:0·`pointer-events:none`·`aria-hidden`·`userSelect:none`·테마 반투명·긴 이름 ellipsis·빈 폴더 비중첩) · `ui/theme/palette.ts`/`tokens.ts`(`--c-watermark-opacity`/`--c-watermark-color` 4테마 토큰) | 신규 슬라이스 상태 0 — 기존 `panelsSlice`(panel.path)·`sidebarSlice`(favorites·favoriteLabels) 셀렉터 구독으로 파생(2/4분할 패널 독립 판정) | **`domain/rules/favoriteWatermark.ts`**(`resolveFavoriteWatermark` 순수 함수) — normalizeDisplay 정확 일치 판정·텍스트=`favoriteLabels[path]`?? `baseName`(J7 폴백 재사용)·"내 PC"(`''`)·원격(`locationKindOf!=='local'`) 비매치·부분/하위 비매치·다중일치 첫1개(결정론) | **신규 채널 0**(렌더러 전용·FS/IPC 미사용) · DTO 변경 0 | ADR-002, ADR-005(불변) | ✅ |
| **즐겨찾기 드래그 정렬** (N2, US-13.2, F18, S) ✅ | `ui/sidebar/Sidebar.tsx`(즐겨찾기 행 드래그 핸들·DropLine 삽입 위치 인디케이터·드래그 항목 0.4 강조·`Alt+Shift+↑/↓` 키 핸들러(로컬·`altKey&&shiftKey&&!ctrl&&!meta` 가드)·행 `tabindex=0`·role=option·정렬 ARIA `posinset/setsize`·`aria-grabbed`·섹션 격리) · `ui/sidebar/useFavoriteReorder.ts`(경량 드래그 훅 + 외부 pub/sub — 파일 `dragState`와 별개) | `app/stores/sidebarSlice.ts` **`reorderFavorite(from,to)`** — `favorites` 배열 splice 재배열·기존 add/remove/label 불변 · `app/usecases/session.ts`(**불변** — `[...s.favorites]` 직렬화가 순서 자동 영속) | 순서 데이터 = 기존 `SidebarSnapshot.favorites: string[]` **배열 순서 자체**(별도 order 필드 0)·영속=기존 `session:save`/`coerceSidebar`(순서 보존·`SESSION_SCHEMA_VERSION` 미상향·persistence verify 94)·J7/경로 불변·순서만 변경 | **신규 채널 0** · DTO **변경 0**(`SidebarSnapshot.favorites` 재사용) | ADR-002, SA §5(세션 영속·불변) | ✅ |

> **§N 정합 메모(구현 완료·실 GUI 동작 런타임 스모크 🟡)**: N1·N2는 **신규 IPC 채널·신규 npm 의존성·신규 ADR·신규 영속 필드를 추가하지 않는다**(채널 0·SA §5-N.0). N1=기존 C4 즐겨찾기 + J7 별칭(`favoriteLabels`)의 표시 확장(도메인 규칙 1개 + UI 배경 레이어), N2=기존 `SidebarSnapshot.favorites` 배열 순서 재사용 + 슬라이스 액션 1개(`reorderFavorite`). 키보드 대체수단 **`Alt+Shift+↑/↓`**(확정·구현 일치)는 전역 `KeyBindingRegistry`(`domain/keybindings`) **미배정 조합**(`alt+shift+arrow*` 미등록 확인)이라 `KeyboardDispatcher` capture 단계가 가로채지 못해 Sidebar 로컬 핸들러가 동작한다 — **컨텍스트 분리가 아니라 키 조합 자체가 미사용이라 충돌 0**(`KeyContext` 확장 불요). 1차안 `Alt+↑/↓`는 `alt+arrowup→nav.up`(context `'list'`)이 capture에서 가로채므로 폐기(review-N [높음-1]·옵션①에서 정정). **PRD §8 단축키 메모는 `Alt+Shift+↑/↓`로 갱신됨(아래 §2 표 참조).** **§N 구현 완료로 개발 잔여(🔜) 0** — §M(✅)·P6(잔여 0)과 별개의 신규 챕터다. 헤드리스 verify(domain 49·store 107·persistence 94·contrast 실패 0)는 순수 로직·불변식만 증명 — **실 GUI 동작(워터마크 실 렌더·z-index·4테마 반투명도·긴이름 ellipsis·2/4분할 격리 / 실 마우스 드래그·DropLine·`Alt+Shift+↑/↓` 키 이동·ARIA 발화·재시작 후 순서 유지·한국어 IME `Alt+Shift` 점유 가능성)은 런타임 스모크 권장 🟡**(qa-integration-N 미검증 항목·[낮음-1]/[낮음-2]).

---

## 1-O. 파일/폴더 상단 고정(§O, 2026-06-09 편입) → 설계 매핑 — 구현 완료(코드 정합·verify 충족) / 실 GUI 런타임 스모크 🟡

> features §O(O1 파일/폴더 상단 고정 pin)·user-stories 에픽14(US-14.1)·flows F19·PRD §6 **Should**. **상태: 구현 완료(코드 정합·verify 충족) — 실 GUI 동작은 런타임 스모크 권장 🟡(✅ 위장 아님).** 경과(은폐 금지): 본 기능은 직전(2026-06-09 헤더)까지 **ref.md 아이디어 기반 비계획 구현으로 "스코프 일탈"** 취급돼 정식 추적 항목에서 제외돼 있었으나(구현 사실은 roadmap §0.5 "추가" 줄에만 기록), product-planner가 §O/에픽14/US-14.1/F19(PRD §6 Should)로 **정식 편입**함에 따라 본 §1-O 매핑으로 승격한다. **아래 표 모듈/파일은 실재 코드로 확인됨**(2026-06-09 Grep/Read 검증 — 유령 매핑 0). **신규 IPC 채널 0 · 신규 npm 의존성 0 · 신규 ADR 0**(작은 기능 — 기존 C4 즐겨찾기/J7 별칭의 per-위치 메타(`pinnedByDir`)·B1/B2 정렬 인프라(`sort.ts`)·SA §5 세션 영속·`SidebarSnapshot` 비파괴 확장을 승계). O1은 Main FS/OS 접근·신규 채널 없이 **렌더러 도메인 순수함수(`applyPins`)·`sidebarSlice.pinnedByDir`·기존 세션 영속(`SidebarSnapshot`+`coerce*`)**로 처리된다. §N(렌더러 전용·세션 영속)과 동일한 비파괴 확장 패턴이다.

| 기능(추적원) | UI 컴포넌트 | 유스케이스/스토어 | 도메인/영속 | IPC 채널 / DTO | 관련 ADR | 상태 |
|---|---|---|---|---|---|---|
| **파일/폴더 상단 고정(pin)** (O1, US-14.1, F19, S) | `ui/panel/views/FileListView.tsx`(`pinnedHere` 구독[`s.pinnedByDir[panel.path]`]·visible `useMemo` 의존성에 `pinnedHere` 추가·`FileRow` `pinned` prop·`RowProps.pinned`·📌 표식[그리드 좌상단 배지·목록 이름 앞] · **[2026-06-09 변경] 상단 고정 동작 "정렬 최상단"→"스크롤 고정(sticky)" — 목록/자세히 전용**: `pinnedSet` `useMemo`[`new Set(pinnedHere)`]·`sticky=!isGrid && pinnedSet.size>0`·선두 연속 고정 묶음 길이 `stickyCount` 계산·`stickyBandH=stickyCount*cellH`·`renderRow(i,entry)` 헬퍼[윈도 본문 + sticky 밴드 공용]·본문 렌더 윈도 루프에서 고정행(`sticky && i<stickyCount`) 제외·`stickyRows` 배열로 별도 렌더·sticky 컨테이너 `position:sticky top:0 zIndex:3 height:stickyBandH`·키보드 `scrollIntoView` 시 `el.scrollTop = Math.max(0, top - stickyBandH)` 보정[밴드가 가린 만큼 위로 더 스크롤] · **그리드는 sticky 미적용**[wrapping 부분행 점유로 레이아웃 깨짐 → 기존 "정렬 최상단 유지"만 적용·밴드 비활성]) · `ui/contextmenu/ContextMenu.tsx`(메뉴 항목 렌더) | `app/usecases/contextMenu.ts`(buildMenuItems 단일 파일/폴더 그룹에 "상단 고정/해제" 토글 항목 id `'pin'`·`isPinned` 라벨 분기) · `app/stores/sidebarSlice.ts`(상태 `pinnedByDir: Record<string,string[]>` + 액션 `togglePin(dir,entry)`/조회 `isPinned(dir,entry)`/`pinnedIn(dir)` + 상수 `EMPTY_PINNED`[안정 빈배열 참조·메모 안정] + `hydrateSidebar` 확장[pinnedByDir 복원·빈배열 키 제외]) · `app/usecases/selectors.ts`(`computeVisible`가 `sortEntries` 후 `applyPins` 적용·메모 슬롯 `pinned` 키 추가로 참조 동일성 무효화 · **불변** — sticky 는 렌더 계층 전용이라 선두 정렬 hoist 로직 무변경) | **`domain/rules/sort.ts#applyPins(entries, pinned)`** — 순수함수: 고정 항목 최상단 hoist(폴더 우선보다 위)·각 그룹 내부 정렬 순서 보존·`pinned` 비면 입력 그대로 복사 반환(원본 불변·참조만 새로) · **불변**(고정 선두 정렬은 그대로 — sticky 는 FileListView 렌더 계층에서만 처리) · 영속=기존 `session:save`/`coerceSidebar`(맵 보존·`SESSION_SCHEMA_VERSION` 미상향) | **신규 채널 0**(렌더러 전용·FS/IPC 미사용) · **DTO 비파괴 확장**: `shared/dto/index.ts SidebarSnapshot.pinnedByDir?: Record<string,string[]>`(옵셔널 추가) · `main/persistence/defaults.ts#coercePinnedByDir`(빈배열 키 제거·비문자열 정리·구버전 호환) + `defaultSidebar` 확장 + `coerceSidebar` 호출 · `renderer/app/usecases/session.ts`(buildSessionSnapshot이 `sidebar.pinnedByDir` 직렬화·applySnapshot이 hydrate로 전달) | ADR-002, SA §5(세션 영속·비파괴), ADR-005(불변·렌더러 직접 FS 접근 0) | 구현 완료(코드) / 실 GUI 🟡 |

> **§O 정합 메모(구현 완료·실 GUI 동작 런타임 스모크 🟡)**: O1은 **신규 IPC 채널·신규 npm 의존성·신규 ADR·신규 영속 스키마 버전을 추가하지 않는다**(채널 0·의존성 0·ADR 0). 데이터는 per-위치 메타 `pinnedByDir: Record<dirPath, entryPath[]>`(C4 즐겨찾기·J7 `favoriteLabels`와 동형 패턴)로 보관되고, 정렬은 기존 B1/B2 정렬 인프라(`sort.ts`)에 순수함수 `applyPins` 1개를 추가해 sortEntries 직후 hoist하는 방식이다(폴더 우선보다 위·그룹 내부 정렬 보존). 영속은 기존 `SidebarSnapshot` 비파괴 확장(옵셔널 필드 + `coercePinnedByDir` 정규화·빈배열 키 제외로 구버전 스냅샷 호환·스키마 버전 미상향).
> **§O 상단 고정 동작 변경(2026-06-09·"정렬 최상단"→"스크롤 고정 sticky")**: product-planner가 기존 US-14.1/F19 수용기준을 갱신(신규 챕터/US 신설 0)함에 따라, 고정 항목이 **목록을 아래로 스크롤해도 최상단에 고정 표시**되도록 sticky 밴드를 추가했다. **변경 범위는 렌더 계층 1곳뿐**(`FileListView.tsx`) — `domain/rules/sort.ts#applyPins`(고정 선두 hoist 정렬)·`app/usecases/selectors.ts#computeVisible`는 **불변**(sticky 는 정렬이 아니라 표시 위치 고정이라 도메인/셀렉터 비변경). **보기별 범위 정직 구분**: **목록(`list`)·자세히(`details`)는 sticky 밴드 적용**(colCount=1·`top=i*cellH` 로컬=글로벌이라 밴드 행 위치 정확), **그리드(`icons-*`)는 sticky 미적용**(wrapping 특성상 고정 항목이 행을 부분 점유해 레이아웃이 깨지므로 기존 "정렬 최상단 유지"만 유지·밴드 비활성). 핵심 심볼은 실재 확인(2026-06-09 Grep/Read): `sticky`(L199)·`stickyCount`(L200~206)·`stickyBandH`(L208)·`pinnedSet`(L198)·`renderRow`(L535)·`stickyRows`(L579~585)·sticky 컨테이너 `position:'sticky'`(L625)·키보드 보정 `stickyBandH`(L283~285). **신규 IPC 채널 0·신규 npm 의존성 0·신규 ADR 0(기존 렌더 계층 내 변경)** → ADR 불요. 상태 "구현 완료(코드)·실 GUI 🟡"(✅ 위장 아님 — 실 스크롤 중 밴드 고정·키보드 scrollIntoView 보정·2/4분할 독립은 런타임 스모크 권장 🟡). **검증 매핑(헤드리스)**: `verify:domain`(tests/domain.verify.ts — applyPins hoist/정렬 보존/원본 불변/빈 pinned·원격경로 등 케이스 ~60건)·`verify:store`(tests/store.verify.ts — togglePin add/remove·빈배열 키 삭제·isPinned/pinnedIn·hydrateSidebar 복원·세션 영속 roundtrip 등 ~121건)·`verify:persistence`(scripts/verify-persistence.ts — coercePinnedByDir 6케이스[빈배열 키 제거·비문자열 정리·구버전 호환 등]·roundtrip ~101건). 헤드리스 verify는 **순수 로직·정규화·불변식만 증명** — **실 GUI 동작(우클릭 "상단 고정/해제" 토글·📌 배지/이름앞 표식 실 렌더·고정 항목 최상단 정렬 실 반영·2/4분할 패널 독립·재시작 후 고정 상태 유지)은 런타임 스모크 권장 🟡**(✅ 위장 아님). **유령 매핑 0**(2026-06-09 코드 확인 — `domain/rules/sort.ts#applyPins`·`app/usecases/selectors.ts#computeVisible`·`app/stores/sidebarSlice.ts pinnedByDir/togglePin/isPinned/pinnedIn/EMPTY_PINNED/hydrateSidebar`·`app/usecases/contextMenu.ts id 'pin'`·`ui/panel/views/FileListView.tsx pinnedHere/RowProps.pinned/📌`·`shared/dto/index.ts SidebarSnapshot.pinnedByDir`·`main/persistence/defaults.ts coercePinnedByDir`·`renderer/app/usecases/session.ts pinnedByDir` 모두 실재 확인).

---

## 1-W. 자세히 보기 컬럼 헤더 + 너비 드래그 조절 + 헤더 클릭 정렬(§W, 2026-06-10 편입·2026-06-11 헤더 클릭 정렬 추가) → 설계 매핑 — 구현 완료(코드 정합·verify 충족) / 실 GUI 런타임 스모크 🟡

> features §W1(자세히 보기 컬럼 헤더 + 너비 드래그 조절)·user-stories 에픽21(US-21.1)·flows F34·PRD §6 **Should**. **상태: 구현 완료(코드 정합·verify 충족) — 실 GUI 동작(헤더 밴드 실 렌더·디바이더 드래그·전 패널 폭 반영·재시작 후 폭 유지)은 런타임 스모크 권장 🟡(✅ 위장 아님).** **아래 표 모듈/파일은 실재 코드로 확인됨**(2026-06-10 Grep/Read 검증 — 유령 매핑 0). **신규 IPC 채널 0 · 신규 npm 의존성 0 · 신규 ADR 0 · `SESSION_SCHEMA_VERSION` 1 불변**(작은 UX — 기존 B1/B2 목록·자세히 보기(`FileListView`)·SA §5 세션 영속·`SessionSnapshot.ui` 비파괴 확장 승계). W1은 Main FS/OS 접근·신규 채널 없이 **렌더러 도메인 순수함수(`clampColumnWidth`/`coerceDetailsColumnWidths`)·`columnsSlice`·기존 세션 영속(`SessionSnapshot.ui.detailsColumnWidths?`+`coerce*` 미러)**로 처리된다. §N(렌더러 전용·세션 영속)·§O(pin sticky 밴드)와 동일한 비파괴 확장 패턴이다(헤더 밴드는 §O pin sticky 밴드 위에 위치). 너비는 **패널·탭별이 아닌 전역 설정**(모든 패널의 자세히 보기가 동일 폭·`columnsSlice` 주석)이며, 이름(name) 열은 남은 공간 flex 라 저장 폭이 없다(헤더/행 동일 규칙).

| 기능(추적원) | UI 컴포넌트 | 유스케이스/스토어 | 도메인/영속 | IPC 채널 / DTO | 관련 ADR | 상태 |
|---|---|---|---|---|---|---|
| **자세히 보기 컬럼 헤더 + 너비 드래그 조절** (W1, US-21.1, F34, S) | `ui/panel/views/FileListView.tsx`(`ColumnHeader`[이름 \| 크기 \| 유형 \| 수정한 날짜 헤더 밴드·`colWidths=useRootStore(s=>s.detailsColumnWidths)` 구독·sticky 헤더 밴드(§O pin sticky 밴드 위)]·`ColumnDivider`[열 왼쪽 경계 드래그 리사이즈 핸들·`col`/`widthNow`/`onResize`]·details 행 셀이 동일 `colWidths` 읽어 헤더와 정렬 유지) | `app/stores/columnsSlice.ts`(상태 `detailsColumnWidths: DetailsColumnWidths`[전역·기본 `DEFAULT_DETAILS_COLUMN_WIDTHS`] + 액션 `setDetailsColumnWidth(col,px)`[`clampColumnWidth` 클램프] + `hydrateColumns(raw)`[세션 복원·coerce 정규화 후 적용] — `rootStore.ts`/`types.ts` 등록) · `app/usecases/session.ts`(buildSessionSnapshot이 `ui.detailsColumnWidths` 직렬화·applySnapshot이 `hydrateColumns(snap.ui.detailsColumnWidths)` 호출) | **`domain/rules/columnWidths.ts`** — 순수: `clampColumnWidth(px,min?,max?)`[비유한수→min·반올림·`[COLUMN_MIN_WIDTH=48, COLUMN_MAX_WIDTH=600]` 클램프]·`coerceDetailsColumnWidths(raw)`[객체 아님→전부 기본값·열별 클램프·누락 키 기본값·구버전 round-trip 동등]·타입 `DetailsColumn`/`DetailsColumnWidths`·상수 `DEFAULT_DETAILS_COLUMN_WIDTHS={size:90,type:60,mtime:140}`(헤더 도입 전 행 하드코딩 값과 동일·기존 레이아웃 보존) · 영속=기존 `session:save`/`coerceSession`(ui 블록 비파괴·`SESSION_SCHEMA_VERSION` 미상향) | **신규 채널 0**(렌더러 전용·FS/IPC 미사용) · **DTO 비파괴 확장**: `shared/dto/index.ts SessionSnapshot.ui.detailsColumnWidths?: DetailsColumnWidthsDTO`(옵셔널 추가) · `main/persistence/defaults.ts#coerceDetailsColumnWidths`(domain/rules 동일 규칙 미러·구버전 호환·undefined 폴백→ui 블록에서 조건부 포함) | ADR-002, SA §5(세션 영속·비파괴·전역 `ui`), ADR-005(불변·렌더러 직접 FS 접근 0) | 구현 완료(코드) / 실 GUI 🟡 |
| **자세히 보기 열 헤더 클릭 정렬** (W1 동작 확장, 2026-06-11) | `ui/panel/views/FileListView.tsx`(`ColumnHeader`에 정렬 props `sortKey`/`sortDir`/`onSort` 추가·각 열 `role="columnheader"`+`aria-sort`(ascending/descending/none)·헤더 행 `role="row"` + 신규 **`SortLabel`** 버튼[투명 `<button>`·활성 열 강조·`active`면 `▲`(asc)/`▼`(desc) 표식·Enter/Space 키보드 정렬]·`onSort=(key)=>setSort(panelId,key)`) · **정렬 진입점 이전: `ui/toolbar/PanelToolbar.tsx`에서 기존 정렬 `<select>`(name/size/ext/mtime)+방향 버튼(▲/▼)+`setSort` 구독 제거**(주석 "정렬은 자세히 보기 열 헤더 클릭으로 수행한다") | `app/stores/panelsSlice.ts#setSort(panelId, key)`(**기존 액션 재사용** — 같은 키 재클릭 시 방향 토글 처리)·`view.sortKey`/`view.sortDir` 구독 | `@shared/dto SortKey`/`SortDir`(기존 타입 재사용·신규 0)·정렬 규칙 불변(`computeVisible`/정렬 도메인 무변경) | **신규 채널 0**(렌더러 전용·기존 `setSort` 스토어 액션 재사용·FS/IPC 미사용·정렬 진입점만 PanelToolbar→ColumnHeader 이전) | ADR-002, ADR-005(렌더러 전용·불변) | 구현 완료(코드) / 실 GUI 🟡 |

> **§W 정합 메모(구현 완료·실 GUI 동작 런타임 스모크 🟡)**: W1은 **신규 IPC 채널·신규 npm 의존성·신규 ADR·신규 영속 스키마 버전을 추가하지 않는다**(채널 0·의존성 0·ADR 0·`SESSION_SCHEMA_VERSION` 1 불변). 너비 클램프·정규화 단일 출처는 순수함수 `domain/rules/columnWidths.ts`(`clampColumnWidth`/`coerceDetailsColumnWidths`)이고, UI(`FileListView` `ColumnHeader`/`ColumnDivider`)·슬라이스(`columnsSlice` `setDetailsColumnWidth`/`hydrateColumns`)·main coerce 미러(`defaults.ts#coerceDetailsColumnWidths`)가 이를 공유한다. 영속은 기존 `SessionSnapshot.ui` 비파괴 확장(옵셔널 `detailsColumnWidths?` + coerce 폴백·누락/손상은 `DEFAULT_DETAILS_COLUMN_WIDTHS`로 폴백·스키마 버전 미상향). **검증 매핑(헤드리스)**: `verify:domain`(tests/domain.verify.ts — `clampColumnWidth`(비유한수/범위밖/반올림)·`coerceDetailsColumnWidths`(객체 아님 폴백·열별 클램프·누락 키·구버전 round-trip) **28케이스**). 헤드리스 verify는 **순수 로직·정규화·불변식만 증명** — **실 GUI 동작(헤더 밴드 실 렌더·`ColumnDivider` 마우스 드래그 리사이즈·전 패널 동일 폭 반영·재시작 후 폭 유지·sticky 헤더가 §O pin 밴드와 정렬)은 런타임 스모크 권장 🟡**(✅ 위장 아님). **유령 매핑 0**(2026-06-10 코드 확인 — `domain/rules/columnWidths.ts clampColumnWidth/coerceDetailsColumnWidths/DEFAULT_DETAILS_COLUMN_WIDTHS`·`app/stores/columnsSlice.ts detailsColumnWidths/setDetailsColumnWidth/hydrateColumns`(`rootStore.ts`/`types.ts` 등록)·`ui/panel/views/FileListView.tsx ColumnHeader/ColumnDivider`·`app/usecases/session.ts ui.detailsColumnWidths/hydrateColumns`·`main/persistence/defaults.ts coerceDetailsColumnWidths`·`shared/dto/index.ts SessionSnapshot.ui.detailsColumnWidths?/DetailsColumnWidthsDTO` 모두 실재 확인).
>
> **§W 헤더 클릭 정렬 추가 메모(2026-06-11·구현 완료·실 GUI 🟡)**: §W1(컬럼 헤더·너비 드래그)에 **헤더 클릭 정렬** 동작을 추가했다(미커밋 작업 트리·git diff/grep 확인·`npm run typecheck` node+web PASS). `FileListView.tsx#ColumnHeader`에 정렬 props(`sortKey`/`sortDir`/`onSort`)와 신규 `SortLabel` 버튼 컴포넌트를 추가하고, 각 열에 `role="columnheader"`+`aria-sort`·헤더 행에 `role="row"`를 부여했다(직전 `aria-hidden` presentational 헤더 → 정렬 가능 헤더로 전환). 정렬은 **기존 `panelsSlice#setSort` 액션을 재사용**(신규 채널 0·신규 의존성 0·정렬 도메인/`computeVisible` 불변). **정렬 UI 진입점이 PanelToolbar 드롭다운 → ColumnHeader(SortLabel)로 이전**됐다 — `PanelToolbar.tsx`의 정렬 `<select>`+방향 버튼+`setSort` 구독을 제거(유령 매핑 방지: 이전 매핑이 PanelToolbar 정렬 UI를 가리켰다면 ColumnHeader로 정정·실코드 일치). **검증(헤드리스)**: `typecheck`(node+web) PASS·신규 verify 케이스 0(렌더 계층)·verify 누계 약 1523 불변·회귀 0. **실 GUI(헤더 클릭 정렬·재클릭 방향 토글·활성 열 ▲/▼·Enter/Space 키보드·aria-sort 발화·툴바 드롭다운 제거 후 정렬 도달성)는 런타임 스모크 권장 🟡(✅ 위장 아님). 유령 매핑 0**(2026-06-11 git diff/grep — `ColumnHeader sortKey/sortDir/onSort`·`SortLabel`·`PanelToolbar.tsx` 정렬 UI 제거·`panelsSlice#setSort` 모두 실재 확인).

---

## 1-U4. 탭 사용자 지정 이름(§U4, 2026-06-10 편입) → 설계 매핑 — 구현 완료(코드 정합·verify 충족) / 실 GUI 런타임 스모크 🟡

> features §U4(탭 사용자 지정 이름)·user-stories US-20.4·flows F35·PRD §6 **Should**. **상태: 구현 완료(코드 정합·verify 충족) — 실 GUI 동작(탭 더블클릭·우클릭 "이름 바꾸기" 인라인 편집·Enter/Esc·재시작 후 이름 유지)은 런타임 스모크 권장 🟡(✅ 위장 아님).** **아래 표 모듈/파일은 실재 코드로 확인됨**(2026-06-10 Grep/Read 검증 — 유령 매핑 0). **신규 IPC 채널 0 · 신규 npm 의존성 0 · 신규 ADR 0 · `SESSION_SCHEMA_VERSION` 불변**(작은 UX — 기존 A1 탭 관리(`TabBar`·`tabsSlice`)·SA §5 세션 영속·`TabSnapshot` 비파괴 확장 승계). U4는 Main FS/OS 접근·신규 채널 없이 **렌더러 슬라이스(`tabsSlice.setTabName`/`clearTabName`)·기존 세션 영속(`TabSnapshot.customName?`+`coerceTab` 미러)**로 처리된다. §N/§O/§W(렌더러 전용·세션 영속·비파괴)와 동일한 확장 패턴이다. 탭 라벨은 `customName`(trim 후 비-빈)이 있으면 그것을, 없으면 활성 패널 경로 마지막 세그먼트(자동 제목)를 표시한다.

| 기능(추적원) | UI 컴포넌트 | 유스케이스/스토어 | 도메인/영속 | IPC 채널 / DTO | 관련 ADR | 상태 |
|---|---|---|---|---|---|---|
| **탭 사용자 지정 이름** (U4, US-20.4, F35, S) | `ui/tabbar/TabBar.tsx`(`customName` 구독[`s.tabs[tabId]?.customName`]·`label = customName?.trim() ? customName : derivedTitle` 분기·인라인 `TabRenameInput`[`FileListView` RenameInput 패턴 미러·Enter commit→`setTabName`·빈 문자열→자동 제목 복귀·Esc 취소]·`TabContextMenu` 우클릭 "이름 바꾸기" 항목→인라인 편집 진입) | `app/stores/tabsSlice.ts`(액션 `setTabName(tabId,name)`[trim 후 빈 문자열이면 `delete t.customName`=자동 제목 복귀·아니면 `t.customName=trimmed`]·`clearTabName(tabId)`[`delete t.customName`]) · `app/usecases/session.ts`(buildSessionSnapshot이 `customName` 직렬화[`...(tabSnap.customName ? { customName } : {})`]·applySnapshot 복원) | `domain/entities/index.ts`(`Tab.customName?: string`) · 영속=기존 `session:save`/`coerceTab`(비파괴·`SESSION_SCHEMA_VERSION` 미상향) | **신규 채널 0**(렌더러 전용·FS/IPC 미사용) · **DTO 비파괴 확장**: `shared/dto/index.ts TabSnapshot.customName?: string`(옵셔널 추가) · `main/persistence/defaults.ts#coerceTab`(`customName` 비-빈 문자열만 trim 보존·비문자열/빈값 생략→자동 제목 복귀·구버전 호환) | ADR-002, SA §5(세션 영속·비파괴), ADR-005(렌더러 직접 FS 접근 0) | 구현 완료(코드) / 실 GUI 🟡 |

> **§U4 정합 메모(구현 완료·실 GUI 동작 런타임 스모크 🟡)**: U4는 **신규 IPC 채널·신규 npm 의존성·신규 ADR·신규 영속 스키마 버전을 추가하지 않는다**(채널 0·의존성 0·ADR 0·`SESSION_SCHEMA_VERSION` 불변). 라벨 결정 단일 규칙은 `TabBar`(`customName`(trim 후 비-빈) 우선·없으면 자동 제목)이고, 슬라이스(`tabsSlice.setTabName`/`clearTabName`)·세션 직렬화(`session.ts`)·main coerce 미러(`defaults.ts#coerceTab`)가 동일 "비-빈 문자열만 보존" 규칙을 공유한다. 영속은 기존 `TabSnapshot` 비파괴 확장(옵셔널 `customName?`·누락/빈값은 자동 제목 폴백·스키마 버전 미상향). **검증 매핑(헤드리스)**: `verify:store`(tests/store.verify.ts — `setTabName`/`clearTabName` add/clear·trim·빈 문자열→자동 제목 복귀·세션 roundtrip)·`verify:persistence`(scripts/verify-persistence.ts — `coerceTab` customName 비-빈/비문자열/빈값 정규화·구버전 호환). 헤드리스 verify는 **순수 로직·store·세션 영속만 증명** — **실 GUI 동작(탭 더블클릭/우클릭 "이름 바꾸기" 인라인 편집·Enter/Esc·전 탭 라벨 실 반영·재시작 후 이름 유지)은 런타임 스모크 권장 🟡**(✅ 위장 아님). **유령 매핑 0**(2026-06-10 코드 확인 — `domain/entities/index.ts Tab.customName`·`shared/dto/index.ts TabSnapshot.customName`·`app/stores/tabsSlice.ts setTabName/clearTabName`·`ui/tabbar/TabBar.tsx TabRenameInput/우클릭 이름 바꾸기`·`main/persistence/defaults.ts coerceTab customName`·`app/usecases/session.ts customName` 모두 실재 확인).

---

## 1-X1. 좌측 사이드바 "빠른 위치 ▸ 다운로드"(§X1, 2026-06-10 편입) → 설계 매핑 — 구현 완료(코드 정합·verify 충족) / 실 GUI 런타임 스모크 🟡

> features §X1(좌측 사이드바 "빠른 위치 ▸ 다운로드")·user-stories US-22.1·flows F36·PRD §6 **Should**. **상태: 구현 완료(코드 정합·verify 충족) — 실 GUI 동작(사이드바 "빠른 위치" 섹션 실 렌더·다운로드 노드 클릭→활성 패널 이동·`app.getPath` 실 경로 조회)은 런타임 스모크 권장 🟡(✅ 위장 아님).** **아래 표 모듈/파일은 실재 코드로 확인됨**(2026-06-10 Grep/Read 검증 — 유령 매핑 0). **신규 IPC 채널 `fs:known-folders`(1종 — 이 영역의 유일 신규 채널) · 신규 npm 의존성 0 · 신규 ADR 0**(기존 C3 트리 사이드바(`Sidebar`·`sidebarSlice`)·SA §4 Main FS 격리 승계). X1은 Main의 `app.getPath`로 OS 알려진 폴더 경로를 조회해 부팅/마운트 시 1회 로드하고 렌더러는 직접 FS 접근 없이 IPC 결과만 표시한다. 신규 채널 `fs:known-folders`는 P1 동결 후 신기능 선례(`preview:read`·`shell:open-terminal`·`analyze:scan:*`·`hash:*`·`queue:*`·`search:content:*` 등)와 동일 규약(동결 위반 아님). **현재 다운로드 + 바탕화면(2026-06-11 V13 추가) 렌더**되며 `KnownFoldersDTO`의 documents/home은 조회되나 UI 미표시(예약).

| 기능(추적원) | UI 컴포넌트 | 유스케이스/스토어 | 도메인/영속 | IPC 채널 / DTO | 관련 ADR | 상태 |
|---|---|---|---|---|---|---|
| **빠른 위치 ▸ 다운로드 + 바탕화면** (X1, US-22.1, F36, S · **2026-06-11 바탕화면 노드 추가=V13**) | `ui/sidebar/Sidebar.tsx`("빠른 위치" 섹션[`knownFolders` 구독·마운트 시 `loadKnownFolders` 1회·`knownFolders.desktop \|\| knownFolders.downloads`일 때 렌더]·`QuickFolderNode` **바탕화면(🖳·desktop·다운로드 위)** + **다운로드(⬇️·downloads)**·클릭=활성 패널을 해당 폴더로 이동) | `app/stores/sidebarSlice.ts`(상태 `knownFolders: KnownFoldersDTO \| null`[미로드 시 null] + 액션 `loadKnownFolders()`[`fsApi.knownFolders()` invoke→`s.knownFolders=res.value`·부팅/마운트 1회]) · `infra/api/index.ts`(`knownFolders()` 브리지) | (도메인 규칙 없음 — Main FS 격리·렌더러는 표시만) · 영속 없음(부팅 시 1회 조회) | **신규 채널 `fs:known-folders`**: `shared/ipc/channels.ts FS_KNOWN_FOLDERS='fs:known-folders'` · `shared/ipc/contracts.ts`(IpcRequestMap `{req:void; res:Result<KnownFoldersDTO>}`) · `main/ipc/fs.handlers.ts`(`ipcMain.handle(FS_KNOWN_FOLDERS)`·sender 검증·`app.getPath`) · `preload/api.ts`(`fs.knownFolders`) · **DTO `KnownFoldersDTO{downloads,desktop,documents,home}`**(`shared/dto/index.ts`·조회 불가 항목 빈 문자열) | ADR-005(Main FS 격리·렌더러 직접 FS 접근 0·P1 동결 후 신기능 선례 동일 규약) | 구현 완료(코드) / 실 GUI 🟡 |

> **§X1 정합 메모(구현 완료·실 GUI 동작 런타임 스모크 🟡)**: X1은 **신규 IPC 채널 1종(`fs:known-folders` — 이 영역의 유일 신규 채널)을 추가**하고, 신규 npm 의존성 0·신규 ADR 0·영속 변경 0이다. OS 알려진 폴더 경로는 Main(`app.getPath`)이 단일 출처로 조회하고(렌더러 직접 FS 접근 0·ADR-005 준수), 렌더러는 부팅/사이드바 마운트 시 `loadKnownFolders`로 1회 끌어가 `knownFolders` 상태에 보관·표시만 한다(영속 없음). 신규 채널 `fs:known-folders`는 void 요청·`Result<KnownFoldersDTO>` 응답으로 P1 동결 후 신기능 선례(`preview:read`·`shell:open-terminal`·`analyze:scan:*`·`hash:*`·`queue:*`·`search:content:*`)와 동일 규약(동결 위반 아님). **현재 다운로드 + 바탕화면 노드 렌더**(2026-06-11 V13 desktop 노드 추가·기존 채널/DTO 재사용·신규 채널 0)되며 `KnownFoldersDTO`의 documents/home은 조회되나 UI 미표시(예약 — 향후 노출 시 채널/DTO 재사용·신규 채널 0). 실 GUI(섹션 렌더·노드 클릭 이동·`app.getPath` 실 경로)는 런타임 스모크 권장 🟡(✅ 위장 아님). **유령 매핑 0**(2026-06-10 코드 확인 — `shared/ipc/channels.ts FS_KNOWN_FOLDERS`·`shared/ipc/contracts.ts FS_KNOWN_FOLDERS 매핑`·`shared/dto/index.ts KnownFoldersDTO`·`main/ipc/fs.handlers.ts FS_KNOWN_FOLDERS handler`·`preload/api.ts fs.knownFolders`·`renderer/infra/api/index.ts knownFolders`·`app/stores/sidebarSlice.ts knownFolders/loadKnownFolders`·`ui/sidebar/Sidebar.tsx 빠른 위치/QuickFolderNode` 모두 실재 확인).

---

## 1-P~1-U. 파워 기능 14종(§P~§U, 2026-06-09 편입) → 매핑 — M6 2종(§1-P1·§1-R1) + M7 4종(§1-P1 해시·재귀 확장·§1-R2·§1-R3·§1-R4 + 공용 해시 W1·전송 큐 W2 인프라) + M8 6종(§1-S1·§1-S2·§1-T1·§1-T2·§1-U1·§1-U2) + **M9 2종(§1-Q1·§1-U3)** 구현 완료(코드) · §1-T3 폐기(제거) · **§P~§U 14종 전부 완료(M6~M9 종료)**

> features §P~§U·user-stories 에픽15~20(US-15.1~US-20.3)·flows F20~F33·PRD §6 MoSCoW(§P~§U)·§12 마일스톤 M6~M9. **상태(2026-06-09 M6 doc-sync): M6 파워기능 1차 — P1 듀얼 패널 폴더 비교(메타·단일깊이)·R1 고급 일괄 이름변경 — 이 구현 완료(코드 정합·verify 충족)·실 GUI 런타임 스모크 🟡로 격상됐고, §1-P1·§1-R1은 실재 코드를 가리킨다(유령 0·2026-06-09 Grep/Read 확인). **T3 정렬/필터 프리셋(§1-T3)은 M6에서 구현 완료(코드 ✅)됐으나 2026-06-09 사용자 결정으로 폐기·코드 전면 제거 → "제거(폐기)"로 정정됨(`filterComposition.ts`·`presetsSlice`·`usecases/presets`·`ui/preset/*`·`FilterPreset` DTO 삭제·grep 부재 확인·`SESSION_SCHEMA_VERSION` 2→1 환원·유령 매핑 0).** **상태(2026-06-09 M7 doc-sync 추가): M7 파워기능 2차 — R2 중복 파일 찾기·R3 전송 큐 매니저·R4 복사 시 체크섬 검증·P1 해시/재귀 비교 확장(+공용 해시 W1·전송 큐 W2 인프라) — 이 구현 완료(코드 정합·verify 충족·`verify:hash` 46·`verify:queue` 47)·실 워커/실 GUI 런타임 스모크 🟡로 격상됐고, §1-R2·§1-R3·§1-R4·§1-P1 행은 실재 코드를 가리킨다(유령 0·2026-06-09 Grep/Read 확인·`src/main/hash/`·`src/main/workers/hashWorker.ts`·`src/main/operations/TransferQueue.ts`·`src/main/ipc/{hash,queue}.handlers.ts`·`ui/dedup/`·`ui/queue/` 실재). **상태(2026-06-10 M8 doc-sync 추가): M8 파워기능 6종 — S1 내용 검색(grep)·S2 명령 팔레트·T1 파일 태그/색상 라벨·T2 폴더 용량 인라인·U1 Space 퀵룩·U2 브레드크럼 드롭다운 — 이 구현 완료(코드 정합·verify 충족)·실 GUI 런타임 스모크 🟡로 격상됐고, §1-S1·§1-S2·§1-T1·§1-T2·§1-U1·§1-U2 행은 실재 코드를 가리킨다(유령 0·2026-06-10 Grep/Read 확인: `src/main/search/`·`src/main/workers/grepWorker.ts`·`src/main/ipc/search.handlers.ts`·`ui/palette/CommandPalette.tsx`·`domain/rules/{tags,paletteMatch,breadcrumbSiblings,contentSearch}.ts`·`app/stores/{searchSlice,tagsSlice}.ts`·`app/usecases/{contentSearch,folderSize,breadcrumbDropdown}.ts`·`ui/quicklook/QuickLookOverlay.tsx`·`ui/toolbar/BreadcrumbDropdown.tsx` 실재). **상태(2026-06-10 M9 doc-sync 추가): M9 파워기능 2종 — Q1 압축파일 `archive://` 어댑터·U3 탭 색상/잠금·탭 분리(새 창) — 이 구현 완료(코드 정합·verify 충족·`verify:archive` 56·`verify:archiveui` 43)·실 GUI/실 워커/멀티윈도우 런타임 스모크 🟡로 격상됐고, §1-Q1·§1-U3 행은 실재 코드를 가리킨다(유령 0·2026-06-10 Grep/Read 확인: `src/main/archive/*`·`src/main/workers/archiveWorker.ts`·`src/shared/archive/{safePath,archivePath}.ts`·`renderer/app/usecases/archive.ts`·`src/main/windows/windowManager.ts`·`renderer/app/usecases/windowSplit.ts`·신규 채널 `archive:*` 5종·`window:split-tab`/`window:get-init` 2종 실재). **M9 잔여 0 — §P~§U 14종 전부 완료(M6~M9 종료). Q1=신규 채널 `archive:*` 5종+신규 의존성 yauzl/yazl(MIT·네이티브 0)·U3 탭 분리=신규 채널 2종·색상/잠금=세션 메타 신규 채널 0. U3 정직 한계: 멀티 윈도우 세션 복원은 주 창만(분리 창 reopen-only·재시작 복원 안 함·의도적 MVP).** 신규 ADR: archive=ADR-008·해시=ADR-009·grep=ADR-010·전송 큐=ADR-011·메타/필터=ADR-012(T3·R1 구현은 ADR-012 메타/세션 영속 규약 적용·T1은 태그 영속 부분만 유효). **신규 채널을 추가한 구현 기능은 M7의 P1(`hash:compare:*`)·R2(`hash:dup:*`)·R4(`hash:verify:*`)·R3(`queue:*`)·공용 `hash:cancel`, M8의 S1(`search:content:*` 5종 — ADR-010, P1 동결 후 신기능 선례 동일 규약). 미구현 중 신규 채널 예정은 Q1(`archive:*`)뿐 — M8 나머지 5종(S2·T1·T2·U1·U2)은 전부 신규 채널 0(S2/U1=렌더러 전용·preview:read 재사용·T1=세션 메타·T2=analyze:scan 재사용·U2=fs:tree-children 재사용).** 외부로 나가는 신규 네트워크/실행 0(압축·grep·해시 전부 로컬·SHA-256=node:crypto 내장·grep=node 내장 스트림/정규식·ADR-005 준수). 미구현 2종(Q1·U3) 코드 매핑은 구현 시 team-dev가 채우고 doc-sync가 상태를 🔜→✅로 갱신한다.

| 기능(추적원) | UI(예정) | 유스케이스/스토어(예정) | 도메인/엔진(예정) | IPC 채널 / DTO(예정) | 관련 ADR | 마일스톤 | 상태 |
|---|---|---|---|---|---|---|---|
| **P1 듀얼 패널 폴더 비교(메타·단일깊이)** (US-15.1, S) | `ui/compare/CompareView.tsx`·`CompareToolbar.tsx`(diff 뷰·"차이만 보기")·`useSyncScroll.ts`(좌우 동기 스크롤)·`CompareMirrorDialog.tsx`(미러 미리보기·확인) · `ui/layout/LayoutHost.tsx`(비교모드 전환) · `ui/toolbar/iconBarItems.ts`(`compare.toggle`) · `ui/contextmenu`(폴더 비교 항목) | `app/usecases/compare.ts`·`app/stores/compareSlice.ts`(짝지음·4상태·옵션·미러)·`app/stores/tabsSlice.ts`(`compareActive`/`clearCompare`·탭/분할 전환 시 해제)·`commandBus.ts`(`compare.toggle`) | `domain/rules/compare.ts`(`compareEntries` 4상태 분류[left-only/right-only/diff/same]·`summarize`·`planMirror`·`DEFAULT_COMPARE_OPTIONS` 순수) | 메타(이름·크기·mtime)·단일깊이 비교는 기존 `fs:list` 결과 사용(M6, 신규 채널 0)·미러=기존 `op:*`(복사/이동/휴지통 D4) 재사용 · `CompareStatus`/`CompareEntry`/`ComparePairDTO.relPath?`(`shared/dto/index.ts`) · **M7 확장: 해시·재귀 비교 = 신규 채널 `hash:compare:*`·`hash:cancel`(`main/hash/compareEngine.ts`·`main/ipc/hash.handlers.ts`)·`compare.ts` CompareOptions useHash/recursive·`fromCompareResult`·`compareSlice` 해시잡·`usecases/compare` 구독(옵션 off는 M6 메타 동치)** | ADR-009(메타 M6·해시/재귀 M7 확장 완료) | M6(메타·단일깊이) · **M7(해시·재귀 확장 완료)** | 구현 완료(코드) / 실 GUI·실 워커 🟡 |
| **Q1 압축 폴더처럼 열기·추출·추가** (US-16.1, S) | `renderer/ui/sidebar/Sidebar.tsx`/패널 압축 배지·"폴더처럼 열기"/"추출" 액션(로컬 UX 재사용) | `renderer/app/usecases/archive.ts`·panelsSlice(`ArchiveLocation`) | `renderer/domain/rules/archiveSafePath.ts`(`shared/archive/safePath.ts` re-export·**Zip Slip**·`ARCHIVE_CAPS` zip bomb 완화 순수)·`renderer/domain/rules/transferRoute.ts`(archive↔local)·`shared/archive/archivePath.ts`(`archive://` URI 순수) · `main/archive/*`(`ArchiveService`·`ZipReader`(yauzl)·`ZipWriter`(yazl)·`ArchiveSessionManager`·`archiveProtocol`·`archiveErrors`)·`main/workers/archiveWorker.ts` | **신규 채널 `archive:open/list/close/extract/add`**: `shared/ipc/channels.ts ARCHIVE_OPEN~ADD`·`shared/ipc/contracts.ts`(ArchiveOpenReq/Res 등) · `main/ipc/archive.handlers.ts` · 추출/추가=기존 `op:*` 재사용 | ADR-008 | M9 | 구현 완료(코드) / 실 GUI·실 워커 🟡 |
| **R1 고급 일괄 이름변경** (US-17.1, S) | `ui/rename/BatchRenameDialog.tsx`(규칙·실시간 미리보기·충돌경고) · `keybindings`(`Ctrl+Shift+R`→`file.batchRename`) · `commandBus.ts` · `ui/contextmenu`(다중선택 항목) | `app/usecases/batchRename.ts`(규칙 적용·실행 오케스트레이션·undo 묶음 push) | `domain/rules/batchRename.ts`(규칙→매핑·충돌검사 순수) | **신규 채널 0** — 실행=기존 `fs:rename` 반복·K1 undo `kind:'batchRename'`(`undoSlice.ts`·`undo.ts` 역연산·부분복원·순환 2단계) | — (SW §14.1) | M6 | 구현 완료(코드) / 실 GUI 🟡 |
| **R2 중복 파일 찾기** (US-17.2, S) | `ui/dedup/DuplicatesDialog.tsx`(중복 그룹 패널·정리·진입 commandBus/contextMenu/iconBar) | `app/usecases/dedup.ts`·`app/stores/dedupSlice.ts` | `domain/rules/dupGroup.ts`(순수)·`main/hash/dupEngine.ts`(크기 그룹핑→SHA-256·Worker)·`main/hash/{HashManager,fsDeps}.ts`·`main/workers/{hashWorker,hashProtocol}.ts` | `hash:dup:start/progress/done`·`hash:cancel`(`main/ipc/hash.handlers.ts`) · 정리=기존 `op:trash` 재사용(확인·휴지통·undo) · `DupGroupDTO` | ADR-009 | M7 | 구현 완료(코드) / 실 워커·GUI 🟡 |
| **R3 전송 큐 매니저** (US-17.3, S) | `ui/queue/(QueuePanel·QueueItemRow·QueueConcurrencyControl·queueFormat).tsx`(작업 목록·진행률·제어·StatusBar 합산 인디케이터·iconBar) | `app/usecases/queue.ts`·`operationsSlice`(큐 확장)·`queueBridge` | `main/operations/TransferQueue.ts`·`OperationManager`(큐 승격·비파괴·기본 무한동시성=단발 동치·일시정지/재시도·SharedArrayBuffer 2워드 cancel+pause) | `queue:list/state/pause/resume/retry/set-concurrency`(`main/ipc/queue.handlers.ts`) · 전송 자체=기존 `op:*` 재사용 · `QueueItemDTO` | ADR-011 | M7 | 구현 완료(코드) / 실 스케줄러·GUI 🟡 |
| **R4 복사 시 체크섬 검증** (US-17.4, C) | SettingsDialog 토글(`SettingsSnapshot.verifyOnCopy` 기본 off)·검증 결과(불일치 경고) | `app/usecases/checksum.ts`·`operationsBridge`(op:done 후 트리거) | `domain/rules/checksumVerdict.ts`(순수)·`main/hash/{verifyEngine,hashEngine}.ts`(원본/사본 스트리밍 SHA-256) | `hash:verify:start/progress/done`·`hash:cancel`(`main/ipc/hash.handlers.ts`) · `defaults.ts` coerce | ADR-009 | M7(후순위) | 구현 완료(코드) / 실 검증 타이밍 🟡 |
| **S1 내용 검색(grep)** (US-18.1, S) | `ui/search/ContentSearchDialog.tsx`(전용 다이얼로그·결과 라인·점프) | `usecases/contentSearch.ts`·`app/stores/searchSlice.ts`·`domain/rules/contentSearch.ts`·`infra/api/index.ts`(searchApi) | `main/search/grepEngine.ts`·`binaryDetect.ts`·`GrepManager.ts`·`fsDeps.ts`·`main/workers/grepWorker.ts`·`grepProtocol.ts`(스트리밍·바이너리 제외·Worker) | `search:content:start/progress/match/done/cancel`(`main/ipc/search.handlers.ts`) · 점프=기존 `preview:read` 재사용 · `GrepMatchDTO` | ADR-010 | M8 | 구현 완료(코드) / 실 워커·GUI 🟡 |
| **S2 명령 팔레트** (US-18.2, S) | `ui/palette/CommandPalette.tsx`(오버레이) | 기존 `commandBus.execCommand`(`palette.open`)·uiSlice `paletteOpen`·즐겨찾기/최근/드라이브 렌더러 상태 | `domain/rules/paletteMatch.ts`(매칭 점수 순수)·`keybindings`(`Ctrl+Shift+P`→`palette.open`) | **신규 채널 0**(렌더러 전용) | — (SW §14.2) | M8 | 구현 완료(코드) / 실 GUI 🟡 |
| **T1 파일 태그/색상 라벨** (US-19.1, S) | `contextMenu.ts`"태그" 서브메뉴·`SearchBar` 태그칩 필터·`FileListView` 태그 점 | `app/stores/tagsSlice.ts`·`selectors.computeVisible`(이름+태그 합성) | `domain/rules/tags.ts`(7색 팔레트·순수 `matchesTags`·합성 규칙 **T1에서 신설**·T3 폐기로 삭제됐던 `filterComposition.ts` 태그 합성을 재설계함 — 메모 해소) | **신규 채널 0** — 세션 메타 `SidebarSnapshot.tagsByPath`·`coerceTagsByPath`·기존 `session:save/load`·`SESSION_SCHEMA_VERSION` 무변경 | ADR-012(태그 영속 부분만 유효·프리셋/필터합성 부분 폐기) | M8 | 구현 완료(코드) / 실 GUI 🟡 |
| **T2 폴더 용량 인라인** (US-19.2, S) | `FileListView`(details 폴더 행 용량 인라인) | `app/usecases/folderSize.ts`(지연·캐시·디듀프) | 기존 `main/operations/scanEngine.ts` 재사용(단일 폴더·순환차단·권한 skip·취소) | **신규 채널 0** — 기존 `analyze:scan:*` 재사용 | — (SW §14.4·ADR-012 결정④) | M8 | 구현 완료(코드) / 실 GUI 🟡 |
| ~~**T3 정렬/필터 프리셋** (US-19.3, S)~~ | ~~`ui/preset/PresetBar.tsx`·`PresetManageDialog.tsx`~~ **(삭제됨)** | ~~`app/usecases/presets.ts`·`app/stores/presetsSlice.ts`~~ **(삭제됨)** | ~~`domain/rules/filterComposition.ts`~~ **(삭제됨)** · `selectors.computeVisible`→기존 `filterEntries` **환원** | **세션 메타·DTO 제거**: `SessionSnapshot.filterPresets`·`FilterPreset`/`PresetSort`/`PresetFilter`/`TagColor`·`coerceFilterPresets`·`applyPresetToPanel`·`presetManageOpen` **제거** · `SESSION_SCHEMA_VERSION` **2→1 환원** | ~~ADR-012~~(프리셋·필터합성 부분 폐기·태그 부분만 유효) | M6→**폐기** | **제거(폐기) — 2026-06-09 사용자 결정·코드 전면 제거(유령 매핑 0)** |
| **U1 Space 퀵룩** (US-20.1, S) | `ui/quicklook/QuickLookOverlay.tsx`(J5 프리뷰 뷰어 재사용·이미지/텍스트/코드/마크다운) | uiSlice 오버레이 상태 | `keybindings`(`Space`→`quicklook.toggle`·list 컨텍스트 한정·입력/오버레이 억제) | **신규 채널 0** — 기존 `preview:read` 재사용(CSP·DOMPurify) | — (SW §14.5) | M8 | 구현 완료(코드) / 실 GUI 🟡 |
| **U2 브레드크럼 드롭다운** (US-20.2, S) | `ui/toolbar/BreadcrumbDropdown.tsx`(형제 폴더 ▾ 드롭다운·`PanelToolbar` 배선) | `app/usecases/breadcrumbDropdown.ts` | `domain/rules/breadcrumbSiblings.ts`(폴더만·키보드 내비) | **신규 채널 0** — 기존 `fs:tree-children` 재사용·원격 경로는 ▾ 비표시 | — (SW §14.6) | M8 | 구현 완료(코드) / 실 GUI 🟡 |
| **U3 탭 색상/잠금·탭 분리(새 창)** (US-20.3, C) | `renderer/ui/tabbar/TabBar.tsx`(탭 우클릭 색상[TAG_PALETTE 재사용]/잠금·새 창 분리) | `renderer/app/usecases/windowSplit.ts`(`splitTabToNewWindow`·잠긴 탭 가드)·tabsSlice(`Tab.color`/`locked`)·commandBus(잠금 가드) | `renderer/domain/entities/index.ts`(`Tab.color?`/`Tab.locked?` 세션 메타)·**멀티 윈도우(`main/windows/windowManager.ts` `createWindow` 단일 출처·BrowserWindow 다중·창별 초기 상태)** | **신규 채널 `window:split-tab`/`window:get-init`**: `shared/ipc/channels.ts WINDOW_SPLIT_TAB/WINDOW_GET_INIT`·`shared/ipc/contracts.ts`(WindowSplitTabReq/WindowInitRes) · `main/ipc/window.handlers.ts`(탭→새 창 분리·부팅 초기 상태) · 색상/잠금=세션 메타(신규 채널 0) | — (SW §14.7) | M9 | 구현 완료(코드) / 실 GUI·멀티윈도우 🟡 |

> **§P~§U 정합 메모(M6 2종 + M7 4종 + M8 6종 구현 완료·T3 폐기·나머지 2종(Q1·U3) 설계만·은폐 금지)**: 본 절은 **M6 2종(P1 폴더비교 메타·단일깊이·R1 일괄 이름변경) + M7 4종(P1 해시·재귀 확장·R2 중복 파일 찾기·R3 전송 큐 매니저·R4 체크섬 검증 + 공용 해시 W1·전송 큐 W2 인프라) + M8 6종(S1 내용 검색·S2 명령 팔레트·T1 파일 태그·T2 폴더 용량·U1 Space 퀵룩·U2 브레드크럼 드롭다운)이 실재 코드를 가리키고(M6·M7=2026-06-09·M8=2026-06-10 구현 완료·코드 정합·verify 충족·실 GUI/실 워커 🟡), 나머지 2종(Q1·U3)은 "설계(예정)" 매핑**이다(§1-A~§1-O는 구현 완료/부분 매핑). **M8 정직 한계: 헤드리스 verify(search 58·palette 20·contentsearch 38 신규 + domain 204·store 222·persistence 119 — 순수 로직·store·세션 영속·계약 불변식)는 충족이나, 실 GUI/실 동작(grep 워커 스트리밍 실행·결과 점프·미리보기 표출·팔레트 검색/실행·Space 퀵룩·태그 부여/필터·폴더용량 실 스캔·브레드크럼 ▾ 이동)은 런타임 스모크 권장 🟡(✅ 위장 아님). S1 신규 채널 `search:content:*` 5종 외 M8 나머지 5종은 신규 채널 0.** **T3 정렬/필터 프리셋은 M6 구현 완료(코드)됐다가 2026-06-09 사용자 결정으로 폐기·코드 전면 제거(§1-T3 "제거(폐기)" 행 참조).** **M7 정직 한계: 헤드리스 verify(hash 46·queue 47·domain 168·store 207·persistence 128 — 순수 로직·store·해시/큐 불변식)는 충족이나, 실 동작(해시 워커 잡·큐 스케줄러 일시정지/재개·중복 정리·복사후 검증 트리거·해시/재귀 실 GUI 비교)은 런타임 스모크 권장 🟡. R4 비원자 복사 검증 타이밍·원격 큐 일시정지 미배선 정직 표기(✅ 위장 아님).** **M6 3종 정직 한계: 헤드리스 verify(domain 132·store 162·persistence 123 — 순수 로직·store·세션 영속)는 충족이나, 실 GUI 동작(프리셋 드롭다운/관리 다이얼로그·일괄 rename 실 파일 왕복·Ctrl+Z 실복원·비교 진입/미러/동기 스크롤)은 런타임 스모크 권장 🟡. P1은 메타·단일깊이 한정·해시 비교·재귀 비교는 M7 연기(✅ 위장 아님).** 14개 중 **신규 IPC 채널을 추가하는 것은 P1(`hash:compare:*`)·Q1(`archive:*`)·R2(`hash:dup:*`)·R3(`queue:*`)·R4(`hash:verify:*`)·S1(`search:content:*`·구현 완료)** 뿐이며, 나머지 8개(R1·S2·T1·T2·T3·U1·U2·U3)는 **렌더러 전용·세션 메타 확장·기존 채널 재사용으로 신규 채널 0**(§N/§O 정신 계승). **신규 npm 의존성은 `yauzl`+`yazl`(Q1·zip·네이티브 0)뿐**, 해시(Node 내장 SHA-256)·grep(Node 내장)은 의존성 0. **외부로 나가는 신규 네트워크/실행 0**(압축=로컬·grep=로컬 워커 스캔·해시=로컬·외부 7z/ripgrep 바이너리 비채택). 보안 임계 표면은 **Q1 Zip Slip 차단**(ADR-008 결정④·`archiveSafePath.ts` 순수+워커 양쪽 검증·헤드리스 verify 권고) 1종. **마일스톤 의존성**: M6(P1 메타·R1·T3) → M7(공용 해시 워커·전송 큐·grep 워커: R2·R3·R4·P1 해시 옵션 연결) → M8(경량 메타/표시·빠른 보기: T1·T2·S1·S2·U1·U2) → M9(Q1 archive·U3 멀티 윈도우). **P1 해시 옵션·R2·R4는 M7 공용 해시 엔진(ADR-009) 선행 필수**, **R2 정리·R4 검증·S1 진행률은 M7 전송 큐(ADR-011)와 정합**, **Q1 추출은 M7 큐·op:* 재사용**. M8 6종(S1·S2·T1·T2·U1·U2)은 구현 완료(코드)로 본 표가 실 파일 경로를 가리킨다 — **M9 2종(Q1 압축 `archive://`·U3 탭 색상/잠금·탭 분리)도 2026-06-10 구현 완료(코드)로 본 표가 실 파일 경로를 가리킨다(유령 0·`verify:archive` 56·`verify:archiveui` 43). §P~§U 14종 전부 완료(M6~M9 종료·T3 폐기 제외). Q1 신규 의존성 `yauzl`+`yazl` 추가(zip·MIT·네이티브 0)·해시/grep은 의존성 0 유지.**

---

## 2. 단축키 표(PRD 8장) → 처리 위치 매핑

> 단일 출처: `renderer/domain/keybindings`(키→commandId). 디스패치는 SW 7장.

| 키 | commandId | 처리 슬라이스/유스케이스 |
|---|---|---|
| `Tab` | `panel.focusNext`(순환) | tabsSlice |
| `Ctrl+←/→` | `panel.focusDir(dir)`(방향) | tabsSlice |
| ~~`F5` / `F6`~~ | ~~`panel.copyToOther`/`moveToOther`~~ | **삭제됨(2026-06-07 사용자 요청)** — 키 미배정·commandId/usecase 코드에서 제거. 패널 간 복사/이동은 D&D·클립보드(`Ctrl+C/X/V`)가 단일 경로 |
| `Ctrl+R` | `panel.refresh` | panelsSlice → fs:list:start |
| `Ctrl+T/W/Shift+T/D`, `Ctrl+Tab`, `Ctrl+1~9` | `tab.*` | tabsSlice |
| `Ctrl+\` | `layout.toggleSplit2` | tabsSlice |
| `Ctrl+B` (S) | `sidebar.toggle` | sidebarSlice(`toggleSidebar`) |
| `Alt+←/→/↑`, `Backspace` | `nav.back/forward/up` | panelsSlice(navHistory) |
| `Alt+Shift+↑/↓` (S, 사이드바 즐겨찾기 포커스 한정) | (전역 미배정 — Sidebar 로컬 핸들러) | sidebarSlice(`reorderFavorite`)·§1-N(N2 즐겨찾기 재정렬 키보드 대체수단 ✅·전역 `alt+shift+arrow*` 미등록·충돌 0) |
| `Ctrl+L` / `Ctrl+F` | `address.edit`/`search.open` | 컨텍스트 스코프 진입 |
| `Ctrl+C/X/V` | `file.copy/cut/paste` | clipboard usecase |
| `F2`, `Ctrl+Shift+N` | `file.rename`/`file.newFolder`(+새 파일) | file usecase → fs:rename/fs:mkdir/fs:create-file |
| `Delete`/`Shift+Delete` | `file.trash`/`file.deletePermanent` | file usecase → op:trash/op:delete |
| `Ctrl+Z` (S) | `file.undo` → `performUndo` | Undo 스택 ✅(K1: `undoSlice.ts` cap 50·`undo.ts` 역연산·§1-K) |
| `Ctrl+A`, `Ctrl+클릭`, `Shift+클릭` | `select.all/toggle/range` | selectionSlice |
| `Ctrl+P` (S) | `preview.toggle` | uiSlice |
| `Ctrl+Shift+P` (S, 구현 완료(코드)/실 GUI 🟡) | `palette.open` | S2 명령 팔레트 ✅(`ui/palette/CommandPalette.tsx`·`domain/rules/paletteMatch.ts`·SW §14.2) — PRD §8 신규·미배정 조합·충돌 0 |
| `Space` (S, 구현 완료(코드)/실 GUI 🟡, 목록 컨텍스트 한정) | `quicklook.toggle` | U1 Space 퀵룩 ✅(`ui/quicklook/QuickLookOverlay.tsx`·SW §14.5·preview 재사용) — 텍스트 입력/이름편집/오버레이 중 비활성 |

> **충돌 회피 검증 반영**(결정-D4): `Tab`/`Ctrl+R`가 고유 commandId로 매핑됨(~~`F5`/`F6`~~은 2026-06-07 제거되어 미배정). 레지스트리가 동일 컨텍스트 중복 매핑을 부팅 시 assert.
> **포커스 명령 구분**: `panel.focusNext`(Tab=순환)와 `panel.focusDir(dir)`(Ctrl+←/→=방향)는 별개 commandId다(SW §7.2와 통일).

---

## 3. 비기능 요구(PRD 7장) → 구조적 대응

| 비기능 요구 | 구조적 대응 | 근거 문서 |
|---|---|---|
| 10,000개 1.5초 첫 렌더 | 디렉토리 스트리밍(fs:list:chunk) + 가상 스크롤 첫 청크 즉시 렌더 / **P7 ✅ 불변식 검증**(`windowing.ts`·`verify:perf` 25), 🟡 실측 숫자 런타임 | SA 3.1, SW 6.2, ADR-004, §1-P7 |
| 진행률 200ms·UI 비차단 | Worker 실행 + Main 200ms 스로틀 + 이벤트 스트림 + 셀렉터 리렌더 격리 | SA 4.1, ADR-005, ADR-002 |
| 검색 입력 200ms (US-4.1 Must) | 도메인 순수함수 메모이즈 + deferred/transition; 미달 시 가시영역 우선 필터 → (필요 시) Web Worker 폴백, M1 측정 항목화 | SW 6.3, SW §10-1 |
| 삭제 휴지통 경유·되돌리기 | op:trash + Undo 스택 ✅(K1: `undoSlice.ts`·`undo.ts`·§1-K) + 휴지통 관리 `trash:*`(K2) | SA 4.2, §1-K |
| 충돌 임의 덮어쓰기 금지 | op:conflict 질의 → 사용자 명시 선택 | SA 4.1, SW 8 |
| 비정상 종료 복원 | 변경 시 디바운스 저장 + 원자적 쓰기 + 스키마 버전 | SA 5장 |
| 보안(로컬·권한·옵트인) | contextIsolation+sandbox, Main FS 격리, 양단 검증, 네트워크 차단 기본 | ADR-005, SA 3.3 |
| 롱패스/링크/네트워크/권한 | FileSystemService 예외 처리 + FileOpError 전파 + 패널 단위 오류 격리 | SA 4장, ADR-003 |
| 단일 인스턴스 | Main requestSingleInstanceLock | SA 2.3 |
| 접근성(키보드/포커스/ARIA) | 중앙 단축키 디스패치·컨텍스트 스코프, ARIA 행 레이블 / **P7 ✅**: `useFocusTrap`·모달 6종 role/aria/Esc·행 `aria-posinset/setsize`·`:focus-visible`·Shift+F10·WCAG AA 4팔레트 전수(`verify:contrast`), 🟡 실 스크린리더 | SW 7, ADR-004, §1-P7 |

---

## 1-V. 비계획/개선 구현 19건(V장 가칭, 2026-06-10~11 — V1~V3·⚠️V5·V11·V12=⚠️ 기획 미수록·스코프 일탈 / V4·V6·V7·V8·V9·V13 + 버그·UX 7건[F1·F2·B1·B3·B4·I8·I9]=기존 기능 개선·스코프 무변경) → 구현 파일 매핑 — 구현 완료(코드·typecheck PASS·ESLint 0·V4~V9 및 버그7건 verify:ops 35/0·F2·B3·V9 실 FS 스모크 통과·V11~V13 verify:store 281/persistence 147 0fail) / 실 GUI·설치·robocopy 런타임 스모크 🟡

> **⚠️ 스코프 상태(중요)**: 아래 표의 **V1·V2·V3·⚠️V5·V11·V12 6건은 PRD/features/user-stories/flows에 정식 기능으로 없는 신규 스코프(스코프 일탈)**다(V5=robocopy 고속 미러는 신규 채널 `op:robocopy:start`+§P1 미러 확장 / V11=설정 2단 재구성+단축아이콘 커스터마이즈·V12=아이콘바 드래그 재배열은 신규 채널 0이나 PRD/features/user-stories/flows 미수록 신규 동작·2026-06-11 추가). **V13(빠른 위치 "바탕화면")은 기존 §X1[빠른 위치 ▸ 다운로드]의 사소 노드 확장 — 스코프 일탈 아님(§1-X1 교차).** doc-sync 규약(스코프는 사람이 정한다)에 따라 기획문서에는 추가하지 않고, **코드 사실 기록(roadmap §0.5 2026-06-10 단락 + 이 추적 매핑)만** 수행한다. **PM/사용자의 정식 편입(US 신설·PRD §6 Should 추가·features §V·flows F 신설) 결정 대기.** 정식 편입이 결정되면(§O pin 선례처럼) product-planner가 기획 4종을 추가하고 본 매핑을 "정식 편입"으로 격상한다. **반면 V4·V6·V7·V8·V9 5건은 기존 기능(P4 copy/move·trash·delete·A3 드래그)의 견고성/성능 개선 — 스코프·MoSCoW·신규 채널 무변경(기획문서 무수정·스코프 일탈 아님·코드 사실 기록만).** **상태: 전 항목 구현 완료(코드 정합·typecheck PASS·ESLint 0·V4~V9는 `verify:ops` 35/0·실 FS 스모크 통과) / 실 GUI·실 설치·실 우클릭(한글 메뉴 포함)·V5 robocopy 실행·V4 대용량 처리량 동작은 미검증 → 런타임/설치 스모크 권장 🟡(✅ 위장 아님).**

| 기능(추적용) | UI 컴포넌트 | 유스케이스/스토어 | 도메인/OS | IPC 채널 / DTO | 관련 ADR | 상태 |
|---|---|---|---|---|---|---|
| **V1 대시보드 스캔결과 표 "동작" 컬럼**(📂이동·✨Google AI 모드 질의) | `ui/dashboard/DashboardModalBody.tsx`(TopTable 상위폴더/상위파일 표에 "동작" 컬럼 + 📂이동·✨AI 버튼) | `app/usecases/dashboard.ts`(`jumpToScanEntry`[폴더=진입·파일=상위 폴더로 이동 후 대시보드 닫힘]·`askGoogleAiAboutEntry`[항목 이름·종류 프리필 → `google.com/search?udm=50` 외부 브라우저]) · `renderer/infra/api/index.ts`(shellApi.openExternal) | `main/os/shell.ts`(openExternalUrl) · `main/ipc/guard.ts`(zShellOpenExternalReq) · `main/ipc/shell.handlers.ts`(sender+zod+프로토콜 검증) | **신규 채널 `shell:open-external`**(요청-응답 invoke·http/https 화이트리스트·ESECURITY/EINVAL 거부·`channels.ts` impl 주석 V1·EVENT_CHANNELS 무변)·`contracts.ts ShellOpenExternalReq`+ChannelMap·`preload/api.ts shell.openExternal` | ADR-005(쉘 보안 모델·`shell:open` 선례 동형) | 구현 완료(코드)·실 GUI 🟡 |
| **V2 디렉토리 노출 시 드라이브 레이블 일관화** | `ui/toolbar/PanelToolbar.tsx`(브레드크럼 드라이브 루트 크럼) · `ui/tabbar/TabBar.tsx`(드라이브 루트 탭 제목) · `ui/sidebar/Sidebar.tsx`(즐겨찾기 FavoriteRow + 최근 RecentRow 중앙화) | (렌더러 셀렉터) | `app/selectors/driveLabel.ts`(신규 `resolveDriveLabel(path, tree, fallback)`: 드라이브 루트면 트리 label, 아니면 fallback) — 기존 사이드바 트리 드라이브 노드 label(`fs:drives`가 "볼륨명 (C:)"로 채움)을 단일 출처 재사용 | **신규 채널 0·신규 상태 0**(기존 `fs:drives` label 재사용·렌더러 전용). 의도적 제외(실경로 유지): 리터럴 전체 경로(상태바·미리보기·주소창 편집모드) | — | 구현 완료(코드)·실 GUI 🟡 |
| **V3 Windows 탐색기 "AGT-Finder로 열기"** | `ui/App.tsx`(부팅 시 `initOpenPathBridge` 구독) | `app/usecases/launchOpen.ts`(신규 `initOpenPathBridge`: 폴더/드라이브 그대로·파일이면 상위 폴더로 새 탭) · `renderer/infra/api/index.ts`(subscribeOpenPath) | `main/os/launchPath.ts`(신규 `extractPathArg`: argv 경로 추출 + `normalizePath` 상위이탈 차단 + 존재 확인) · `main/index.ts`(최초 실행 argv 푸시 + second-instance forward·기존 단일 인스턴스 락 활용) · **`resources/installer.nsh`**(신규·UTF-8 BOM·customInstall/customUnInstall 매크로로 HKCU\Software\Classes 의 Directory/Directory\Background/Drive/* 4종 등록·제거) · `electron-builder.yml`(nsis.include) | **신규 푸시 채널 `app:open-path`**(Main→Renderer·**EVENT_CHANNELS 추가**·`channels.ts`·`contracts.ts AppOpenPathEvt`+IpcEventMap·`preload/api.ts app.onOpenPath`)·**신규 npm 의존성 0**(reg.exe 아님 — NSIS 등록·perMachine:false=HKCU·관리자 불요) | ADR-005(단일 인스턴스·푸시 채널 선례) | 구현 완료(코드)·실 설치/우클릭 🟡 |
| **V4 복사 엔진 성능 최적화**(기존 P4 copy/move 개선·스코프/채널 0) | — | `main/workers/fileOpWorker.ts`(`pickConcurrency` — 소스↔대상 다른 볼륨 4·같으면 1) | `main/operations/engine.ts`(하이브리드 복사 `copyFileHybrid`[64MB 미만 `fs.copyFile` OS네이티브·이상 청크 스트림 `STREAM_COPY_THRESHOLD`]·적응형 병렬 `ParCtx`·`Mutex`·`runPool`[디렉토리 파일자식 동시·하위폴더 순차·충돌 질의 직렬화]·utimes 재-stat 제거·`readdir({withFileTypes})`·runCopy/runMove `concurrency` 옵셔널 기본1=기존동치) | **신규 채널 0·신규 의존성 0·계약 무변경**(기본값=기존 동작 동치) | — | 구현 완료(코드)·실 대용량/병렬 처리량 🟡 |
| **⚠️ V5 robocopy 고속 미러**(⚠️ **스코프 일탈=폴더 비교 §P1 미러 확장·신규 채널** — 기획 미수록·정식 편입 대기) | `ui/compare/CompareToolbar.tsx`(⚡고속미러 토글·해시모드 비활성) · `ui/compare/CompareMirrorDialog.tsx`(경고·휴지통 경유 안내) | `app/stores/compareSlice.ts`(`compareFastMirror` 토글) · `app/usecases/fileOps.ts`(`startRobocopyMirror` — 복사분 undo 미제공) · `app/usecases/compare.ts`(`applyMirrorConfirmed` 분기: 고속+메타+sourceDir→robocopy·해시→op:* 폴백) · `renderer/infra/api/index.ts`(opApi.robocopyStart) | `main/os/robocopy.ts`(신규 `runRobocopyCopy` — spawn 인자배열·셸 미경유·`/E /MT` 복사전용·**`/PURGE` 없음**·stdout 파일라인 파싱·종료코드≥8 실패·cancel=kill) · `main/operations/OperationManager.ts`(`startRobocopyMirror` — registerExternalOperation 재사용·win32 가드) · `main/ipc/op.handlers.ts`(핸들러·경로정규화+디렉토리 stat 검증) | **신규 invoke 채널 `op:robocopy:start`**(진행/취소/완료는 기존 `op:*` 재사용·**EVENT_CHANNELS 무변**)·`channels.ts OP_ROBOCOPY_START`·`contracts.ts OpRobocopyStartReq`+ChannelMap·`guard.ts zOpRobocopyStartReq`·`preload/api.ts op.robocopyStart` | ADR-005(spawn 셸 미경유·sender+zod 검증) | 구현 완료(코드)·실 robocopy/GUI 🟡 |
| **V6 휴지통 실패 메시지 actionable화**(기존 P4 trash UX·스코프/채널 0) | — | — | `main/operations/OperationManager.ts`(`describeTrashFailure` — `shell.trashItem` errno 없는 영어 원문을 원인추정 한국어로[사용중/보호·정션·미존재 구분]) | **신규 채널 0·신규 의존성 0** | — | 구현 완료(코드)·실 동작 🟡 |
| **V7 다중선택 드래그 이동 수정**(기존 A3 드래그 버그 수정·스코프/채널 0·렌더러 전용) | `ui/panel/views/FileListView.tsx`(`onRowMouseDown`[수정자 없이 이미 다중선택된 항목은 단일붕괴를 mouseup으로 지연]·`onRowClickSelect`[드래그 아니면 단일 정리·드래그면 click 미발화로 다중 보존]·그리드/목록/자세히 3모드 행 전부·RowProps onMouseDownSelect/onClickSelect) | (선택은 기존 selectionSlice) | — | **신규 채널 0·렌더러 전용** | — | 구현 완료(코드)·실 GUI 🟡 |
| **V8 휴지통 실패 → 영구삭제 폴백**(기존 P4 delete 동작 추가·확인 게이트·채널 0) | `ui/dialogs/ConfirmDialog.tsx`(reason ⚠ 표시) | `app/stores/uiSlice.ts`(`ConfirmDeleteState.reason?`·`openConfirmDelete(paths, reason?)`) · `app/usecases/operationsBridge.ts`(trash op:done 실패 항목 → openConfirmDelete 폴백 제안·확정 시 기존 `confirmPermanentDelete`→op:start delete) | — | **신규 채널 0**(사용자 결정 게이트) | — | 구현 완료(코드)·실 GUI 🟡 |
| **V9 EPERM 견고 영구삭제**(기존 P4 delete/move 버그 수정·채널 0) | — | — | `main/operations/engine.ts`(`forceRemove`/`removeWithRetry` — 읽기전용 chmod 해제·정션/심볼릭은 대상 미재귀[링크만 제거·데이터 손실·EPERM 방지]·rmdir/unlink EPERM·EBUSY chmod후 재시도·runDelete 및 copyEntry move 원본제거에 `fs.rm` 대체) | **신규 채널 0** | — | 구현 완료(코드)·실 FS 스모크 통과·광범위 EPERM/EBUSY 🟡 |
| **F1 asar 가상화 ENOENT 수정**(2026-06-10·기존 P4 delete 버그·채널 0) | — | — | `main/workers/fileOpWorker.ts`(`process.noAsar=true` — `.asar` 를 일반 파일로 취급) · `main/operations/engine.ts`(`forceRemove` ENOENT 방어) | **신규 채널 0·신규 의존성 0** | — | 구현 완료(코드)·asar 가상화 Electron 한정 헤드리스 재현 불가 🟡 |
| **F2 특수파일 EACCES best-effort 삭제**(2026-06-10·기존 P4 delete 버그·채널 0) | — | — | `main/operations/engine.ts`(`forceRemove` 재설계 — 삭제 가능한 건 모두 제거·**못 지운 항목만 보고**[한 항목 실패가 전체 중단 안 함]·lstat 실패[소켓/파이프]도 unlink→rmdir 시도·readdir 실패 폴더도 rmdir 시도) | **신규 채널 0** | — | 구현 완료(코드)·실 FS 스모크 통과(읽기전용/정션 보존) |
| **B1 기본 시작위치 새 탭 미적용 수정**(2026-06-10·기존 P3 탭·E6 설정 교차 버그·채널 0·렌더러 전용) | — | `renderer/app/stores/tabsSlice.ts`(`newTab` — 인자 없으면 `startLocation`[없으면 내 PC]·`"E:"`→`"E:\\"` `normalizeDisplay` 보정) | — | **신규 채널 0·렌더러 전용** | — | 구현 완료(코드)·실 GUI 🟡 |
| **B3 미러 재귀 복사 평탄화 수정**(2026-06-10·기존 §P1 미러/P4 copy 버그·**기존 `op:start` 옵셔널 `baseDir` 확장·신규 채널 0**) | — | `renderer/app/usecases/fileOps.ts`(startOperation `baseDir` 파라미터) · `renderer/app/usecases/compare.ts`(applyMirrorConfirmed `baseDir=sourceDir`) | `main/ipc/op.handlers.ts`(guard+전달) · `main/operations/OperationManager.ts`(start/startWorker→job) · `main/workers/fileOpWorker.ts`(runCopy 전달) · `main/operations/engine.ts`(runCopy/runCopyOrMove — `relative(baseDir,src)` 상위폴더 재현·mkdir 후 copyEntry) | `shared/ipc/contracts.ts`(`OpStartReq.baseDir?`) · `main/ipc/guard.ts`(`zOpStartReq.baseDir`) · `main/workers/protocol.ts`(`WorkerJob.baseDir`) — **기존 `op:start` 확장·`baseDir` 옵셔널·미지정 시 기존 copy 동작 동치·신규 채널 아님** | — | 구현 완료(코드)·실 FS 스모크 통과(top/sub/deep 구조 보존) |
| **B4 휴지통 실패 폴백 UX**(2026-06-10·기존 P4 trash·V8 연장·채널 0·렌더러 전용) | — | `renderer/app/usecases/operationsBridge.ts`(휴지통 실패→영구삭제 폴백 제안 시 실패한 trash op 작업패널 dismiss) | — | **신규 채널 0·렌더러 전용** | — | 구현 완료(코드)·실 GUI 🟡 |
| **I8 익명 사용 통계 도움말**(2026-06-10 추가 → **2026-06-11 UI 제거**·기존 E6 설정 UX·채널 0·렌더러 전용) | ~~`renderer/ui/settings/SettingsDialog.tsx`(텔레메트리 설명 보강)~~ → **`SettingsDialog.tsx`에서 "익명 사용 통계" 체크박스·도움말 블록 + `telemetryOptIn` 구독 + `changeTelemetryOptIn` import 제거(UI 노출 제거)**. **단 정의는 store/usecase/persistence/verify에 잔존(grep 확인): `uiSlice.ts`(`telemetryOptIn`)·`usecases/settings.ts`(`changeTelemetryOptIn`)·`main/persistence/SettingsStore.ts`·`tests/p5.verify.ts`(telemetry IPC 모킹·"기본 false 유지")** | — | telemetry 정의(`telemetryOptIn`) 유지·UI 진입점만 제거 | **신규 채널 0·렌더러 전용** | — | UI 제거(정의는 store/persistence 잔존)·실 GUI 🟡 |
| **I9 브라우저 기본 단축키 차단**(2026-06-10·기존 키보드/SA §7.1·채널 0·렌더러 전용) | `renderer/ui/keyboard/KeyboardDispatcher.tsx`(미사용 브라우저 단축키[F11 전체화면·F3 찾기·Ctrl+P 인쇄·Ctrl±/0 줌·비-dev F5/Ctrl+R 리로드·F12/Ctrl+Shift+I/J/C 개발자도구] `preventDefault`·앱 매핑 단축키는 통과) | — | — | **신규 채널 0·렌더러 전용** | — | 구현 완료(코드)·실 GUI 🟡 |
| **⚠️ V11 설정 팝업 좌우 2단 구성 + 단축아이콘(신규)**(2026-06-11·⚠️ **기획 미수록·스코프 일탈**·채널 0) | `renderer/ui/settings/SettingsDialog.tsx`(전면 재구성 — 좌측 `<nav>` 카테고리[layout/system/workspace/shortcuts]·우측 `LayoutCategory`/`SystemCategory`/`WorkspaceCategory`/`ShortcutsCategory`·레이아웃 카테고리 **단축아이콘** 그룹별 체크박스[`ICON_BAR_ITEMS` 4그룹·표시/숨김 토글]+"기본값으로 초기화"·시스템=기존 항목·**워크스페이스=`<WorkspacePanel onLoaded={close}/>` 임베드(2026-06-11 독립 `WorkspaceDialog` 폐지·`openWorkspace` 액션 제거)**·단축키=`listShortcutGroups()` 인라인) | `app/usecases/settings.ts`(`toggleIconBarItem(commandId)`→`iconBarHidden` 영속·`resetIconBar()`→hidden/order 빈배열) · `app/stores/uiSlice.ts`(`iconBarHidden`·`setIconBarHidden`·applySettings 반영) | `ui/toolbar/iconBarItems.ts`(`ICON_BAR_ITEMS`·`iconBarItemTitle`) · `ui/keyboard/shortcuts.ts`(`listShortcutGroups`/`prettyChord` 읽기 API 재사용) | **신규 채널 0**·`SettingsSnapshot.iconBarHidden?`(`shared/dto/index.ts` 옵셔널 추가·비파괴)·`main/persistence/defaults.ts`(defaultSettings 빈배열·coerceSettings `asStrArray` 폴백)·`SETTINGS_SCHEMA_VERSION` 무변경 | — | 구현 완료(코드)·실 GUI 🟡 / **[2026-06-11 후속] 카테고리 딥링크 구현 완료: `openSettings('workspace')`·워크스페이스 본문 `WorkspacePanel` 추출·설정 임베드·독립 `WorkspaceDialog` 폐지(직전 "딥링크 미구현" 정직 표기 해소)** |
| **⚠️ V12 상단 아이콘바 아이콘 드래그 위치 변경(신규)**(2026-06-11·⚠️ **기획 미수록·스코프 일탈**·채널 0·렌더러 전용) | `renderer/ui/toolbar/IconBar.tsx`(각 버튼 `draggable`·`onDragStart`/`onDragOver`/`onDrop`/`onDragEnd`·`DragState`{dragId,overId}·삽입 위치 강조·목록 끝 빈영역 드롭=맨 뒤·`commitReorder`[id 기준 전체 순서(숨김 포함) 재구성·변동 없으면 영속 생략]) | `app/usecases/settings.ts`(`reorderIconBar(order)`→`iconBarOrder` 영속) · `app/stores/uiSlice.ts`(`iconBarOrder`·`setIconBarOrder`·applySettings 반영) | `ui/toolbar/iconBarItems.ts`(`resolveIconBarItems(order, hidden)` 헬퍼 신규 — order override 선두+미포함 항목 기본순서 뒤·hidden 제외·알수없는 id 무시·비파괴) | **신규 채널 0·렌더러 전용**·`SettingsSnapshot.iconBarOrder?`(옵셔널·비파괴)·`defaults.ts` `asStrArray` 폴백 | — | 구현 완료(코드)·실 GUI 🟡 |
| **V13 빠른 위치 "바탕화면" 추가**(2026-06-11·**§X1 사소 노드 확장·스코프 일탈 아님**·채널 0·렌더러 전용) | `renderer/ui/sidebar/Sidebar.tsx`("빠른 위치" 섹션에 `knownFolders.desktop` `QuickFolderNode`[🖳 바탕화면·다운로드 위]·`desktop \|\| downloads`일 때 섹션 렌더) | `app/stores/sidebarSlice.ts`(`knownFolders`·`loadKnownFolders` 기존 재사용) | (Main FS 격리·렌더러 표시만) | **신규 채널 0**(기존 `fs:known-folders`·`KnownFoldersDTO.desktop` 이미 제공·UI 노출만 추가) | ADR-005 | 구현 완료(코드)·실 GUI 🟡 / §1-X1 교차(desktop 노드 정식 노출) |

> **§V 정합 메모(동결·검증·정직 한계)**: **신규 IPC 채널 3종(V1 `shell:open-external`·V3 `app:open-path`·V5 `op:robocopy:start`)은 P1 채널 동결 이후 Should/신기능 신규 채널 추가 선례(`preview:read`·`shell:open-terminal`·`analyze:scan:*`·`fs:watch:*`·`trash:*`·`preview:thumbnail`·`hash:*`·`queue:*`)와 동일 규약 — 동결 위반 아님**(요청-응답형 2·푸시형 1·전부 sender/zod/프로토콜 검증 또는 EVENT_CHANNELS 정식 등록·V5는 진행/취소/완료를 기존 `op:*` 재사용·EVENT_CHANNELS 무변). 신규 npm 의존성 0. **검증: V1~V3은 `typecheck:node`+`typecheck:web` PASS·ESLint 0(NSIS 포함 헤드리스 verify 범위 밖) / V4~V9(2026-06-10 추가분)은 `npm run typecheck`(node+web) PASS·변경 파일 ESLint 0·`verify:ops` 35/0(회귀 0)·일회성 실 FS 스모크 통과(복사 병렬 80/80·forceRemove 읽기전용/정션). 9건 모두 메인 엔진/렌더러 배선 또는 NSIS 스크립트 → 신규 verify 케이스 0·verify 누계 약 1200 불변·회귀 0(M7 baseline).** 유령 매핑 0(위 모든 파일 실재 확인). **스코프 상태(V4~V9): V4·V6·V7·V8·V9 5건은 기존 기능(P4 copy/move·trash·delete·A3 드래그) 개선 — 스코프·MoSCoW·신규 채널 무변경(기획 무수정). ⚠️ V5 robocopy만 신규 채널+신규 스코프(§P1 미러 확장) → 위 V1~V3과 동일하게 기획 정식 편입 아님·PM/사용자 결정 대기.** **스코프 상태(버그·UX 7건, 2026-06-10 추가 — F1·F2·B1·B3·B4·I8·I9): 7건 모두 기존 기능(P4 copy/move/delete·trash·P3 탭·E6 설정·키보드·§P1 미러) 버그 수정/UX 개선 — 스코프·MoSCoW·신규 채널 무변경(기획 무수정·스코프 일탈 아님·코드 사실 기록만). B3은 기존 `op:start` 계약을 옵셔널 `baseDir`로 확장한 것으로 신규 채널 아님(미지정 시 기존 copy 동작 동치).** **검증(버그·UX 7건): `npm run typecheck`(node+web) PASS·변경 파일 ESLint 0·`verify:ops` 35/0(회귀 0)·F2·B3 일회성 실 FS 스모크 통과(F2=읽기전용/정션 보존·B3=top/sub/deep 구조 보존)·신규 verify 케이스 0·verify 누계 약 1200 불변. 유령 매핑 0(위 파일 실재 확인).** **정직 한계(✅ 위장 아님): 실 GUI(대시보드 동작 버튼 왕복·외부 브라우저 udm=50 열림·드라이브 레이블 GUI 표기·B1 새 탭 시작위치·B4 폴백 dismiss·I8 표기·I9 실 브라우저 단축키 차단)·실 설치/우클릭/한글 메뉴/새 탭·V4 대용량/병렬 처리량·V5 robocopy 실행/stdout 파싱·F1 asar(Electron 한정 미재현)·NSIS 설치는 미검증 → 런타임/설치 스모크 권장 🟡. 실 빌드 스모크 미실행.** **보류 백로그 4건(구현 아님·잔여): B2 전송큐 일시정지 미반영(설계 한계)·I5 드래그 고스트 스택 비주얼·I6 새 탭 워크스페이스 피커·I7 단축키 도움말 설정 분리 — 🔜 미착수(roadmap §0.5 비고).**
>
> **§V 추가(2026-06-11, V11~V13 — 비계획 3건)**: **⚠️ V11(설정 2단 재구성+단축아이콘 커스터마이즈)·⚠️ V12(아이콘바 드래그 재배열)는 PRD/features/user-stories/flows 미수록 신규 스코프(스코프 일탈) — 기획 정식 편입 아님·PM/사용자 결정 대기**(딥링크 미구현 후속 포함). **V13(빠른 위치 "바탕화면")은 기존 §X1 사소 노드 확장 — 스코프 일탈 아님(§1-X1 desktop 노드 정식 노출).** 3건 모두 **신규 IPC 채널 0·신규 npm 의존성 0·`SETTINGS_SCHEMA_VERSION` 무변경**(`SettingsSnapshot.iconBarHidden?`/`iconBarOrder?` 비파괴 옵셔널 추가). 검증: `npm run typecheck`(node+web) PASS·ESLint 0·**신규 `verify:persistence` 138→147(단축아이콘 coerce 9케이스)·`verify:store` 274→281(`resolveIconBarItems` 7케이스)**·전부 0 fail·회귀 0·verify 누계 약 1523→약 1539. 유령 매핑 0(`SettingsDialog.tsx`·`IconBar.tsx`·`iconBarItems.ts#resolveIconBarItems`·`Sidebar.tsx desktop`·`uiSlice.ts setIconBar*`·`usecases/settings.ts toggle/reorder/reset`·`shared/dto SettingsSnapshot.iconBar*`·`defaults.ts asStrArray` 실재 확인). 실 GUI(설정 2단 전환·단축아이콘 체크/초기화·아이콘바 실 드래그/순서 유지·바탕화면 노드 이동)는 런타임 스모크 권장 🟡(✅ 위장 아님). 상태 단일 출처 roadmap §0.5(2026-06-11 비계획 구현 3건 단락).

---

## 1-Y. Windows 셸 컨텍스트 메뉴 연동(§Y1·US-23.1·F37, 2026-06-12 편입) → 매핑 — 구현 완료(코드)·실 GUI/실 패키지 🟡

> **상태: 구현 완료(코드)·통합 QA PASS([qa-integration-Y](../reviews/qa-integration-Y.md))·실 GUI/실 패키지 런타임 스모크 🟡.** 2026-06-12 사용자 직접 요청으로 기획 편입(PRD §6 Should·features §Y/§Y1·user-stories 에픽23/US-23.1·flows F37·reviewer PASS) → chief-architect 설계([ADR-013](./adr/ADR-013-shell-context-menu-verbs.md)) → T1~T6(backend+frontend) 구현 → 통합 QA PASS. **아래 파일/심볼은 실재 확인됨**(grep·typecheck/build PASS·`verify:shellverbs` 75/0·유령 매핑 0). 기술 방향: COM Verbs `FolderItem.Verbs()`/`verb.DoIt()` + **상주 PowerShell 자식 프로세스 `shellVerbsWorker.ps1`**(stdin/stdout JSON·명령행 합성 0)·신규 네이티브/npm 의존성 0·신규 IPC 채널 2종(invoke·EVENT_CHANNELS 무변)·`FileOpErrorCode`에 `'EVERB'` 비파괴 확장. **[설계→구현 차이(드리프트 정정)]: 설계에선 단일 `src/main/os/shellVerbs.ts`로 예정됐으나 구현에서 ① 블랙리스트/정규화/verbId 합성 순수 로직을 `src/main/os/shellVerbsBlacklist.ts`로, ② 메뉴 섹션 병합 렌더 로직을 `src/renderer/app/usecases/shellVerbsSection.ts`로 별도 분리했다(실코드 기준·아래 매핑 갱신). uiSlice 필드명도 설계의 `winVerbs` 예시와 실코드(`ContextMenu.tsx` 섹션 병합·`shellVerbsSection.ts#buildWinVerbsSection`)에 맞춰 실재 기준 표기.** 실 GUI(섹션 표출·verb 클릭 실행)·실 패키지 설치본(asar ps1·ExecutionPolicy)은 런타임 스모크 🟡(✅ 위장 금지). **별도 트랙(§Y 무관): `verify:worker` FAIL은 §Y 회귀 아님 — clean HEAD에서도 재현되는 Node 22.17 워커 atomics 사전 환경 결함.**

| 기능(추적용) | UI 컴포넌트 | 유스케이스/스토어 | 도메인/OS | IPC 채널 / DTO | 관련 ADR | 상태 |
|---|---|---|---|---|---|---|
| **Y1 "Windows 메뉴" 섹션 — 셸 verb 조회·병합 렌더**(US-23.1·F37·단일 선택 한정·정직 한계 ①②③⑤) | `renderer/ui/contextmenu/ContextMenu.tsx`("Windows 메뉴" 섹션 렌더·로딩/채움/숨김 상태) · `renderer/app/stores/uiSlice.ts`(컨텍스트 메뉴 셸 verb 상태 필드) | `renderer/app/usecases/contextMenu.ts`(단일 선택 경로 말미 섹션 병합·B6 자체 명령 불변) · `renderer/app/usecases/shellVerbs.ts#loadWinVerbs`(메뉴 열림 시 `shell:context-verbs` 조회·TTL 캐시·로딩 상태 반영) · **`renderer/app/usecases/shellVerbsSection.ts#buildWinVerbsSection`(신규 분리 — 섹션 병합 렌더 로직·설계엔 없던 별도 파일)** | `src/main/os/shellVerbs.ts#shellVerbsService`(`ShellVerbsService` — 상주 PowerShell 워커 수명·요청 큐·타임아웃→섹션 비노출) · **`src/main/os/shellVerbsBlacklist.ts`(신규 분리 — `normalizeVerbName`/`BLACKLIST`/`isBlacklisted`/`makeVerbId`/`parseVerbId`/`filterVerbs` 순수 로직·설계엔 단일 파일이었으나 분리)** · `src/main/os/shellVerbsWorker.ps1`(고정 PowerShell 스크립트 — stdin JSON 루프·`Shell.Application`·`Verbs()` 열거·UTF-8) · `src/main/ipc/shell.handlers.ts`(`SHELL_CONTEXT_VERBS` 핸들러·sender+zod+로컬 한정) · `src/main/ipc/guard.ts`(`§Y1` 스키마) · `src/main/index.ts`(before-quit `shellVerbsService.dispose()`) | **신규 채널 `shell:context-verbs`**(invoke → `Result<ShellContextVerbsRes>`·EVENT_CHANNELS 무변)·`shared/ipc/channels.ts SHELL_CONTEXT_VERBS`·`contracts.ts ShellContextVerbsReq/Res`+ChannelMap·`shared/dto/index.ts ShellVerbDTO{ verbId; display }`·`preload/api.ts shell.contextVerbs`·`renderer/infra/api/index.ts` | [ADR-013](./adr/ADR-013-shell-context-menu-verbs.md) · ADR-005(sender·zod·로컬 한정·명령행 합성 0) · ADR-003(invoke/Result) | 구현 완료(코드)·실 GUI 🟡 |
| **Y1 verb 실행 — `verb.DoIt()` fire-and-forget**(US-23.1·F37·정직 한계 ④·외부 프로그램 실행) | `renderer/ui/contextmenu/ContextMenu.tsx`("Windows 메뉴" 항목 클릭 → 실행·메뉴 닫힘·실패만 가벼운 토스트) | `renderer/app/usecases/shellVerbs.ts#invokeWinVerb`(`shell:invoke-verb` 호출·결과 미추적) | `src/main/os/shellVerbs.ts#ShellVerbsService.invokeVerb`(**실행 시 Verbs() 재열거 후 index+정규화표시명 교차검증**: 일치=실행·없음=`EVERB` 실행 거부[스테일 메뉴 오실행 방지]) · `shellVerbsWorker.ps1`(invoke op → `$verb.DoIt()`) · `src/main/ipc/shell.handlers.ts`(`SHELL_INVOKE_VERB` 핸들러·sender+zod+로컬 한정) · `src/main/ipc/guard.ts`(`§Y1` 스키마) | **신규 채널 `shell:invoke-verb`**(invoke → `Result<void>`·`ok`=DoIt 호출 성공[결과 미추적]·`EVERB`=stale/미존재·EVENT_CHANNELS 무변)·`channels.ts SHELL_INVOKE_VERB`·`contracts.ts ShellInvokeVerbReq`+ChannelMap·`dto/index.ts FileOpErrorCode 'EVERB'`·`preload/api.ts shell.invokeVerb` | [ADR-013](./adr/ADR-013-shell-context-menu-verbs.md) · ADR-005(실행 표면 미추가=탐색기 동일 신뢰 모델) | 구현 완료(코드)·실 GUI 🟡 |

> **§1-Y 정합 메모(설계 단계·정직 한계)**: **신규 IPC 채널 2종(`shell:context-verbs`·`shell:invoke-verb`)은 P1 채널 동결 이후 Should/신기능 신규 채널 추가 선례(`shell:open-terminal`·`shell:icon`·`shell:open-external`·`analyze:scan:*`·`fs:watch:*`·`trash:*`·`preview:thumbnail`·`hash:*`·`queue:*`·`archive:*`·`window:*`·`fs:known-folders`)와 동일 규약 — 동결 위반 아님**(둘 다 invoke·EVENT_CHANNELS 무변·sender/zod/guardPath/로컬 한정 검증). **신규 npm 의존성 0·신규 네이티브 의존성 0(COM·PowerShell 시스템 내장·`showProperties` COM `Verbs()`/`DoIt()` 선례)·`SESSION_SCHEMA_VERSION` 무변경·`FileOpErrorCode`에 `'EVERB'` 비파괴 유니온 확장(RemoteErrorCode 선례 동형).** **상태: 구현 완료(코드)·통합 QA PASS — 위 파일/심볼은 실재 확인됨(grep·typecheck/build PASS·`verify:shellverbs` 75/0·유령 매핑 0). [설계→구현 드리프트 정정]: 설계의 단일 `shellVerbs.ts` 예정이 구현에서 순수 로직 `shellVerbsBlacklist.ts`(main)·섹션 병합 `shellVerbsSection.ts`(renderer)로 분리됐고, uiSlice `winVerbs` 필드 예시명은 실코드 기준으로 환원 표기. 실 GUI/실 패키지는 런타임 스모크 🟡(✅ 위장 금지).** **정직 한계(✅ 위장 금지·ADR-013 결정 ④⑥): ① canonical name 부재 → 블랙리스트는 표시명 정규화 매칭(영/한 보장·그 외 OS 언어 best-effort 미필터=중복 표시 무해)·② 캐스케이드 서브메뉴 평탄화/누락(보이는 것만 best-effort·1차 비범위)·③ 다중 선택 섹션 숨김(단일 한정)·④ 실행 fire-and-forget(성공/실패 미추적). 비범위: 네이티브 HMENU 팝업·서브메뉴 완전 재현·다중 선택 일괄 invoke.** 마일스톤: 후속 Should(M10 가칭·PRD §12·구현 완료). 상태 단일 출처 roadmap §0.5(2026-06-12 §Y 단락).

---

## 4. 추적성이 커버한 핵심 기능 (요약)

탭 관리 · 2분할/4분할 패널 · 패널 간 이동/복사(D&D·클립보드 — ~~F5/F6~~ 2026-06-07 제거) · 목록 보기/정렬(**보기 5종 J3: `ViewMode`**) · 생성/이름변경(fs:mkdir/create-file/rename) · 삭제(휴지통/영구) · 파일 실행/열기(B6) · 복사/붙여넣기 · 충돌 해결 · 주소표시줄/탐색 · 트리 사이드바 · 즐겨찾기/최근(**별칭 J7: `favoriteLabels`**) · 현재 폴더 검색 · 확장자 필터 · 미리보기(S, **2단 뷰어 J5·폭 조절 J6**) · 다중 선택/일괄(**박스 선택 J1: `boxSelect`**) · 진행률/취소 · 대용량 빠른 첫 렌더(성능) · 테마(라이트/다크/시스템/**블루라이트 차단 I2**) · 설정(E6) · 키보드 워크플로(8장 전체) · 자동 세션 복원 · 상태바 · 명시적 워크스페이스(S) · 휴지통 연동 · Windows 특수케이스 · 텔레메트리 옵트인 · **사용량 대시보드(S, I1: `analyze:scan:*`·recharts)** · **패널 실시간 갱신(S, J2 ✅: `fs:watch:*`·`WatchService`(UNC + 매핑 드라이브 eager + reactive 폴링)·`os/driveType.ts`(GetDriveType 연동)·`panelsSlice` softRefresh/보존; `subst`·일부 클라우드(`DriveType≠4`)만 미포함 한계)** · **브랜딩 AGT-Finder(M릴리스, J4: appId `com.agtfinder.app`)**.

> 위 항목 모두 컴포넌트·유스케이스/스토어·IPC 채널·ADR로 매핑 완료. 생성/이름변경(B3)은 전용 `fs:mkdir`/`fs:create-file`/`fs:rename` 채널(SA §3.2), 실행(B6)은 `shell:*`, 설정(E6)은 `settings:get/set`으로 실 채널까지 연결했다(유령 매핑 제거). **Should 4종(4분할·미리보기·워크스페이스·텔레메트리)은 P6에서 실 파일·채널까지 매핑 완료 ✅** — 미리보기는 신규 `preview:read`(기존 `fs:stat` 잠정 매핑 정정), 텔레메트리는 신규 `telemetry:get-opt-in` 추가. **연결 프로그램으로 열기(`shell:open-with`)·컨텍스트 메뉴(우클릭) 인프라는 P6 시점에 구현 완료 ✅**(후자는 P4 산출물이 실제 누락돼 있던 드리프트를 해소 — `ui/contextmenu/`·속성→`shell:show-properties` 호출 UI 포함). **신규 UX 6종(H장: 아이콘바 H1·사이드바 토글 H2·분할 크기조절 H3·터미널 열기 H4·경로 직접 입력 H5·파일 유형 아이콘 H6)도 구현 완료 ✅**(§1-H 매핑 — renderer commandId 4건·`TabSnapshot.splitRatios` DTO 확장·`coerceSplitRatios`; **H4 신규 채널 `shell:open-terminal`·H6 예약 채널 `shell:icon` 정식 구현(예약→구현 드리프트 해소)**; US-7.2/7.3 드래그·H4 `wt.exe`·H6 `app.getFileIcon` 실제 네이티브 실행은 런타임 스모크 권장). **신규 보기·실시간·뷰어·브랜딩(J장: 박스 선택 J1·패널 실시간 갱신 J2·보기 5종 J3·AGT-Finder 브랜딩 J4·미리보기 2단 뷰어 J5·폭 조절 J6·즐겨찾기 별칭 J7)도 구현 완료 ✅**(§1-J 매핑 — J2 신규 채널 `fs:watch:*`·J3 `ViewMode`·J4 appId `com.agtfinder.app`·J5 highlight.js/marked/dompurify·J6 `previewWidth`·J7 `favoriteLabels`; **J2 실시간 갱신은 보류 2건(선택/스크롤 보존·UNC 폴링 폴백) 구현 완료 + 매핑 네트워크 드라이브 `GetDriveType` 연동(`os/driveType.ts` PowerShell CIM `DriveType=4`·`paths.isNetworkDriveRoot`)으로 🟡→✅ 격상 — `panelsSlice` softRefresh/`pendingScrollRestore`·`WatchService` UNC + 매핑 드라이브 eager + reactive 폴링·`paths.ts` isUncPath/isNetworkDriveRoot; `subst`·일부 클라우드(`DriveType≠4`)만 미포함 한계**; 워처/뷰어 네이티브 동작·박스선택 드래그는 런타임 스모크 권장). **F5/F6 복사·이동 매핑은 코드 제거에 맞춰 §1·§2에서 정정(유령 매핑 제거).** **신규 K장 3종(되돌리기 Ctrl+Z K1·휴지통 관리 화면 K2·파일 유형별 비중 K3)도 구현 완료 ✅**(§1-K 매핑 — K1 `undoSlice.ts`(cap 50)·`undo.ts` 역연산·`commandBus` `notYet`→`performUndo`(신규 채널 0)·K2 신규 채널 `trash:*` 3종·`recycleBin.ts` Shell COM·`TrashDialog.tsx`·K3 `categorize.ts`·`scanEngine` byCategory 1패스·`ScanResult.byCategory` DTO 비파괴 확장(신규 채널 0); **이로써 직전 "Should 잔여"였던 되돌리기·휴지통 관리 화면이 해소**됨; copy-undo 보수적·영구삭제 미push·휴지통 COM 런타임 스모크 권장). **신규 L장 1종(그리드 보기 이미지 썸네일 자체 생성 L1)도 구현 완료 ✅**(§1-L 매핑 — 신규 채널 `preview:thumbnail`·`os/thumbnail.ts` nativeImage 비율보존 resize·30MB 상한·LRU 256·세마포어 4·`thumbnailCache.ts`·`domain/image.ts`·`FileListView ThumbnailIcon`·신규 의존성 0·verify:thumbnail 33; **이로써 직전 "Should 잔여"였던 그리드 썸네일 이미지 자체 생성이 해소** → Should 잔여(🔜) 0). **이로써 §L 완료 시점(2026-06-07) 기준 개발 잔여(🔜)는 0이었다 — 남은 것은 P7 릴리스 실측·실제 코드서명·런타임 스모크(🟡)뿐이었다(아래 §1-P7). 2026-06-08 §M(외부 연계) 3기능(M1·M2 Should·M3 Could, US-12.1~12.5)이 기획 편입(🔜)됐다가 같은 날 MP0~MP5로 구현 완료·통합 QA PASS(✅)됨(매핑 §1-M 실 파일로 확정·실 동작은 런타임 스모크 권장 🟡). 같은 날 §N(N1 즐겨찾기 경로 워터마크·N2 즐겨찾기 드래그 정렬 Should·US-13.1/13.2)도 기획 편입(🔜)됐다가 **같은 날 구현 완료·통합 QA PASS(✅)됨**(§1-N 매핑 실 파일로 확정 — N1 `favoriteWatermark.ts`·`FavoriteWatermark.tsx`, N2 `useFavoriteReorder.ts`·`sidebarSlice.reorderFavorite`·키 `Alt+Shift+↑/↓`·렌더러 전용·신규 채널 0·verify 302). **이로써 §N 완료 시점(2026-06-08) 기준 개발 잔여(🔜)는 0이다 — 남은 것은 P7 릴리스 실측·§M·§N 실 동작 런타임 스모크(🟡)뿐**. roadmap §0.5 머리말이 단일 출처.** nativeImage 실 디코드·GUI 그리드 렌더(L1)·§M·§N 실 GUI 동작은 런타임 스모크 권장 🟡.
