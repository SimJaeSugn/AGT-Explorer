# 기획 산출물 독립 재검증 보고 — Explorer (멀티 디렉토리 파일탐색기)

> 검증일: 2026-06-06 · 검증: 독립 검증 담당(Reviewer) · 회차: 2 (재검증)
> 대상: `docs/PRD.md`, `docs/features.md`, `docs/user-stories.md`, `docs/flows.md`
> 기준: 1회차 보고서 `docs/reviews/planning-review.md` (High 4 / Med 6 / Low 2)

---

## 판정: **PASS**

1회차 FAIL의 핵심 사유였던 **High 4건이 전부 해소**되었고, Med 6건·Low 2건도 모두
반영되었다. 특히 우려했던 **단축키 충돌(F5/F6)이 명세 전반에서 완전히 단일화**되었고,
키 재배정이 **4개 문서에 누락 없이 일관 반영**되어 새로운 모순이 발견되지 않았다.
출시 전 기준(완료성·범위명확성·추적성·검증가능성·일관성·예외처리)을 충족한다.
남은 항목은 출시를 막지 않는 **경미한 문서 정리(잔여 용어 1건)** 뿐이다.

---

## 항목별 처리 확인 표

| 이전 항목 | 처리 | 근거 |
|---|---|---|
| **[H1] F6 이중 할당 (포커스 이동 vs 파일 이동)** | **처리됨** | 포커스 이동 = `Tab`/`Ctrl+←·→`, 파일 이동 = `F6`로 완전 분리. `PRD.md` 185·189행(표), 173~175·204행(검증 주석), `features.md` 38행("파일 이동(F6)과 키 분리됨"), `user-stories.md` 28행, `flows.md` 58행에서 일관. F6가 두 동작에 매핑된 곳 없음(grep 교차확인 완료). |
| **[H2] F5 충돌 (새로고침 vs 패널 간 복사) 방치** | **처리됨** | 새로고침을 `Ctrl+R`로 재배정해 F5를 복사 전용으로 확정. `PRD.md` 188·193행, `features.md` 54·179행, `user-stories.md` 40·169행, `flows.md` 58행 모두 "새로고침 `Ctrl+R`과 충돌 없음" 명시. 미확정 키를 참조하던 수용기준(US-1.3)도 확정 키로 정리됨. |
| **[H3] 충돌 처리 수용 기준 누락** | **처리됨** | 독립 스토리 **US-2.4 "복사/이동 충돌 해결"(M)** 신설(`user-stories.md` 77~86행). 덮어쓰기/건너뛰기/둘 다 유지/폴더 병합/모두 적용/읽기전용/사용중·권한없음 실패목록/크기·수정일 비교까지 8개 수용기준으로 전개. `features.md` D4(152~166행)와 1:1 대응. US-2.3 75행이 US-2.4로 연결. 백로그표(204행) 등재. |
| **[H4] 성능 정량 기준 미연결** | **처리됨** | US-5.2 150행 "진행률 표시가 200ms 이내 간격으로 갱신", 성능 전용 스토리 **US-5.6 신설** 157행 "10,000개 항목 첫 화면 1.5초 이내", US-4.1 121행 "키 입력 후 **200ms 이내** 점증 필터"로 비측정 표현("즉시") 정량화. PRD 3장(39~40행)·7장 목표와 추적 연결. |
| **[M1] 세션 복원 우선순위 비일관(M/S 혼합)** | **처리됨** | US-5.5 자동 세션 복원(M)과 US-5.8 명시적 워크스페이스 저장(S)로 분리. 백로그표 215·218행 각각 단일 우선순위 행. PRD 11장 D3에서 동일 결정 기록. |
| **[M2] 탭 복제 추적성 단절** | **처리됨** | PRD Must 99행("탭 복제"), 8장 단축키표 182행(`Ctrl+D`), `features.md` 22행(M·US-1.1 명시), US-1.1 18행 수용기준에 반영. PRD→features→user-stories 연결 완성. |
| **[M3] 상태바 독립 스토리 부재** | **처리됨** | **US-5.7 "상태바 정보 표시"(M)** 신설(178~183행): 전체 항목 수/선택 개수/선택 용량/활성 패널 경로/필터 결과 갱신 수용기준. 백로그표 217행 등재. |
| **[M4] 용어 비일관** | **처리됨(경미 잔여 1건)** | PRD 5-1 용어 사전(75~89행) 신설로 표준어 확정, 3개 문서가 사전을 단일 출처로 참조. "주소창/미니주소/다중 페인/프리뷰"는 전 문서에서 제거됨(grep 확인). 단, `flows.md` 112행에 통합 대상어 "**패널 영역**"이 1건 잔존(사전상 "분할 패널"로 통일 대상). 출시 차단 아님 → 남은 반영 항목 참조. |
| **[M5] IA 다이어그램과 기능 정의 불일치** | **처리됨** | `flows.md` IA 다이어그램(17~23행)에 미리보기 패널(S, `Ctrl+P`, 우측/하단) 위치 추가, 상태바 영역(27행)에 진행률 인디케이터 표기, 영역 역할표(38~39행)에 미리보기 패널·진행률 추가. |
| **[M6] 미해결 질문 분산·"5개" 불일치** | **처리됨** | PRD 11장 결정 기록(228~241행)에 미해결 질문 5건(D1~D5)을 단일 목록으로 통합·확정값·우선순위·근거 명시. 명세 모순(F5/F6)은 "질문"에서 빼고 결함으로 분류해 8장에서 확정. `flows.md` 5장(116~124행)은 시각/배치 디자인 질문만 남기고 기능·단축키 질문은 PRD로 위임. |
| **[L1] 드래그&드롭 자기참조/순환 미정의** | **처리됨** | `features.md` A3 57행("자기 위치 드롭=무시")·58행("순환 이동 차단·안내"), F장 218·219행, `flows.md` 100·101행, US-1.3 41·42행 수용기준에 반영. |
| **[L2] 빈 탭 상태 모호** | **처리됨** | `features.md` 19행 "마지막 탭 닫으면 **내 PC(드라이브 목록) 기본 탭** 유지(빈 화면 아님)", US-1.1 20행 동일 수용기준 반영. |

**집계: 처리됨 12 / 부분 0 / 미처리 0** (High 4 전부 처리, Med 6 전부 처리, Low 2 전부 처리)

---

## 새로 생긴 모순 점검 (회귀 검사)

키 재배정·스토리 신설 과정에서 문서 간 누락·역전이 없는지 교차 확인했다.

- **단축키 일관성**: `F5`(복사)·`F6`(이동)·`Tab`(포커스)·`Ctrl+R`(새로고침)이 PRD/features/user-stories/flows 전 문서에서 동일하게 기술됨. 한 키가 두 동작에 매핑된 사례 없음(grep 전수 확인). 새 모순 없음.
- **단일 출처 원칙**: features·user-stories·flows가 단축키 전체 목록을 자체 보유하지 않고 PRD 8장을 단일 출처로 링크 → 향후 키 변경 시 동기화 누락 위험 낮음. 양호.
- **신설 스토리 ID·백로그 정합**: US-2.4 / US-5.6 / US-5.7 / US-5.8 모두 본문과 백로그 요약표에 등재. ID 충돌·누락 없음.
- **MVP 범위 정합**: PRD 6장 Must 목록(충돌 해결·상태바·자동 세션 복원·탭 복제)과 user-stories M 표기, features M 표기, 마일스톤(M2·M3)이 상호 일치.

---

## 남은 반영 항목 (출시 차단 아님 — 후속 정리 권장)

### [잔여-1] flows.md "패널 영역" 용어 1건 잔존 — 심각도 Low
- **무엇이**: `flows.md` 112행 "미리보기 패널(S)은 **패널 영역** 우측 또는 하단에 토글로 부착."
- **문제**: PRD 5-1 용어 사전에서 "패널 영역"은 "분할 패널"로 통합 대상으로 지정한 말이다. 사전과 본문이 미세하게 어긋남.
- **권고**: 해당 문구를 "분할 패널 우측 또는 하단에 부착"으로 정정. 문서 정리 차원이며 의미 혼동·구현 위험은 없어 출시를 막지 않는다.

---

## 종합

1회차 FAIL 사유가 전부 해소되었고 재검증 과정에서 새로운 결함이 발견되지 않았다.
추적성(핵심 기능 PRD→features→user-stories→flows 연결), 검증 가능성(정량 수용기준),
범위 명확성(MoSCoW·결정 기록 단일화), 예외 처리(자기참조/순환/충돌/권한)가 모두 충족된다.
**PASS** — 아키텍처 설계 단계로 진행 가능하다. [잔여-1]은 다음 문서 갱신 시 함께 정정 권장.
