# 개발 로드맵 — AGT-Finder (멀티 디렉토리 파일탐색기 · 코드네임 Explorer)

> 작성: 테크리드 · 2026-06-06 · **갱신: 2026-06-10** · 상태: **P0~P5(MVP) 구현 완료 · P6(Should) 구현 완료 · 신규 UX(H장: 아이콘바·사이드바 토글·분할 크기조절·터미널 열기·경로 직접 입력·파일 유형 아이콘) 구현 완료 · 신규 분석·접근성(I장: 사용량 대시보드·블루라이트 차단 테마) 구현 완료 · 신규 보기·실시간·뷰어·브랜딩(J장: 박스 선택·패널 실시간 갱신·보기 5종·AGT-Finder 브랜딩·미리보기 2단 뷰어·미리보기 폭 조절·즐겨찾기 별칭) 구현 완료 · 신규 되돌리기·휴지통·유형별 비중(K장: feat-K1 되돌리기 Ctrl+Z·feat-K2 휴지통 관리·feat-K3 유형별 비중) 구현 완료 — P6 잔여 "되돌리기·휴지통 관리 화면" 해소 · 신규 그리드 이미지 썸네일(L장: feat-L1 그리드 이미지 썸네일 자체 생성) 구현 완료 — P6 잔여 "그리드 썸네일 이미지 자체 생성" 해소 · **신규 외부 연계·원격(M장: M1 외부 D&D 복사·M2 클립보드 CF_HDROP 외부연계·M3 FTP/SFTP 원격, 에픽12 US-12.1~12.5) 구현 완료·QA PASS(2026-06-08) — 코드 정합·verify 충족 ✅ / 실 동작(외부 앱 실드롭·탐색기 양방향 왕복·실 SFTP/FTP/FTPS 핸드셰이크·실 DPAPI 암복호·실 전송) 런타임 스모크 권장 🟡 · **결함 수정(2026-06-09): M3 원격 디렉토리 더블클릭 ENOENT(원격 URI 미재구성·parentOf/breadcrumbs win32 오해)·M1 외부 드래그 "여기에 드롭" 오버레이 잔존(OS 드래그 인계 후 내부 dragState 미종료) 2건 수정 — 직전 ✅위장(코드 정합이라 표기했으나 원격 진입·드래그 종료가 실제로 깨져 있던 드리프트) 정정·코드 정합 회복, 실 런타임 스모크는 여전히 🟡** · **신규 즐겨찾기 UX(N장: N1 즐겨찾기 경로 워터마크·N2 즐겨찾기 드래그 정렬, 에픽13 US-13.1/13.2) 구현 완료·QA PASS(2026-06-08) — 코드 정합·verify 충족 ✅(렌더러 전용·신규 채널 0·키 `Alt+Shift+↑/↓`) / 실 GUI 동작(워터마크 렌더·드래그/키보드 정렬·테마별 반투명도·재시작 순서 유지) 런타임 스모크 권장 🟡** · **신규 파일/폴더 상단 고정(O장: O1 파일/폴더 상단 고정 pin, 에픽14 US-14.1·F19, PRD §6 Should) 정식 편입 완료(2026-06-09 — 직전 doc-sync 스코프 일탈 플래그 → 사용자 정식 편입 결정 → 4종 기획+traceability §1-O) — 구현 완료(코드 정합·verify 충족: domain 60·store 121·persistence 101)·렌더러 전용·신규 채널 0·신규 의존성 0 / 실 GUI 동작(우클릭 토글·📌 렌더·고정 표시·재시작 후 유지) 런타임 스모크 권장 🟡 (※ 고정 표시 2026-06-09 동작 변경: 목록/자세히=스크롤 고정 sticky·그리드=정렬 상단)** · **파워기능 M6 1차 3종(§P P1 듀얼 패널 폴더 비교(메타·단일깊이)·§R R1 고급 일괄 이름변경·§T T3 정렬/필터 프리셋, US-15.1/17.1/19.3) 구현 완료(코드)·통합 검증 PASS(2026-06-09) — 렌더러+세션영속·신규 채널 0·신규 의존성 0·verify domain 132/store 162/persistence 123/operations 75/perf 25/p5 52(0 fail·회귀 0) / 실 GUI(프리셋·일괄rename·Ctrl+Z 복원·비교 진입/미러/동기스크롤) 런타임 스모크 🟡·P1 해시/재귀 비교 M7 연기 · **파워기능 M7 2차(공용 해시 인프라 W1·전송 큐 인프라 W2 + §R R2 중복 파일 찾기·R3 전송 큐 매니저·R4 체크섬 검증·P1 해시/재귀 비교 확장, US-17.2/17.3/17.4·15.1) 구현 완료(코드)·통합 검증 PASS(2026-06-09) — 신규 채널 `hash:*`·`queue:*`·신규 의존성 0(node:crypto 내장)·verify hash 46/queue 47(+domain 168/store 207/persistence 128, 0 fail·회귀 0) / 실 워커·실 GUI(해시 잡·큐 일시정지/재개·중복 정리·복사후 검증·해시/재귀 실 비교) 런타임 스모크 🟡** · 나머지 §P~§U 7종(Q1·S1·S2·T1·T2·U1·U2·U3) 🔜 미착수(M8~M9)** · **⚠️ 비계획 3건(2026-06-10·기획 미수록·스코프 일탈, 정식 편입 아님): V1 대시보드 동작 컬럼(📂이동·✨AI, 신규 채널 `shell:open-external`)·V2 드라이브 레이블 일관화(신규 채널 0)·V3 탐색기 "AGT-Finder로 열기"(신규 푸시 채널 `app:open-path`·NSIS HKCU 등록) — 구현 완료(코드·typecheck PASS·ESLint 0)/실 GUI·설치 런타임 스모크 🟡·PM/사용자 정식 편입 결정 대기(§0.5 2026-06-10 단락·traceability §1-V)** · **견고성·성능 6건(2026-06-10, V4~V9): V4 복사 엔진 성능 최적화·V6 휴지통 실패 메시지 actionable화·V7 다중선택 드래그 이동 수정·V8 휴지통 실패→영구삭제 폴백·V9 EPERM 견고 영구삭제 5건=기존 P4/A3 기능 개선(스코프·채널 무변경) / ⚠️ V5 robocopy 고속 미러=신규 채널 `op:robocopy:start`·스코프 일탈(§P1 미러 확장·PM/사용자 정식 편입 결정 대기) — 구현 완료(코드·typecheck node+web PASS·ESLint 0·verify:ops 35/0·실 FS 스모크 통과)/실 동작·robocopy·설치 런타임 스모크 🟡(§0.5 2026-06-10 V4~V9 단락·traceability §1-V4)** → **개발 잔여(🔜)는 §P~§U 7종(M8~M9)** · F5/F6 제거 · P7 부분(헤드리스분 ✅: 접근성·WCAG AA·성능/F장 검증 하니스·npm audit 점검·sourcemap 분리·코드서명 설정 / 런타임 잔여 🟡: 성능 실측·실제 서명·NSIS 실측·F장 실케이스)**
> 입력 설계: [system-architecture.md](./architecture/system-architecture.md) · [software-architecture.md](./architecture/software-architecture.md) · [directory-structure.md](./architecture/directory-structure.md) · [traceability.md](./architecture/traceability.md) · [adr/](./architecture/adr/)
> 우선순위 출처: [PRD.md §6 MoSCoW](./PRD.md) · [user-stories.md](./user-stories.md)
> 상태 범례: ✅ 완료 · 🟡 부분 · 🔜 미착수

---

## 0. 목적 / 읽는 법

이 문서는 확정된 아키텍처를 **독립적으로 빌드·검증 가능한 순서 있는 단계(Phase)** 로 분해한 개발 로드맵이다.

- **MVP = PRD §6 Must 전체** → P0~P5에서 완성. **Should(4분할·미리보기·워크스페이스·텔레메트리)** → P6 이후로 분리.
- 각 단계는 **목표 / 산출물(모듈·파일 영역) / 의존 / 담당 역할 / 완료 기준(DoD, 측정 가능) / QA 검증 포인트 / 추적성(US·feat·설계 모듈)** 을 명시한다.
- **역할 분담 원칙**: P0은 devops 골격 → 이후 backend(Main: IPC/FS/파일작업)와 frontend(Renderer: React UI/상태)가 `shared/ipc` 계약을 합의한 뒤 병렬 → qa는 각 단계 경계면 교차 검증.
- **계약 단일화**: 모든 요청/응답 shape은 `src/shared/ipc/contracts.ts` + `src/shared/ipc/channels.ts` + `src/shared/dto/`에 단일 출처로 둔다. backend·frontend·preload가 동일 타입을 import → 컴파일 타임에 계약 위반 검출.

---

## 0.5 진행 현황 (2026-06-10 기준)

> **MVP(Must, P0~P5) 구현 완료 + P6(Should 4종: 4분할·미리보기·워크스페이스·텔레메트리) 구현 완료 + 신규 UX(H장 6기능: 상단 아이콘바·사이드바 토글·분할 크기조절·터미널 열기·경로 직접 입력·파일 유형 아이콘) 구현 완료 + 신규 분석·접근성(I장 2기능: 사용량 대시보드·블루라이트 차단 테마) 구현 완료 + 신규 보기·실시간·뷰어·브랜딩(J장 7기능: 박스 선택·패널 실시간 갱신·보기 5종·AGT-Finder 브랜딩·미리보기 2단 뷰어·미리보기 폭 조절·즐겨찾기 별칭) 구현 완료 + 신규 되돌리기·휴지통·유형별 비중(K장 3기능: feat-K1 되돌리기 Ctrl+Z·feat-K2 휴지통 관리 화면·feat-K3 유형별 비중) 구현 완료(P6 잔여 "되돌리기·휴지통 관리 화면" 해소) + 신규 그리드 이미지 썸네일(L장 1기능: feat-L1 그리드 이미지 썸네일 자체 생성) 구현 완료(P6 잔여 "그리드 썸네일 이미지 자체 생성" 해소) + F5/F6 복사·이동 단축키 제거. → §K·§L 완료 시점(2026-06-07) 기준 개발 잔여(🔜)는 0이었다.**
>
> **2026-06-08 §M 외부연계·원격 3기능(M1 외부 D&D 복사·M2 클립보드 외부연계·M3 FTP/SFTP, 에픽12 US-12.1~12.5) 구현 완료·QA PASS ✅ → 개발 잔여(🔜) 0(M1·M2 Should·M3 Could).** **경과(은폐 금지): 2026-06-08 기획 편입(🔜 미착수) → 설계(ADR-007)·MP0~MP5 구현 → 통합 QA PASS([qa-integration-M](./reviews/qa-integration-M.md))·최종 추적성 PASS([final-traceability-M](./reviews/final-traceability-M.md)) → 🔜→✅ 동기화(2026-06-08 doc-sync). 직전 동기화의 "미착수 🔜, 개발 잔여 §M 3건" 단언은 본 구현 완료로 정정됨.** 상세: PRD §M·features §M·user-stories 에픽12·flows F14~F16·아래 §3-M(MP0~MP5)·traceability §1-M. **코드 정합·verify 충족 ✅ / 실 동작(외부 앱 실드롭·드래그 고스트·탐색기 양방향 복사·이동(Move) 실 왕복·실 SFTP/FTP/FTPS 핸드셰이크·호스트키 모달·실 DPAPI 암복호·실 전송 진행률/취소/충돌·.part rename·실 타임아웃/끊김 세션격리·평문 FTP 경고)은 헤드리스로 미증명 → 런타임 스모크 권장 🟡**(§K·§L 표기 양식 동일·✅ 위장 아님).
>
> **2026-06-08 §N 즐겨찾기 UX 2기능(N1 즐겨찾기 경로 워터마크·N2 즐겨찾기 드래그 정렬, 에픽13 US-13.1/13.2) 구현 완료·QA PASS ✅ → §N 구현 완료로 개발 잔여(🔜) 0 — 남은 것은 P7 릴리스 실측·§M·§N 런타임 스모크(🟡)이다.** **경과(은폐 금지·🔜→✅ 경과 보존): 2026-06-08 기획 편입(🔜 미착수) → 설계([architecture-review-N](./reviews/architecture-review-N.md) PASS 조건부·키 `Alt+Shift+↑/↓` 확정)·구현 → 통합 QA PASS([qa-integration-N](./reviews/qa-integration-N.md): 블로커·높음 0·verify 핵심 302/0(domain 49·store 107·p5 52·persistence 94)+contrast 0실패·typecheck/lint/build 0·신규 채널 0·렌더러 전용) → 이번 doc-sync로 §N 전 문서 🔜→✅ 동기화. 직전 동기화의 "현재 개발 잔여(🔜) = §N 2건" 단언은 본 구현 완료로 정정됨.** **§N 한 줄 요약: N1=`domain/rules/favoriteWatermark.ts`(정확일치·J7 별칭 우선·basename 폴백 순수함수)+`ui/panel/FavoriteWatermark.tsx`+`Panel.tsx`(z-index 뒤 배경·`pointer-events:none`·`aria-hidden`)+`ui/theme/palette.ts`/`tokens.ts`(4테마 반투명도 토큰), N2=`ui/sidebar/useFavoriteReorder.ts`+`sidebarSlice.reorderFavorite`+`Sidebar.tsx`(드래그 정렬·`Alt+Shift+↑/↓` 키보드 대체수단). 신규 IPC 채널 0·신규 npm 의존성 0·렌더러 전용·verify 302 통과.** N1·N2는 기존 즐겨찾기(C4)·J7 별칭(US-9.7·`SidebarSnapshot.favoriteLabels`)의 비파괴 확장(N1=별칭 텍스트 워터마크·없으면 basename, N2=`SidebarSnapshot.favorites` 배열 순서 영속). 상세: PRD §N·features §N·user-stories 에픽13·flows F17~F18·traceability §1-N(US-13.1/13.2 ✅). **코드 정합·verify 충족 ✅ / 실 GUI 동작(N1: 실 배경 워터마크 렌더·z-index·4테마 반투명도·긴이름 ellipsis·2/4분할 격리·빈폴더 비중첩 / N2: 실 마우스 드래그 DropLine·강조·`Alt+Shift+↑/↓` 키 이동(한국어 IME `Alt+Shift` 언어전환 OS 점유 가능성 낮음 주의)·ARIA 발화·재시작 후 순서 유지)은 헤드리스로 미증명 → 런타임 스모크 권장 🟡**(§K·§L·§M 표기 양식 동일·✅ 위장 아님).
> 자동 verify 하니스 누계 **약 1015 pass / 0 fail**(직전 731 + **§M 구현 검증: 신규 7종 213 pass**: `verify:eslint-remote` 29(remote/ 밖 tls·ssh2·ssh2-sftp-client·basic-ftp·기존8 차단·remote/ 안 allow·renderer 금지 유지)·`verify:clipboard-hdrop` 33(DROPFILES 방어적 파싱·copy/cut 왕복·DropEffect=Move·외부 합성 CF_HDROP·손상 폴백)·`verify:dnd` 29(원격 prefix ESECURITY·미존재 ENOENT·빈 paths EINVAL·아이콘 fallback·wc destroyed 가드)·`verify:credentials` 17(safeStorage 라운드트립·평문 0·미가용 시 EUNSUPPORTED 거부·평문 폴백 금지)·`verify:remote` 23(TOFU·세션 격리·FileEntryDTO·EAUTH 전파·traversal·operationId+op:done)·`verify:remote-trust` 35(파일명 sanitize·.part 원자 rename·Zip Slip 차단·hostkey/errno 분류·비밀 0)·`verify:remote-route` 47(local/remote 라우팅 upload/download/copy/move/unsupported·remoteLocation 파싱); 그 직전 731 = 698 + **§L 구현 검증: 신규 `verify:thumbnail` 33**(비율 보존 단일축 resize·폴백 매트릭스[미지원/빈/대용량 30MB 상한]·LRU 256 evict·실패 비캐싱·세마포어 4·헤드리스 nativeImage 스텁 주입); 그 직전 698 = 627 + **§K 구현 검증: `verify:store` 76→99(+23 — undoSlice 스택 cap 50·판별유니온·trashSlice)·`verify:scan` 28→39(+11 — categorize 7카테고리·byCategory 1패스 집계)·신규 `verify:recyclebin` 37(recycleBin PowerShell Shell COM·$Recycle.Bin 화이트리스트·Move-Item 폴백·throw 0·헤드리스 주입)**; 그 직전 627 = 570 + P7 헤드리스 검증 `verify:perf` 25(windowing computeWindow 불변식·1만 항목 가상 스크롤 DOM 후보 수십개·200ms 스로틀·검색 필터)·`verify:fmatrix` 32(롱패스·정션 순환·UNC/매핑 판정·ENOENT/ENOTDIR throw 0); 추가로 **WCAG 대비 게이트 `verify:contrast`** 4종 팔레트(LIGHT/DARK/BLUELIGHT) 주요 토큰쌍 AA 전수 통과(실패 0·경고 0). 그 직전 570은 J2 매핑 네트워크 드라이브 `GetDriveType` 연동으로 `verify:watch` 55→77(DriveTypeService PowerShell CIM `Win32_LogicalDisk DriveType=4` 조회·원자 Set 교체·throttle·재진입 가드·빈집합 폴백·헤드리스 주입, `paths.isNetworkDriveRoot` 캐시 조회·`isLikelyRemotePath=isUncPath||isNetworkDriveRoot`); 직전 누계엔 `verify:store` 55→76·`verify:watch` 23→55(WatchService non-recursive·디바운스·격리·경로 교체 + UNC eager 폴링·reactive fs.watch error 폴백·4s readdir diff·stat 승계·pollBusy·>20k 비활성·stop 정리, panelsSlice softRefresh/capturePreserve/_applyPreserve·navigate resetSelection·pendingScrollRestore) 포함), typecheck/lint 0, 빌드 성공(메인 번들 + 대시보드·미리보기 코드강조/마크다운 **별도 lazy 청크 분리**),
> NSIS 인스톨러 + 앱 아이콘 생성 완료. 기획→설계→구현 추적성 검증 **PASS(Must 18/18 = 100%)**,
> 통합 QA **PASS**(P6·신규 UX·I장·J장 블로커/높음/보통 0건, 결함 0; 잔여 성능 실측·네이티브 statfs·대용량 스캔 성능·워처/뷰어 네이티브 동작은 런타임 스모크 권장).
> **추가(2026-06-07):** Should `shell:open-with`(연결 프로그램으로 열기) 구현 완료 ✅, **P4 산출물 컨텍스트 메뉴(우클릭)가 P6 시점에 뒤늦게 구현되어 드리프트 해소**(직전까지 P4가 ✅였으나 `ui/contextmenu/`·속성→show-properties 호출 UI는 미구현이었음 — 아래 §0.5 주석 참조), **신규 UX 3기능(H장: feat-H1/H2/H3 · 에픽7 US-7.1~7.3)이 구현·QA PASS**, 그리고 **신규 UX 추가 3기능(H장: feat-H4/H5/H6 · 에픽7 US-7.4~7.6, PRD §6 Should)이 구현·QA PASS** 됨(아래 "신규 UX(H장)" 표 참조). **이때 P1에서 "전 채널 동결" 후 P2/P4 단계의 시스템 아이콘 캐시로 예약·미구현이던 `shell:icon` 채널이 feat-H6로 정식 구현(호출부 추가)되어 예약→구현 드리프트가 해소**됐고, **터미널 열기용 신규 채널 `shell:open-terminal`이 추가**됐다(동결 예외 — 아래 신규 채널 주석).
> **추가(2026-06-07, I장):** **신규 분석·접근성 2기능(I장: feat-I1 디렉토리 사용량 대시보드 · feat-I2 블루라이트 차단 테마 · 에픽8 US-8.1/8.2, PRD §6 Should)이 구현·QA PASS** 됨(아래 "신규 분석·접근성(I장)" 표 참조). **I1은 신규 채널 `analyze:scan:*` 5종을 추가**(P1 동결 이후 Should 신기능 신규 채널 — `preview:read`·`shell:open-terminal` 선례와 동일 규약, 아래 신규 채널 주석), 디스크 요약은 기존 `DriveDTO.totalBytes/freeBytes`+`diskSpace()` 재사용(backend 신규 0). **차트 라이브러리 recharts 3.8.1(MIT) 신규 도입** — `DashboardModalBody` React.lazy로 **831kB 별도 청크 분리**(메인 번들 비대화 없음). I2는 신규 채널 0(테마 토큰 1종 추가).
> **추가(2026-06-07, J장):** **신규 보기·실시간·뷰어·브랜딩 7기능(J장: feat-J1~J7 · 에픽9 US-9.1~9.7)이 구현·QA PASS** 됨(아래 "신규 보기·실시간·뷰어·브랜딩(J장)" 표 참조). **J2 실시간 갱신은 신규 채널 `fs:watch:*` 4종을 추가**(P1 동결 이후 — `preview:read`·`shell:open-terminal`·`analyze:scan:*` 선례와 동일 규약, 아래 신규 채널 주석). **J4 브랜딩으로 제품명을 `AGT-Finder`(appId `com.agtfinder.app`)로 전환** — `package.json`·`electron-builder.yml`·`index.html`·`mainWindow.ts`·`main/index.ts`·`paths.ts` 일괄(코드네임 "Explorer"는 개발용 유지). **J5 미리보기 2단 뷰어로 신규 의존성 3종 도입: highlight.js 11.x(BSD-3)·marked 18.x(MIT)·dompurify 3.x(MPL-2.0)** — 코드강조/마크다운 렌더러는 **lazy 청크 분리**(recharts 선례 준수, 마크다운은 DOMPurify 새니타이즈). **F5/F6 복사·이동 단축키 제거(사용자 결정 2026-06-07)** — `keybindings`·`commandBus`·`fileOps`(copyToOtherPanel/moveToOtherPanel 제거·panelPaths 보존)·`ShortcutHelp`에서 제거, P4 산출물·DoD·traceability 정정(아래 P4 정정 주석).
> **추가(2026-06-07, J2 보류분 구현 완료):** **J2 US-9.2가 🟡→✅로 격상**. 직전 동기화에서 잔여였던 ① **선택/스크롤 보존**과 ② **네트워크 드라이브 폴링 폴백** 2건을 구현·QA PASS. ①은 `panelsSlice`(`softRefresh`·`capturePreserve`·`_applyPreserve`·navigate 3종 `resetSelection`·`pendingScrollRestore` 1회성)·`selectionSlice.setSelection`·`watchBridge`(onEvent→`softRefresh`)·`FileListView`(1회성 스크롤 복원)로 **완전 구현 ✅**(정렬/필터 패널 anchor는 computeVisible 기준, navigate 시 잔존선택 0). ②는 `WatchService`(UNC eager 폴링 + reactive `fs.watch` error 폴백·4s readdir diff·stat 승계·pollBusy 재진입 가드·>20k 항목 비활성·stop 정리)·`paths.ts`(`isUncPath`·`isLikelyRemotePath`)로 **UNC ✅**. 계약/채널 변경 0(폴링은 backend 내부·투명, P1 동결 무관).
> **추가(2026-06-07, J2 매핑 네트워크 드라이브 `GetDriveType` 연동 완료):** **직전 🟡 부분 한계였던 매핑 네트워크 드라이브(`X:\`)가 ✅로 격상**. 신규 `src/main/os/driveType.ts`(`DriveTypeService`)가 PowerShell CIM `Win32_LogicalDisk -Filter 'DriveType=4'`(=`DRIVE_REMOTE`)을 `execFile`(셸 미경유·고정 상수 스크립트·`^[A-Z]:` 화이트리스트 파싱·`windowsHide`·timeout)로 조회해 **네트워크 드라이브 문자 집합을 캐시**(원자 Set 교체·30s throttle·재진입 가드·실패/비-Windows 빈집합 폴백·헤드리스 `queryFn`/`setNetworkDriveLetters` 주입)한다. `paths.ts#isNetworkDriveRoot`가 동기 캐시 조회로 실구현되고 `isLikelyRemotePath=isUncPath||isNetworkDriveRoot`가 매핑 드라이브를 eager 폴링 대상에 포함한다. `WatchService.start`가 미지 고정 드라이브에서 lazy refresh trigger, `main/index.ts` 부팅 시 non-blocking refresh. **계약/채널/신규 의존성 0**(PowerShell 시스템 내장·backend 내부, P1 동결 무관). **잔여 한계(정직 표기): `subst`·일부 클라우드 드라이브(`DriveType≠4`)는 미포함 → reactive 폴백 유지.** 런타임 매핑 변경 시 첫 진입은 reactive, 차순부터 eager(점진 정확성). `verify:watch` 55→77.
> **추가(2026-06-07, K장 되돌리기·휴지통 관리·유형별 비중 — 구현 완료 ✅):** **신규 3기능(K장: feat-K1 되돌리기 `Ctrl+Z` 다단계 undo · feat-K2 휴지통 관리 화면 · feat-K3 파일 유형별 비중 · 에픽10 US-10.1~10.3, PRD §6 Should)이 구현·QA PASS** 됨(아래 "신규 되돌리기·휴지통·유형별 비중(K장)" 표 참조). **이로써 직전까지 §0.5 "P6 미구현 잔여 🔜"에 남아 있던 "되돌리기(Ctrl+Z)"·"휴지통 관리 화면"이 K1·K2로 구현되어 P6 잔여→완료로 해소**됐고(아래 P6 잔여 표는 "그리드/썸네일 보기"만 남음 — 단 보기 자체는 J3 5종으로 사실상 구현·이미지 썸네일 자체 생성만 잔여), **I1의 "(가능하면) 유형별 비중" 선택항이 K3로 정식 구현**됐다(I1 표·features §I1 "선택항 미구현" 표기를 정식화로 정정). **K2는 신규 채널 `trash:*` 3종(`trash:list`/`trash:restore`/`trash:empty`)을 추가**(P1 동결 이후 Should 신기능 신규 채널 — `preview:read`·`shell:open-terminal`·`analyze:scan:*`·`fs:watch:*` 선례와 동일 invoke·guard/zod/Result 규약·sender 검증·화이트리스트 재검증·confirmed 게이트, 아래 신규 채널 주석). K1·K3은 신규 IPC 채널 0(K1=renderer undo 스택+역연산이 기존 `op:*`·`fsApi`·`trashApi` 재사용, K3=기존 `analyze:scan:*`·`scanEngine` byCategory 1패스 확장). 신규 파일: `app/stores/undoSlice.ts`·`app/usecases/undo.ts`·`src/main/os/recycleBin.ts`·`src/main/ipc/trash.handlers.ts`·`src/main/operations/categorize.ts`·`ui/trash/TrashDialog.tsx`·`ui/dashboard/charts/CategoryBar.tsx`·`ScanResult.byCategory` 비파괴 확장. 신규 검증 `verify:recyclebin` 37, `verify:store` +23(99)·`verify:scan` +11(39). **정직 한계(은폐 금지): ① copy-undo는 보수적 — undo 시 복사 사본을 휴지통으로 보내되, 사본 경로에 동명 충돌이 있으면 새 사본을 만들지 않고 중단·안내(완전 역연산 한계). ② 영구삭제(`Shift+Delete`)는 undo 스택에 push하지 않음(되돌릴 수 없음·토스트 안내). ③ 휴지통 Shell COM(`recycleBin.ts` PowerShell)·undo 역연산의 실제 네이티브 동작은 런타임 스모크 권장(코드 정합·`verify:recyclebin` 37·`verify:store` 99·`verify:scan` 39 충족).** Ctrl+Z는 global(입력/편집 컨텍스트에서는 미발화).
> **추가(2026-06-07, L장 그리드 이미지 썸네일 — 구현 완료 ✅):** **신규 1기능(L장: feat-L1 그리드 보기 이미지 썸네일 자체 생성 · 에픽11 US-11.1, PRD §6 Should)이 구현·QA PASS** 됨(아래 "신규 그리드 이미지 썸네일(L장)" 표 참조). **이로써 직전까지 §0.5 "P6 미구현 잔여 🔜"·"P6 잔여 표"에 남아 있던 마지막 항목 "그리드/썸네일 보기 — 이미지 썸네일 자체 생성"이 feat-L1로 구현되어 P6 잔여→완료로 해소**됐다(P6 잔여 표는 이제 비었다). **이로써 §L 완료 시점(2026-06-07) 기준 개발 잔여(🔜)는 0이었다 — 남은 것은 P7 릴리스 실측·실제 코드서명·NSIS 실측·런타임 스모크(🟡)뿐이었다(P7 전체는 여전히 🟡, L1로 안 바뀜). 단, 2026-06-08 §M(외부 연계) 3건 기획 편입으로 현재 개발 잔여는 🔜 0이 아니라 §M 3건(설계·구현 대기) — 위 머리말 §M 줄 참조.** **L1은 신규 채널 `preview:thumbnail`(invoke) 1종을 추가**(P1 동결 이후 Should 신기능 신규 채널 — `preview:read`·`shell:open-terminal`·`analyze:scan:*`·`fs:watch:*`·`trash:*` 선례와 동일 invoke·guard/zod/Result 규약·sender 검증·guardPath·size 버킷 화이트리스트). 신규 파일: `src/main/os/thumbnail.ts`(nativeImage `createFromPath`→**비율 보존 resize**(종횡비로 긴 변=size 단일 축 지정)→`toDataURL`·30MB 상한·LRU 256·실패 비캐싱·세마포어 4·헤드리스 주입)·`src/renderer/infra/icon/thumbnailCache.ts`(iconCache 동형·negCache·in-flight 디듀프·useSyncExternalStore)·`src/renderer/domain/image.ts`(`isThumbnailableExt`·DPR 버킷 `thumbSizeFor`)·`FileListView.tsx ThumbnailIcon`(그리드 icons-* 한정·이미지=`<img objectFit:contain>`·미지원/null/대용량=OSIcon 폴백·details/list는 OS 아이콘 유지). 가드: `guard.ts#zThumbnailReq`·`THUMB_SIZE_BUCKETS [32,48,64,96,128]`. 보안: dataUrl만(CSP `img-src data:`)·size 버킷 화이트리스트(DoS 차단)·guardPath·렌더러 직접 파일 접근 없음(ADR-005·H6 동일 모델). **신규 의존성 0**(Electron 내장 nativeImage). 신규 검증 `verify:thumbnail` 33(비율 보존 단일축 resize·폴백 매트릭스[미지원/빈/대용량]·LRU evict·실패 비캐싱·세마포어). **정직 한계(은폐 금지): nativeImage 실 디코드·GUI 그리드 렌더(`<img>` 교체·가시 셀 지연·1만 항목 비차단)는 런타임 스모크 권장(코드 정합·typecheck/lint 0·`verify:thumbnail` 33 충족).**
> **추가(2026-06-08, M장 외부 연계·원격 — 구현 완료·QA PASS ✅ / 실 동작 런타임 스모크 🟡):** **신규 3기능(M장: M1 외부 D&D 복사·M2 클립보드 CF_HDROP 외부연계·M3 FTP/SFTP 원격 · 에픽12 US-12.1~12.5, PRD §6 Should M1·M2 / Could M3)이 MP0~MP5로 구현·통합 QA PASS** 됨(아래 §3-M 표 참조). **경과(은폐 금지): 2026-06-08 기획 편입(🔜 미착수) → ADR-007 설계 → MP0~MP5 구현 → 통합 QA PASS([qa-integration-M](./reviews/qa-integration-M.md): 블로커·높음 0·낮음 2(문서/관측)·verify 신규 213/0·회귀 0·경계면 교차 정합·보안 SR5~SR8 충족)·최종 추적성 PASS([final-traceability-M](./reviews/final-traceability-M.md): 수용기준 100% 추적·매핑 17파일 실재·PM 5결정 일관) → 이번 doc-sync로 §M 전 문서 🔜→✅ 동기화.** **신규 채널: `dnd:start-drag`(M1)·파일 `clipboard:write/read/has-files`(M2)·`remote:*`(M3, connect/disconnect/cred:*/profile:*/list/stat/mkdir/rename/delete/download/upload + 푸시 host-key/session-error)** — P1 동결 이후 추가(기존 6선례 동일 규약·전송 진행률/충돌/완료는 신규 채널 없이 기존 `op:*` 재사용). **신규 npm 의존성: `ssh2-sftp-client`·`basic-ftp` 2종(M3만·`src/main/remote/`에서만 import, ESLint 화이트리스트로 경계 강제). M1·M2는 신규 의존성 0**(Electron 내장 startDrag·clipboard buffer·safeStorage). 신규 파일(실재 확인): `os/dragdrop.ts`·`os/shellClipboard.ts`·`os/credentials.ts`(safeStorage/DPAPI)·`remote/{RemoteService,SftpAdapter,FtpAdapter,RemoteSessionManager,remoteTransfer,remoteErrors,remotePath}.ts`·`persistence/RemoteProfileStore.ts`·`ipc/{dnd,clipboard,remote}.handlers.ts`·`app/usecases/{externalDrag,clipboardExternal,remote}.ts`·`app/stores/remoteSlice.ts`·`domain/rules/transferRoute.ts`·`ui/remote/{RemoteDialog,HostKeyModal}.tsx`. **보안(필수 수용기준 충족): 자격증명 평문 0(safeStorage 암호문만·미가용 시 EUNSUPPORTED 거부·평문 폴백 금지·비밀 DTO/로그/Error 배제)·호스트키 TOFU·원격 traversal/Zip Slip 차단·.part 원자 rename·평문 FTP 경고·네트워크 import remote/ 한정(ESLint).** 신규 검증 7종 213/0(`verify:eslint-remote` 29·`verify:clipboard-hdrop` 33·`verify:dnd` 29·`verify:credentials` 17·`verify:remote` 23·`verify:remote-trust` 35·`verify:remote-route` 47). **정직 한계(은폐 금지 — ✅ 위장 아님): verify는 헤드리스 불변식만 증명. 실 외부 앱 드롭·드래그 고스트 아이콘(M1)·탐색기 양방향 복사/이동(Move) 실 왕복(M2)·실 SFTP/FTP/FTPS 핸드셰이크·호스트키 모달·실 DPAPI 암복호·실 전송 진행률/취소/충돌·.part rename·실 타임아웃/끊김 세션격리·평문 FTP 경고(M3)는 런타임 스모크 권장 🟡.** deferral 정직 표기: resume/체크섬(UQ-M2)·원격↔원격(UQ-M3)·자격증명 Cred Manager UI(UQ-M1)·원격 전용 프로세스(UQ-M4)는 1차 범위 밖(ADR-007 미해결질문·구현 비차단).
> **추가(2026-06-08, N장 즐겨찾기 UX 향상 — 구현 완료·QA PASS ✅ / 실 GUI 동작 런타임 스모크 🟡):** **신규 2기능(N장: N1 즐겨찾기 경로 워터마크·N2 즐겨찾기 드래그 정렬 · 에픽13 US-13.1/13.2, PRD §6 Should)이 구현·통합 QA PASS** 됨(상태 정합만·소규모로 별도 §3-N Phase 표는 만들지 않음). **경과(은폐 금지·🔜→✅ 경과 보존): 2026-06-08 기획 편입(🔜 미착수) → 설계([architecture-review-N](./reviews/architecture-review-N.md) PASS 조건부·[높음-1] 키보드 대체수단을 1차안 `Alt+↑/↓`(전역 `alt+arrowup`=nav.up 충돌)에서 **`Alt+Shift+↑/↓`(전역 미배정·충돌 0)**로 확정 정정)·기획 검증([planning-review-N](./reviews/planning-review-N.md) PASS) → 구현 → 통합 QA PASS([qa-integration-N](./reviews/qa-integration-N.md): 블로커·높음 0·낮음 2(개선 제안·비차단)·핵심 verify 302/0(domain 49·store 107·p5 52·persistence 94)+contrast 실패 0·typecheck/lint/build 0·회귀 C4/J7/J1/세션복원 0) → 이번 doc-sync로 §N 전 문서 🔜→✅ 동기화. 직전 동기화의 "현재 개발 잔여(🔜) = §N 2건" 단언은 본 구현 완료로 정정됨.** **신규 IPC 채널 0 · 신규 npm 의존성 0 · 신규 ADR 0 · 렌더러 전용(Main FS/IPC 미사용·기존 C4 즐겨찾기·J7 별칭·`SidebarSnapshot.favorites` 배열 순서 재사용).** 신규 파일(실재 확인): N1 `domain/rules/favoriteWatermark.ts`(정확일치 판정·J7 별칭 우선·basename 폴백 순수함수)·`ui/panel/FavoriteWatermark.tsx`(목록 뒤 z-index 0·`pointer-events:none`·`aria-hidden`·`userSelect:none`·테마 반투명·긴 이름 ellipsis)·`ui/panel/Panel.tsx`(`position:relative` 배경 레이어 마운트)·`ui/theme/palette.ts`/`tokens.ts`(`--c-watermark-opacity`/`--c-watermark-color` 4테마 토큰)·N2 `ui/sidebar/useFavoriteReorder.ts`(경량 드래그 훅+외부 pub/sub·파일 dragState와 분리)·`app/stores/sidebarSlice.ts#reorderFavorite(from,to)`(배열 splice 재배열·add/remove/label 불변)·`ui/sidebar/Sidebar.tsx`(드래그 핸들·DropLine 인디케이터·드래그 항목 강조·`Alt+Shift+↑/↓` 로컬 키 핸들러·row tabindex·role=option·aria-posinset/setsize/grabbed). **키보드 대체수단 `Alt+Shift+↑/↓`(사이드바 즐겨찾기 포커스 한정·전역 미배정·충돌 0)** — `domain/keybindings`에 `alt+shift+arrow*` 미등록 확인(`alt+arrowup`=nav.up만), 로컬 `onKeyDown`은 `altKey&&shiftKey&&!ctrl&&!meta` 가드. **영속: 순서 데이터 = 기존 `SidebarSnapshot.favorites` 배열 순서 자체(별도 order 필드 0)·`session.ts` `[...s.favorites]` 직렬화·`coerceSidebar`·`SESSION_SCHEMA_VERSION` 무변경(persistence verify 94 통과)·main/preload/shared 0변경.** 신규 검증: `verify:domain`(N1 워터마크 10케이스 포함·49/0)·`verify:store`(N2 reorder·영속 11케이스 포함·107/0)·`verify:contrast`(4팔레트 워터마크 변경 후에도 본문 대비 게이트 유지·실패 0)·`verify:persistence`(94/0). **정직 한계(은폐 금지 — ✅ 위장 아님): verify는 헤드리스 순수 로직·불변식만 증명. 실 GUI 동작은 런타임 스모크 권장 🟡 — N1: 실 배경 워터마크 렌더·z-index 비중첩·4테마 반투명도 적정성·긴 이름 ellipsis·2/4분할 패널별 독립·빈 폴더 안내문 비중첩·`9vw` 폰트 2/4분할 과대 여부([낮음-2]) / N2: 실 마우스 드래그(DropLine·0.4 투명 강조)·`Alt+Shift+↑/↓` 키 이동·포커스 추종·스크린리더 ARIA 발화·재시작 후 순서 유지, 그리고 **한국어 IME `Alt+Shift` 언어전환과의 OS 점유 가능성([낮음-1]·가능성 낮음)**.** ([architecture-review-N](./reviews/architecture-review-N.md)·§K·§L·§M 양식 동일.)
> **추가(2026-06-09, §M 결함 수정 2건 + ref.md 아이디어 기반 비계획 구현 1건 — doc-sync):** docs/temp/ref.md 항목 3건을 처리했다. 코드 사실로 확인됨(typecheck 0·lint 0·build 성공·verify 회귀 0).
> - **[결함 수정·드리프트 정정] §M M3 원격 디렉토리 더블클릭 ENOENT**: 원격(SFTP/FTP) 연결 후 디렉토리 더블클릭 시 `ENOENT: opendir 'D\mnt'`로 진입 실패. 원인 = `remote:list` 결과 `entries.path`가 POSIX 절대경로(`/mnt`)로 패널에 주입돼 `isRemotePath()`=false → 로컬 `fs:list`로 오라우팅, 또한 `parentOf/breadcrumbs`가 원격 URI를 win32 경로로 오해. 수정 = `app/usecases/remote.ts#listRemoteDir`(각 항목 path를 `makeRemotePath(protocol,host,path)`로 원격 URI 재구성 후 주입)·`domain/paths/index.ts`(`parentOf`·`baseName`·`breadcrumbs`·`normalizeDisplay`가 `remoteLocation` 규칙에 위임해 원격 URI 인식). **드리프트(은폐 금지·✅위장 정정): 직전 doc-sync(2026-06-08)는 §M을 "코드 정합 ✅ / 실 동작 🟡"로 표기했으나, 실제로는 원격 디렉토리 진입(더블클릭)이 코드 레벨에서 깨져 있었다 → "코드 정합 ✅"가 부분적으로 거짓이었음(런타임 스모크가 잡았어야 할 결함). 본 수정으로 코드 정합 회복, 실 SFTP/FTP 런타임 스모크는 여전히 🟡.** 신규 스코프 아님(기존 §M M3 결함 수정).
> - **[결함 수정·드리프트 정정] §M M1 외부 드래그 "여기에 드롭" 오버레이 잔존**: OLE 드래그앤드롭 시 "여기에 드롭" 오버레이가 드롭/드래그 종료 후에도 남고 빈 곳 클릭 전까지 사라지지 않음. 원인 = `useExternalDragSource.onDragStart`가 OS 드래그 인계 후 내부 pointer `dragState`를 종료하지 않아 `pointerup(→endDrag)`이 OS의 포인터 점유로 영영 미발생. 수정 = `ui/dnd/useDrag.ts`(native `dragstart` 시 `endDrag()` 호출 + `onDragEnd` 노출)·`ui/panel/views/FileListView.tsx`(그리드/리스트 행에 `onDragEnd` 연결). **드리프트(✅위장 정정): 직전 표기 "코드 정합 ✅"에도 오버레이 정리가 실제로 깨져 있었음 → 본 수정으로 정정, 실 OLE 런타임 스모크는 여전히 🟡.** 신규 스코프 아님(기존 §M M1 결함 수정).
> - **[정식 편입 완료·§O·에픽14·US-14.1·F19, PRD §6 Should — 구현 완료(코드)·실 GUI 런타임 스모크 🟡] 파일/폴더 상단 고정(pin)**: docs/temp/ref.md "아이디어"(파일을 상단에 고정) 기반 구현. 컨텍스트 메뉴 "상단 고정/해제"·디렉토리별 고정 항목을 목록 최상단 표시(📌)·세션 영속. (※ 고정 표시 방식은 2026-06-09 후속 동작 변경으로 목록/자세히 보기에서 "스크롤 고정 sticky"로 정련됨 — 아래 "추가(2026-06-09, ref.md 동작 변경 2건)" 단락 참조.) **경과(은폐 금지·경위 보존): 본 기능은 직전 doc-sync(2026-06-09)에서 "비계획 구현(스코프 일탈)"으로 플래그만 됐었으나(기획문서에 항목 없음·구현 사실만 여기 기록) → 사용자가 정식 편입을 결정 → product-planner가 PRD §6 Should·features §O(O1)·user-stories 에픽14(US-14.1)·flows F19로 정식 편입하고 chief-architect가 traceability §1-O 매핑을 추가 → 이번 doc-sync로 §O를 정식 편입 상태(구현 완료·코드 정합·verify 충족 / 실 GUI 런타임 스모크 🟡)로 동기화한다. "스코프 일탈 플래그" 단언은 정식 편입으로 정정됨.** 신규 IPC 채널 0·신규 npm 의존성 0·신규 ADR 0·렌더러+영속(coerce)만(기존 즐겨찾기 C4·별칭 J7 per-위치 메타 패턴 재사용). 파일(실재 확인): `domain/rules/sort.ts#applyPins`(순수함수·고정 그룹 최상단·원본 불변)·`app/usecases/selectors.ts#computeVisible`(고정 상단 적용·메모 pinned 키)·`app/stores/sidebarSlice.ts`(`pinnedByDir`·`togglePin`·`isPinned`·`pinnedIn`·hydrate 확장)·`app/usecases/contextMenu.ts`(상단 고정/해제 메뉴·단일 파일·폴더)·`ui/panel/views/FileListView.tsx`(📌 표식·`pinnedHere` 구독)·`shared/dto/index.ts`(`SidebarSnapshot.pinnedByDir?` 비파괴 추가)·`main/persistence/defaults.ts`(`coercePinnedByDir`·`defaultSidebar` 확장)·`app/usecases/session.ts`(buildSessionSnapshot/applySnapshot 영속). 상세: PRD §6·features §O·user-stories 에픽14·flows F19·traceability §1-O·아래 §1 요약표 §O 행. **정직 한계(은폐 금지·✅위장 아님): verify는 헤드리스 순수/불변식만 증명. 실 GUI 동작(우클릭 토글·📌 렌더·최상단 표시·재시작 후 고정 유지)은 미증명 → 런타임 스모크 권장 🟡.**
> - **검증(헤드리스)**: typecheck 0·lint 0·build 성공(기존 dynamic-import 청크 경고만·본 변경 무관). `verify:domain` 49→**60**(+11: applyPins 3 + 원격 parentOf/baseName/breadcrumbs/normalizeDisplay 8)·`verify:store` 107→**121**(+14: pin 액션·hydrate·세션영속)·`verify:persistence` 93(+1fail)→**101/0**(roundtrip 스냅샷에 pinnedByDir 포함·coercePinnedByDir 6케이스)·회귀 0(`verify:p5` 52·operations 75·`verify:remote` 24·`verify:dnd` 29·`verify:remote-route` 47). **자동 verify 하니스 누계 약 1015 → 약 1040 pass / 0 fail**(domain +11·store +14·persistence +8 순증). **정직 한계: verify는 헤드리스 순수/불변식만 증명 — 실 GUI 핀 렌더·실 SFTP/FTP 원격 디렉토리 진입·실 OLE 드래그 오버레이 정리는 미증명 → 런타임 스모크 권장 🟡. ✅ 위장 아님.**
>
> **추가(2026-06-09, ref.md 동작 변경 2건 — 상태 표기·추적성 동기화 / 구현 완료(코드)·실 GUI 런타임 스모크 🟡):** docs/temp/ref.md 신규 동작 변경 2건을 구현·검증 완료(코드 사실 확인: typecheck 0·lint 0·build 성공·verify 회귀 0). product-planner가 기획 4종(PRD/features/user-stories/flows)에, chief-architect가 traceability(§1-O·§1-M·§1-H)에 반영했고, 본 doc-sync로 상태 단일 출처(§0.5·§1 요약표)를 정합한다. **신규 IPC 채널 0·신규 npm 의존성 0·신규 ADR 0·렌더러 계층 한정.**
> - **[동작 변경·§O 정련] 상단 고정 "정렬 최상단" → "스크롤 고정(sticky)"**: §O O1(파일/폴더 상단 고정)의 고정 항목 표시 방식을 변경. 직전엔 정렬 결과의 최상단에 고정 항목을 배치(스크롤 시 함께 위로 사라짐)했으나, 이제 **목록/자세히(list/details) 보기에서는 스크롤해도 목록 상단에 sticky로 고정 표시**한다(`position:sticky` 밴드). **그리드(icons-*) 보기는 종전대로 정렬 상단 유지**(sticky 미적용·보기별 범위 구분). 구현: `ui/panel/views/FileListView.tsx`(sticky 밴드 — `sticky`/`stickyCount`/`stickyBandH`/`renderRow`/`stickyRows`·`position:sticky` 레이어·키보드 이동 시 sticky 밴드 높이만큼 `scrollIntoView` 보정으로 가림 방지). **`domain/rules/sort.ts#applyPins`·`app/usecases/selectors.ts#computeVisible`(정렬·고정 그룹화 순수 로직)은 불변 — sticky는 렌더 계층의 표시 방식 변경일 뿐 데이터/정렬 불변식 무변경.** 신규 스코프 아님(기존 §O O1 동작 정련·PRD §6 Should 등급 무변경). 상세: PRD §6·features §O·user-stories 에픽14·flows F19·traceability §1-O.
> - **[동작 보강·§M M3 / H5 교차] 원격 주소창 경로-only 입력**: 원격(SFTP/FTP) 연결 상태에서 주소창(경로 직접 입력 H5) 편집 시 **호스트 프리픽스(`sftp://host`)는 고정 표시하고 입력은 경로만** 받는다(잘못된 호스트 재입력으로 인한 오라우팅 방지). 구현: `ui/toolbar/PanelToolbar.tsx`(원격일 때 호스트 프리픽스 고정 UI·경로만 편집·`makeRemotePath(protocol,host,path)`로 원격 URI 재결합·전체 URI 직접 입력도 허용)·`app/usecases/navigate.ts`(원격 URI 는 로컬 `fs:validate-path` 우회 후 그대로 navigate→`remote:list` 탐색·오류는 패널 상태로 표면화). 신규 스코프 아님(기존 H5 경로 직접 입력 + §M M3 원격 연계의 교차 보강). 상세: PRD §6·§8·features §H/§M·traceability §1-M·§1-H.
> - **검증(헤드리스·이번 변경)**: 두 변경 모두 **렌더 계층(FileListView sticky 표시·PanelToolbar 입력 UI)** 으로 신규 verify 케이스 없음 — **verify 누계 약 1040 불변·회귀 0**(`verify:domain` 60·`verify:store` 121·`verify:persistence` 101·`verify:perf` 25(windowing 불변)·`verify:remote-route` 47 재실행 PASS). typecheck 0·lint 0·build 성공. **정직 한계(✅ 위장 아님): sticky 밴드 실 렌더(스크롤 고정·목록/자세히 vs 그리드 보기별 차이·키보드 가림 보정)·원격 경로-only 입력 실 GUI 동작은 헤드리스로 미증명 → 런타임 스모크 권장 🟡. 헤드리스 verify(domain 60·store 121·persistence 101·perf 25·remote-route 47)는 순수 로직·불변식만 증명한다.**
>
> **추가(2026-06-09, T3 정렬/필터 프리셋 폐기 — 사용자 결정으로 코드 전면 제거 ✅구현→제거):** 파워기능 M6에서 구현 완료(코드)됐던 **T3 정렬/필터 프리셋(US-19.3·§T·F30)이 2026-06-09 사용자 결정으로 폐기·코드 전면 제거**됐다. **경과(은폐 금지·경과 보존): 2026-06-09 §P~§U 14종 기획 편입(설계만·🔜) → M6에서 T3 구현 완료(코드 ✅) → 2026-06-09 사용자 결정으로 폐기(코드 제거)** → 본 doc-sync로 T3 상태를 "✅ 구현 완료"에서 **"제거(폐기)"** 로 정정한다(✅위장 방지 — 코드에 없으므로 ✅ 유지 금지). **범위 한정: §T 챕터 3기능 중 T3만 폐기. T1(태그)·T2(폴더용량)는 🔜 미착수(M8) 그대로 유지(§T 전체를 지우지 않음).** **삭제된 파일(grep 부재 확인): `domain/rules/filterComposition.ts`·`app/stores/presetsSlice.ts`·`app/usecases/presets.ts`·`ui/preset/PresetBar.tsx`·`ui/preset/PresetManageDialog.tsx`(디렉토리째).** **환원: `selectors.computeVisible`→기존 `filterEntries`(이름 필터)·`FilterState`의 extPatterns/tagColors/diffOnly 제거·dto `FilterPreset`/`PresetSort`/`PresetFilter`/`TagColor`/`SessionSnapshot.filterPresets` 제거·`panelsSlice.applyPresetToPanel`·`uiSlice.presetManageOpen` 제거·**세션 `SESSION_SCHEMA_VERSION` 2→1 환원**(`persistence/defaults.ts` + `domain/session` 미러 동시).** **무손상(유지·건드리지 않음): §O 상단고정(`applyPins`)·P1 폴더비교(`compareDiffOnly`·diffOnlyPairs)·R4 체크섬(`verifyOnCopy`=SETTINGS 스키마·세션 스키마와 무관)·M7 전체.** verify: domain 146·store 201·persistence 112·operations 75·hash 46·queue 47·p5 52·fs 19·scan 39·perf 25 — 전부 0 fail(T3 제거 반영 누계 정정·아래 M6 행 참조). 코드가 진실 출처.
>
> **추가(2026-06-09, M6 파워기능 1차 3종 — P1·R1 구현 완료(코드) ✅ / 실 GUI 🟡 · T3는 폐기·제거):** 파워 기능 마일스톤 **M6**에서 R1 고급 일괄 이름변경 US-17.1·§R·F22 / P1 듀얼 패널 폴더 비교(메타·단일깊이) US-15.1·§P·F20·ADR-009 / ~~T3 정렬/필터 프리셋 US-19.3·§T·F30~~(**폐기 2026-06-09 — 위 단락 참조**)을 구현·통합 검증 완료. **(잔존)P1·R1 2기능은 렌더러+세션영속·신규 IPC 채널 0·신규 npm 의존성 0.** **경과(은폐 금지): 2026-06-09 §P~§U 14종 일괄 기획 편입(설계만·🔜 미착수) → 그중 M6 배정 3종(P1·R1·T3)을 구현 → 통합 검증 PASS → 그 후 T3는 사용자 결정으로 폐기·코드 제거(위 단락). 본 doc-sync로 P1·R1 §1-P1·§1-R1은 "구현 완료(코드)" 유지, T3 §1-T3은 "제거(폐기)"로 정정. 나머지 11종(Q1·R2·R3·R4·S1·S2·T1·T2·U1·U2·U3)은 🔜 미착수 유지(M7~M9).**
> - ~~**T3 정렬/필터 프리셋(§T·US-19.3·F30·ADR-012)**~~: **폐기됨(2026-06-09 사용자 결정·코드 전면 제거).** `domain/rules/filterComposition.ts`·`app/stores/presetsSlice.ts`·`app/usecases/presets.ts`·`ui/preset/PresetBar.tsx`·`ui/preset/PresetManageDialog.tsx`·`FilterPreset` DTO 모두 삭제, `selectors.computeVisible`→기존 `filterEntries` 환원, `SESSION_SCHEMA_VERSION` **2→1 환원**(상단 폐기 단락 상세).
> - **R1 고급 일괄 이름변경(§R·US-17.1·F22)**: `domain/rules/batchRename.ts`(규칙→매핑·충돌검사 순수)·`app/usecases/batchRename.ts`·`ui/rename/BatchRenameDialog.tsx`(실시간 미리보기·충돌경고)·`undoSlice.ts`(`kind:'batchRename'`)·`undo.ts`(역연산·부분복원·순환 2단계)·`keybindings`(`Ctrl+Shift+R`→`file.batchRename`)·`commandBus`·`contextMenu`(다중선택). **신규 채널 0** — 실행=기존 `fs:rename` 반복·K1 undo 묶음 push.
> - **P1 듀얼 패널 폴더 비교(메타·단일깊이)(§P·US-15.1·F20·ADR-009)**: `domain/rules/compare.ts`(`compareEntries` 4상태 분류 left-only/right-only/diff/same·`summarize`·`planMirror`)·`app/stores/compareSlice.ts`·`app/usecases/compare.ts`·`ui/compare/`(`CompareView`·`CompareToolbar`·`useSyncScroll`·`CompareMirrorDialog`)·`shared/dto/index.ts`(`CompareStatus`/`CompareEntry`)·`LayoutHost`(비교모드 전환)·`tabsSlice`(`compareActive`/`clearCompare`)·`iconBarItems`(`compare.toggle`)·`commandBus`·`contextMenu`. 미러는 기존 `op:*` 재사용·**신규 채널 0**. **해시 비교·재귀 비교는 M7 연기**(메타·단일깊이만 M6·정직 표기·`main/hash/compareEngine.ts`·`hash:compare:*` 채널은 미구현 유지).
> - **검증(헤드리스·통합 — T3 폐기 반영 후)**: typecheck 0·lint 0·build 0. `verify:domain` **146**·`verify:store` **201**·`verify:persistence` **112**·`verify:operations` **75**·`verify:hash` **46**·`verify:queue` **47**·`verify:p5` **52**·`verify:fs` **19**·`verify:scan` **39**·`verify:perf` **25** — 전부 0 fail·회귀 0(T3 프리셋 케이스 제거·`filterComposition`/`presetsSlice`/`coerceFilterPresets` verify 삭제분 반영). **정직 한계(✅ 위장 아님): 헤드리스 verify는 순수 로직·store·세션 영속 불변식만 증명. 실 GUI 동작(P1 비교 진입/미러/동기 스크롤·R1 일괄 rename 실 파일 왕복·Ctrl+Z 실복원)은 미증명 → 런타임 스모크 권장 🟡. P1 메타·단일깊이는 M6·해시/재귀는 M7. ~~T3 프리셋은 2026-06-09 사용자 결정으로 폐기·코드 제거(위 폐기 단락) — verify 누계에서 T3분 제거.~~**
>
> **추가(2026-06-09, M7 파워기능 2차 — 공용 인프라 W1·W2 + 4기능 구현 완료(코드)·통합 검증 PASS ✅ / 실 워커·실 GUI 런타임 스모크 🟡):** 파워 기능 마일스톤 **M7**(공용 해시 인프라 W1 + 전송 큐 인프라 W2 + 4기능 R2·R3·R4·P1 해시·재귀 확장)을 구현·통합 검증 완료. **신규 IPC 채널 `hash:*`·`queue:*` 추가·신규 npm 의존성 0(SHA-256=`node:crypto` 내장).** **경과(은폐 금지·🔜→✅): 2026-06-09 §P~§U 14종 일괄 기획 편입 중 M7 배정분(R2·R3·R4·P1 해시 옵션)이 🔜 미착수였고, M6 시점 P1은 "해시/재귀 비교 M7 연기"로 정직 표기돼 있었다 → 본 M7 구현으로 해당 4종을 "🔜 설계 → 구현 완료(코드)"로 동기화. 나머지 7종(Q1·S1·S2·T1·T2·U1·U2·U3)은 🔜 미착수 유지(M8~M9).**
> - **공용 해시 인프라(W1)**: `src/main/hash/`(`hashEngine`·`dupEngine`·`compareEngine`·`verifyEngine`·`fsDeps`·`HashManager`)·`src/main/workers/`(`hashWorker`·`hashProtocol`)·`src/main/ipc/hash.handlers.ts`. **신규 채널 `hash:compare:*`·`hash:dup:*`·`hash:verify:*`·`hash:cancel`**·preload/infra `hashApi`. SHA-256 1차·Worker Threads·취소·진행률. `verify:hash` 46/0.
> - **전송 큐 인프라(W2)**: `src/main/operations/TransferQueue.ts`·`OperationManager` 큐 승격(비파괴·기본 무한동시성=단발 동치)·`src/main/ipc/queue.handlers.ts`. **신규 채널 `queue:*`(list/pause/resume/retry/set-concurrency·`queue:state` 푸시)**·SharedArrayBuffer 2워드(cancel 불변+pause 신규)·`operationsSlice` 확장·`queueBridge`. `verify:queue` 47/0.
> - **R2 중복 파일 찾기(US-17.2·§R·F23)**: `domain/rules/dupGroup.ts`·`dedupSlice`·`usecases/dedup.ts`·`ui/dedup/DuplicatesDialog.tsx`·진입(commandBus·contextMenu·iconBar). `hash:dup:*` 사용·정리는 기존 `op:trash` 재사용(확인 모달·휴지통·undo).
> - **R3 전송 큐 매니저 UI(US-17.3·§R·F24)**: `usecases/queue.ts`·`ui/queue/(QueuePanel·QueueItemRow·QueueConcurrencyControl·queueFormat)`·StatusBar 합산 인디케이터·iconBar.
> - **R4 체크섬 검증(US-17.4·§R·F25·Could)**: `domain/rules/checksumVerdict.ts`·`usecases/checksum.ts`·`SettingsSnapshot.verifyOnCopy`(기본 false·`defaults.ts` coerce)·SettingsDialog 토글·`operationsBridge` op:done 후 `hash:verify` 트리거.
> - **P1 해시/재귀 비교 확장(US-15.1 확장)**: `ComparePairDTO.relPath?`·`compare.ts`(`CompareOptions useHash/recursive`·`fromCompareResult`)·`compareSlice` 해시잡 상태·`usecases/compare`(`hash:compare:*` 구독·옵션 off는 M6 메타 동치)·CompareToolbar/CompareView 토글·진행률·재귀 표시·`main/hash/compareEngine.ts`. M6 메타·단일깊이는 불변(옵션 off 동치).
> - **검증(헤드리스·통합)**: typecheck/lint/build 0. `verify:domain` 168·`verify:store` 207·`verify:persistence` 128·`verify:operations` 75·`verify:hash` **46**·`verify:queue` **47**·`verify:ops` 35·`verify:paste` 13·`verify:scan` 39·`verify:fs` 19·`verify:perf` 25·`verify:p5` 52 — 전부 0 fail·회귀 0. **자동 verify 하니스 누계 약 1090 → 약 1200 pass / 0 fail**(M7 신규 `hash` 46·`queue` 47 + domain/store/persistence M7 케이스 순증). **정직 한계(✅ 위장 아님): 헤드리스 verify는 순수 로직·store·세션 영속·해시/큐 불변식만 증명. 실 동작(해시 워커 잡 실행·큐 스케줄러 일시정지/재개·실 중복 정리·복사후 검증 트리거·해시/재귀 실 GUI 비교)은 미증명 → 런타임 스모크 권장 🟡. R4 비원자 복사 검증 타이밍·원격 큐 일시정지 미배선은 정직 표기.**
>
> **추가(2026-06-10, 비계획 구현 3건 — 구현 완료(코드)·실 GUI/설치 런타임 스모크 🟡 — doc-sync):** 이번 세션 신규 구현 3건을 코드 기준으로 사실 기록한다(typecheck `typecheck:node`+`typecheck:web` 전부 PASS·변경 파일 ESLint 0 error·실 런타임/빌드 스모크 미실행 🟡). **⚠️ 3건 모두 기획문서(PRD/features/user-stories/flows)에 없던 신규 스코프(스코프 일탈)** — doc-sync 규약대로 기획문서에 정식 편입하지 않고 여기(roadmap §0.5 개발현황)에 사실 기록 + traceability 매핑(§1-V1·§1-V2·§1-V3)만 하며, MoSCoW/요구사항 문구는 무변경. **PM/사용자의 정식 편입(PRD §6 Should 추가 등) 결정 대기**(보고서 스코프 일탈 플래그 3건 참조).
> - **[비계획·스코프 일탈·구현 완료(코드)·실 GUI 🟡] V1 사용량 대시보드 스캔결과 표 "동작" 컬럼(📂이동·✨AI)**: 대시보드 상위폴더/상위파일 표에 동작 컬럼 추가 — 폴더=그 폴더로 진입·파일=상위 폴더로 이동 후 대시보드 닫힘(`jumpToScanEntry`)·✨AI=항목 이름/종류 프리필해 `google.com/search?udm=50`(Google AI 모드) 외부 브라우저 열기(`askGoogleAiAboutEntry`). 구현: `usecases/dashboard.ts`·`ui/dashboard/DashboardModalBody.tsx`(TopTable 동작 컬럼). **신규 IPC 채널 `shell:open-external`(요청-응답 invoke·http/https 프로토콜 화이트리스트·ESECURITY/EINVAL 거부·`channels.ts` impl 주석 V1)** — P1 동결 이후 Should 신기능 신규 채널 추가 선례(`preview:read`·`shell:open-terminal`·`analyze:scan:*`·`hash:*`·`queue:*`)와 **동일 규약(동결 위반 아님)**. ADR-005 쉘 보안 모델(`shell:open` 선례) 동형(sender+zod+프로토콜 검증). EVENT_CHANNELS 무변(요청-응답형). 파일: `shared/ipc/channels.ts`(SHELL_OPEN_EXTERNAL)·`contracts.ts`(ShellOpenExternalReq+ChannelMap)·`main/os/shell.ts`(openExternalUrl)·`main/ipc/guard.ts`(zShellOpenExternalReq)·`main/ipc/shell.handlers.ts`·`preload/api.ts`(shell.openExternal)·`renderer/infra/api/index.ts`(shellApi.openExternal).
> - **[비계획·스코프 일탈·구현 완료(코드)·실 GUI 🟡] V2 디렉토리 노출 시 드라이브 레이블 일관화**: 브레드크럼·탭 제목·즐겨찾기의 드라이브 루트 표기를 `C:\` → `볼륨명 (C:)`로 통일(이미 라벨 쓰던 사이드바 트리·내PC목록·대시보드·최근과 합류). 신규 IPC/상태 0 — 기존 `fs:drives` label("볼륨명 (C:)" 형식)을 단일 출처로 재사용. 구현: `app/selectors/driveLabel.ts`(신규 `resolveDriveLabel(path,tree,fallback)`)·`ui/toolbar/PanelToolbar.tsx`(브레드크럼 드라이브 크럼)·`ui/tabbar/TabBar.tsx`(탭 제목)·`ui/sidebar/Sidebar.tsx`(즐겨찾기/최근 행 중앙화). **의도적 제외(실경로 유지): 리터럴 전체 경로(상태바·미리보기·주소창 편집모드).** 신규 채널 0·신규 의존성 0·렌더러 전용.
> - **[비계획·스코프 일탈·구현 완료(코드)·실 설치/우클릭 🟡] V3 Windows 탐색기 "AGT-Finder로 열기" 컨텍스트 메뉴**: 탐색기 폴더/배경/드라이브/파일 우클릭 → AGT-Finder로 열기 → 새 탭. `perMachine:false`(사용자 단위 설치)라 HKCU 등록·관리자 불요. 구현: NSIS `resources/installer.nsh`(신규·UTF-8 BOM·customInstall/customUnInstall 매크로로 HKCU\Software\Classes 의 Directory/Directory\Background/Drive/* 4종 등록·제거)·`electron-builder.yml`(nsis.include). 런타임 배선: `main/os/launchPath.ts`(신규 `extractPathArg`: argv 경로 추출 + `normalizePath` 상위이탈 차단 + 존재 확인)·`main/index.ts`(최초 실행 argv 푸시 + second-instance forward·기존 단일 인스턴스 락 활용)·`usecases/launchOpen.ts`(신규 `initOpenPathBridge`: 폴더/드라이브 그대로·파일이면 상위 폴더로 새 탭)·`ui/App.tsx`(부팅 구독)·`preload/api.ts`(app.onOpenPath)·`renderer/infra/api/index.ts`(subscribeOpenPath). **신규 IPC 푸시 채널 `app:open-path`(Main→Renderer·EVENT_CHANNELS 추가·코드 확인됨)** — 위 V1과 동일 신규 채널 선례·동결 위반 아님. **신규 npm 의존성 0(reg.exe 아님 — NSIS 등록 방식).**
> - **검증(헤드리스)**: `typecheck:node`+`typecheck:web` 전부 PASS·변경 파일 ESLint 0 error. **3건 모두 렌더러+메인 배선 / NSIS 설치 스크립트로 헤드리스 verify 범위 밖** → 신규 verify 케이스 0·**verify 누계 약 1200 불변·회귀 0**(M7 baseline 유지). **정직 한계(✅ 위장 아님): 실 GUI(대시보드 동작 버튼 왕복·드라이브 레이블 GUI 표기) 및 실 설치/우클릭/한글 메뉴 동작은 미검증 → 런타임/설치 스모크 권장 🟡. 실 빌드 스모크 미실행.**
>
> **추가(2026-06-10, 견고성·성능 개선 5건 + robocopy 고속 미러 1건 — 구현 완료(코드)·실 동작 런타임 스모크 🟡 — doc-sync):** 직전 doc-sync(2026-06-10, 비계획 V1~V3) **이후** 추가된 6건(V4~V9 가칭)을 코드 기준으로 사실 기록한다(`npm run typecheck` node+web 전부 PASS·변경 파일 ESLint 0 error·`verify:ops` 35/0 회귀 0·일회성 실 FS 스모크 통과[복사 병렬 80/80·forceRemove 읽기전용/정션]). **V4·V6·V7·V8·V9 5건은 기존 기능(P4 copy/move·trash·delete·A3 드래그)의 견고성/성능 개선 — 스코프·MoSCoW·신규 채널 무변경(기획문서 무수정).** **V5 robocopy 고속 미러만 신규 invoke 채널 `op:robocopy:start` + 신규 스코프(폴더 비교 §P1 미러 확장) — 기획문서에 정식 편입하지 않고 여기 사실 기록 + traceability §1-V4 매핑만 하며, PM/사용자의 정식 편입(PRD §6·features §P·flows F20 추가 등) 결정 대기**(보고서 스코프 일탈 플래그 참조). ※ 코드 인라인 주석은 robocopy 채널을 dev 자체 태그 "V3"으로 적고 있으나(이전 V1~V3 번호와 별개의 per-feature 태그), 본 동기화는 세션 명명 V4~V9를 사용한다.
> - **[기존 P4 개선·스코프/채널 0·구현 완료(코드)·실 동작 🟡] V4 복사 엔진 성능 최적화**: `src/main/operations/engine.ts` 하이브리드 복사(64MB 미만 `fs.copyFile` OS네이티브·이상 청크 스트림 `STREAM_COPY_THRESHOLD`/`copyFileHybrid`)·적응형 병렬(`ParCtx`·`Mutex`·`runPool` — 디렉토리 파일자식 동시 복사·하위폴더 순차로 전역 동시 I/O 상한·충돌 질의 직렬화)·syscall 절감(utimes용 재-stat 제거·`readdir({withFileTypes})`)·runCopy/runMove `concurrency` 옵셔널(기본 1=기존 동치). `src/main/workers/fileOpWorker.ts` 적응형 동시성 휴리스틱(`pickConcurrency` — 소스↔대상 다른 볼륨 4·같으면 1). **신규 채널 0·신규 의존성 0·계약 무변경**(기본값으로 기존 동작 동치). 실 대용량/병렬 복사 처리량은 런타임 스모크 🟡.
> - **[⚠️ 스코프 일탈·신규 채널·§P1 미러 확장·구현 완료(코드)·실 robocopy/GUI 🟡] V5 robocopy 고속 미러**: 폴더 비교 미러를 robocopy 복사 전용으로 가속. **신규 invoke 채널 `op:robocopy:start`**(진행/취소/완료는 기존 `op:*` 재사용·**EVENT_CHANNELS 무변**·invoke형). 파일: `shared/ipc/channels.ts`(OP_ROBOCOPY_START)·`contracts.ts`(OpRobocopyStartReq+ChannelMap)·`main/ipc/guard.ts`(zOpRobocopyStartReq)·`main/os/robocopy.ts`(신규 `runRobocopyCopy` — spawn 인자배열·셸 미경유 ADR-005·`/E /MT` 복사전용·**`/PURGE` 없음**·stdout 파일라인 파싱·종료코드≥8 실패·cancel=kill)·`main/operations/OperationManager.ts`(`startRobocopyMirror` — registerExternalOperation 재사용·win32 가드)·`main/ipc/op.handlers.ts`(핸들러·경로정규화+디렉토리 stat 검증)·`preload/api.ts`(op.robocopyStart)·`renderer/infra/api/index.ts`(opApi.robocopyStart)·`renderer/app/stores/compareSlice.ts`(compareFastMirror 토글)·`renderer/app/usecases/fileOps.ts`(startRobocopyMirror — 복사분 undo 미제공)·`renderer/app/usecases/compare.ts`(applyMirrorConfirmed 분기: 고속+메타모드+sourceDir이면 robocopy·해시모드면 op:* 폴백)·`ui/compare/CompareToolbar.tsx`(⚡고속미러 토글·해시모드 비활성)·`ui/compare/CompareMirrorDialog.tsx`(경고). **동결 위반 아님: 신규 채널 `op:robocopy:start`는 P1 동결 후 신기능 신규채널 선례(`preview:read`·`shell:open-terminal`·`analyze:scan:*`·`fs:watch:*`·`trash:*`·`preview:thumbnail`·`hash:*`·`queue:*`·`shell:open-external`·`app:open-path`)와 동일 규약(sender+zod 검증).** **정직 한계(✅ 위장 아님): 삭제는 항상 휴지통(trash op·undo 보존)·robocopy는 복사측만·`/PURGE` 없음·복사분 undo 미제공. robocopy 실행·stdout 진행률 파싱·실 GUI는 미검증 → 런타임 스모크 권장 🟡.**
> - **[기존 P4 trash UX·스코프/채널 0·구현 완료(코드)·실 동작 🟡] V6 휴지통 실패 메시지 actionable화**: `main/operations/OperationManager.ts#describeTrashFailure` — `shell.trashItem`의 errno 없는 영어 원문("Failed to perform delete operation")을 원인추정 한국어로(사용중/보호·정션·미존재 구분). 신규 채널 0·신규 의존성 0.
> - **[기존 A3 드래그 버그 수정·스코프/채널 0·렌더러 전용·구현 완료(코드)·실 GUI 🟡] V7 다중선택 드래그 이동 수정**: `ui/panel/views/FileListView.tsx` — 선택을 mousedown 즉시 처리하던 것을 분리(`onRowMouseDown`[수정자 없이 이미 다중선택된 항목은 단일붕괴를 mouseup으로 지연]·`onRowClickSelect`[드래그 아니면 단일 정리·드래그면 click 미발화로 다중 보존]). 그리드/목록/자세히 3모드 행 전부 적용(RowProps onMouseDownSelect/onClickSelect). 렌더러 전용·신규 채널 0.
> - **[기존 P4 delete 동작 추가·확인 게이트·채널 0·구현 완료(코드)·실 GUI 🟡] V8 휴지통 실패 → 영구삭제 폴백**: `app/stores/uiSlice.ts`(`ConfirmDeleteState.reason?`·`openConfirmDelete(paths, reason?)`)·`ui/dialogs/ConfirmDialog.tsx`(reason ⚠ 표시)·`app/usecases/operationsBridge.ts`(trash op:done 실패 항목 → openConfirmDelete 폴백 제안·확정 시 기존 `confirmPermanentDelete`→op:start delete). 사용자 결정 게이트·신규 채널 0.
> - **[기존 P4 delete/move 버그 수정·채널 0·구현 완료(코드)·실 FS 스모크 통과·잔여 🟡] V9 EPERM 견고 영구삭제**: `main/operations/engine.ts#forceRemove`/`removeWithRetry` — 읽기전용 chmod 해제·정션/심볼릭은 대상 미재귀(링크만 제거·데이터 손실·EPERM 방지)·rmdir/unlink EPERM·EBUSY chmod후 재시도. runDelete 및 copyEntry move 원본제거에 적용(`fs.rm` 대체). **일회성 실 FS 스모크 통과(읽기전용 트리·정션 대상 보존).** 신규 채널 0.
> - **검증(헤드리스)**: `npm run typecheck`(node+web) 전부 PASS·변경 파일 ESLint 0 error·`verify:ops` 35/0(회귀 0). 추가로 일회성 실 FS 스모크 통과(복사 병렬 80/80·forceRemove 읽기전용/정션). **6건 모두 메인 엔진/렌더러 배선으로 신규 verify 케이스 0·verify 누계 약 1200 불변·회귀 0**(M7 baseline 유지). **정직 한계(✅ 위장 아님): V4 대용량/병렬 복사 처리량·V5 robocopy 실행·stdout 파싱·실 GUI·NSIS 설치는 미실행 → 런타임/설치 스모크 권장 🟡. V9는 일회성 실 FS 스모크는 통과했으나 광범위 EPERM/EBUSY 케이스는 런타임 🟡.**
>
> **추가(2026-06-07, P7 릴리스 안정화 — 헤드리스분 구현 완료 ✅ / 런타임·인증서 잔여 🟡):** 직전 🟡(NSIS·앱 아이콘만)이던 P7에서 **헤드리스로 가능한 부분을 구현·QA PASS**(typecheck/lint 0·build 성공(sourcemap 생성)·verify 누계 627/0·qa-engineer 결함 0). 항목별 정직 구분:
> - **접근성 ✅(코드)**: 공용 `src/renderer/ui/keyboard/useFocusTrap.ts`(첫 포커스·Tab 순환·opener 복귀·Esc 위임), 모달 6종 `role=dialog`·`aria-modal`·Esc 규칙(ConfirmDialog/DashboardModal 기존·ConflictDialog/WorkspaceDialog/SettingsDialog 신규 Esc·ProgressDialog Esc 무동작), `FileListView` 행 `aria-posinset/setsize`, `:focus-visible` 전역+인라인 outline 제거, Shift+F10 컨텍스트 메뉴. **실 스크린리더 ARIA 발화·포커스 육안 확인은 런타임 🟡.**
> - **WCAG AA ✅(검증)**: `scripts/verify-contrast.ts` 4종 팔레트(LIGHT/DARK/BLUELIGHT) 주요 토큰쌍 전수 통과(실패 0), LIGHT text-muted·DARK danger 미세 보정.
> - **성능 측정 하니스 ✅(헤드리스 불변식) / 실측 🟡**: `src/renderer/ui/panel/views/windowing.ts` 순수함수 추출(computeWindow·`startIdx≤endIdx` 클램프)·`scripts/verify-perf.ts`(25 pass)로 windowing 불변식·1만 항목 DOM 후보 수십개·200ms 스로틀·검색 필터 검증. **실측 숫자(첫 렌더 ≤1.5초·진행률/검색 ≤200ms)는 GUI 런타임 측정 필요 → `docs/P7-perf-measurement.md` 절차로 사용자 환경 실측 잔여 🟡.**
> - **F장 QA 매트릭스 ✅(헤드리스분) / 실케이스 🟡**: `scripts/verify-fmatrix.ts`(32 pass — 롱패스·정션 순환·UNC/매핑 판정·ENOENT/ENOTDIR throw 0)·`docs/P7-qa-matrix.md`. **실 네트워크 드라이브·실 symlink·실 ACL deny(EACCES)는 권한/환경 필요 → 런타임 🟡.**
> - **npm audit ✅(점검·판정, 릴리스 차단 아님)**: 9건 전부 major 업그레이드 필요(비파괴 `npm audit fix` 0건). 8건은 빌드 툴체인(배포 산출물 영향 0)·1건은 electron 본체(ADR-005 하드닝으로 완화). **major 업그레이드는 사용자 결정 보류 → 릴리스 차단 아님.** `docs/P7-security-audit.md`.
> - **sourcemap 분리 ✅**: `electron.vite.config.ts`(main/preload `sourcemap:true`·renderer `'hidden'`)·`electron-builder.yml`(`!out/**/*.map` 배포 제외).
> - **코드서명 설정 ✅(준비완료) / 실제 서명 🟡**: `electron-builder.yml`이 env(`CSC_LINK`/`CSC_KEY_PASSWORD`) 구조·`signingHashAlgorithms`를 갖추고 미설정 시 미서명 빌드 성공. **실제 서명은 코드서명 인증서(.pfx) 필요 → 인증서/사용자 환경 잔여 🟡.**
> **P7 전체는 아직 🟡(릴리스 실측·실제 서명·NSIS 설치/실행/제거 실측 미완) — ✅ 위장 아님.** 신규 검증: `verify:perf`·`verify:fmatrix`·`verify:contrast`. 신규 문서: `docs/P7-security-audit.md`·`docs/P7-perf-measurement.md`·`docs/P7-qa-matrix.md`(+`docs/P7-execution-plan.md`).

| Phase | 상태 | 비고 |
|---|---|---|
| **P0** 골격·빌드 | ✅ 완료 | install/typecheck/build/lint/NSIS 패키징 통과 |
| **P1** IPC 계약·FS 읽기 | ✅ 완료 | 전 채널 타입 동결, `fs:*` 읽기 핸들러 (verify:fs 19/19) |
| **P2** 단일 패널 UI | ✅ 완료 | 가상 스크롤·정렬·주소창·트리·파일 실행(`shell:open`) |
| **P3** 탭·2분할·단축키 | ✅ 완료 | KeyBindingRegistry, 활성 패널 포커스 |
| **P4** 파일작업·진행률·충돌·D&D | ✅ 완료 | Worker Threads·휴지통·충돌해결 (BUG-001 수정 완료) |
| **P5** 검색·세션복원·설정·테마·상태바 | ✅ 완료 | **MVP(Must 18/18) 완성** |
| **P6** (Should) 4분할·미리보기·워크스페이스·텔레메트리 | ✅ 완료 | 4종 구현(verify:p6 26/26) **+ 연결 프로그램으로 열기(`shell:open-with`) 구현 완료(verify:open-with 12/12)**. **Should 잔여 중 되돌리기 Ctrl+Z·휴지통 관리 화면은 §K(feat-K1/K2)로, 그리드 썸네일 이미지 자체 생성은 §L(feat-L1)로 구현 완료 ✅ — 아래 §K·§L 표. P6 Should 잔여 0(그리드 보기 5종 J3 ✅ + 이미지 썸네일 자체 생성 L1 ✅)** |
| **P7** 안정화·성능실측·접근성·패키징 | 🟡 부분 | **헤드리스분 ✅**: 접근성(`useFocusTrap`·모달 6종 role/aria/Esc·행 aria·focus-visible·Shift+F10)·WCAG AA(`verify:contrast` 4팔레트 전수)·성능 측정 하니스(`windowing.ts`·`verify:perf` 25)·F장 매트릭스(`verify:fmatrix` 32)·npm audit 점검(릴리스 차단 아님)·sourcemap 분리·코드서명 설정(준비완료)·NSIS 패키징·앱 아이콘. **런타임 잔여 🟡**: 성능 3종 실측·실제 코드서명(.pfx)·NSIS 설치/실행/제거 실측·F장 실케이스(실 네트워크/symlink/ACL)·실 스크린리더 |
| **릴리스/도구**(G장, 기획 정식 편입) | ✅ 완료 | 앱 아이콘(G1·US-6.1) · 원클릭 빌드 `build-installer.ps1`(G2·US-6.2) · 가상 스크롤 결함 수정(G3·US-5.6 보강) — PRD §6·§7·§12, features §G, user-stories 에픽6 |
| **신규 UX**(H장, 기획 정식 편입, Should) | ✅ 완료 | 상단 전역 아이콘바(H1·US-7.1) · 사이드바 온오프 토글(H2·US-7.2, `Ctrl+B`) · 분할 크기조절(H3·US-7.3) · **우클릭 터미널 열기(H4·US-7.4, 신규 채널 `shell:open-terminal`) · 디렉토리 경로 직접 입력(H5·US-7.5) · 파일 유형별 OS 아이콘(H6·US-7.6, 예약 채널 `shell:icon` 정식 구현)** — PRD §6 Should·§8 `Ctrl+B`, features §H, user-stories 에픽7. **US-7.2/7.3 실제 마우스 드래그·US-7.4 `wt.exe`·US-7.6 `app.getFileIcon` 실제 네이티브 실행은 런타임 의존 → 런타임 스모크 권장(코드 정합 충족)** |
| **신규 분석·접근성**(I장, 기획 정식 편입, Should) | ✅ 완료 | 디렉토리 사용량 대시보드(I1·US-8.1, **신규 채널 `analyze:scan:*` 5종**·recharts 도넛/막대+표·진행률·취소·비차단·자동 팝업 토글) · 블루라이트 차단 테마(I2·US-8.2, #FBF0D9 크림 배경 4번째 테마) — PRD §6 Should·§10 R4/R5, features §I, user-stories 에픽8. **WCAG: 블루라이트 본문 11.04:1·muted 5.25:1 등 AA 통과. 네이티브 실제 statfs·대용량 스캔 성능 실측은 런타임 스모크 권장(코드 정합·verify:scan 28 충족). 파일 유형별 비중 인사이트는 "(가능하면)" 선택항으로 미구현** |
| **신규 보기·실시간·뷰어·브랜딩**(J장, 기획 정식 편입, Should + J4 M릴리스) | ✅ 완료 | 박스 선택(J1·US-9.1, 러버밴드·자동스크롤·`boxSelect.ts`) · 패널 실시간 갱신(J2·US-9.2, **신규 채널 `fs:watch:*` 4종**·non-recursive·디바운스·격리·**선택/스크롤 보존 ✅·UNC + 매핑 네트워크 드라이브 폴링 폴백 ✅(`GetDriveType` 연동: `os/driveType.ts` PowerShell CIM `DriveType=4`·`paths.isNetworkDriveRoot`)**; **`subst`·일부 클라우드 드라이브(`DriveType≠4`)만 미포함 한계 — reactive 폴백 유지**) · 보기 5종(J3·US-9.3, `ViewMode` icons-large/medium/small/list/details·그리드 OSIcon) · **브랜딩 AGT-Finder(J4·US-9.4, M릴리스, appId `com.agtfinder.app`)** · 미리보기 2단 뷰어(J5·US-9.5, InfoCard+코드강조/마크다운, **신규 의존성 highlight.js/marked/dompurify·lazy 청크**) · 미리보기 폭 조절(J6·US-9.6, SplitDivider 재사용·`ui.previewWidth`) · 즐겨찾기 별칭(J7·US-9.7, `SidebarSnapshot.favoriteLabels`·인라인 편집) — PRD §6 Should·§8(F5/F6 제거), features §J, user-stories 에픽9. **F5/F6 복사·이동 단축키 제거(사용자 결정)·P4 정정. 워처/뷰어 네이티브 동작·박스선택 드래그는 런타임 스모크 권장(코드 정합·verify:watch 77·verify:store 76 충족).** |
| **신규 되돌리기·휴지통·유형별 비중**(K장, 기획 정식 편입, Should) | ✅ 완료 | 되돌리기 Ctrl+Z 다단계 undo(K1·US-10.1, `undoSlice.ts` cap 50·`undo.ts` 역연산) · 휴지통 관리 화면(K2·US-10.2, **신규 채널 `trash:*` 3종**·`recycleBin.ts` Shell COM·`TrashDialog.tsx`) · 파일 유형별 비중(K3·US-10.3, `categorize.ts`·`scanEngine` byCategory 1패스·`CategoryBar.tsx`) — PRD §6 Should, features §K, user-stories 에픽10. **P6 잔여 "되돌리기·휴지통 관리 화면" 해소. copy-undo 보수적·영구삭제 미push·휴지통 COM·undo 역연산 네이티브 동작은 런타임 스모크 권장(코드 정합·verify:recyclebin 37·verify:store 99·verify:scan 39 충족).** |
| **신규 그리드 이미지 썸네일**(L장, 기획 정식 편입, Should) | ✅ 완료 | 그리드 보기 이미지 썸네일 자체 생성(L1·US-11.1, **신규 채널 `preview:thumbnail`**·`os/thumbnail.ts` nativeImage 비율보존 resize·30MB 상한·LRU 256·세마포어 4·`thumbnailCache.ts`·`domain/image.ts`·`FileListView ThumbnailIcon`) — PRD §6 Should, features §L, user-stories 에픽11. **이미지=내용 썸네일(`<img objectFit:contain>`)·미지원/손상/대용량/null=OSIcon 폴백·details/list는 OS 아이콘 유지·dataUrl(CSP img-src data:)·size 버킷 화이트리스트·렌더러 직접 파일 접근 없음(ADR-005). 신규 의존성 0. P6 잔여 "그리드 썸네일 이미지 자체 생성" 해소 → 개발 잔여(🔜) 0. nativeImage 실 디코드·GUI 그리드 렌더는 런타임 스모크 권장(코드 정합·verify:thumbnail 33 충족).** |
| **신규 즐겨찾기 UX**(N장, 기획 정식 편입, Should) | ✅ 완료 | 즐겨찾기 경로 워터마크(N1·US-13.1, `domain/rules/favoriteWatermark.ts`·`ui/panel/FavoriteWatermark.tsx`·4테마 반투명 토큰) · 즐겨찾기 드래그 정렬(N2·US-13.2, `useFavoriteReorder.ts`·`sidebarSlice.reorderFavorite`·`Alt+Shift+↑/↓` 키보드 대체수단·`SidebarSnapshot.favorites` 순서 영속) — PRD §6 Should·§8, features §N, user-stories 에픽13. **렌더러 전용·신규 IPC 채널 0·신규 의존성 0. N1 실 배경 워터마크 렌더/4테마 반투명도·N2 실 마우스 드래그/키 이동/재시작 후 순서 유지는 런타임 스모크 권장(코드 정합·verify 302 충족, 한국어 IME `Alt+Shift` OS 점유 가능성 낮음).** |
| **파워기능 M6 1차**(§P·§R, 기획 정식 편입, Should) — ~~T3 폐기~~ | ✅ P1·R1 완료(코드)/실 GUI 🟡 · ❌ T3 제거(폐기) | ~~**T3 정렬/필터 프리셋**(US-19.3·§T·F30·ADR-012)~~ — **폐기됨(2026-06-09 사용자 결정·코드 전면 제거: `filterComposition.ts`·`presetsSlice`·`usecases/presets`·`ui/preset/*`·`FilterPreset` DTO 삭제·`computeVisible`→`filterEntries` 환원·`SESSION_SCHEMA_VERSION` **2→1 환원**·§0.5 T3 폐기 단락 참조).** · **R1 고급 일괄 이름변경**(US-17.1·§R·F22, **신규 채널 0**·`domain/rules/batchRename.ts`·`usecases/batchRename`·`ui/rename/BatchRenameDialog`·`undoSlice kind:'batchRename'`·`undo.ts` 역연산·`Ctrl+Shift+R`·기존 `fs:rename` 반복) · **P1 듀얼 패널 폴더 비교(메타·단일깊이)**(US-15.1·§P·F20·ADR-009, **신규 채널 0**·`domain/rules/compare.ts` 4상태·`planMirror`·`compareSlice`·`usecases/compare`·`ui/compare/`[CompareView·CompareToolbar·useSyncScroll·CompareMirrorDialog]·`CompareStatus` DTO·미러=기존 `op:*`·**해시/재귀 비교 M7 연기**) — PRD §6 Should, features §P/§R(§T는 T3 폐기·T1/T2 🔜 M8), user-stories 에픽15/17(에픽19 US-19.3 폐기), flows F20/F22(F30 폐기), traceability §1-P1/§1-R1(§1-T3 폐기). **(잔존 P1·R1) 렌더러+세션영속·신규 IPC 채널 0·신규 의존성 0. verify domain 146·store 201·persistence 112·operations 75·perf 25·p5 52(0 fail·회귀 0·T3 제거 반영). 실 GUI(일괄 rename 실 파일 왕복·Ctrl+Z 실복원·비교 진입/미러/동기 스크롤)는 런타임 스모크 권장 🟡(코드 정합 충족·✅ 위장 아님). **P1 해시/재귀 비교는 M6 시점 연기였으나 아래 "파워기능 M7 2차" 행에서 구현 완료(코드)됨.** M7 추가 4종(R2·R3·R4·P1 해시/재귀) 후 나머지 §P~§U 7종(Q1·S1·S2·T1·T2·U1·U2·U3)은 🔜 미착수(M8~M9·traceability §1 설계 매핑).** |
| **파워기능 M7 2차(공용 인프라 W1·W2 + 4기능)**(§P·§R, 기획 정식 편입, Should/Could) | ✅ 완료(코드)/실 워커·GUI 🟡 | **공용 해시 인프라 W1**(`src/main/hash/`[hashEngine·dupEngine·compareEngine·verifyEngine·fsDeps·HashManager]·`src/main/workers/`[hashWorker·hashProtocol]·`hash.handlers.ts`·**신규 채널 `hash:compare:*`/`hash:dup:*`/`hash:verify:*`/`hash:cancel`**·`hashApi`·SHA-256 node:crypto·Worker Threads·취소·진행률·`verify:hash` 46) · **전송 큐 인프라 W2**(`TransferQueue.ts`·`OperationManager` 큐 승격(비파괴·기본 무한동시성=단발 동치)·`queue.handlers.ts`·**신규 채널 `queue:*`**(list/pause/resume/retry/set-concurrency·`queue:state` 푸시)·SharedArrayBuffer 2워드(cancel+pause)·`operationsSlice`·`queueBridge`·`verify:queue` 47) · **R2 중복 파일 찾기**(US-17.2·§R·F23·Should, `domain/rules/dupGroup.ts`·`dedupSlice`·`usecases/dedup.ts`·`ui/dedup/DuplicatesDialog.tsx`·`main/hash/dupEngine.ts`·`hash:dup:*`·정리=기존 `op:trash` 재사용) · **R3 전송 큐 매니저 UI**(US-17.3·§R·F24·Should, `usecases/queue.ts`·`ui/queue/(QueuePanel·QueueItemRow·QueueConcurrencyControl·queueFormat)`·StatusBar 합산 인디케이터) · **R4 체크섬 검증**(US-17.4·§R·F25·Could, `domain/rules/checksumVerdict.ts`·`usecases/checksum.ts`·`SettingsSnapshot.verifyOnCopy`·SettingsDialog 토글·`operationsBridge` op:done 후 `hash:verify` 트리거) · **P1 해시/재귀 비교 확장**(US-15.1, `ComparePairDTO.relPath?`·`compare.ts` CompareOptions useHash/recursive·`fromCompareResult`·`compareSlice` 해시잡·`usecases/compare` `hash:compare:*` 구독·`main/hash/compareEngine.ts`·옵션 off는 M6 메타 동치) — PRD §6 Should/Could, features §P/§R, user-stories 에픽15/17, flows F20/F23/F24/F25, traceability §1-P1/§1-R2/§1-R3/§1-R4. **신규 IPC 채널 `hash:*`·`queue:*`(동결 예외 — Should 신기능·기존 8선례 동일 규약)·신규 npm 의존성 0(SHA-256=node:crypto 내장). verify domain 168·store 207·persistence 128·operations 75·hash 46·queue 47·ops 35·paste 13·scan 39·fs 19·perf 25·p5 52(전부 0 fail·회귀 0). 실 동작(해시 워커 잡·큐 스케줄러 일시정지/재개·중복 정리·복사후 검증 트리거·해시/재귀 실 GUI 비교)은 런타임 스모크 권장 🟡(코드 정합 충족·✅ 위장 아님·R4 비원자 복사 검증 타이밍·원격 큐 일시정지 미배선 정직 표기). 나머지 §P~§U 7종(Q1·S1·S2·T1·T2·U1·U2·U3)은 🔜 미착수(M8~M9).** |
| **신규 파일/폴더 상단 고정**(O장, 기획 정식 편입, Should) | ✅ 완료 | 파일/폴더 상단 고정 pin(O1·US-14.1·F19, **신규 채널 0**·`domain/rules/sort.ts#applyPins` 순수함수·`app/usecases/selectors.ts#computeVisible`·`sidebarSlice.pinnedByDir`/`togglePin`/`isPinned`/`pinnedIn`·`contextMenu.ts` "상단 고정/해제" 토글·`FileListView.tsx` 📌 표식·`SidebarSnapshot.pinnedByDir?` 비파괴 확장·`coercePinnedByDir` 영속) — PRD §6 Should, features §O, user-stories 에픽14, flows F19, traceability §1-O. **고정 표시 방식(2026-06-09 동작 변경): 목록/자세히(list/details) 보기는 스크롤해도 상단에 sticky 고정 표시(`FileListView.tsx` `position:sticky` 밴드·`stickyCount`/`stickyBandH`/`stickyRows`·키보드 가림 보정)·그리드(icons-*) 보기는 정렬 상단 유지(sticky 미적용·보기별 범위 구분). `applyPins`/`computeVisible` 정렬·고정 그룹화 로직은 불변(sticky는 렌더 계층).** **정식 편입 경위: 직전 doc-sync(2026-06-09) 스코프 일탈 플래그 → 사용자 정식 편입 결정 → 4종 기획 + traceability §1-O 매핑 → 본 동기화로 정식화. 렌더러+세션영속만·신규 의존성 0·기존 C4/J7 per-위치 메타·B1/B2 정렬 인프라 재사용. 고정 항목 최상단(폴더 우선보다 위)·그룹 내부 정렬 순서 유지·디렉토리 단위·세션 영속·다중선택/수동 재정렬 1차 범위 밖. 실 GUI(우클릭 토글·📌 렌더·sticky 고정 표시·재시작 후 유지)는 런타임 스모크 권장 🟡(코드 정합·verify:domain 60/store 121/persistence 101 충족·✅ 위장 아님).** |
| **비계획 3건**(V장 가칭, ⚠️ **기획 미수록·스코프 일탈**) | 구현 완료(코드)/실 GUI·설치 🟡 | **⚠️ PRD/features/user-stories/flows 미수록 신규 스코프 — 기획 정식 편입 아님(PM/사용자 결정 대기).** **V1 대시보드 스캔결과 표 "동작" 컬럼(📂이동·✨AI)**(신규 채널 `shell:open-external`[http/https 화이트리스트·sender+zod+프로토콜 검증·ADR-005 동형]·`usecases/dashboard.ts#jumpToScanEntry`/`askGoogleAiAboutEntry`[udm=50 Google AI 모드]·`ui/dashboard/DashboardModalBody.tsx`) · **V2 드라이브 레이블 일관화**(신규 채널 0·`app/selectors/driveLabel.ts#resolveDriveLabel`·브레드크럼/탭/즐겨찾기를 `fs:drives` label "볼륨명 (C:)" 단일 출처로 통일·리터럴 전체경로는 실경로 유지) · **V3 탐색기 "AGT-Finder로 열기"**(신규 푸시 채널 `app:open-path`[EVENT_CHANNELS 추가]·NSIS `resources/installer.nsh` HKCU 4종 등록·`main/os/launchPath.ts#extractPathArg`[상위이탈 차단]·`usecases/launchOpen.ts#initOpenPathBridge`·perMachine:false·npm 의존성 0). **신규 채널 2종은 P1 동결 후 신기능 선례(preview:read·shell:open-terminal·analyze:scan:*·hash:*·queue:*)와 동일 규약·동결 위반 아님.** traceability §1-V1·§1-V2·§1-V3. **typecheck PASS·ESLint 0·verify 누계 약 1200 불변(신규 케이스 0·헤드리스 범위 밖)·실 GUI/설치/우클릭/한글메뉴 미검증 🟡(✅ 위장 아님).** |
| **견고성·성능 6건**(V장 가칭 V4~V9, V4·V6·V7·V8·V9=**기존 기능 개선·스코프 무변경** / V9 robocopy=⚠️ **기획 미수록·스코프 일탈**) | 구현 완료(코드)/실 동작·robocopy·설치 🟡 | **V4 복사 엔진 성능 최적화**(기존 P4 copy/move 내부·**신규 채널 0**·`engine.ts` 하이브리드 복사[64MB 미만 fs.copyFile·이상 청크 스트림 `STREAM_COPY_THRESHOLD`/`copyFileHybrid`]·적응형 병렬[`ParCtx`·`Mutex`·`runPool`]·utimes 재-stat 제거·`fileOpWorker.ts#pickConcurrency` 볼륨별 4/1·`concurrency` 옵셔널 기본1=기존동치) · **⚠️ V5 robocopy 고속 미러**(**신규 invoke 채널 `op:robocopy:start`**·**스코프 일탈=§P1 미러 확장**·`os/robocopy.ts#runRobocopyCopy`[spawn 인자배열·셸 미경유 ADR-005·`/E /MT` 복사전용·`/PURGE` 없음·종료코드≥8 실패·cancel=kill]·`OperationManager#startRobocopyMirror`·`op.handlers.ts`·`guard.ts#zOpRobocopyStartReq`·`compareSlice.compareFastMirror`·`compare.ts#applyMirrorConfirmed` 분기[고속+메타+sourceDir→robocopy·해시→op:* 폴백]·CompareToolbar ⚡토글·진행/취소/완료=기존 op:* 재사용·EVENT_CHANNELS 무변·삭제는 항상 휴지통·복사분 undo 미제공) · **V6 휴지통 실패 메시지 actionable화**(기존 P4 trash UX·채널 0·`OperationManager#describeTrashFailure` 한국어 원인추정) · **V7 다중선택 드래그 이동 수정**(기존 A3 버그·채널 0·렌더러 전용·`FileListView.tsx` onRowMouseDown/onRowClickSelect 선택을 mousedown→mouseup 분리) · **V8 휴지통 실패→영구삭제 폴백**(기존 P4·채널 0·확인 게이트·`uiSlice ConfirmDeleteState.reason?`·`ConfirmDialog` ⚠·`operationsBridge` trash 실패→openConfirmDelete) · **V9 EPERM 견고 영구삭제**(기존 P4 delete/move 버그·채널 0·`engine.ts#forceRemove`/`removeWithRetry` 읽기전용 chmod·정션 미재귀·EPERM/EBUSY 재시도·`fs.rm` 대체·**실 FS 스모크 통과**). **V5 신규 채널 `op:robocopy:start`는 P1 동결 후 신기능 선례(preview:read·shell:open-terminal·analyze:scan:*·fs:watch:*·trash:*·preview:thumbnail·hash:*·queue:*·shell:open-external·app:open-path)와 동일 규약·동결 위반 아님.** traceability §1-V4(V4~V9 매핑). **`npm run typecheck`(node+web) PASS·변경 파일 ESLint 0·`verify:ops` 35/0·일회성 실 FS 스모크 통과(복사 병렬 80/80·forceRemove 읽기전용/정션)·verify 누계 약 1200 불변(신규 케이스 0)·회귀 0.** **정직 한계(✅ 위장 아님): V4 대용량/병렬 처리량·V5 robocopy 실행/stdout 파싱/실 GUI·NSIS 설치는 미실행 → 런타임/설치 스모크 권장 🟡.** |

**P6(Should) — 구현 완료분 ✅ (구현 파일)**

| 기능 | 구현 상태 / 핵심 파일 |
|---|---|
| 4분할(grid-4) US-1.4 | ✅ `ui/layout/LayoutHost.tsx`(2x2 row-major), `tabsSlice.ts`(`toggleGrid4`·`focusPanelDir`), `usecases/fileOps.ts`(grid-4 `activeIdx^1` 분기), `domain/keybindings/index.ts`(`ctrl+shift+\`→`layout.toggleGrid4`), `commandBus.ts` |
| 미리보기 패널 US-4.3 | ✅ 신규 채널 `preview:read`(`channels.ts`·`contracts.ts`·`dto`(PreviewData)), `main/fs/FileSystemService.ts#readPreview`, `main/ipc/preview.handlers.ts`, `ui/preview/PreviewPanel.tsx`+`renderers/*`, `uiSlice.previewOpen`, `commandBus#preview.toggle`, `Ctrl+P` |
| 워크스페이스 저장/복원 US-5.8 | ✅ `main/persistence/WorkspaceStore.ts`, `main/ipc/workspace.handlers.ts`(save/list/load/delete), `usecases/workspace.ts`, `ui/workspace/WorkspaceDialog.tsx`, `tabsSlice.resetWorkspace`+`session.ts#applySnapshot`(복원 단일화) |
| 텔레메트리 옵트인 결정-D5 | ✅ 신규 채널 `telemetry:get-opt-in`(부팅 재수화), `session.handlers.ts` 핸들러, `settings.ts#loadSettings`, `SettingsDialog` 체크박스, `.eslintrc.cjs` main 오버라이드 `no-restricted-imports`(node:http/https/net/dgram = 전송 전무 정적 가드), src 전역 네트워크 송신 grep 0건 |
| 연결 프로그램으로 열기(`shell:open-with`, features §B6 Should) | ✅ `main/os/shell.ts#openWith`(`execFile rundll32 shell32.dll,OpenAs_RunDLL`), `main/ipc/shell.handlers.ts`(SHELL_OPEN_WITH 핸들러, shell:open 동일 ADR-005 3중 검증), `main/ipc/guard.ts#zShellOpenWithReq`, frontend `usecases/open.ts#openWithEntry` + 컨텍스트 메뉴 "연결 프로그램으로 열기"(파일 전용). 검증 `verify:open-with` 12/12 |
| 컨텍스트 메뉴(우클릭) 인프라 (P4 산출물 — 뒤늦게 구현, 드리프트 해소) | ✅ `ui/contextmenu/ContextMenu.tsx`, `usecases/contextMenu.ts`(buildMenuItems·openRowContextMenu·openEmptyContextMenu), `uiSlice.contextMenu` 상태/액션, `FileListView`(onContextMenu), `App.tsx` 마운트. 항목: 열기·연결프로그램(파일전용)·복사·잘라내기·이름바꾸기·삭제(휴지통)·영구삭제·**속성→`shell:show-properties`**·빈영역(붙여넣기·새폴더·새로고침, My PC는 새로고침만). 우클릭이 키보드/툴바와 동일 commandId 경로로 수렴 |

> **드리프트 해소 주석(은폐 금지)**: P4 DoD "컨텍스트 메뉴 '속성'이 `shell:show-properties` 호출"과 P4 산출물 `ui/contextmenu/`는 직전(P6) 동기화 시점까지 **실제로는 미구현**이었으나 문서상 P4가 ✅로 표기돼 있었다(`shell:show-properties` backend 핸들러는 P4부터 있었으나 이를 호출하는 컨텍스트 메뉴 UI가 없었음). 이번에 컨텍스트 메뉴 인프라가 구현되어 P4 산출물·DoD가 **뒤늦게 충족(드리프트 해소)**됐다.

**P6(Should) — 미구현 잔여 🔜 : 없음(전부 해소)**

| 기능 | 현재 상태 |
|---|---|
| — | **P6 Should 잔여 🔜 0** — 되돌리기·휴지통 관리 화면은 §K, 그리드 썸네일 이미지 자체 생성은 §L로 모두 구현 완료 ✅ (이 표는 **P6 한정**. 별도 신규 챕터 §M 3건은 2026-06-08 구현 완료·QA PASS ✅ — §0.5 머리말 §M 줄·§3-M 참조. **별도 신규 챕터 §N 2건(N1·N2 Should)은 2026-06-08 구현 완료·QA PASS ✅ — §0.5 머리말 §N 줄·§1-N 참조**) |

> **P6 잔여 전부 해소(2026-06-07)**: 직전까지 이 표에 있던 **되돌리기(`Ctrl+Z`)·휴지통 관리 화면**은 §K(feat-K1·feat-K2)로 **구현 완료 ✅**되어 잔여에서 제거됐고(`Ctrl+Z`는 `commandBus`의 `notYet('되돌리기')`가 `performUndo()` 호출로 대체 — 예약→구현), 마지막으로 남아 있던 **그리드/썸네일 보기의 "이미지 썸네일 자체 생성"**도 §L(feat-L1)로 **구현 완료 ✅**됐다(아래 §K·§L 표). 보기 모드 자체는 J3 5종으로 이미 구현됐고, 이미지 내용 썸네일은 `os/thumbnail.ts`(nativeImage)·`preview:thumbnail` 채널·`ThumbnailIcon`로 완성. **이로써 P6 Should 미구현 잔여(🔜)는 0이며, §K·§L 완료 시점(2026-06-07) 기준 개발 잔여 전체가 🔜 0이었다(남은 것은 P7 릴리스 실측·실제 코드서명·런타임 스모크 🟡뿐). 2026-06-08 §M(외부 연계) 3건이 기획 편입(🔜)됐다가 같은 날 MP0~MP5로 구현 완료·QA PASS(✅)됐고, 같은 날 §N(즐겨찾기 UX 향상) 2건(N1·N2 Should)도 기획 편입(🔜)됐다가 **같은 날 구현 완료·QA PASS(✅)되어, §N 완료 시점(2026-06-08) 기준 개발 잔여(🔜)는 0이다 — 남은 것은 P7 릴리스 실측·실제 코드서명·§M·§N 런타임 스모크(🟡)뿐이다.** (경과 보존: 직전 동기화 시점엔 §N 2건이 🔜 미착수였으나 본 구현 완료로 🔜→✅ 정정됨.) §M 상세는 §0.5 머리말 §M 줄·§3-M 참조, §N 상세는 §0.5 머리말 §N 줄·§1-N(traceability) 참조. (P6 잔여 표 자체는 비어 있음 유지 — §M·§N은 P6가 아닌 신규 챕터.)**

**신규 되돌리기·휴지통·유형별 비중(K장, Should) — 구현 완료 ✅ (구현 파일)**: roadmap §0.5 "P6 미구현 잔여 🔜"의 되돌리기·휴지통 관리 화면을 정식 편입·구현하고, I1 "(가능하면) 유형별 비중" 선택항을 K3로 정식화. 2026-06-07 정식 편입(features §K·user-stories 에픽10·PRD §6 Should). 코드 사실로 ✅ 확인됨(typecheck/lint 0·`verify:recyclebin` 37/0·`verify:store` 99/0·`verify:scan` 39/0·빌드 성공·QA PASS).

| 기능 | 구현 상태 / 핵심 파일 |
|---|---|
| 되돌리기(`Ctrl+Z`) 다단계 undo (K1·US-10.1) | ✅ `app/stores/undoSlice.ts`(스택 상한 `UNDO_STACK_CAP=50`·`UndoEntry` 판별유니온 rename/create/move/copy/trash)·`app/usecases/undo.ts`(`performUndo`·역연산: rename↔rename·create→휴지통·move→역move·copy→사본휴지통·trash→`trashApi.restore`·**영구삭제 미push**·역연산 전 충돌 선검증)·`usecases/fileOps.ts`/`operationsBridge.ts`(작업 시 `undoMeta` hook 적재)·`commandBus.ts`(`file.undo`→`performUndo`, `notYet('되돌리기')` 제거). `Ctrl+Z` global(입력/편집 중 미발화). **정직 한계: copy-undo는 보수적(충돌 시 사본 미생성 중단)·영구삭제 되돌릴 수 없음(토스트 안내).** ※ undo 역연산 실제 네이티브 동작은 런타임 스모크 권장 |
| 휴지통 관리 화면 (K2·US-10.2) | ✅ **신규 채널 `trash:list`/`trash:restore`/`trash:empty`(3종)** — `src/main/os/recycleBin.ts`(PowerShell Shell COM·env 주입·`$Recycle.Bin` 화이트리스트·`Move-Item` 폴백·throw 0·헤드리스 주입)·`src/main/ipc/trash.handlers.ts`(sender·zod·화이트리스트 재검증·`confirmed` 게이트)·`ui/trash/TrashDialog.tsx`(목록[이름·원경로·삭제일·크기]·복원·비우기 확인·focus trap[중첩 확인 포함]·Esc)·`uiSlice.trashOpen`+가드·`app/stores/trashSlice.ts`·`usecases/trash.ts`·사이드바/아이콘바 진입. 검증 `verify:recyclebin` 37/0. ※ 휴지통 COM 실제 동작은 런타임 스모크 권장 |
| 파일 유형별 비중 인사이트 (K3·US-10.3) | ✅ `src/main/operations/categorize.ts`(7카테고리 확장자 매핑)·`scanEngine.ts`(byCategory **1패스 집계·추가 I/O 0**)·`shared/dto ScanResult.byCategory`(`CategoryUsage[]` 비파괴 확장)·`ui/dashboard/charts/CategoryBar.tsx`+표(비중%·절대 용량·파일 수·가장 큰 유형). I1의 "(가능하면) 유형별 비중" 선택항을 정식 구현. 검증 `verify:scan` 39/0(byCategory bytes desc 정렬 포함) |

> **K장 신규 IPC 채널(설계 원칙 정합)**: `trash:list/restore/empty`(3종)은 P1 "전 채널 타입 동결" 이후 K장에서 **추가된** 채널이다. P1 동결 원칙은 *기존* MVP 채널 계약을 고정해 병렬화 출발선을 잡으려는 것이며, **Should 신기능에 필요한 신규 채널 추가는 위반이 아니다** — P6 `preview:read`·`telemetry:get-opt-in`, H4 `shell:open-terminal`, I장 `analyze:scan:*`, J장 `fs:watch:*`과 **동일 선례·동일 invoke·guard/zod/Result 규약**(sender 검증·`$Recycle.Bin` 화이트리스트 재검증·`confirmed` 게이트) 준수. 스코프 일탈 아님(product-planner가 PRD §6 Should·features §K2·US-10.2로 정식 편입). **K1 되돌리기·K3 유형별 비중은 신규 IPC 채널 0**(K1=renderer undo 스택+기존 `op:*`/`fsApi`/`trashApi` 역연산, K3=기존 `analyze:scan:*`·`scanEngine` byCategory 1패스 확장·`ScanResult` DTO 비파괴 확장). **신규 의존성 0**(recycleBin은 PowerShell 시스템 내장·`execFile`).

**신규 그리드 이미지 썸네일(L장, Should) — 구현 완료 ✅ (구현 파일)**: roadmap §0.5 "P6 미구현 잔여 🔜"의 **마지막 항목 "그리드 썸네일 이미지 자체 생성"**을 정식 편입·구현. J3(보기 5종 ✅)의 아이콘 그리드에서 이미지 파일만 형식 아이콘 대신 실제 내용 축소 썸네일로 표시(그 외/실패는 H6 OS 아이콘 폴백). 2026-06-07 정식 편입(features §L·user-stories 에픽11·PRD §6 Should). 코드 사실로 ✅ 확인됨(typecheck/lint 0·`verify:thumbnail` 33/0·빌드 성공·QA PASS US-11.1 8/8 결함 0). **이로써 개발 잔여(🔜) 0.**

| 기능 | 구현 상태 / 핵심 파일 |
|---|---|
| 그리드 보기 이미지 썸네일 자체 생성 (L1·US-11.1) | ✅ **신규 채널 `preview:thumbnail`(invoke)** — `src/main/os/thumbnail.ts`(`getThumbnailDataUrl`·nativeImage `createFromPath`→**비율 보존 resize**(`getSize()` 종횡비로 **긴 변=size 단일 축**만 지정)→`toDataURL`·`THUMB_MAX_BYTES` 30MB 상한·LRU 256(`MAX_THUMB_CACHE`)·**실패(null/빈/예외) 비캐싱**·세마포어 4·헤드리스 nativeImage 주입)·`src/main/ipc/preview.handlers.ts`(`handleGuarded`·`guardPath`)·`src/main/ipc/guard.ts`(`zThumbnailReq`·`THUMB_SIZE_BUCKETS [32,48,64,96,128]` 화이트리스트). frontend: `src/renderer/infra/icon/thumbnailCache.ts`(iconCache 동형·negCache·in-flight 디듀프·useSyncExternalStore)·`src/renderer/domain/image.ts`(`isThumbnailableExt`·`thumbSizeFor` DPR 버킷 스냅)·`FileListView.tsx ThumbnailIcon`(그리드 icons-* 한정·이미지=`<img objectFit:contain>` 비율보존 레터박스·미지원/null/대용량=`OSIcon` 폴백·로드 전 OSIcon 자리표시·가시 셀만 요청·details/list는 OS 아이콘 유지). **보안: dataUrl만(CSP `img-src data:`)·size 버킷 화이트리스트(DoS 차단)·guardPath·렌더러 직접 파일 접근 없음(ADR-005·H6 동일 모델). 신규 의존성 0**(Electron 내장 nativeImage). 검증 `verify:thumbnail` 33/0. ※ nativeImage 실 디코드·GUI 그리드 `<img>` 교체·1만 항목 비차단은 런타임 스모크 권장 |

> **L장 신규 IPC 채널(설계 원칙 정합)**: `preview:thumbnail`(invoke 1종)은 P1 "전 채널 타입 동결" 이후 L장에서 **추가된** 채널이다. P1 동결 원칙은 *기존* MVP 채널 계약을 고정해 병렬화 출발선을 잡으려는 것이며, **Should 신기능에 필요한 신규 채널 추가는 위반이 아니다** — P6 `preview:read`, H4 `shell:open-terminal`, I장 `analyze:scan:*`, J장 `fs:watch:*`, K장 `trash:*`과 **동일 선례·동일 invoke·guard/zod/Result 규약**(sender 검증·`guardPath`·size 버킷 화이트리스트) 준수. 스코프 일탈 아님(product-planner가 PRD §6 Should·features §L1·US-11.1로 정식 편입 — J3에서 보류했던 "이미지 썸네일 자체 생성"의 정식 구현). **신규 의존성 0**(nativeImage는 Electron 내장).

**P6 신규 IPC 채널 2종(설계 원칙 정합)**: `preview:read`·`telemetry:get-opt-in`은 P1 "전 채널 타입 동결" 이후 P6에서 **추가된** 채널이다. P1 동결 원칙은 *기존* MVP 채널 계약을 동결해 병렬화 출발선을 고정하려는 것이며, Should 신기능에 필요한 신규 채널 추가는 그 원칙의 위반이 아니다(`channels.ts`에 "신규(P6 Should)"로 명시·동일 guard/zod/Result 규약 준수). 스코프 일탈 아님 — PRD §6 Should·US-4.3/D5에 근거. 

**릴리스/도구 — 기획 정식 편입 완료 ✅ (구 "로드맵 외 추가")**: 아래 3건은 더 이상 로드맵 외 유지보수가 아니라 기획에 **정식 편입**됐다 — features 신설 **§G**, user-stories 신설 **에픽6**, PRD **§6 릴리스 요건·§7·§12**. 코드 사실로 ✅ 확인됨.
> - **앱 아이콘 / 브랜딩** (G1·US-6.1, **M 릴리스 요건**): `resources/icon.ico`·`resources/icon.png`·`scripts/gen-icon.ps1`(겹친 폴더), electron-builder `win.icon` 연결.
> - **원클릭 인스톨러 빌드** (G2·US-6.2, **개발 도구**): 루트 `build-installer.ps1`(의존성→typecheck→build→NSIS 일괄·인스톨러 경로 출력).
> - **가상 스크롤 뷰포트 높이 측정 결함 수정** (G3·US-5.6 보강): `FileListView`를 콜백 ref 기반 ResizeObserver로 전환 + 전역 CSS 리셋(`index.html`) — 기존 Must "가상 스크롤" 품질 결함 수정(신규 스코프 아님).

**신규 UX(H장, Should) — 구현 완료 ✅ (구현 파일)**: 기존 동작(분할 A2·사이드바 C3·단축키 E1)을 더 빠르게 호출·조정하는 UX 확장. 2026-06-07 정식 편입(features §H·user-stories 에픽7·PRD §6 Should). 코드 사실로 ✅ 확인됨.

| 기능 | 구현 상태 / 핵심 파일 |
|---|---|
| 상단 전역 아이콘바 (H1·US-7.1) | ✅ `ui/toolbar/IconBar.tsx`·`iconBarItems.ts`(4그룹 20버튼, 활성조건·`aria-pressed`·툴팁에 단축키 표기, execCommand 수렴), `ui/App.tsx` 마운트. 신규 commandId 4건 `sidebar.toggle`·`theme.toggle`·`view.setMode.list`·`view.setMode.details`(`commandBus.ts`), `settings.ts#toggleThemeMode`·`applyTheme.ts#systemPrefersDark` export |
| 사이드바 온오프 토글 (H2·US-7.2) | ✅ `domain/keybindings/index.ts`(`ctrl+b`→`sidebar.toggle`) + 아이콘바 버튼 → `sidebarSlice.toggleSidebar`(collapsed 상태·세션 영속 기존 재사용). **실제 토글 마우스 클릭/단축키는 런타임 DOM 의존 → 런타임 스모크 권장** |
| 분할 패널 크기조절 (H3·US-7.3) | ✅ `ui/layout/SplitDivider.tsx`·`splitMath.ts`(`ratioFromPoint` 순수함수)·`LayoutHost.tsx`(2분할 flex·4분할 grid 컨테이너 ref 측정·2축 독립), `tabsSlice.setSplitRatio`(클램프 `SPLIT_MIN_RATIO=0.15`~0.85), `Tab.splitRatios`(`domain/entities`)·`shared/dto TabSnapshot.splitRatios?`·`usecases/session.ts` 직렬화·`main/persistence/defaults.ts#coerceSplitRatios` 정규화. **실제 분할선 드래그는 런타임 DOM 의존 → 런타임 스모크 권장** |
| 우클릭 "터미널 열기" (H4·US-7.4) | ✅ **신규 채널 `shell:open-terminal`**(`channels.ts`·`contracts.ts ShellOpenTerminalReq`), `src/main/os/shell.ts#openTerminal`(`wt.exe -d`→`powershell.exe -NoExit` 폴백·`execFile` 인자 배열), `shell.handlers.ts`(sender 검증·zod·guardPath·stat 디렉토리 검증 ADR-005), `guard.ts#zShellOpenTerminalReq`, `usecases/open.ts#openTerminalAt`·`terminalErrorMessage`, `contextMenu.ts`(단일 폴더·빈 영역 항목; 파일·My PC 미표시). **네이티브 `wt.exe` 실제 실행은 런타임 스모크 권장** |
| 디렉토리 경로 직접 입력 (H5·US-7.5) | ✅ `PanelToolbar.tsx` 단일 클릭 편집 진입(`closest('button')` 가드로 브레드크럼 버튼 클릭과 분리), 기존 `Ctrl+L`·더블클릭·`validateAndNavigate` 재사용(신규 채널·단축키 불요) |
| 파일 유형별 OS 아이콘 (H6·US-7.6) | ✅ **예약 채널 `shell:icon` 정식 구현(호출부 추가)** — `src/main/os/icon.ts`(`getFileIconDataUrl`·`cacheKeyFor`·LRU512·실패 비캐싱), `shell.handlers.ts SHELL_ICON`, `iconCache.ts`(`iconKeyFor`·`iconRequestFor`·디듀프·구독), `usecases/icons.ts`, `FileListView.tsx OSIcon`. 확장자 단위 캐시·per-file(exe/lnk 등) path 키·가상 스크롤 `iconRef` 지연. **네이티브 `app.getFileIcon` 실제 실행은 런타임 스모크 권장** |

> **신규 UX 설계 원칙 정합(드리프트 아님)**: H1~H3·H5의 신규 commandId·`splitRatios`는 **renderer 내부 commandId / 세션 스냅샷 DTO(`TabSnapshot`) 필드 확장**으로 P1 "전 채널 타입 동결"(IPC 채널 계약 동결)과 **무관**하다 — `Ctrl+B`·단일 클릭 편집도 신규 IPC 채널이 아니라 기존 슬라이스/`validateAndNavigate` 호출일 뿐이다. **H4의 신규 채널 `shell:open-terminal`은 P1 동결 이후 추가된 채널이나, 동결 원칙은 *기존* MVP 채널 계약을 고정해 병렬화 출발선을 잡으려는 것이고 Should 신기능에 필요한 신규 채널 추가는 위반이 아니다** — `preview:read`·`telemetry:get-opt-in`(P6)과 동일 선례·동일 guard/zod/Result 규약 준수. **H6의 `shell:icon`은 P1에 계약만 동결되고 호출부가 없어 "P2/P4 시스템 아이콘 캐시"로 예약·미구현이던 채널을 이번에 정식 구현(호출부 추가)** 한 것으로, 계약·보안·캐시 설계를 변경하지 않고 따른다(예약→구현, 드리프트 해소). 스코프 일탈 아님(product-planner가 PRD §6 Should·§8·features §H·US-7.4~7.6로 정식 편입).

**신규 분석·접근성(I장, Should) — 구현 완료 ✅ (구현 파일)**: 디스크 사용 현황 시각화(읽기 전용 스캔)와 저청색광 접근성 테마. 2026-06-07 정식 편입(features §I·user-stories 에픽8·PRD §6 Should·§10 R4/R5). 코드 사실로 ✅ 확인됨(typecheck/lint 0·`verify:scan` 28/0·QA PASS).

| 기능 | 구현 상태 / 핵심 파일 |
|---|---|
| 디렉토리 사용량 대시보드 (I1·US-8.1) | ✅ **신규 채널 `analyze:scan:*` 5종**(`channels.ts`·`contracts.ts`·`dto ScanResult/ScanEntry`) — `src/main/operations/scanEngine.ts`(재귀 집계·Top10 힙·순환 realpath Set·skipped·취소·truncated)·`src/main/workers/scanWorker.ts`·`src/main/workers/scanProtocol.ts`(SharedArrayBuffer 취소)·`src/main/operations/ScanManager.ts`(scanId·200ms 스로틀·푸시)·`src/main/ipc/analyze.handlers.ts`(guardPath·디렉토리 검증). 디스크 요약은 기존 `DriveDTO.totalBytes/freeBytes`+`diskSpace()` 재사용(backend 신규 0). frontend: `app/stores/analyzeSlice.ts`·`app/usecases/dashboard.ts`(scanId 상관 브리지)·`ui/dashboard/DashboardModal.tsx`(+`DashboardModalBody.tsx` **React.lazy**)·recharts 도넛/막대+표 병행·`iconBarItems`(dashboard.open)·`App.tsx` 자동 팝업·`uiSlice.showDashboardOnStartup`(기본 `true`)·SettingsDialog 토글. **recharts 3.8.1(MIT) 설치, 831kB 별도 lazy 청크 분리.** ※ 파일 유형별 비중 인사이트는 "(가능하면)" 선택항으로 미구현(정직 표기). 네이티브 statfs·대용량 스캔 성능은 런타임 스모크 권장 |
| 블루라이트 차단 테마 (I2·US-8.2) | ✅ `ui/theme/palette.ts BLUELIGHT_PALETTE`(13토큰·`#FBF0D9`)·`applyTheme.ts ResolvedTheme` 확장(bluelight **독립 resolved**·light 폴백 아님)·`ThemeMode` 4종(+`'bluelight'`)·`main/persistence/defaults.ts THEME_MODES`/`guard.ts zThemeMode` 화이트리스트·SettingsDialog 테마 4종 선택. `toggleThemeMode`는 light↔dark 유지(블루라이트는 설정에서 선택). 신규 채널 0. **WCAG: 본문 11.04:1·muted 5.25:1 등 AA 통과** |

**I장 신규 IPC 채널(설계 원칙 정합)**: `analyze:scan:start/progress/done/error/cancel`(5종)은 P1 "전 채널 타입 동결" 이후 I장에서 **추가된** 채널이다. P1 동결 원칙은 *기존* MVP 채널 계약을 고정해 병렬화 출발선을 잡으려는 것이며, **Should 신기능에 필요한 신규 채널 추가는 위반이 아니다** — P6 `preview:read`·`telemetry:get-opt-in`, H4 `shell:open-terminal`과 **동일 선례·동일 guard/zod/Result 규약** 준수(`channels.ts`에 "신규(I장)"로 명시). 스코프 일탈 아님(product-planner가 PRD §6 Should·§10·features §I·US-8.1로 정식 편입). I2는 신규 채널 0(테마 토큰 추가). **recharts(MIT)는 차트 라이브러리 신규 의존성**이나 PRD §10 R5에서 라이선스·번들 영향을 사전 검토했고, lazy 청크 분리로 메인 번들 비대화를 피했다(드리프트 아님 — 기획 근거 있음).

**신규 보기·실시간·뷰어·브랜딩(J장, Should) — 구현 완료 ✅ (구현 파일)**: 기존 보기(B1)·미리보기(D3)·분할 크기조절(H3)·즐겨찾기(C4)를 Windows 탐색기 수준으로 확장하고 제품명을 AGT-Finder로 전환. 2026-06-07 정식 편입(features §J·user-stories 에픽9·PRD §6). 코드 사실로 ✅ 확인됨(typecheck/lint 0·`verify:watch` 77/0·`verify:store` 76/0·QA PASS·빌드 성공; J2 보류 2건(선택/스크롤 보존·UNC 폴링 폴백) 구현 완료 + 매핑 네트워크 드라이브 `GetDriveType` 연동 완료 ✅, `subst`·일부 클라우드(`DriveType≠4`)만 미포함 한계).

| 기능 | 구현 상태 / 핵심 파일 |
|---|---|
| 박스 선택(러버밴드) (J1·US-9.1) | ✅ `domain/rules/boxSelect.ts`(사각형 교차 판정 순수함수)·`app/stores/selectionSlice.ts#boxSelect`(교체/누적/범위 모드)·`FileListView`(러버밴드 오버레이·경계 자동 스크롤·가상 스크롤 마운트 항목 포함). ※ 실제 마우스 드래그·자동 스크롤은 런타임 DOM 의존 → 런타임 스모크 권장 |
| 패널 실시간 갱신(워처) (J2·US-9.2) | ✅ **완료** — **신규 채널 `fs:watch:start/event/stop/error`** · `src/main/fs/WatchService.ts`(non-recursive `fs.watch`·디바운스 병합·권한/네트워크/미지원 격리 throw 0 + **UNC + 매핑 네트워크 드라이브 eager 폴링·reactive fs.watch error 폴백·4s readdir diff·stat 승계·pollBusy 재진입 가드·>20k 항목 비활성·stop 정리·미지 고정 드라이브 lazy refresh trigger**)·`src/main/os/driveType.ts`(`DriveTypeService` — PowerShell CIM `Win32_LogicalDisk DriveType=4` execFile 조회·네트워크 드라이브 문자 캐시 원자 Set 교체·throttle·재진입 가드·빈집합 폴백·헤드리스 주입)·`src/main/fs/paths.ts`(`isUncPath`·`isNetworkDriveRoot`(캐시 조회)·`isLikelyRemotePath`)·`watch.handlers.ts`·`main/index.ts`(부팅 non-blocking refresh)·`app/usecases/watchBridge.ts`(watchId 상관·경로 교체 시 이전 감시 해제·리소스 정리·onEvent→`softRefresh`). **정렬/필터/검색 유지·디바운스·대량 폴백·권한/삭제 예외 정리·경로 교체 누수 없음 충족 ✅.** **선택/스크롤 보존 ✅ 완전 구현** — `panelsSlice`(`softRefresh`·`capturePreserve`·`_applyPreserve`·`pendingScrollRestore` 1회성·navigate 3종 `resetSelection`)·`selectionSlice.setSelection`·`FileListView`(1회성 스크롤 복원). **네트워크 폴백: UNC ✅ + 매핑 네트워크 드라이브(`X:\`) ✅(`GetDriveType` 연동)**. **잔여 한계(정직 표기): `subst`·일부 클라우드 드라이브(`DriveType≠4`)는 미포함 → reactive 폴백 유지. 런타임 매핑 변경 시 첫 진입 reactive→차순 eager(점진 정확성).** ※ 네이티브 워처 실제 이벤트는 런타임 스모크 권장 |
| Windows 표준 보기 5종 (J3·US-9.3) | ✅ `shared/dto ViewMode`(`'icons-large'|'icons-medium'|'icons-small'|'list'|'details'`)·`FileListView` 아이콘 그리드(H6 `OSIcon`/`shell:icon` 재사용·가상 스크롤 정합)·`ui/toolbar/PanelToolbar.tsx` 보기 드롭다운·`panelsSlice` 패널별 기억·`defaults.ts VIEW_MODES` 화이트리스트. 박스 선택(J1)은 그리드 2차원 교차에서도 동작 |
| **브랜딩 AGT-Finder (J4·US-9.4, M for 릴리스)** | ✅ `package.json`(name `agt-finder`·description)·`electron-builder.yml`(`appId: com.agtfinder.app`·`productName: AGT-Finder`)·`src/renderer/index.html`(`<title>AGT-Finder`)·`src/main/windows/mainWindow.ts`·`src/main/index.ts`·`src/main/persistence/paths.ts`(userData 경로). 코드네임 "Explorer"는 내부(ExplorerApi 타입·코드주석)만 유지 — 사용자 노출 표면 0 잔존. ※ 실제 exe/인스톨러 파일명·바로가기는 패키징 산출물 런타임 확인 권장 |
| 미리보기 2단 뷰어(정보+확장 뷰어) (J5·US-9.5) | ✅ `ui/preview/PreviewPanel.tsx`(상단 `PreviewInfoCard`+하단 뷰어 2단)·`renderers/CodePreview.tsx`(**highlight.js** 언어 감지 강조)·`renderers/MarkdownPreview.tsx`(**marked**+**DOMPurify** 새니타이즈)·기존 Image/Text/Meta/Unsupported 폴백·`FileSystemService.readPreview`(lang/isMarkdown 판별). 기존 `preview:read`·`Ctrl+P` 재사용. **신규 의존성 highlight.js(BSD-3)·marked(MIT)·dompurify(MPL-2.0), lazy 청크 분리·CSP eval 0·원격 로드 차단** |
| 미리보기 패널 폭 조절 (J6·US-9.6) | ✅ H3 `SplitDivider.tsx`·`splitMath.ts#ratioFromPoint` 재사용 · `uiSlice.previewWidth`(클램프·기본 폭 복귀)·`usecases/session.ts` 직렬화·세션 영속(미리보기 토글 off→on 후 폭 유지). 우측 부착 가로 축 |
| 즐겨찾기 별칭(표시 이름) 변경 (J7·US-9.7) | ✅ `shared/dto SidebarSnapshot.favoriteLabels`·`sidebarSlice`(별칭 설정/초기화·basename 폴백)·`ui/sidebar/Sidebar.tsx` 인라인 편집(우클릭/`F2`·Enter 확정·Esc 취소)·`session.ts` 영속. 별칭은 표시 전용(경로 불변) |

**J장 신규 IPC 채널(설계 원칙 정합)**: `fs:watch:start/event/stop/error`(4종)은 P1 "전 채널 타입 동결" 이후 J장에서 **추가된** 채널이다. P1 동결 원칙은 *기존* MVP 채널 계약을 고정해 병렬화 출발선을 잡으려는 것이며, **Should 신기능에 필요한 신규 채널 추가는 위반이 아니다** — P6 `preview:read`·`telemetry:get-opt-in`, H4 `shell:open-terminal`, I장 `analyze:scan:*`과 **동일 선례·동일 guard/zod/Result 규약** 준수(`channels.ts`에 "신규(J장 J2)"로 명시·`guardPath`·sender 검증). 스코프 일탈 아님(product-planner가 PRD §6 Should·features §J·US-9.2로 정식 편입). J1/J3/J5(렌더러)·J6/J7은 신규 IPC 채널이 아니라 **renderer slice·DTO 확장**(ViewMode·previewWidth·favoriteLabels). **신규 의존성 highlight.js(BSD-3)·marked(MIT)·dompurify(MPL-2.0)** 는 미리보기 코드강조/마크다운용으로 lazy 청크 분리·마크다운 DOMPurify 새니타이즈로 도입(features §J5·flows §F11 근거 — 드리프트 아님). **J2 매핑 네트워크 드라이브 `GetDriveType` 연동(`os/driveType.ts`)은 PowerShell(Windows 시스템 내장)을 `execFile`로 호출하므로 신규 npm 의존성·신규 IPC 채널 0이며, 폴링은 backend 내부·투명(계약 변경 0).**

> **F5/F6 복사·이동 단축키 제거(2026-06-07 사용자 결정 — 스코프 축소, 은폐 금지)**: "활성 패널 선택 항목을 다른 패널로 복사=`F5`/이동=`F6`" 단축키를 **제거**했다(사유: 듀얼 패널 파일관리자 외 일반 사용자에게 일반적이지 않은 단축키). 코드 반영: `domain/keybindings`(F5/F6 매핑 제거)·`commandBus`(panel.copyToOther/moveToOther 명령 제거)·`usecases/fileOps.ts`(`copyToOtherPanel`/`moveToOtherPanel` 함수 제거·`panelPaths` 헬퍼는 D&D용으로 보존)·`ShortcutHelp`(목록에서 제거). **패널 간 복사/이동은 D&D(A3)·클립보드(`Ctrl+C/X/V`)가 단일 경로로 유지**된다. `verify:operations`는 F5/F6 테스트 제거로 80→75. **아래 §3 P4 단계 표·DoD·QA 포인트·추적성에서 F5/F6 항목을 "제거됨(2026-06-07 사용자 요청)"으로 정정함**(traceability §1·§2·§4도 doc-synchronizer가 정정).

---

## 1. 단계 요약 표

| Phase | 상태 | 한 줄 목표 | 핵심 산출물 | 의존 | 주 담당 | MoSCoW |
|---|---|---|---|---|---|---|
| **P0** | ✅ | 프로젝트 골격·3엔트리 빌드·빈 창 실행 | electron-vite/builder 설정, main/preload/renderer 부트, TS/React/Zustand 초기화 | — | devops | Must(기반) |
| **P1** | ✅ | IPC 계약 기반 + FS 읽기 계층(목록 스트리밍) | `shared/ipc`, preload `window.api`, Result/FileOpError, `fs:*` 핸들러, FileSystemService, infra/api 브리지 | P0 | backend(+frontend 계약합의) | Must |
| **P2** | ✅ | 단일 패널 파일목록 UI(가상 스크롤·정렬·주소창·탐색·트리·파일 실행/열기) | FileListView(가상화), PanelHeader/주소표시줄, nav(뒤/앞/위), Sidebar 트리, panels/selection 슬라이스, open usecase + `shell:open` 핸들러 | P1 | frontend(+backend `shell:open`) | Must |
| **P3** | ✅ | 탭 + 2분할 레이아웃 + 패널 포커스 + 단축키 디스패치 | TabBar, LayoutHost(2분할), tabsSlice, KeyBindingRegistry/Dispatcher/CommandBus | P2 | frontend | Must |
| **P4** | ✅ | 파일 작업(CRUD·휴지통) + 진행률/취소 + 충돌해결 + 패널 간 D&D | `op:*`/`fs:mkdir·create-file·rename` 핸들러, Worker, OperationManager, ProgressDialog/ConflictDialog, dnd/ ~~F5/F6~~(2026-06-07 제거) | P3 (계약은 P1) | backend+frontend | Must |
| **P5** | ✅ | 검색/필터 + 설정 화면(숨김·확장자 토글) + 즐겨찾기/최근 + 자동 세션복원 + 다크모드 + 상태바 | SearchBar/필터, `ui/settings/`(설정·숨김/확장자 토글), sidebarSlice(즐겨찾기·최근), session/settings 영속화, theme/, StatusBar | **P5a: P2** / **P5b: P3·P4** | backend+frontend | Must → **MVP 완성** |
| **P6** | ✅ | (Should) 4분할·미리보기·워크스페이스 저장·텔레메트리(옵트인)·연결프로그램으로 열기 | LayoutHost(grid-4), PreviewPanel/렌더러, `preview:read`, `workspace:*`, `telemetry:get/set-opt-in`, `shell:open-with`(+컨텍스트 메뉴 인프라) | P5 | backend+frontend | Should |
| **P7** | 🟡 | 안정화·성능검증·접근성·패키징(M4 릴리스 준비) — **헤드리스분 ✅(접근성·WCAG AA·성능/F장 검증 하니스·audit 점검·sourcemap·서명 설정) / 런타임 잔여 🟡(성능 실측·실제 서명·NSIS 실측·F장 실케이스)** | 1.5초/200ms 성능검증, 접근성, NSIS 인스톨러, QA 매트릭스 | P5(+P6 일부) | 전원 | Must(릴리스) |

> **마일스톤 매핑(PRD §12)**: P0~P2 ≈ M1(단일 패널 코어) · P3~P4 ≈ M2(핵심 차별점) · P5 ≈ M3(MVP 기능 완성) · P7 ≈ M4(1차 릴리스) · P6 ≈ M5+(후속).

### 스파이크(선행 또는 병행)
| Spike | 목적 | 시점 | 산출 |
|---|---|---|---|
| **SPK-Worker** | Worker 실행 모델 결정: UtilityProcess vs Worker Threads (취소 지연·진행률 정확도·휴지통/롱패스 네이티브 모듈 로드 벤치) | P1 후반~P4 시작 전 | ADR-005 갱신 |
| **SPK-Perf** | "1만 개 목록 입력 후 200ms 내 가시 결과" + "1만 개 첫 렌더 1.5초" 측정 | P2 말 ~ P5 검색 착수 전 | SW §6.3 폴백 채택 여부, SW §10-1 확정 |

---

## 2. 임계 경로 (Critical Path)

```
P0 → P1 → P2 → P3 → P4 → P5(=MVP 완성) → P7(릴리스)
                         └ P5a(검색/필터·테마·상태바·설정 화면)은 P2 위에서 선행 가능. P5b(세션복원·즐겨찾기·최근 영속)만 P3(탭/레이아웃)·P4(작업) 완료가 전제
```

- **MVP 임계경로 = P0 → P1 → P2 → P3 → P4 → P5.** 이 사슬이 직렬 의존이며 전체 일정을 지배한다.
- **병렬화로 단축 가능한 지점**:
  - P1에서 IPC 계약(`shared/ipc`)을 **가장 먼저 합의·동결**하면, 이후 backend(Main 핸들러 구현)와 frontend(infra/api 모킹 위에서 UI 구현)가 **계약을 경계로 병렬** 진행 → P2~P5의 backend·frontend 작업이 겹친다.
  - P4의 Worker/OperationManager(backend)와 ProgressDialog/ConflictDialog/dnd(frontend)는 `op:*` 계약 합의 후 병렬.
  - **SPK-Worker는 P4의 선행 블로커** → P1~P2 진행 중 병행 착수해 P4 시작 전 결론 필요(임계경로 보호).
  - **SPK-Perf는 P5 검색 착수 전 결론** 필요(폴백 3 Web Worker 도입 여부 결정).
- **임계경로 단축 핵심 한 줄**: "P1의 IPC 계약 동결을 최우선으로 끝내라. 계약이 곧 backend/frontend 병렬화의 출발선이다."

---

## 3. 단계별 상세

---

### P0 — 프로젝트 골격 · 빌드 파이프라인 · 빈 창 실행  ✅ 완료

| 항목 | 내용 |
|---|---|
| **목표** | electron-vite 3엔트리(main/preload/renderer) 빌드가 돌고, 보안 옵션이 적용된 **빈 창**이 dev(HMR)/prod 모두에서 실행된다. 이후 모든 단계의 토대. |
| **산출물(모듈·파일)** | `package.json`, `electron.vite.config.ts`, `electron-builder.yml`, `tsconfig.json`/`tsconfig.node.json`/`tsconfig.web.json`, `.eslintrc.cjs`(계층/프로세스 import 경계 규칙), `src/main/index.ts`(단일 인스턴스 락·창 생성·보안 webPreferences), `src/main/windows/`, `src/preload/index.ts`(빈 contextBridge), `src/renderer/index.html`·`main.tsx`·`ui/App.tsx`, Zustand 빈 store 부트, `src/shared/` 디렉토리 골격, `resources/`(앱 아이콘) |
| **의존** | 없음 |
| **담당** | **devops**(전담). frontend는 React/Zustand 부트 합류 가능 |
| **완료 기준(DoD)** | ☑ `npm run dev` → HMR 동작, 빈 창 표시<br>☑ `npm run build` → 3엔트리 번들 성공, 타입체크(`tsc --noEmit`) 0 에러<br>☑ `npm run package`(electron-builder) → Windows NSIS 인스톨러 산출물 생성<br>☑ BrowserWindow에 `contextIsolation:true, nodeIntegration:false, sandbox:true, webSecurity:true` 적용 확인<br>☑ 단일 인스턴스 락 동작(2회 실행 시 기존 창 포커스)<br>☑ ESLint import 경계 규칙이 위반(예: renderer에서 `node:fs` import)을 에러로 잡음<br>☑ 엄격 CSP 헤더 적용, 원격 콘텐츠 로드 없음 |
| **QA 검증 포인트** | dev/prod 양쪽 실행 스모크, 보안 옵션 4종 실측(devtools/소스 확인), import 경계 규칙이 의도대로 막는지 위반 샘플로 교차 확인, 인스톨러 설치/실행 |
| **추적성** | PRD §7(단일 인스턴스·보안), ADR-001(빌드), ADR-005(프로세스/보안), ADR-006(패키징), DS §2~5 |

---

### P1 — IPC 계약 기반 + FS 읽기 계층(디렉토리 목록·스트리밍)  ✅ 완료

| 항목 | 내용 |
|---|---|
| **목표** | 타입 안전 IPC RPC + 이벤트 스트림의 **전 채널 타입 계약을 동결**하고, Renderer가 `window.api`로 디렉토리 목록(소형 단발 + 대형 스트리밍)·stat·드라이브·트리·경로검증을 받을 수 있다. **계약 동결 = 이후 backend/frontend 병렬화의 출발선.** (핸들러 구현 범위는 단계별로 분리 — 아래 (a)/(b) 참조) |
| **산출물(모듈·파일)** | **(a) 전 채널 타입 계약 동결**: `src/shared/ipc/channels.ts`(채널 상수 — `fs:*`·`shell:*`·`op:*`·`clipboard:*`·`session:*`·`settings:*`·`workspace:*` **전부**), `src/shared/ipc/contracts.ts`(위 모든 채널의 요청/응답·이벤트 타입 shape를 동결), `src/shared/dto/`(FileEntryDTO·DriveDTO·세션/설정 스냅샷 DTO 등), `Result<T,FileOpError>` 판별유니온·`FileOpError` 코드(EEXIST/EINVAL/EACCES/ENOENT 등). **미구현 채널은 contracts.ts에 타입만 두고 핸들러는 명시 stub/주석(`// impl: P4` 등)으로 계약만 고정** → frontend가 모킹 위에서 P2/P4/P5 UI를 병렬 선행 가능.<br>**(b) 핸들러 구현(P1 범위 = `fs:*` 읽기 계열만)**: `src/main/ipc/fs.handlers.ts`(`fs:list`/`fs:stat`/`fs:drives`/`fs:tree-children`/`fs:validate-path` + 스트리밍 `fs:list:start/chunk/done/error/cancel`), `src/main/ipc/guard.ts`(senderFrame 검증·경로 정규화·zod 스키마), `src/main/fs/`(FileSystemService: 스트리밍 읽기·롱패스·링크·네트워크 예외). **`shell:*`/`op:*`/`clipboard:*`/`session:*`/`settings:*`/`workspace:*` 핸들러 실구현은 각 해당 Phase(P2/P4/P5/P6)에서 수행.**<br>`src/preload/api.ts`(채널별 래퍼 1메서드 + 1차 검증·이벤트 구독), `src/renderer/infra/api/`(window.api 래핑 + IPC 이벤트→스토어 브리지) |
| **의존** | P0 |
| **담당** | **backend**(Main 핸들러·FileSystemService·guard) + **frontend**(infra/api 어댑터·preload 래퍼 검토). **첫 작업 = 양측이 `shared/ipc` 계약 공동 합의·동결** |
| **완료 기준(DoD)** | ☑ `shared/ipc` 타입이 동결되고 main/preload/renderer가 동일 타입 import(타입체크 0 에러)<br>☑ **contracts.ts에 전 채널(`fs:*`·`shell:*`·`op:*`·`clipboard:*`·`session:*`·`settings:*`·`workspace:*`)의 요청/응답·이벤트 타입이 모두 존재. 미구현 채널은 핸들러 stub/주석으로 계약만 고정(타입체크 0 에러)**<br>☑ **frontend가 미구현 채널을 infra/api 모킹으로 호출해도 타입 계약과 일치(병렬 선행 가능성 검증)**<br>☑ `fs:list`/`fs:stat`/`fs:drives`/`fs:tree-children`/`fs:validate-path`가 실제 경로에 대해 `Result` 반환(P1 구현 범위)<br>☑ 대형 폴더(1만 항목)가 `fs:list:start`→`chunk`(증분)→`done` 스트림으로 전달되고 `cancel` 동작<br>☑ 권한 거부/미존재 경로가 throw 아닌 `FileOpError(code)`로 1급 전파<br>☑ guard가 `..` 상위이탈·시스템 보호경로 정규화·차단, senderFrame 검증 동작<br>☑ infra/api 통해 호출 시 이벤트 스트림이 스토어 액션으로 브리지됨(콘솔/테스트 확인) |
| **QA 검증 포인트** | 계약 경계면 교차 검증(요청/응답 shape이 contracts.ts와 일치), 스트림 청크 순서·done total 정확성, 취소 후 추가 청크 유입 없음, 오류코드 매핑(EACCES/ENOENT) 실측, 롱패스/유니코드/링크/네트워크 드라이브 케이스 |
| **추적성** | US-5.6(스트리밍 첫 렌더), US-3.1(경로검증), US-3.2(드라이브·트리), F장(Windows 특수케이스), SA §3~4, SW §3.1~3.2, ADR-003(IPC 계약) |

---

### P2 — 단일 패널 파일목록 UI(가상 스크롤·보기/정렬·주소창·탐색·트리)  ✅ 완료

| 항목 | 내용 |
|---|---|
| **목표** | 한 개 패널에서 폴더를 열고, 가상 스크롤로 목록을 보고, 정렬/보기 전환·다중 선택·주소창/뒤·앞·위 탐색·트리 사이드바 이동이 된다. **항목 활성화(더블클릭/`Enter`)로 폴더는 진입, 파일은 OS 연결 프로그램으로 실행**한다(아직 탭/분할 없음, 파일 작업(CRUD/속성창)은 없음). |
| **산출물(모듈·파일)** | `src/renderer/app/stores/`(panelsSlice: path·navHistory·view·directoryView / selectionSlice), `src/renderer/domain/rules/`(자연정렬·폴더우선), `src/renderer/domain/paths/`, `src/renderer/domain/entities/`; `ui/panel/`(Panel 셸·PanelHeader), `ui/panel/views/FileListView`(가상 스크롤 list/details·인라인 편집 자리·더블클릭/`Enter` 활성화 핸들러), `ui/toolbar/`(주소표시줄 브레드크럼/편집·뒤/앞/위·보기·정렬 컨트롤), `ui/sidebar/`(트리·드라이브·내 PC, `fs:tree-children` 지연확장), `app/usecases/`(폴더진입·nav·view·selection·**open**(항목 활성화: 폴더=진입/파일=`shell:open` 위임)); **`shell.handlers.ts`(`shell:open` 핸들러 — 경로 정규화·존재·권한 검증 후 `shell.openPath` 위임. ADR-005)** |
| **의존** | P1 |
| **담당** | **frontend**(단일 패널 UI·open usecase 전담) + **backend**(`shell:open` 핸들러·경로 검증 가드). backend는 P1 `fs:*` 안정화 지원, P4 계약 사전 검토 |
| **완료 기준(DoD)** | ☑ 폴더 진입 시 스트림 첫 청크 도착 즉시 첫 화면 렌더(스피너 해제), 1만 항목에서 가상 스크롤 DOM 노드 수십 개 유지<br>☑ details/list 보기 전환, 컬럼 클릭 정렬(이름/크기/형식/수정일·오름/내림), 자연정렬·폴더우선 토글 동작·패널별 기억<br>☑ 브레드크럼 클릭·`Ctrl+L` 경로입력·잘못된 경로 인라인 오류, `Alt+←/→/↑`·`Backspace` 탐색<br>☑ 트리 펼침/접힘·클릭 시 패널 경로 변경, 사이드바 토글/폭조절<br>☑ `Ctrl/Shift/박스/Ctrl+A` 다중 선택, 선택이 셀렉터로 격리 리렌더(다른 영역 비리렌더)<br>☑ **폴더 항목을 더블클릭 또는 `Enter`로 활성화 시 그 폴더로 진입(navHistory 적재)**<br>☑ **파일 항목을 더블클릭 또는 `Enter`로 활성화 시 `shell:open`을 통해 OS 연결 프로그램으로 실행(반환 코드 확인, 미연결 형식은 OS 기본 동작 위임)**<br>☑ **`shell:open` 핸들러가 (a) 경로 정규화(`..` 상위이탈 차단)·(b) 대상 존재·(c) 접근 권한을 모두 통과한 경로만 `shell.openPath`에 위임. 검증 실패 시 실행 없이 `FileOpError`(ENOENT/EACCES/EINVAL) 반환 — 임의/조작 경로 실행 차단(ADR-005)**<br>☑ 권한거부/빈/네트워크 폴더가 패널 단위 오류/빈 상태로 격리 표시<br>☑ 타입체크·린트 0 에러 |
| **QA 검증 포인트** | (SPK-Perf 연계) 1만 항목 첫 렌더 1.5초 측정, 스크롤 시 가시영역만 재계산, 정렬/필터 정확성, 셀렉터 리렌더 격리(다른 패널/선택 변경에 비리렌더), DirectoryView 상태머신(idle→loading→streaming→ready/error/empty/denied) 전이; **항목 활성화 진리표(폴더=진입/파일=실행), `shell:open` 보안 검증 교차(미존재 경로·`..` 이탈 경로·권한밖 경로 주입 시 실행 차단·FileOpError 반환 실측), 실행 실패(미연결 형식) 폴백 안내** |
| **추적성** | US-2.1(보기/정렬), US-3.1(주소표시줄/이동), US-3.2(트리), US-5.1(다중선택 일부), US-5.6(가상스크롤 첫렌더), **US-2.2/B6(파일 실행·열기: 더블클릭/`Enter`)**, feat-B1/B2/B6/C1~C3, SW §4·§6, SA §3.2·§3.3-4, ADR-004(가상화), ADR-002(상태), **ADR-005(shell 실행 검증)** |

---

### P3 — 탭 + 2분할 레이아웃 + 패널 포커스 + 단축키 디스패치  ✅ 완료

| 항목 | 내용 |
|---|---|
| **목표** | 탭(추가/닫기/전환/이동/복제/닫은탭복원)과 2분할(좌우/상하) 레이아웃, 활성 패널 포커스, 중앙 단축키 디스패치가 동작한다. 핵심 차별점의 골격. |
| **산출물(모듈·파일)** | `app/stores/tabsSlice`(windows·tabs·layout·activeTab/activePanel·closedHistory), `app/usecases/`(탭열기/복제/복원·분할토글·패널포커스이동); `ui/tabbar/`(탭바·드래그 순서), `ui/layout/LayoutHost`(single/split-2-h/split-2-v 배치·분할선·최소폭); `ui/keyboard/`(KeyBindingRegistry·Dispatcher·CommandBus·컨텍스트 스코프), `src/renderer/domain/keybindings/`(PRD §8 표 단일 출처: 키→commandId) |
| **의존** | P2 |
| **담당** | **frontend**(전담) |
| **완료 기준(DoD)** | ☑ `Ctrl+T/W/Tab/1~9`·가운데클릭·드래그순서·`Ctrl+D` 복제·`Ctrl+Shift+T` 복원, 마지막 탭 닫으면 "내 PC" 기본 탭 유지<br>☑ 각 탭 독립(경로·정렬·보기·히스토리), 분할 구성 탭별 독립<br>☑ `Ctrl+\` 2분할 토글, 각 패널 독립 상태, 활성 패널 시각 구분<br>☑ `Tab`=패널 포커스 순환, `Ctrl+←/→`=방향 포커스(별개 commandId)<br>☑ KeyBindingRegistry가 동일 컨텍스트 중복 매핑을 부팅 시 assert, 컨텍스트 스코프(주소창/검색/이름편집/다이얼로그)에서 텍스트 입력 우선·전역 단축키 차단<br>☑ 단축키 목록을 **읽을 수 있도록 KeyBindingRegistry가 (키→commandId→표시명) 목록을 노출**(레지스트리 읽기 API). **실제 표시 호스트인 설정 화면 단축키 섹션은 P5a에서 제공** — P3에서는 레지스트리 노출까지만 완료(또는 임시 도움말 패널로 확인) |
| **QA 검증 포인트** | 단축키 충돌 부재(특히 Tab/Ctrl+R 고유성; ~~F5/F6~~은 2026-06-07 제거되어 미배정), 컨텍스트 스코프 전환 정확성, closedHistory 휘발(세션 비직렬화) 확인, 2분할에서 패널별 셀렉터 리렌더 격리, 탭 N개에서 활성 패널 정확히 하나 불변식 |
| **추적성** | US-1.1(탭), US-1.2(2분할), US-5.4(키보드), 결정-D4(단축키 충돌), feat-A1/A2/E1, SW §2·§5·§7, ADR-002 |

---

### P4 — 파일 작업(CRUD·휴지통) + 진행률/취소 + 충돌해결 + 패널 간 D&D  ✅ 완료

> **드리프트 해소(2026-06-07)**: 본 P4 산출물 중 `ui/contextmenu/`(컨텍스트 메뉴)와 DoD "컨텍스트 메뉴 '속성'→`shell:show-properties`"는 P4 당시 backend 핸들러(`shell:show-properties`)만 있고 **호출 UI가 누락**된 채 ✅로 표기돼 있었다. 컨텍스트 메뉴 인프라가 P6 시점에 뒤늦게 구현되어 이제 충족됨(§0.5 P6 표·드리프트 주석 참조).
> **스코프 축소 정정(2026-06-07 사용자 결정)**: 본 P4의 산출물·DoD·QA 포인트·추적성에 있던 **`F5`(복사)/`F6`(이동) 패널 간 단축키는 제거(Deprecated)** 됐다. 코드에서 `domain/keybindings`·`commandBus`·`usecases/fileOps.ts`(copyToOtherPanel/moveToOtherPanel)·`ShortcutHelp`의 F5/F6 경로가 제거됐고, **패널 간 복사/이동은 D&D·클립보드(`Ctrl+C/X/V`)가 단일 경로**다. 아래 본문의 F5/F6 표기는 취소선으로 정정한다.

| 항목 | 내용 |
|---|---|
| **목표** | 생성·이름변경·복사·잘라내기·붙여넣기·삭제(휴지통/영구)가 되고, 대용량 복사/이동이 Worker에서 비차단 실행되며 진행률(200ms)·취소·충돌해결이 동작한다. 패널 간 **D&D 및 클립보드**로 이동/복사한다(~~F5/F6~~ 2026-06-07 제거). **핵심 차별점 완성.** |
| **산출물(모듈·파일)** | (backend) `src/main/ipc/op.handlers.ts`(`op:start/progress/conflict/resolve/done/cancel`), `fs.handlers.ts`에 `fs:mkdir`/`fs:create-file`/`fs:rename` 추가, `src/main/operations/`(OperationManager·작업큐·AbortController·200ms 스로틀), `src/main/workers/`(재귀 복사/이동/삭제·사전집계·청크 진행보고·충돌질의), `src/main/os/`(휴지통 `shell.trashItem`·`dialog:confirm-permanent-delete`), `shell.handlers.ts`에 **`shell:show-properties`(OS 속성창 호출 `shell.showItemInFolder`/네이티브 속성 다이얼로그)·`clipboard:*` 추가**(`shell:open`은 P2에서 구현됨, `shell:open-with`(연결 프로그램 선택)는 Should → P6); `ui/contextmenu/`(컨텍스트 메뉴: 열기·속성·복사/잘라내기/삭제); (frontend) `app/stores/operationsSlice`, `app/usecases/`(파일CRUD·복사이동·D&D·붙여넣기 — ~~F5/F6 copyToOtherPanel/moveToOtherPanel~~ 2026-06-07 제거·`panelPaths` 헬퍼는 D&D용 보존), `domain/rules/`(충돌명명 "이름 (n)"/"복사본"·`resolveDragIntent`·순환이동 차단·동일폴더 무시), `ui/dialogs/`(ProgressDialog·ConflictDialog·ConfirmDialog), `ui/dnd/`(드래그&드롭 핸들러) |
| **의존** | P3 (UI 통합 기준). **계약(`op:*`·`fs:mkdir/rename`)은 P1에서 합의** → backend Worker/OperationManager는 P2~P3 중 병행 가능. **SPK-Worker 선결 필요.** |
| **담당** | **backend**(OperationManager·Worker·휴지통·op/fs 핸들러) + **frontend**(다이얼로그·dnd·usecase·operationsSlice). `op:*` 계약 경계로 병렬 |
| **완료 기준(DoD)** | ☑ `Ctrl+Shift+N` 새 폴더(즉시 이름편집)·새 파일·`F2` 이름변경, 금지문자/예약명/중복명 → EINVAL/EEXIST 안내<br>☑ `Ctrl+C/X/V` OS 클립보드 호환, 잘라내기 항목 흐림·붙여넣기 전 원본 유지, 같은 폴더 "복사본" 자동명명<br>☑ `Delete` 휴지통 이동(되돌리기 가능)·`Shift+Delete` Main 모달 확인 후 영구삭제<br>☑ 대용량 복사/이동이 Worker에서 실행되어 **작업 중 다른 탭/패널 조작이 멈추지 않음**(UI 비차단)<br>☑ 진행률(현재 파일명·전체%·남은 항목/용량)이 **200ms 이내 간격** 갱신, 취소 시 진행분 처리 후 summary, 부분실패 목록 보고<br>☑ 충돌 시 임의 덮어쓰기 없음, 덮어쓰기/건너뛰기/둘다유지("이름 (n)")/폴더 병합/모두적용, 읽기전용·사용중 처리, 크기·수정일 비교 표시<br>☑ 패널 간 D&D: 같은드라이브=이동/다른드라이브=복사 기본, Ctrl=복사·Shift=이동 강제, 폴더 위 드롭=그 폴더 안, 동일폴더 무시, 조상→자손 차단·안내<br>☑ **D&D 중 드롭 가능 대상(패널 빈 영역·폴더 항목)이 시각적으로 하이라이트되고, 현재 의도(복사/이동)가 커서·툴팁으로 상시 표시(수정키 변경 시 즉시 반영) — 드롭 불가 영역은 비하이라이트(US-1.3)**<br>☑ **컨텍스트 메뉴 "속성"이 `shell:show-properties`로 선택 항목의 OS 속성창을 호출(다중 선택 시 규칙대로 처리)**<br>~~☑ `F5`=복사/`F6`=이동이 D&D와 동일 `op:start` 경로로 동작~~ — **삭제됨(2026-06-07 사용자 요청). 패널 간 복사/이동은 D&D·클립보드가 단일 경로** |
| **QA 검증 포인트** | 200ms 진행률 갱신 실측, 취소 지연·진행분 정합, 충돌 매트릭스(파일/폴더/읽기전용/사용중/권한) 교차, FileOperation 상태머신(pending→running→conflict→running→done/partial-failed/cancelling) 전이, 휴지통 이동/복원 실측, 순환이동·동일폴더·자기위치 선검증, D&D 의도 판정(드라이브×수정키) 진리표, **D&D 드롭 대상 하이라이트·커서/툴팁 의도 표시 시각 검증(수정키 변경 즉시 반영)**, **`shell:show-properties` 속성창 호출 실측**, op:* 계약 경계 교차 검증 |
| **추적성** | US-1.3(D&D·~~F5/F6~~ 제거·**드롭 하이라이트·커서/툴팁 피드백**), US-2.2(생성/이름변경/삭제), US-2.3(클립보드), US-2.4(충돌), US-5.1(일괄), US-5.2(진행률/취소), **B6(속성창=`shell:show-properties`)**, feat-A3/B3/B4/B5/B6/feat-D4, SA §4, SW §8, ADR-003/005 |

---

### P5 — 검색/필터 + 즐겨찾기/최근 + 설정 화면(숨김·확장자 토글) + 자동 세션복원 + 다크모드 + 상태바  → **MVP 완성**  ✅ 완료

> **트랙 분리(병렬 착수)**: **P5a**(검색/필터·테마·상태바·**설정 화면** — 의존 **P2**) / **P5b**(세션복원·즐겨찾기·최근 영속 — 의존 **P3·P4**). P5a는 P4 완료를 기다리지 않고 P2 위에서 선행 착수 가능. 설정 화면 골격은 P3 DoD("단축키 목록을 설정에서 표시")의 의존 대상이므로 P5a에서 우선 제공한다.

| 항목 | 내용 |
|---|---|
| **목표** | 현재 폴더 검색(200ms)·확장자/이름 필터, **설정 화면(테마·기본 시작 위치·숨김 파일 표시·확장자 표시·최근 개수·단축키 보기)과 숨김/확장자 표시 토글의 목록 즉시 반영**, 즐겨찾기·최근, 정상·비정상 종료 후 자동 세션복원, 다크/라이트/시스템 테마, 상태바가 동작한다. **Must 전체 완성 = MVP.** |
| **산출물(모듈·파일)** | **(P5a, frontend) `ui/panel/SearchBar`**, `app/usecases/`(검색/필터), `domain/rules/`(필터/패턴매칭·하이라이트 순수함수), `panelsSlice.filter`, `ui/theme/`(다크/라이트/시스템), `ui/statusbar/`(항목수·선택개수/용량·활성경로·필터결과); **`ui/settings/`(설정 화면 — 테마·기본 시작 위치·숨김 파일 표시·확장자 표시·최근 개수·단축키 목록 보기), `app/usecases/settings`, `app/stores/uiSlice.settings`(theme·startLocation·recentLimit) + `panelsSlice`(또는 `uiSlice`)에 `showHidden`·`showExtensions` 상태 추가, FileListView가 `showExtensions`로 확장자 표시 토글, 폴더 진입 시 `fs:list(showHidden)` 파라미터를 설정값으로 전달(사용자 제어 경로 연결)**.<br>**(P5b, frontend) `app/usecases/`(즐겨찾기·최근·세션복원), `app/stores/sidebarSlice`(favorites·recent), `ui/sidebar/`(즐겨찾기·최근 섹션)**; (backend) `src/main/ipc/session.handlers.ts`(`session:load/save`·`settings:get/set` 핸들러 실구현), `src/main/persistence/`(session.json/settings.json 원자적 쓰기·디바운스·스키마 버전·손상 폴백); App 부트스트랩 세션복원 |
| **의존** | **P5a**: P2(패널 목록·`fs:list`). **P5b**: P3(탭/레이아웃 스냅샷)·P4(작업 후 상태). (계약 `session:*`·`settings:*`은 P1에서 동결) |
| **담당** | **frontend**(검색/필터·설정 화면·숨김·확장자 토글·즐겨찾기·테마·상태바·복원 로직) + **backend**(persistence·session/settings 핸들러). 세션·설정 스냅샷 스키마는 `shared/dto`에 합의(P1 동결) |
| **완료 기준(DoD)** | ☑ `Ctrl+F` 검색창, 입력 후 **200ms 이내** 점증 필터·하이라이트, 해제 시 전체 복귀(SPK-Perf 폴백 적용)<br>☑ `*.png`/`report*` 패턴 필터, 결과 개수 상태바 반영<br>☑ **설정 화면에서 테마·기본 시작 위치·숨김 파일 표시·확장자 표시·최근 개수를 변경하면 `settings:set` 통해 settings.json에 영속, 재시작 후 유지**<br>☑ **숨김 파일 표시 토글 변경 시 `fs:list(showHidden=true/false)`로 재요청되어 패널 목록에 숨김/시스템 파일이 즉시 포함/제외(기본 off)**<br>☑ **확장자 표시 토글 변경 시 FileListView 이름 표기에 확장자 즉시 표시/숨김(기본 on)**<br>☑ **설정 화면 단축키 섹션이 KeyBindingRegistry를 읽어 PRD §8 단축키 목록을 표시(P3 DoD "설정에서 단축키 표시"의 실제 호스트)**<br>☑ 폴더 즐겨찾기 추가/제거·사이드바 고정, 최근 자동기록·개별/전체 삭제·**최근 개수가 설정값(recentLimit) 따름**<br>☑ 정상 재시작 시 마지막 탭/경로/레이아웃 복원, **비정상 종료(크래시) 후에도 복원**(원자적 쓰기), 패널 정렬/보기 복원<br>☑ closedHistory·선택·진행작업은 복원 제외(휘발) 확인<br>☑ 손상/구버전 스냅샷 → 안전 폴백("내 PC" 탭) 부팅(크래시 프리)<br>☑ 라이트/다크/시스템 테마 즉시 적용<br>☑ 상태바: 전체 항목수·선택개수/합계용량·활성경로·필터결과 갱신 |
| **QA 검증 포인트** | (SPK-Perf) 1만 항목 검색 입력 200ms 실측, 디바운스 트랜지션 블로킹 부재; **설정 변경→settings.json 영속→재시작 유지 실측, 숨김 토글 on/off 시 목록 항목 수 변화(`fs:list` showHidden 연동) 실측, 확장자 토글 표기 변화, 설정 단축키 목록과 PRD §8 일치**; 세션 디바운스 저장+before-quit 플러시, 크래시(강제종료) 후 복원, 원자적 쓰기(쓰기 중 종료 시 파일 무손상), 스키마 마이그레이션·손상 폴백, 테마 대비 WCAG AA 지향 |
| **추적성** | US-4.1(검색), US-4.2(필터), US-3.3(즐겨찾기/최근), US-5.3(테마), US-5.5(세션복원), US-5.7(상태바), **feat-E6(설정), F장(숨김/시스템 파일 표시 토글·확장자 표시 토글)**, 결정-D3(세션), feat-D1/D2/C4/C5/E2/E5, **settings:get/set·session:save/load**, SA §5, SW §5.3·§6.3 |

---

### P6 — (Should) 4분할 · 미리보기 패널 · 워크스페이스 저장 · 텔레메트리(옵트인) · 연결프로그램으로 열기  ✅ 완료(5종) · Should 잔여 0(되돌리기·휴지통 관리는 §K로, 이미지 썸네일 자체 생성은 §L로 해소 ✅)

| 항목 | 내용 |
|---|---|
| **목표** | MVP 안정화 후 Should 기능: 4분할(2x2), 미리보기 패널(이미지/텍스트/메타), 명시적 워크스페이스 저장/복원, 텔레메트리 옵트인. |
| **산출물(모듈·파일)** | ✅ `ui/layout/LayoutHost`(grid-4·포커스 순환 확장), `domain` 포커스 순환 규칙; ✅ `ui/preview/PreviewPanel`+형식별 렌더러(`renderers/*`), `uiSlice.previewOpen`, **신규 `preview:read` 채널**(`FileSystemService.readPreview`·`preview.handlers.ts`); ✅ `src/main/persistence/WorkspaceStore.ts`·`workspace:save/list/load/delete`, 워크스페이스 UI(`WorkspaceDialog`); ✅ `telemetry:get/set-opt-in`(기본 false)·옵트인 시에만 익명 집계·`.eslintrc.cjs` 네트워크 정적 가드; ✅ **`shell:open-with`(연결 프로그램으로 열기, features §B6 Should): `os/shell.ts#openWith`(OpenAs_RunDLL)·`shell.handlers.ts`(3중 검증)·`guard.ts#zShellOpenWithReq`·`usecases/open.ts#openWithEntry`·컨텍스트 메뉴 항목**; ✅ **컨텍스트 메뉴(우클릭) 인프라(`ui/contextmenu/`·`usecases/contextMenu.ts`·`uiSlice.contextMenu`) — P4 산출물이 뒤늦게 구현돼 P4 드리프트 해소(속성→`shell:show-properties` 호출 UI 포함)**. **되돌리기(Ctrl+Z)·휴지통 관리 화면은 §K(feat-K1/K2)로, 그리드 썸네일 이미지 자체 생성은 §L(feat-L1·신규 채널 `preview:thumbnail`·`os/thumbnail.ts`·`ThumbnailIcon`)로 구현 완료 ✅(P6 잔여 전부 해소). 보기 5종 J3 ✅ + 이미지 썸네일 L1 ✅ → P6 Should 잔여 0** |
| **의존** | P5(MVP 완성·스냅샷 구조·LayoutHost·uiSlice 재사용) |
| **담당** | backend+frontend(변화 격리 경계 내 확장: `ui/panel/views/*`·`ui/preview/renderers/*`·`workspace:*` 채널) |
| **완료 기준(DoD)** | ☑ 2x2 4분할·4패널 독립 상태·포커스 순환(`panel.focusNext`)과 방향이동 구분<br>☑ 이미지 축소·텍스트 앞부분·기본 메타·미지원 형식 아이콘 미리보기, `Ctrl+P` 토글<br>☑ 워크스페이스 이름저장/불러오기(탭·패널·경로 복원)/이름변경/삭제<br>☑ 텔레메트리 기본 꺼짐, 동의 시에만 외부 전송·미동의 시 전송 전무 |
| **QA 검증 포인트** | 4분할 포커스 순환 vs 방향이동 차이, 미리보기 형식별 렌더·대용량/미지원 폴백, 워크스페이스 스냅샷 재사용 정합, 텔레메트리 옵트인 네트워크 차단 검증 |
| **추적성** | US-1.4(4분할), US-4.3(미리보기), US-5.8(워크스페이스), 결정-D1/D2/D5, feat-D3, SW §9(변화 격리), SA §5.2 |

---

### P7 — 안정화 · 성능검증 · 접근성 · 패키징(1차 릴리스 준비)  🟡 부분(헤드리스분 ✅ / 런타임·인증서 잔여 🟡)

> **헤드리스/런타임 정직 구분(2026-06-07)**: 헤드리스로 증명 가능한 부분(접근성 코드·WCAG 대비·검증 불변식·audit 점검·빌드 설정)은 **✅ 완료**, GUI 실행/인증서/실 환경이 필요한 부분(성능 실측 숫자·실제 코드서명·NSIS 설치 실측·F장 실케이스·실 스크린리더)은 **🟡 잔여**. P7 전체는 아직 🟡 — ✅ 위장 아님.

| 항목 | 내용 |
|---|---|
| **목표** | PRD 성능/안정/접근성 목표를 실측 검증하고, 인스톨러·QA 매트릭스를 완비해 1차 릴리스(M4) 준비. |
| **산출물(모듈·파일)** | ✅ 접근성: `src/renderer/ui/keyboard/useFocusTrap.ts`(첫 포커스·Tab 순환·opener 복귀·Esc 위임)·모달 6종 `role=dialog`/`aria-modal`/Esc 규칙·`FileListView` 행 `aria-posinset/setsize`·`:focus-visible`·Shift+F10. ✅ WCAG: `scripts/verify-contrast.ts`(4팔레트 AA 전수). ✅ 성능 하니스: `src/renderer/ui/panel/views/windowing.ts`(순수함수)·`scripts/verify-perf.ts`(25). ✅ F장 매트릭스: `scripts/verify-fmatrix.ts`(32)·`docs/P7-qa-matrix.md`. ✅ 보안 점검: `docs/P7-security-audit.md`(npm audit 9건 판정). ✅ `electron.vite.config.ts`(sourcemap main/preload true·renderer hidden)·`electron-builder.yml`(`!out/**/*.map` 제외·`CSC_LINK`/`CSC_KEY_PASSWORD` 서명 설정·`signingHashAlgorithms`). 🟡 실측/실제 서명/NSIS 실측/실케이스(런타임). |
| **의존** | P5(+P6 일부) |
| **담당** | 전원(qa 주도 검증, devops 패키징, backend/frontend 결함 수정) |
| **완료 기준(DoD)** | **헤드리스 충족 ✅**: ☑ 가상 스크롤 windowing 불변식·1만 항목 DOM 후보 수십개·200ms 스로틀·검색 필터 검증(`verify:perf` 25) ☑ 핵심 동작 키보드 접근·포커스 트랩·포커스 표시(`:focus-visible`)·행 ARIA 레이블·모달 role/aria/Esc 코드 충족 ☑ WCAG AA 대비 4팔레트 전수 통과(`verify:contrast`) ☑ F장 매트릭스 롱패스/링크(정션 순환)/네트워크(UNC·매핑 판정)/권한(ENOENT·ENOTDIR throw 0) 코드경로 검증(`verify:fmatrix` 32) ☑ npm audit 점검·판정(릴리스 차단 아님) ☑ sourcemap 분리·코드서명 설정(준비완료)·NSIS 인스톨러 산출.<br>**런타임 잔여 🟡**: 🟡 1만 항목 첫 렌더 1.5초·진행률 200ms·검색 200ms **실측 숫자**(GUI 실행 — `docs/P7-perf-measurement.md`) 🟡 크래시 프리 세션 99.5% 실측 🟡 실 스크린리더 ARIA 발화·포커스 육안 🟡 NSIS 인스톨러(**실제 코드서명** .pfx 필요) 설치/실행/제거 실측 🟡 F장 실케이스(실 네트워크 드라이브·실 symlink·실 ACL deny EACCES). |
| **QA 검증 포인트** | (헤드리스 ✅) windowing 불변식·F장 코드경로·WCAG 대비 게이트·audit 판정·sourcemap/서명 설정·빌드. (런타임 🟡) 성능 3종 실측, 회귀 스위트, Windows 특수케이스 실케이스, 보안(네트워크 차단·CSP·senderFrame) 최종 점검. |
| **추적성** | PRD §3·§7(성능·안정·접근성·보안), §12 M4, traceability §3·§1-P7(P7 매핑) |

---

## 3-M. §M 외부 연계 구현 Phase (MP0~MP5, 2026-06-08 비파괴 추가) — ✅ 구현 완료·QA PASS (실 동작 런타임 스모크 🟡)

> 비파괴 추가. 기존 P0~P7·G~L 무손상(상태·DoD 무변경). §M 3기능(M1 외부 D&D·M2 클립보드 CF_HDROP·M3 FTP/SFTP)의 구현 Phase다. 설계 단일 출처: [ADR-007](./architecture/adr/ADR-007-remote-protocol-and-network-boundary.md)·[system-architecture §5-M](./architecture/system-architecture.md)·[software-architecture §11](./architecture/software-architecture.md)·[directory-structure §5-M·§6](./architecture/directory-structure.md)·[traceability §1-M](./architecture/traceability.md). **코드베이스 수준 세부 계획(파일·시그니처·DTO·zod·검증)은 [docs/M-implementation-plan.md](./M-implementation-plan.md) 단일 출처.** **상태 단일 출처는 §0.5: MP0~MP5 전부 ✅ 구현 완료·통합 QA PASS(2026-06-08·[qa-integration-M](./reviews/qa-integration-M.md)·[final-traceability-M](./reviews/final-traceability-M.md)). 코드 정합·verify 충족 ✅ / 실 동작은 런타임 스모크 권장 🟡(외부 앱 실드롭·탐색기 양방향 왕복·실 SFTP/FTP/FTPS·실 DPAPI·실 전송).**
>
> **분해 근거(순서)**: ① MP0 골격/의존성/ESLint 화이트리스트는 **선행 필수**(remote/ ESLint 예외 없이는 MP4 remote 코드가 lint 실패). ② MP1 IPC 계약 동결은 "**인터페이스 먼저**" — backend(MP2~MP4)·frontend(MP5)의 병렬 출발선(P1 "전 채널 동결" 선례). ③ **M1·M2는 신규 의존성 0**(Electron 내장 startDrag·clipboard buffer)이라 저위험 → MP2·MP3로 **먼저**(상호 병렬). ④ **M3는 신규 라이브러리(ssh2-sftp-client·basic-ftp)·네트워크 경계 개정·자격증명·신뢰경계**로 가장 크고 위험 → MP4(backend)·MP5(UI)로 **뒤**. ⑤ 각 Phase는 §5 이중검증 루프(reviewer 계획검증→구현→qa 구현검증, 최대 2회) + 완료 직후 doc-sync 게이트(CLAUDE.md).

| Phase | 상태 | 목표 / 포함 모듈 | 의존 | 담당 | 완료 기준(DoD) 요지 | QA 검증 포인트 | 추적성 |
|---|---|---|---|---|---|---|---|
| **MP0** 골격·의존성·ESLint 화이트리스트 | ✅ | `src/main/remote/`·`ui/remote/` 빈 골격, `ssh2-sftp-client`·`basic-ftp` 추가(신규 0은 M1/M2), `.eslintrc.cjs` main override에 `node:tls`·`ssh2`·`ssh2-sftp-client`·`basic-ftp` **신규 차단 추가** + `src/main/remote/**` 예외(allow) | — (단독 선행) | devops | `npm install`/`lint`/`typecheck` 0·기존 verify 회귀 0·remote/ 밖 네트워크 import lint 차단·안 lint 통과 | ESLint 회귀(신규4차단·remote/예외·기존8유지)·`verify:eslint-remote` | ADR-007 ②·DS §5-M |
| **MP1** IPC 계약 동결 | ✅ | `shared/ipc/channels.ts`·`contracts.ts`·`shared/dto`·`guard.ts`(zod)·`preload/api.ts` — `dnd:start-drag`·`clipboard:write/read/has-files`·`remote:*` 전 채널·DTO·zod 동결(핸들러 구현은 후속) | MP0 병렬 가능 | backend 주도+frontend 합의 | typecheck/lint 0·frontend 모킹 위 MP5 선행 가능·기존 채널/DTO 무변경 | 계약 경계·`RemoteProfileDTO` 비밀배제(strict)·채널/맵 정합·`verify:m-contract` | ADR-003·ADR-007 ③⑥·traceability §1-M |
| **MP2** M2 클립보드 CF_HDROP | ✅ | `os/shellClipboard.ts`(DROPFILES·Preferred DropEffect 바이트)·`clipboard.handlers.ts`(write/read/has 확장)·`usecases/clipboardExternal.ts`·commandBus 라우팅 | MP1 | backend+frontend 병렬 | write→read 왕복·cut=Move·비파일=none·방어적 파싱 throw 0·B4 무변경 | CF_HDROP 라운드트립·외부입력 불신·`verify:clipboard-hdrop`·SR8 | ADR-007 ⑦·SA F15·US-12.2 |
| **MP3** M1 외부 D&D | ✅ | `os/dragdrop.ts`(startDrag 래퍼)·`dnd.handlers.ts`·`usecases/externalDrag.ts`·`ui/dnd` 확장·`index.ts` 등록 | MP1 (MP2와 병렬) | backend+frontend 병렬 | 로컬 단일/다중/폴더 드래그 시작·원격/미존재 거부·내부=A3 분기·외부노출=경로뿐 | 외부/내부 분기 진리표·원격 제외·보안·`verify:dnd` | ADR-007 ⑦·SA F14·US-12.1 |
| **MP4** M3 RemoteService·safeStorage·전송 | ✅ | `os/credentials.ts`(safeStorage/DPAPI)·`persistence/RemoteProfileStore.ts`·`remote/{RemoteService,SftpAdapter,FtpAdapter,RemoteSessionManager,remoteTransfer}.ts`·`remote.handlers.ts`·`OperationManager` 원격 진행률 연동 | MP0(필수)·MP1 | backend 주도(내부 병렬: 자격증명/persistence·SFTP·FTP·세션/전송) | 프로필 CRUD·자격증명 평문 0·SFTP 비번/키·호스트키 TOFU·FTPS·평문FTP 경고·다운/업로드(.part 원자rename·op:* 재사용·취소)·세션 격리·**전 응답/로그 비밀 0**·원격응답 불신 | 신뢰경계(traversal/Zip Slip/심볼릭/새니타이즈)·호스트키·비밀 grep 0·`verify:credentials`/`verify:remote`/`verify:remote-trust`·SR5/SR6/SR7 | ADR-007 ①②③④⑤⑥·SA F16·US-12.3~12.5 |
| **MP5** 원격 UI·전송 라우팅 통합 | ✅ | `domain/rules/transferRoute.ts`·`domain/entities` 확장·`usecases/remote.ts`·`stores/remoteSlice.ts`·`ui/remote/*`·`infra/api`·`navigation`·`FileListView`/`PanelHeader` 확장 | MP1(모킹 선행)·MP4(실연동) | frontend 주도 | 접속→탐색→다운/업로드(D&D·붙여넣기)·D4/E4 재사용·프로필 UI·호스트키/비암호화 경고·세션 격리 표시·US-12.3~12.5 충족 | 전송 라우팅 진리표·세션 격리·수용기준 교차·`verify:remote-route` | ADR-007 ⑤⑥·SW §11·US-12.3~12.5 |
| ↳ **원격 주소창 경로-only 입력**(2026-06-09 동작 보강·H5 교차) | ✅(코드)·🟡(실 GUI) | `ui/toolbar/PanelToolbar.tsx`(원격 시 호스트 프리픽스 `sftp://host` 고정 표시·경로만 편집·`makeRemotePath`로 URI 재결합)·`app/usecases/navigate.ts`(원격 URI 는 `fs:validate-path` 우회 후 navigate→`remote:list`) | MP5 | frontend | 원격 연결 상태 주소창에서 경로만 입력·호스트 오입력 방지·잘못된 경로는 패널 오류 표면화 | 원격 URI 라우팅(`verify:remote-route` 47)·신규 채널 0·실 입력 GUI 런타임 스모크 🟡 | §M M3 / H5 교차·traceability §1-M·§1-H |

> **§M 신규 IPC 채널(설계 원칙 정합·드리프트 아님)**: `dnd:start-drag`·파일 `clipboard:write/read/has-files`·`remote:*`는 P1 "전 채널 타입 동결" 이후 추가되나, 기존 6선례(`preview:read`·`shell:open-terminal`·`analyze:scan:*`·`fs:watch:*`·`trash:*`·`preview:thumbnail`)와 **동일 규약**(invoke/이벤트·`Result`·sender·zod·경로/세션 검증·화이트리스트) 준수. P1 동결은 *기존* MVP 채널 계약 고정이 목적이고 Should/Could 신기능의 신규 채널 추가는 위반이 아니다(product-planner가 PRD §M·features §M·US-12.1~12.5로 정식 편입). **신규 npm 의존성: `ssh2-sftp-client`·`basic-ftp` 2종(M3만·`src/main/remote/`에서만 import). M1·M2는 신규 의존성 0.**
>
> **§M 보안 게이트(필수 수용기준)**: 자격증명 평문 금지·safeStorage(DPAPI)만(D6·ADR-007 ③)·비밀 DTO/로그/Error 구조적 배제(⑥)·네트워크 import `src/main/remote/` ESLint 화이트리스트(D7·②)·원격 응답 불신(traversal·Zip Slip·심볼릭·호스트키 TOFU·평문 FTP 경고·.part 원자 rename·⑥). 미해결질문(safeStorage UI 미노출·resume/체크섬·원격↔원격·원격 전용 프로세스)은 [ADR-007 미해결질문 절](./architecture/adr/ADR-007-remote-protocol-and-network-boundary.md#미해결-질문-설계-deferral) 단일 출처 — 전부 1차 범위 밖·구현 비차단.
>
> **확인 필요(PM/reviewer)**: M-implementation-plan §부록C(CN-1 기존 clipboard 채널 병존 vs 대체·CN-2 startDrag 아이콘 소스·CN-3 credentials.enc와 atomic.ts 헤더 정합·CN-4 원격 전송 OperationManager 재사용 방식·CN-5 ssh2 가속 optional 서명 영향).

---

## 4. 역할 분담 & 병렬화 가이드

| Phase | devops | backend (Main) | frontend (Renderer) | qa |
|---|---|---|---|---|
| P0 | **골격·빌드·패키징·CSP·보안옵션** | — | React/Zustand 부트 합류 | 빌드 스모크·보안옵션 |
| P1 | CI 빌드 게이트 | **fs:* 핸들러·FileSystemService·guard** | **infra/api·preload 검토**, 계약 공동합의 | 계약 경계·스트림·오류코드 |
| P2 | — | **`shell:open` 핸들러·경로검증 가드** + fs:* 안정화 지원 | **단일 패널 UI(+open usecase·파일 실행/`Enter`) 전담** | 첫렌더 성능·리렌더 격리·상태머신·**shell:open 보안 검증** |
| P3 | — | (P4 계약 사전검토) | **탭·2분할·단축키(+레지스트리 노출) 전담** | 단축키 충돌·컨텍스트·불변식 |
| P4 | — | **OperationManager·Worker·휴지통·op/fs 핸들러·`shell:show-properties`** | **다이얼로그·dnd(+하이라이트/커서피드백)·컨텍스트메뉴·usecase·operationsSlice** | 200ms·충돌매트릭스·D&D 진리표·하이라이트·상태머신 |
| P5 | — | **persistence·session/settings 핸들러** | **P5a: 검색/필터·테마·상태바·설정화면(숨김/확장자 토글) / P5b: 즐겨찾기·최근·복원** | 검색200ms·설정영속·숨김토글 연동·크래시복원·원자적쓰기 |
| P6 | — | workspace:*·telemetry·썸네일 | 4분할·미리보기·워크스페이스 UI | 옵트인 차단·미리보기 폴백 |
| P7 | **코드서명·인스톨러** | 결함 수정 | 접근성·결함 수정 | **성능 3종·F장 매트릭스·보안 최종** |
| MP0 (§M) | **의존성·ESLint 화이트리스트 골격** | — | — | ESLint 회귀 게이트 |
| MP1 (§M) | — | **`shared/ipc`·`dto`·`guard` 계약 동결** | **preload/infra·UI 모킹 합의** | 계약 경계·비밀배제 |
| MP2 (§M) | — | **`shellClipboard`·clipboard 핸들러** | **clipboardExternal·commandBus 라우팅** | CF_HDROP 라운드트립·외부입력 불신 |
| MP3 (§M) | — | **`dragdrop`·dnd 핸들러** | **externalDrag·드롭 분기·ui/dnd** | 외부/내부 분기·원격 제외·보안 |
| MP4 (§M) | ssh2 패키징·서명 영향 점검 | **`credentials`·`RemoteProfileStore`·`remote/*`·remote 핸들러·OperationManager 연동** | (MP5 모킹 합의) | **신뢰경계·호스트키·비밀 grep 0** |
| MP5 (§M) | 빌드·청크 | (MP4 실연동 지원) | **`transferRoute`·remote usecase/slice·`ui/remote/*`·navigation 분기** | 전송 라우팅 진리표·세션 격리·수용기준 |

**병렬화 규칙**
1. **계약 우선(Interface First)**: P1에서 **전 채널(`fs:*`·`shell:*`·`op:*`·`clipboard:*`·`session:*`·`settings:*`·`workspace:*`)의 타입 계약**(`shared/ipc`·`shared/dto`)을 동결하는 것이 모든 병렬 작업의 선행 조건. **핸들러 구현은 P1=`fs:*` 읽기 계열만, 나머지는 각 해당 Phase(`shell:open`=P2 / `op:*`·`shell:show-properties`·`clipboard:*`=P4 / `session:*`·`settings:*`=P5 / `workspace:*`=P6)**. 타입이 P1에 동결되므로 frontend는 infra/api 모킹 위에서 UI를, backend는 실제 핸들러를 Phase별로 병렬 구현.
2. **작은 단위 배정**: 한 사람에게 한 번에 한 모듈 경계(예: backend는 OperationManager만, frontend는 ConflictDialog만)를 준다.
3. **즉시 통합**: 모듈 완성 즉시 `op:*`/`fs:*` 경계에서 통합·교차 비교해 타입·shape·계약 위반을 조기 발견.
4. **블로커 에스컬레이션**: SPK-Worker(P4 선결)·SPK-Perf(P5 선결) 결론 지연, 설계 모순, 범위 변경은 PM에 보고.

---

## 5. 단계 게이트(이중 검증 루프)

각 Phase는 다음 게이트를 통과해야 다음으로 넘어간다(최대 2회 재검증, 실패 시 PM 에스컬레이션):

1. **세부 계획**(파일·함수·인터페이스 구체화) → **reviewer 계획 검증**(설계 정합·실행가능성) → 반영
2. **구현** → **qa 구현 검증**(계획대로 됐는지·경계면 교차·수용기준) → 반영 → 통과 시 다음 Phase

**Phase별 핵심 게이트 한 줄**
- P0: 빈 창 dev/prod 실행 + 보안옵션 + 인스톨러 생성
- P1: `shared/ipc` **전 채널 타입 동결** + 스트림/오류코드 교차검증
- P2: 1만 항목 첫 렌더 + 리렌더 격리 + **파일 더블클릭/`Enter` 실행(shell:open 보안 검증)**
- P3: 단축키 충돌 부재 + 탭/분할 불변식 + 단축키 레지스트리 노출
- P4: 200ms 진행률 + 충돌매트릭스 + D&D 의도 진리표 + **드롭 하이라이트·속성창**
- P5: 검색 200ms + **설정 화면(숨김/확장자 토글 즉시 반영·영속)** + 크래시 후 세션복원 (→ MVP 완성)
- P6: Should 기능 (변화 격리 경계 내)
- P7: (헤드리스 ✅) 접근성·WCAG AA·성능/F장 검증 하니스·audit 점검·서명 설정 / (런타임 🟡) 성능 3종 실측 + 실제 코드서명 + NSIS 실측
